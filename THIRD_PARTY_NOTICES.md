# Third-Party Notices

`styles.css` inlines CSS from the following MIT-licensed packages so consumers can
import a single package stylesheet:

- `tw-animate-css`
- `shadcn`

Their full license texts are available in their published npm packages.

## Apache-2.0

This package depends on `class-variance-authority` (Apache-2.0). Per the
Apache License 2.0, a NOTICE is required if the original package ships one;
`class-variance-authority` does not ship a NOTICE file, so no attribution
beyond this notice is required. Full license:
https://www.apache.org/licenses/LICENSE-2.0

## Other dependencies

All other runtime dependencies are MIT or ISC licensed (see `package.json`).
No copyleft (GPL/AGPL/LGPL) dependencies are included.
