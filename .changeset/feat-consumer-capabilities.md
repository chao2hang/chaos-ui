---
"@chaos_team/chaos-ui": minor
---

feat: 补充 6 项消费方迁移需求 (C-01 ~ C-06)

- C-01: Flex 新增 `flex` 数字比例 prop，支持 17:7 等任意比例布局
- C-02: 新增 `AppLogo` / `BrandBadge` 品牌组件，支持 8 种渐变预设和自定义 CSS 渐变
- C-03: Card 新增 `shadow` 变体 (none/default/sm/md/lg/xl/2xl/brand)
- C-04: AuthLayout 新增 `background` prop，支持预设关键字和自定义 CSS 背景值
- C-05: ChartSuite `type="area"` 升级为多系列叠加渲染 + SVG gradient 渐变填充
- C-06: Text 新增侧栏语义变体 (sidebar-foreground/sidebar-muted/sidebar-accent)

同步更新 Storybook stories 和文档站 MDX 文档。
