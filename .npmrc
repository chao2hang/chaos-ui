registry=https://registry.npmjs.org

# 私有包 registry (旧 — 外部服务)
@qxyfoods:registry=https://npm.qxy1828.com
# Token 请从环境变量读取: //npm.qxy1828.com/:_authToken=${NPM_QXYFOODS_TOKEN}

# 私有包 registry (新 — 自建)
@chaos:registry=https://git.nomsg.cn/npm/
# Token 请从环境变量读取: //git.nomsg.cn/npm/:_authToken=${CHAOS_NPM_TOKEN}

audit-level=moderate
save-exact=false