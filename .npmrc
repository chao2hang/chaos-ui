# Package installs and publishes use the public npm registry only.
# Do not add scoped private registries (@scope:registry=...) here.
registry=https://registry.npmjs.org/

audit-level=moderate
save-exact=false
