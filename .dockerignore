# Dependencies
node_modules
.pnpm-store

	# Build outputs
	.next
	storybook-static
	dist
	out

# Git
.git
.gitignore

# IDE
.vscode
.idea
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Docker
Dockerfile
docker-compose*.yml
.dockerignore

# Documentation
*.md
!README.md

# Tests
coverage
.nyc_output
*.test.*
*.spec.*
vitest.config.ts
vitest.setup.ts
vitest.shims.d.ts

# Config files that aren't needed in production
.eslintrc*
eslint.config.*
.prettierrc*
prettier.config.*
postcss.config.*
tailwind.config.*
components.json
# tsconfig.json is needed by Next.js for path resolution (@/ alias)
!apps/docs/tsconfig.json

# Storybook source — apps/docs/src 内的 .stories.tsx 是文档站 live preview 的
# 构建期输入(generate-component-story-previews.mjs 读取它),必须保留。
# 根目录 src/stories/ 仅供根 Storybook 构建用,已被 build-storybook 产物替代。
apps/docs/stories

# Temp files
tmp
temp
*.log
