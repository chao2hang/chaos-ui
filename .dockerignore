# Dependencies
node_modules
.pnpm-store

	# Build outputs
	.next
	storybook-static
	dist
	out

# Git
.git
.gitignore

# IDE
.vscode
.idea
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Docker
Dockerfile
docker-compose*.yml
.dockerignore

# Documentation
*.md
!README.md

# Tests
coverage
.nyc_output
*.test.*
*.spec.*
vitest.config.ts
vitest.setup.ts
vitest.shims.d.ts

# Config files that aren't needed in production
.eslintrc*
eslint.config.*
.prettierrc*
prettier.config.*
postcss.config.*
tailwind.config.*
components.json
# tsconfig.json is needed by Next.js for path resolution (@/ alias)
!apps/docs/tsconfig.json

# Storybook source (we only need the built output)
apps/docs/src
apps/docs/stories

# Temp files
tmp
temp
*.log
