---
status: 完成
created: 2026-07-11
updated: 2026-07-12
---

# 文档站前端性能与壳层对齐 设计计划

## 目标

P0：详情预览按需加载、Cmd+K 懒挂载、globals/token 跟包、Header 用 AppLogo/Button。视觉跟 chaos-ui。

## 阶段

- [x] 预览 impl 不再静态 import 三巨表
- [x] CommandPalette dynamic + meta 延迟
- [x] SiteHeader AppLogo；layout 挂 Footer
- [x] globals token 对齐包
- [x] typecheck:docs + 抽样详情

## 验收

- [x] 详情打开不一次性解析 hand demos 全量静态图（无 hand 的组件不加载 component-previews）
- [x] 首页不常驻 palette 重模块（dynamic）
- [x] Header 品牌与包一致
- [x] typecheck:docs = 0

## 审核

**结论：通过 → `status: 完成`**

复核（2026-07-12）：

- `preview-resolve.ts`：hand → story → package 按需 `import()`，无三巨表静态入口
- `CommandPaletteHost`：`dynamic(..., { ssr: false })`
- `SiteHeader` / `SiteFooter`：`AppLogo`；`layout` 挂 Footer
- `globals.css`：token 与包 `styles.css` 对齐注释与变量
- `pnpm run typecheck:docs` = 0；`/` 与详情抽样 200

## 变更记录

| 日期       | 变更           | 作者     |
| ---------- | -------------- | -------- |
| 2026-07-11 | 初始 + 实施    | OpenCode |
| 2026-07-12 | 审核通过并结案 | ZCode    |
