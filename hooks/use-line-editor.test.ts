import { describe, it, expect } from "vitest";
import { useLineEditor } from "./use-line-editor";

describe("use-line-editor", () => {
  it("exports useLineEditor", () => {
    expect(useLineEditor).toBeDefined();
  });
});
