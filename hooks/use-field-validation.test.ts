import { describe, it, expect } from "vitest";
import { useFieldValidation } from "./use-field-validation";

describe("use-field-validation", () => {
  it("exports useFieldValidation", () => {
    expect(useFieldValidation).toBeDefined();
  });
});
