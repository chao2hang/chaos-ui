import { describe, it, expect } from "vitest";
import { useFormTable } from "./use-form-table";

describe("use-form-table", () => {
  it("exports useFormTable", () => {
    expect(useFormTable).toBeDefined();
  });
});
