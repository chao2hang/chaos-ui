---
title: English/Installation
description: How to install Chaos UI in your project
---

# Installation

Chaos UI is available as an npm package: `@chaos_team/chaos-ui`.

## Requirements

- React >= 19.0.0
- Node.js >= 22.0.0
- Tailwind CSS 4 (recommended)

## Install

**npm**

```bash
npm install @chaos_team/chaos-ui
```

**yarn**

```bash
yarn add @chaos_team/chaos-ui
```

**pnpm**

```bash
pnpm add @chaos_team/chaos-ui
```

## Peer Dependencies

Install the required peer dependencies:

```bash
npm install react@^19 react-dom@^19
```

For Next.js projects:

```bash
npm install next@^16 next-themes@^0.4
```

## Import Styles

Import the CSS file in your app entry point:

```tsx
import "@chaos_team/chaos-ui/styles.css";
```

## Tailwind Configuration (Optional)

If you use Tailwind CSS, extend your config to include Chaos UI's design tokens:

```ts
// tailwind.config.ts
import type { Config } from "tailwindcss";

export default {
  content: [
    "./src/**/*.{ts,tsx}",
    "./node_modules/@chaos_team/chaos-ui/dist/**/*.{js,cjs}",
  ],
} satisfies Config;
```

## Usage

```tsx
import { Button, Input, Dialog } from "@chaos_team/chaos-ui";

function App() {
  return (
    <div>
      <Button variant="default">Click me</Button>
      <Input placeholder="Enter text" />
    </div>
  );
}
```

## Subpath Imports

For tree-shaking, use subpath imports:

```tsx
// Import only UI primitives
import { Button } from "@chaos_team/chaos-ui/ui";

// Import only hooks
import { useDebounce } from "@chaos_team/chaos-ui/hooks";

// Import only lib utilities
import { cn } from "@chaos_team/chaos-ui/lib";

// Import only business components
import { DataTable } from "@chaos_team/chaos-ui/business";
```

## TypeScript

Chaos UI is built with TypeScript and ships with full type definitions. No additional `@types` packages needed.

```tsx
import type { ButtonProps } from "@chaos_team/chaos-ui/ui";
```
