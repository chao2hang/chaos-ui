import { describe, it, expect } from "vitest";
import { BulkActionsToolbar } from "./bulk-actions-toolbar";

describe("bulk-actions-toolbar", () => {
  it("exports BulkActionsToolbar", () => {
    expect(BulkActionsToolbar).toBeDefined();
  });
});
