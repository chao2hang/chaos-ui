import { AspectRatio } from "@/components/ui/aspect-ratio";
import { CodeBlock } from "@/components/code-block";

# AspectRatio Ui Aspect Ratio

> 布局组件 / Layout primitives for structuring pages.

Ui Aspect Ratio — Chaos UI 的布局组件,用于组织页面区域结构。
AspectRatio — a layout primitives for structuring pages. for structuring page regions.

## 何时使用 / When to Use

适用于Ui Aspect Ratio相关场景。

Use when you need an aspect ratio.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { AspectRatio } from "@/components/ui/aspect-ratio"

export function Basic() {
  return <AspectRatio />
}`} lang="tsx" />

## API / Props

### AspectRatio

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **无障碍**: 组件遵循 WAI-ARIA 最佳实践。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-ui-aspect-ratio--docs)。
