import { Grid } from "@/components/ui/grid"
import { CodeBlock } from "@/components/code-block"

# Grid 栅格

> 布局组件 / Layout primitives for structuring pages.

栅格 — Chaos UI 的布局组件,用于组织页面区域结构。
Grid — a layout primitives for structuring pages. for structuring page regions.

## 何时使用 / When to Use

需要栅格功能时使用本组件。

Use when you need a grid.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { Grid } from "@/components/ui/grid"

export function Basic() {
  return <Grid>Default</Grid>
}`} lang="tsx" />


## 样式变体 / Style Variants

**columns / columns**

| columns | 说明 / Description | 默认 |
|---|---|---|
| `1` |  | ✓ |
| `2` |  |  |
| `3` |  |  |
| `4` |  |  |
| `5` |  |  |
| `6` |  |  |
| `12` |  |  |

**gap / gap**

| gap | 说明 / Description | 默认 |
|---|---|---|
| `none` |  |  |
| `xs` |  |  |
| `sm` |  |  |
| `md` |  | ✓ |
| `lg` |  |  |
| `xl` |  |  |

**对齐 / align**

| align | 说明 / Description | 默认 |
|---|---|---|
| `start` |  |  |
| `center` |  |  |
| `end` |  |  |
| `stretch` |  | ✓ |

**主轴对齐 / justify**

| justify | 说明 / Description | 默认 |
|---|---|---|
| `start` |  | ✓ |
| `center` |  |  |
| `end` |  |  |
| `between` |  |  |
| `around` |  |  |

## API / Props

### Grid

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| ...rest | `React.ComponentProps<"div">` | — | 透传 `<div>` 原生属性 <br/> Inherits all `<div>` attributes |

## 注意事项 / Notes

- **CVA 变体**: 通过 `class-variance-authority` 管理样式变体,类型安全。变体对象可导出复用。
- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-grid--docs)。
