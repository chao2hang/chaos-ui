import { ImmersiveLayout } from "@chaos_team/chaos-ui/layout";
import { CodeBlock } from "@/components/code-block";

# ImmersiveLayout

> Layout primitives for structuring pages.

ImmersiveLayout — Immersive full-screen layout that hides sidebar/header/tabs for dashboards.

## When to Use

Use ImmersiveLayout when you need: Immersive full-screen layout that hides sidebar/header/tabs for dashboards. Use to structure page regions before filling in business modules.

## Examples

### Basic Usage

<CodeBlock code={`import { ImmersiveLayout } from "@chaos_team/chaos-ui/layout"

export function Basic() {
  return <ImmersiveLayout />
}`} lang="tsx" />

## API / Props

### ImmersiveLayout

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
