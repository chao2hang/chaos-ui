import { SplitPane } from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# SplitPane 分割面板

> 布局组件 / Layout primitives for structuring pages.

可拖拽分割线的双栏/多栏布局容器。
Multi-pane layout container with a draggable divider.

## 何时使用 / When to Use

需要分割面板功能时使用本组件。

Use when you need a split pane.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { SplitPane } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <SplitPane />
}`} lang="tsx" />

## API / Props

### SplitPane

| Prop          | Type                         | Default      | 说明 / Description                                        |
| ------------- | ---------------------------- | ------------ | --------------------------------------------------------- |
| `className`   | `string`                     | —            | 自定义样式类名 <br/> Custom CSS class                     |
| `direction`   | `"horizontal" \| "vertical"` | `horizontal` | 排列方向 <br/> Direction                                  |
| `defaultSize` | `number`                     | `50`         |                                                           |
| `minSize`     | `number`                     | `20`         |                                                           |
| `maxSize`     | `number`                     | `80`         |                                                           |
| `onResize`    | `() => void`                 | —            |                                                           |
| `first`       | `React.ReactNode`            | —            |                                                           |
| `second`      | `React.ReactNode`            | —            |                                                           |
| `resizer`     | `boolean`                    | `true`       |                                                           |
| ...rest       | `React.HTMLAttributes`       | —            | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-splitpane--docs)。
