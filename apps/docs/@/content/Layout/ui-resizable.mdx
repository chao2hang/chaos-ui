import {
  ResizablePanelGroup,
  ResizablePanel,
  ResizableHandle,
} from "@/components/ui/resizable";
import { CodeBlock } from "@/components/code-block";

# Resizable Ui Resizable

> 布局组件 / Layout primitives for structuring pages.

Ui Resizable — Chaos UI 的布局组件,用于组织页面区域结构。
Resizable — a layout primitives for structuring pages. for structuring page regions.

## 何时使用 / When to Use

适用于Ui Resizable相关场景。

Use when you need a resizable.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { ResizablePanelGroup, ResizablePanel, ResizableHandle } from "@/components/ui/resizable"

export function Basic() {
  return (
    <div className="space-y-4">
  <ResizablePanelGroup />
  <ResizablePanel />
  <ResizableHandle />
    </div>
  )
}`} lang="tsx" />

## API / Props

### ResizablePanelGroup

| Prop            | Type                          | Default | 说明 / Description                                          |
| --------------- | ----------------------------- | ------- | ----------------------------------------------------------- |
| `className`     | `string`                      | —       | 自定义样式类名 <br/> Custom CSS class                       |
| `defaultSize`   | `number`                      | —       |                                                             |
| `minSize`       | `number`                      | —       |                                                             |
| `maxSize`       | `number`                      | —       |                                                             |
| `collapsible`   | `boolean`                     | —       |                                                             |
| `collapsedSize` | `number`                      | —       |                                                             |
| `onCollapse`    | `() => void`                  | —       |                                                             |
| `onExpand`      | `() => void`                  | —       |                                                             |
| `onResize`      | `() => void`                  | —       |                                                             |
| ...rest         | `React.ComponentProps<"div">` | —       | 透传 `<div>` 原生属性 <br/> Inherits all `<div>` attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-ui-resizable--docs)。
