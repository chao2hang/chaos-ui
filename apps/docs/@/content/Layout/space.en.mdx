import { Space } from "@/components/ui/space"
import { CodeBlock } from "@/components/code-block"

# Space

> Layout primitives for structuring pages.

Space — a layout primitives for structuring pages. for structuring page regions.

## When to Use

Use when you need a space.


## Examples

### Basic Usage

<CodeBlock code={`import { Space } from "@/components/ui/space"

export function Basic() {
  return <Space>Default</Space>
}`} lang="tsx" />

### Sizes

<CodeBlock code={`{/* 尺寸对照 */}
<div className="flex flex-wrap items-center gap-3">
  <Space size="xs">Xs</Space>
  <Space size="sm">Sm</Space>
  <Space size="lg">Lg</Space>
</div>`} lang="tsx" />


## Style Variants

**direction**

| direction | Description | 默认 |
|---|---|---|
| `horizontal` |  | ✓ |
| `vertical` |  |  |

**Size**

| Size | Description | 默认 |
|---|---|---|
| `xs` | Extra small |  |
| `sm` | Small | ✓ |
| `md` |  |  |
| `lg` | Large |  |
| `xl` | Extra large |  |

**wrap**

| wrap | Description | 默认 |
|---|---|---|
| `true` |  | ✓ |
| `false` |  |  |

**align**

| align | Description | 默认 |
|---|---|---|
| `start` |  |  |
| `center` |  | ✓ |
| `end` |  |  |
| `baseline` |  |  |

## API / Props

### Space

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `split` | `React.ReactNode` | — | Split items with a separator |
| ...rest | `React.ComponentProps<"div">` | — | Inherits all `<div>` attributes |

## Notes



