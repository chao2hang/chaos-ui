import { Space } from "@/components/ui/space"
import { CodeBlock } from "@/components/code-block"

# Space 间距

> 布局组件 / Layout primitives for structuring pages.

间距 — Chaos UI 的布局组件,用于组织页面区域结构。
Space — a layout primitives for structuring pages. for structuring page regions.

## 何时使用 / When to Use

需要间距功能时使用本组件。

Use when you need a space.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { Space } from "@/components/ui/space"

export function Basic() {
  return <Space>Default</Space>
}`} lang="tsx" />

### 尺寸 / Sizes

<CodeBlock code={`{/* 尺寸对照 */}
<div className="flex flex-wrap items-center gap-3">
  <Space size="xs">Xs</Space>
  <Space size="sm">Sm</Space>
  <Space size="lg">Lg</Space>
</div>`} lang="tsx" />


## 样式变体 / Style Variants

**方向 / direction**

| direction | 说明 / Description | 默认 |
|---|---|---|
| `horizontal` |  | ✓ |
| `vertical` |  |  |

**尺寸 / Size**

| Size | 说明 / Description | 默认 |
|---|---|---|
| `xs` | 超小 / Extra small |  |
| `sm` | 小号 / Small | ✓ |
| `md` |  |  |
| `lg` | 大号 / Large |  |
| `xl` | 超大 / Extra large |  |

**wrap / wrap**

| wrap | 说明 / Description | 默认 |
|---|---|---|
| `true` |  | ✓ |
| `false` |  |  |

**对齐 / align**

| align | 说明 / Description | 默认 |
|---|---|---|
| `start` |  |  |
| `center` |  | ✓ |
| `end` |  |  |
| `baseline` |  |  |

## API / Props

### Space

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| `split` | `React.ReactNode` | — | Split items with a separator |
| ...rest | `React.ComponentProps<"div">` | — | 透传 `<div>` 原生属性 <br/> Inherits all `<div>` attributes |

## 注意事项 / Notes

- **CVA 变体**: 通过 `class-variance-authority` 管理样式变体,类型安全。变体对象可导出复用。
- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-space--docs)。
