import { ScrollArea, ScrollBar } from "@/components/ui/scroll-area";
import { CodeBlock } from "@/components/code-block";

# ScrollArea Ui Scroll Area

> 布局组件 / Layout primitives for structuring pages.

Ui Scroll Area — Chaos UI 的布局组件,用于组织页面区域结构。
ScrollArea — a layout primitives for structuring pages. for structuring page regions.

## 何时使用 / When to Use

适用于Ui Scroll Area相关场景。

Use when you need a scroll area.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { ScrollArea, ScrollBar } from "@/components/ui/scroll-area"

export function Basic() {
  return (
    <div className="space-y-4">
  <ScrollArea />
  <ScrollBar />
    </div>
  )
}`} lang="tsx" />

## API / Props

### ScrollArea

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **Base UI**: 基于 `@base-ui/react` 无头(headless)原语构建,无障碍(a11y)开箱即用。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-ui-scroll-area--docs)。
