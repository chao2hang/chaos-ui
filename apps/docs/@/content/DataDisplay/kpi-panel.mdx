import { KPIPanel, KPICard } from "@/components/ui/kpi-panel"
import { CodeBlock } from "@/components/code-block"

# KpiPanel KPI 面板

> 数据展示 / Components for presenting data and content.

关键绩效指标面板,展示核心业务数值和趋势。
KPI panel showing key business metrics and trends.

## 何时使用 / When to Use

适用于KPI 面板相关场景。

Use when you need a kpi panel.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { KPIPanel } from "@/components/ui/kpi-panel"

export function Basic() {
  return <KPIPanel>Default</KPIPanel>
}`} lang="tsx" />

### 尺寸 / Sizes

<CodeBlock code={`{/* 尺寸对照 */}
<div className="flex flex-wrap items-center gap-3">
  <KPIPanel size="sm">Sm</KPIPanel>
  <KPIPanel size="default">Default</KPIPanel>
  <KPIPanel size="lg">Lg</KPIPanel>
</div>`} lang="tsx" />


## 样式变体 / Style Variants

**尺寸 / Size**

| Size | 说明 / Description | 默认 |
|---|---|---|
| `sm` | 小号 / Small |  |
| `default` | 默认尺寸 / Default size | ✓ |
| `lg` | 大号 / Large |  |

## API / Props

### KPIPanel

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| `items` | `KPIItem[]` | — | 选项/数据项列表 <br/> Items / data list |
| `columns` | `1 \| 2 \| 3 \| 4` | `4` | 列配置 <br/> Column configuration |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **CVA 变体**: 通过 `class-variance-authority` 管理样式变体,类型安全。变体对象可导出复用。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-kpipanel--docs)。
