import { EditableDescriptions } from "@/components/ui/editable-descriptions"
import { CodeBlock } from "@/components/code-block"

# EditableDescriptions 可编辑描述

> 数据展示 / Data display components for presenting information.

可编辑描述 — 可编辑描述列表，支持字段行内编辑。
EditableDescriptions — Editable descriptions list with inline field editing.

## 何时使用 / When to Use

需要可编辑描述功能时使用本组件。

Use when you need a editabledescriptions.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { EditableDescriptions } from "@/components/ui/editable-descriptions"

export function Basic() {
  return <EditableDescriptions />
}`} lang="tsx" />


## API / Props

### EditableDescriptions

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-editabledescriptions--docs)。
