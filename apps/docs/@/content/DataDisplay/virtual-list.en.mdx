import { VirtualList } from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# VirtualList

> Components for presenting data and content.

VirtualList — a components for presenting data and content. from Chaos UI.

## When to Use

Use VirtualList when you need: VirtualList — a components for presenting data and content. from Chaos UI. Use to present lists, status, metrics, and detail content for quick scanning.

## Examples

### Basic Usage

<CodeBlock code={`import { VirtualList } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <VirtualList />
}`} lang="tsx" />

## API / Props

### VirtualList

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
