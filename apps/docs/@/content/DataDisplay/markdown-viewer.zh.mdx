import { MarkdownViewer } from "@/components/ui/markdown-viewer"
import { CodeBlock } from "@/components/code-block"

# MarkdownViewer Markdown查看器

> 数据展示

Markdown查看器 — 只读 Markdown 渲染器，支持语法高亮。

## 何时使用

需要Markdown查看器功能时使用本组件。


## 代码示例

### 基础用法

<CodeBlock code={`import { MarkdownViewer } from "@/components/ui/markdown-viewer"

export function Basic() {
  return <MarkdownViewer />
}`} lang="tsx" />


## API / Props

### MarkdownViewer

| Prop | Type | Default | 说明 |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-markdownviewer--docs)。

