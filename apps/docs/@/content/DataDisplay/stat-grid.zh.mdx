import { StatGrid } from "@chaos_team/chaos-ui/ui"
import { CodeBlock } from "@/components/code-block"

# StatGrid 指标网格

> 数据展示

指标网格 — 响应式指标网格，支持自动排列与趋势指示。

## 何时使用

需要指标网格功能时使用本组件。


## 代码示例

### 基础用法

<CodeBlock code={`import { StatGrid } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <StatGrid />
}`} lang="tsx" />


## API / Props

### StatGrid

| Prop | Type | Default | 说明 |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-statgrid--docs)。

