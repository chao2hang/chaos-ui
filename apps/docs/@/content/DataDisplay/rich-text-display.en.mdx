import { RichTextDisplay } from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# RichTextDisplay

> Data display components for presenting information.

RichTextDisplay — Read-only rich text display supporting HTML and JSON with mentions and attachments.

## When to Use

Use when you need a richtextdisplay.

## Examples

### Basic Usage

<CodeBlock code={`import { RichTextDisplay } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <RichTextDisplay />
}`} lang="tsx" />

## API / Props

### RichTextDisplay

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
