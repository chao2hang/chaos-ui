import { Statistic } from "@/components/ui/statistic";
import { CodeBlock } from "@/components/code-block";

# Statistic Ui Statistic

> 数据展示 / Components for presenting data and content.

Ui Statistic — Chaos UI 的数据展示。
Statistic — a components for presenting data and content. from Chaos UI.

## 何时使用 / When to Use

适用于Ui Statistic相关场景。

Use when you need a statistic.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { Statistic } from "@/components/ui/statistic"

export function Basic() {
  return <Statistic>Default</Statistic>
}`} lang="tsx" />

### 尺寸 / Sizes

<CodeBlock
  code={`{/* 尺寸对照 */}
<div className="flex flex-wrap items-center gap-3">
  <Statistic size="sm">Sm</Statistic>
  <Statistic size="default">Default</Statistic>
  <Statistic size="lg">Lg</Statistic>
</div>`}
  lang="tsx"
/>

## 样式变体 / Style Variants

**尺寸 / Size**

| Size      | 说明 / Description      | 默认 |
| --------- | ----------------------- | ---- |
| `default` | 默认尺寸 / Default size | ✓    |
| `sm`      | 小号 / Small            |      |
| `lg`      | 大号 / Large            |      |

## API / Props

### Statistic

| Prop        | Type                          | Default | 说明 / Description                                          |
| ----------- | ----------------------------- | ------- | ----------------------------------------------------------- |
| `className` | `string`                      | —       | 自定义样式类名 <br/> Custom CSS class                       |
| `value`     | `React.ReactNode`             | —       | The value to display                                        |
| `title`     | `React.ReactNode`             | —       | Label / title                                               |
| `prefix`    | `React.ReactNode`             | —       | Prefix before value                                         |
| `suffix`    | `React.ReactNode`             | —       | Suffix after value                                          |
| `precision` | `number`                      | —       | Precision for number formatting                             |
| `loading`   | `boolean`                     | `false` | Loading state shows skeleton                                |
| ...rest     | `React.ComponentProps<"div">` | —       | 透传 `<div>` 原生属性 <br/> Inherits all `<div>` attributes |

## 注意事项 / Notes

- **CVA 变体**: 通过 `class-variance-authority` 管理样式变体,类型安全。变体对象可导出复用。
- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-ui-statistic--docs)。
