import { Heatmap } from "@/components/ui/heatmap"
import { CodeBlock } from "@/components/code-block"

# Heatmap 热力图

> 数据展示 / Data display components for presenting information.

热力图 — GitHub 风格日历热力图，支持色阶与单元格点击。
Heatmap — GitHub-style calendar heatmap with color scale and cell click.

## 何时使用 / When to Use

需要热力图功能时使用本组件。

Use when you need a heatmap.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { Heatmap } from "@/components/ui/heatmap"

export function Basic() {
  return <Heatmap />
}`} lang="tsx" />


## API / Props

### Heatmap

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-heatmap--docs)。
