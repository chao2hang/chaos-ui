import { ProTable } from "@/components/business/pro-table"
import { CodeBlock } from "@/components/code-block"

# ProTable

> Data display components for presenting information.

ProTable — Enterprise ProTable with column settings, density switch, saved views and row drag.

## When to Use

Use when you need a protable.


## Examples

### Basic Usage

<CodeBlock code={`import { ProTable } from "@/components/business/pro-table"

export function Basic() {
  return <ProTable />
}`} lang="tsx" />


## API / Props

### ProTable

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


