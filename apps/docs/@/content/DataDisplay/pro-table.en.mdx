import { ProTable } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# ProTable

> Data display components for presenting information.

ProTable — Enterprise ProTable with column settings, density switch, saved views and row drag.

## When to Use

Use ProTable when you need: Enterprise ProTable with column settings, density switch, saved views and row drag. Use to present lists, status, metrics, and detail content for quick scanning.

## Examples

### Basic Usage

<CodeBlock code={`import { ProTable } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <ProTable />
}`} lang="tsx" />

## API / Props

### ProTable

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
