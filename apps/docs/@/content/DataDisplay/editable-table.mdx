import { EditableTable } from "@/components/ui/editable-table"
import { CodeBlock } from "@/components/code-block"

# EditableTable 可编辑表格

> 数据展示 / Data display components for presenting information.

可编辑表格 — 行内可编辑表格，支持增删改与批量操作。
EditableTable — Inline editable table with row add, edit, delete and batch operations.

## 何时使用 / When to Use

需要可编辑表格功能时使用本组件。

Use when you need a editabletable.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { EditableTable } from "@/components/ui/editable-table"

export function Basic() {
  return <EditableTable />
}`} lang="tsx" />


## API / Props

### EditableTable

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-editabletable--docs)。
