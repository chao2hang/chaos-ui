import { ImageViewer } from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# ImageViewer 图片查看器

> 数据展示 / Components for presenting data and content.

图片查看器 — Chaos UI 的数据展示。
ImageViewer — a components for presenting data and content. from Chaos UI.

## 何时使用 / When to Use

当你需要「图片查看器」相关能力时使用：图片查看器 — Chaos UI 的数据展示。用于展示列表、状态、统计与内容详情；让用户先看懂数据再操作。

Use ImageViewer when you need: ImageViewer — a components for presenting data and content. from Chaos UI. Use to present lists, status, metrics, and detail content for quick scanning.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { ImageViewer } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <ImageViewer />
}`} lang="tsx" />

## 样式变体 / Style Variants

**open / open**

| open    | 说明 / Description | 默认 |
| ------- | ------------------ | ---- |
| `true`  |                    | ✓    |
| `false` |                    |      |

## API / Props

### ImageViewer

| Prop           | Type                              | Default | 说明 / Description                                          |
| -------------- | --------------------------------- | ------- | ----------------------------------------------------------- |
| `className`    | `string`                          | —       | 自定义样式类名 <br/> Custom CSS class                       |
| `open`         | `boolean`                         | —       | Whether the viewer is open                                  |
| `onClose`      | `() => void`                      | —       | Called when the viewer should close                         |
| `src`          | `string`                          | —       | Image source URL                                            |
| `alt`          | `string`                          | ``      | Image alt text                                              |
| `images`       | `{ src: string; alt?: string }[]` | —       | List of images for gallery mode                             |
| `initialIndex` | `number`                          | `0`     | Initial index for gallery mode                              |
| ...rest        | `React.ComponentProps<"div">`     | —       | 透传 `<div>` 原生属性 <br/> Inherits all `<div>` attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **CVA 变体**: 通过 `class-variance-authority` 管理样式变体,类型安全。变体对象可导出复用。
- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](http://localhost:6006/?path=/docs/components-imageviewer--docs)。
