import { VirtualList } from "@/components/ui/virtual-list"
import { CodeBlock } from "@/components/code-block"

# VirtualList 虚拟列表

> 数据展示 / Components for presenting data and content.

虚拟列表 — Chaos UI 的数据展示。
VirtualList — a components for presenting data and content. from Chaos UI.

## 何时使用 / When to Use

需要虚拟列表功能时使用本组件。

Use when you need a virtual list.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { VirtualList } from "@/components/ui/virtual-list"

export function Basic() {
  return <VirtualList />
}`} lang="tsx" />


## API / Props

### VirtualList

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-virtuallist--docs)。
