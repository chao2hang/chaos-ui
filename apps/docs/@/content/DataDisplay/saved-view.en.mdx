import { SavedViewSwitcher } from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# SavedViewSwitcher

> Data display components for presenting information.

SavedViewSwitcher — Saved view switcher for list pages — save and restore filter+column presets.

## When to Use

Use SavedViewSwitcher when you need: Saved view switcher for list pages — save and restore filter+column presets. Use to present lists, status, metrics, and detail content for quick scanning.

## Examples

### Basic Usage

<CodeBlock code={`import { SavedViewSwitcher } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <SavedViewSwitcher />
}`} lang="tsx" />

## API / Props

### SavedViewSwitcher

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
