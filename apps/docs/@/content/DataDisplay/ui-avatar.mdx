import { Avatar } from "@/components/ui/avatar";
import { CodeBlock } from "@/components/code-block";

# Avatar Ui Avatar

> 数据展示 / Components for presenting data and content.

Ui Avatar — Chaos UI 的数据展示。
Avatar — a components for presenting data and content. from Chaos UI.

## 何时使用 / When to Use

适用于Ui Avatar相关场景。

Use when you need an avatar.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { Avatar, AvatarImage, AvatarFallback, AvatarGroup, AvatarGroupCount, AvatarBadge } from "@/components/ui/avatar"

export function Basic() {
  return (
    <Avatar>
      <AvatarImage>Content</AvatarImage>
    </Avatar>
  )
}`} lang="tsx" />

## API / Props

### Avatar

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **复合组件**: 包含 5 个子组件(AvatarImage, AvatarFallback, AvatarGroup, AvatarGroupCount, AvatarBadge),使用组合模式按需导入。
- **Base UI**: 基于 `@base-ui/react` 无头(headless)原语构建,无障碍(a11y)开箱即用。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-ui-avatar--docs)。
