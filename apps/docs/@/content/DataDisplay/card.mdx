import { Card } from "@/components/ui/card"
import { CodeBlock } from "@/components/code-block"

# Card 卡片

> 数据展示 / Components for presenting data and content.

将信息聚合在卡片容器中展示,最通用的内容分组容器。
Groups content in a container; the most common content-grouping primitive.

## 何时使用 / When to Use

将相关信息聚合展示,如商品、文章、用户等。

Group related information such as products, articles, or users.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { Card, CardHeader, CardTitle, CardContent, CardFooter } from "@/components/ui/card"

export function Basic() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Card Title</CardTitle>
      </CardHeader>
      <CardContent>Card content goes here.</CardContent>
    </Card>
  )
}`} lang="tsx" />


## API / Props

### Card

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **复合组件**: 包含 6 个子组件(CardHeader, CardFooter, CardTitle, CardAction, CardDescription…),使用组合模式按需导入。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-card--docs)。
