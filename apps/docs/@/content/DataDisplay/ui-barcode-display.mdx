import { BarcodeDisplay } from "@/components/ui/barcode-display";
import { CodeBlock } from "@/components/code-block";

# BarcodeDisplay Ui Barcode Display

> 数据展示 / Components for presenting data and content.

Ui Barcode Display — Chaos UI 的数据展示。
BarcodeDisplay — a components for presenting data and content. from Chaos UI.

## 何时使用 / When to Use

适用于Ui Barcode Display相关场景。

Use when you need a barcode display.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { BarcodeDisplay } from "@/components/ui/barcode-display"

export function Basic() {
  return <BarcodeDisplay />
}`} lang="tsx" />

## API / Props

### BarcodeDisplay

| Prop         | Type                          | Default   | 说明 / Description                                          |
| ------------ | ----------------------------- | --------- | ----------------------------------------------------------- |
| `className`  | `string`                      | —         | 自定义样式类名 <br/> Custom CSS class                       |
| `value`      | `string`                      | —         | Text value to encode                                        |
| `width`      | `number`                      | `200`     | Barcode width in pixels                                     |
| `height`     | `number`                      | `80`      | Barcode height in pixels                                    |
| `background` | `string`                      | `#ffffff` | Background color                                            |
| `color`      | `string`                      | `#000000` | Bar color                                                   |
| ...rest      | `React.ComponentProps<"div">` | —         | 透传 `<div>` 原生属性 <br/> Inherits all `<div>` attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-ui-barcode-display--docs)。
