import { VirtualTable } from "@/components/ui/virtual-table";
import { CodeBlock } from "@/components/code-block";

# VirtualTable Ui Virtual Table

> 数据展示 / Components for presenting data and content.

Ui Virtual Table — Chaos UI 的数据展示。
VirtualTable — a components for presenting data and content. from Chaos UI.

## 何时使用 / When to Use

适用于Ui Virtual Table相关场景。

Use when you need a virtual table.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { VirtualTable } from "@/components/ui/virtual-table"

export function Basic() {
  return <VirtualTable />
}`} lang="tsx" />

## API / Props

### VirtualTable

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-ui-virtual-table--docs)。
