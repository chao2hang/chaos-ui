import { SegmentedFilter } from "@/components/ui/segmented-filter"
import { CodeBlock } from "@/components/code-block"

# SegmentedFilter 分段筛选

> 数据展示

分段筛选 — 分段筛选控件，支持计数徽标，适用于 BI/仪表盘。

## 何时使用

需要分段筛选功能时使用本组件。


## 代码示例

### 基础用法

<CodeBlock code={`import { SegmentedFilter } from "@/components/ui/segmented-filter"

export function Basic() {
  return <SegmentedFilter />
}`} lang="tsx" />


## API / Props

### SegmentedFilter

| Prop | Type | Default | 说明 |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-segmentedfilter--docs)。

