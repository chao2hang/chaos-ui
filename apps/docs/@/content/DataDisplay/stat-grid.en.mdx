import { StatGrid } from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# StatGrid

> Data display components for presenting information.

StatGrid — Responsive metric/stat grid with auto-layout and trend indicators.

## When to Use

Use StatGrid when you need: Responsive metric/stat grid with auto-layout and trend indicators. Use to present lists, status, metrics, and detail content for quick scanning.

## Examples

### Basic Usage

<CodeBlock code={`import { StatGrid } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <StatGrid />
}`} lang="tsx" />

## API / Props

### StatGrid

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
