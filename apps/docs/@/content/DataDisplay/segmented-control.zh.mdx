import { SegmentedControl } from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# SegmentedControl 分段控制器

> 数据展示

分段控制器 — Chaos UI 的数据展示。

## 何时使用

当你需要「分段控制器」相关能力时使用：分段控制器 — Chaos UI 的数据展示。用于展示列表、状态、统计与内容详情；让用户先看懂数据再操作。

## 代码示例

### 基础用法

<CodeBlock code={`import { SegmentedControl } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <SegmentedControl />
}`} lang="tsx" />

### 尺寸

<CodeBlock
  code={`{/* 尺寸对照 */}
<div className="flex flex-wrap items-center gap-3">
  <SegmentedControl size="sm">Sm</SegmentedControl>
  <SegmentedControl size="default">Default</SegmentedControl>
  <SegmentedControl size="lg">Lg</SegmentedControl>
</div>`}
  lang="tsx"
/>

### 状态

<CodeBlock
  code={`<SegmentedControl>Normal</SegmentedControl>
<SegmentedControl disabled>Disabled</SegmentedControl>`}
  lang="tsx"
/>

## 样式变体

**尺寸**

| Size      | 说明     | 默认 |
| --------- | -------- | ---- |
| `default` | 默认尺寸 | ✓    |
| `sm`      | 小号     |      |
| `lg`      | 大号     |      |

**全宽**

| fullWidth | 说明 | 默认 |
| --------- | ---- | ---- |
| `true`    |      | ✓    |
| `false`   |      |      |

## API / Props

### SegmentedControl

| Prop           | Type                       | Default | 说明              |
| -------------- | -------------------------- | ------- | ----------------- |
| `className`    | `string`                   | —       | 自定义样式类名    |
| `options`      | `SegmentedControlOption[]` | —       | 可选项列表        |
| `value`        | `string`                   | —       | 当前值(受控)      |
| `onChange`     | `() => void`               | —       | 值变化回调        |
| `defaultValue` | `string`                   | —       | 默认值(非受控)    |
| ...rest        | `React.HTMLAttributes`     | —       | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **CVA 变体**: 通过 `class-variance-authority` 管理样式变体,类型安全。变体对象可导出复用。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](http://localhost:6006/?path=/docs/business-segmentedcontrol--docs)。
