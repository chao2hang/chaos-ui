import { Popover } from "@/components/ui/popover";
import { CodeBlock } from "@/components/code-block";

# Popover Ui Popover

> 数据展示 / Components for presenting data and content.

Ui Popover — Chaos UI 的数据展示。
Popover — a components for presenting data and content. from Chaos UI.

## 何时使用 / When to Use

适用于Ui Popover相关场景。

Use when you need a popover.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { Popover, PopoverTrigger, PopoverContent, PopoverHeader, PopoverTitle, PopoverDescription } from "@/components/ui/popover"

export function Basic() {
  return (
    <Popover>
      <PopoverTrigger>Open</PopoverTrigger>
      <PopoverContent>
      <PopoverHeader>
        <PopoverTitle>Title</PopoverTitle>
        <PopoverDescription>Description.</PopoverDescription>
      </PopoverHeader>
      </PopoverContent>
    </Popover>
  )
}`} lang="tsx" />

## API / Props

### Popover

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **复合组件**: 包含 5 个子组件(PopoverContent, PopoverDescription, PopoverHeader, PopoverTitle, PopoverTrigger),使用组合模式按需导入。
- **Base UI**: 基于 `@base-ui/react` 无头(headless)原语构建,无障碍(a11y)开箱即用。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-ui-popover--docs)。
