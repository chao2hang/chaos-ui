import { LazyImage } from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# LazyImage 懒加载图片

> 数据展示 / Data display components for presenting information.

懒加载图片 — 懒加载图片，支持模糊占位与视口检测。
LazyImage — Lazy-loaded image with blur-up placeholder and viewport detection.

## 何时使用 / When to Use

需要懒加载图片功能时使用本组件。

Use when you need a lazyimage.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { LazyImage } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <LazyImage />
}`} lang="tsx" />

## API / Props

### LazyImage

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-lazyimage--docs)。
