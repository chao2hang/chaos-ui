import { ProTable } from "@/components/business/pro-table"
import { CodeBlock } from "@/components/code-block"

# ProTable 高级表格

> 数据展示

高级表格 — 企业级高级表格，支持列设置、密度切换、保存视图与行拖拽。

## 何时使用

需要高级表格功能时使用本组件。


## 代码示例

### 基础用法

<CodeBlock code={`import { ProTable } from "@/components/business/pro-table"

export function Basic() {
  return <ProTable />
}`} lang="tsx" />


## API / Props

### ProTable

| Prop | Type | Default | 说明 |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-protable--docs)。

