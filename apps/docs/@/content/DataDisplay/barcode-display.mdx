import { BarcodeDisplay } from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# BarcodeDisplay 条形码展示

> 数据展示 / Components for presenting data and content.

条形码展示 — Chaos UI 的数据展示。
BarcodeDisplay — a components for presenting data and content. from Chaos UI.

## 何时使用 / When to Use

当你需要「条形码展示」相关能力时使用：条形码展示 — Chaos UI 的数据展示。用于展示列表、状态、统计与内容详情；让用户先看懂数据再操作。

Use BarcodeDisplay when you need: BarcodeDisplay — a components for presenting data and content. from Chaos UI. Use to present lists, status, metrics, and detail content for quick scanning.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { BarcodeDisplay } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <BarcodeDisplay />
}`} lang="tsx" />

## API / Props

### BarcodeDisplay

| Prop         | Type                          | Default   | 说明 / Description                                          |
| ------------ | ----------------------------- | --------- | ----------------------------------------------------------- |
| `className`  | `string`                      | —         | 自定义样式类名 <br/> Custom CSS class                       |
| `value`      | `string`                      | —         | Text value to encode                                        |
| `width`      | `number`                      | `200`     | Barcode width in pixels                                     |
| `height`     | `number`                      | `80`      | Barcode height in pixels                                    |
| `background` | `string`                      | `#ffffff` | Background color                                            |
| `color`      | `string`                      | `#000000` | Bar color                                                   |
| ...rest      | `React.ComponentProps<"div">` | —         | 透传 `<div>` 原生属性 <br/> Inherits all `<div>` attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](http://localhost:6006/?path=/docs/business-barcodedisplay--docs)。
