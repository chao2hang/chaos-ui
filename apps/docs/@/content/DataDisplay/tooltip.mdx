import { Tooltip, TooltipTrigger, TooltipContent, TooltipProvider } from "@chaos_team/chaos-ui/ui"
import { CodeBlock } from "@/components/code-block"

# Tooltip 文字提示

> 数据展示 / Components for presenting data and content.

鼠标悬停时出现的简单文本提示,用于解释按钮/图标含义。
Simple text bubble appearing on hover; used to explain button/icon meanings.

## 何时使用 / When to Use

鼠标悬停时展示补充说明信息,不占用常驻版面。

Reveal supplementary information on hover without occupying permanent space.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { Tooltip, TooltipProvider, TooltipTrigger, TooltipContent } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return (
    <TooltipProvider>
      <Tooltip>
        <TooltipTrigger>Hover me</TooltipTrigger>
        <TooltipContent>Tooltip content</TooltipContent>
      </Tooltip>
    </TooltipProvider>
  )
}`} lang="tsx" />


## API / Props

### Tooltip

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **复合组件**: 包含 3 个子组件(TooltipTrigger, TooltipContent, TooltipProvider),使用组合模式按需导入。
- **Base UI**: 基于 `@base-ui/react` 无头(headless)原语构建,无障碍(a11y)开箱即用。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-tooltip--docs)。
