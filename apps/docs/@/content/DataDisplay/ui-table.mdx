import { Table } from "@/components/ui/table";
import { CodeBlock } from "@/components/code-block";

# Table Ui Table

> 数据展示 / Components for presenting data and content.

Ui Table — Chaos UI 的数据展示。
Table — a components for presenting data and content. from Chaos UI.

## 何时使用 / When to Use

适用于Ui Table相关场景。

Use when you need a table.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { Table, TableHeader, TableBody, TableRow, TableHead, TableCell } from "@/components/ui/table"

export function Basic() {
  return (
    <Table>
      <TableHeader>
        <TableRow>
          <TableHead>Name</TableHead>
          <TableHead>Status</TableHead>
        </TableRow>
      </TableHeader>
      <TableBody>
        <TableRow>
          <TableCell>Item 1</TableCell>
          <TableCell>Active</TableCell>
        </TableRow>
      </TableBody>
    </Table>
  )
}`} lang="tsx" />

## API / Props

### Table

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **复合组件**: 包含 7 个子组件(TableHeader, TableBody, TableFooter, TableHead, TableRow…),使用组合模式按需导入。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-ui-table--docs)。
