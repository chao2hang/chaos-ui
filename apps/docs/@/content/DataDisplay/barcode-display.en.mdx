import { BarcodeDisplay } from "@chaos_team/chaos-ui/ui"
import { CodeBlock } from "@/components/code-block"

# BarcodeDisplay

> Components for presenting data and content.

BarcodeDisplay — a components for presenting data and content. from Chaos UI.

## When to Use

Use when you need a barcode display.


## Examples

### Basic Usage

<CodeBlock code={`import { BarcodeDisplay } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <BarcodeDisplay />
}`} lang="tsx" />


## API / Props

### BarcodeDisplay

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `value` | `string` | — | Text value to encode |
| `width` | `number` | `200` | Barcode width in pixels |
| `height` | `number` | `80` | Barcode height in pixels |
| `background` | `string` | `#ffffff` | Background color |
| `color` | `string` | `#000000` | Bar color |
| ...rest | `React.ComponentProps<"div">` | — | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


