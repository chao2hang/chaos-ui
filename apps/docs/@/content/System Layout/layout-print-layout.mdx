import { PrintLayout } from "@/components/layout/print-layout";
import { CodeBlock } from "@/components/code-block";

# PrintLayout Layout Print Layout

> 系统布局 / System-level layout shells for admin pages.

Layout Print Layout — 系统级布局外壳,用于后台/仪表盘/认证等整页布局。
PrintLayout — a system-level layout shell for admin/dashboard/auth pages.

## 何时使用 / When to Use

适用于Layout Print Layout相关场景。

Use when you need a print layout.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { PrintLayout } from "@/components/layout/print-layout"

export function Basic() {
  return <PrintLayout />
}`} lang="tsx" />

## API / Props

### PrintLayout

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **无障碍**: 组件遵循 WAI-ARIA 最佳实践。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-layout-print-layout--docs)。
