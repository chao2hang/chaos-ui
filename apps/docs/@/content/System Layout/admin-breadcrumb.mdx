import { AdminBreadcrumb } from "@/components/layout/admin-breadcrumb";
import { CodeBlock } from "@/components/code-block";

# AdminBreadcrumb 管理后台面包屑

> 系统布局 / System-level layout shells for admin pages.

管理后台面包屑 — 系统级布局外壳,用于后台/仪表盘/认证等整页布局。
AdminBreadcrumb — a system-level layout shell for admin/dashboard/auth pages.

## 何时使用 / When to Use

需要管理后台面包屑功能时使用本组件。

Use when you need an admin breadcrumb.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { AdminBreadcrumb } from "@/components/layout/admin-breadcrumb"

export function Basic() {
  return <AdminBreadcrumb />
}`} lang="tsx" />

## API / Props

### AdminBreadcrumb

| Prop        | Type                          | Default | 说明 / Description                                          |
| ----------- | ----------------------------- | ------- | ----------------------------------------------------------- |
| `className` | `string`                      | —       | 自定义样式类名 <br/> Custom CSS class                       |
| `items`     | `AdminBreadcrumbItem[]`       | —       | 选项/数据项列表 <br/> Items / data list                     |
| `separator` | `React.ReactNode`             | `/`     |                                                             |
| ...rest     | `React.ComponentProps<"div">` | —       | 透传 `<div>` 原生属性 <br/> Inherits all `<div>` attributes |

## 注意事项 / Notes

- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-admin-breadcrumb--docs)。
