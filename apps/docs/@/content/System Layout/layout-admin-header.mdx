import { AdminHeader } from "@/components/layout/admin-header";
import { CodeBlock } from "@/components/code-block";

# AdminHeader Layout Admin Header

> 系统布局 / System-level layout shells for admin pages.

Layout Admin Header — 系统级布局外壳,用于后台/仪表盘/认证等整页布局。
AdminHeader — a system-level layout shell for admin/dashboard/auth pages.

## 何时使用 / When to Use

适用于Layout Admin Header相关场景。

Use when you need an admin header.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { AdminHeader } from "@/components/layout/admin-header"

export function Basic() {
  return <AdminHeader />
}`} lang="tsx" />

## API / Props

### AdminHeader

| Prop        | Type                          | Default | 说明 / Description                                          |
| ----------- | ----------------------------- | ------- | ----------------------------------------------------------- |
| `className` | `string`                      | —       | 自定义样式类名 <br/> Custom CSS class                       |
| `logo`      | `React.ReactNode`             | —       |                                                             |
| `actions`   | `React.ReactNode`             | —       |                                                             |
| `userMenu`  | `React.ReactNode`             | —       |                                                             |
| ...rest     | `React.ComponentProps<"div">` | —       | 透传 `<div>` 原生属性 <br/> Inherits all `<div>` attributes |

## 注意事项 / Notes

- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-layout-admin-header--docs)。
