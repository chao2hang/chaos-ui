import { ChatLayout } from "@chaos_team/chaos-ui/layout";
import { CodeBlock } from "@/components/code-block";

# ChatLayout Chat Layout

> 系统布局 / System-level layout shells for admin pages.

Chat Layout — 系统级布局外壳,用于后台/仪表盘/认证等整页布局。
ChatLayout — a system-level layout shell for admin/dashboard/auth pages.

## 何时使用 / When to Use

适用于Chat Layout相关场景。

Use when you need a chat layout.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { ChatLayout } from "@chaos_team/chaos-ui/layout"

export function Basic() {
  return <ChatLayout />
}`} lang="tsx" />

## API / Props

### ChatLayout

| Prop           | Type                   | Default | 说明 / Description                                        |
| -------------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className`    | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `sidebar`      | `React.ReactNode`      | —       |                                                           |
| `messagesArea` | `React.ReactNode`      | —       |                                                           |
| `inputArea`    | `React.ReactNode`      | —       |                                                           |
| ...rest        | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/layouts-chatlayout--docs)。
