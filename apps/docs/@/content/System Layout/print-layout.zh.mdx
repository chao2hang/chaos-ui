import { PrintLayout } from "@chaos_team/chaos-ui/layout";
import { CodeBlock } from "@/components/code-block";

# PrintLayout 打印布局

> 系统布局

打印布局 — 系统级布局外壳,用于后台/仪表盘/认证等整页布局。

## 何时使用

当你需要「打印布局」相关能力时使用：打印布局 — 系统级布局外壳,用于后台/仪表盘/认证等整页布局。用于后台壳层与系统级布局（侧栏、页签、顶栏）；保证多页应用导航一致。

## 代码示例

### 基础用法

<CodeBlock code={`import { PrintLayout } from "@chaos_team/chaos-ui/layout"

export function Basic() {
  return <PrintLayout />
}`} lang="tsx" />

## API / Props

### PrintLayout

| Prop        | Type                   | Default | 说明              |
| ----------- | ---------------------- | ------- | ----------------- |
| `className` | `string`               | —       | 自定义样式类名    |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 |

## 注意事项

- **无障碍**: 组件遵循 WAI-ARIA 最佳实践。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](http://localhost:6006/?path=/docs/layouts-printlayout--docs)。
