import { RegionLayout } from "@/components/layout/region-layout";
import { CodeBlock } from "@/components/code-block";

# RegionLayout

> System-level layout shells for admin pages.

RegionLayout — a system-level layout shell for admin/dashboard/auth pages.

## When to Use

Use when you need a region layout.

## Examples

### Basic Usage

<CodeBlock code={`import { RegionLayout } from "@/components/layout/region-layout"

export function Basic() {
  return <RegionLayout />
}`} lang="tsx" />

## API / Props

### RegionLayout

| Prop           | Type                          | Default | Description                     |
| -------------- | ----------------------------- | ------- | ------------------------------- |
| `className`    | `string`                      | —       | Custom CSS class                |
| `header`       | `React.ReactNode`             | —       |                                 |
| `left`         | `React.ReactNode`             | —       |                                 |
| `right`        | `React.ReactNode`             | —       |                                 |
| `footer`       | `React.ReactNode`             | —       |                                 |
| `leftWidth`    | `string`                      | `240px` |                                 |
| `rightWidth`   | `string`                      | `240px` |                                 |
| `headerHeight` | `string`                      | `56px`  |                                 |
| `footerHeight` | `string`                      | `48px`  |                                 |
| ...rest        | `React.ComponentProps<"div">` | —       | Inherits all `<div>` attributes |

## Notes
