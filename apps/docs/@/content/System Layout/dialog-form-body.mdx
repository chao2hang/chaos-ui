import { DialogFormBody } from "@chaos_team/chaos-ui/layout"
import { CodeBlock } from "@/components/code-block"

# DialogFormBody 弹窗表单主体

> 系统布局 / System-level layout shells for admin pages.

弹窗表单主体 — 系统级布局外壳,用于后台/仪表盘/认证等整页布局。
DialogFormBody — a system-level layout shell for admin/dashboard/auth pages.

## 何时使用 / When to Use

需要弹窗表单主体功能时使用本组件。

Use when you need a dialog form body.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { DialogFormBody } from "@chaos_team/chaos-ui/layout"

export function Basic() {
  return <DialogFormBody />
}`} lang="tsx" />


## API / Props

### DialogFormBody

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| `scrolling` | `boolean` | `true` |  |
| ...rest | `React.ComponentProps<"div">` | — | 透传 `<div>` 原生属性 <br/> Inherits all `<div>` attributes |

## 注意事项 / Notes

- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/layouts-dialogformbody--docs)。
