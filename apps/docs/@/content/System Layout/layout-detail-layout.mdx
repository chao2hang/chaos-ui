import { DetailLayout } from "@/components/layout/detail-layout";
import { CodeBlock } from "@/components/code-block";

# DetailLayout Layout Detail Layout

> 系统布局 / System-level layout shells for admin pages.

Layout Detail Layout — 系统级布局外壳,用于后台/仪表盘/认证等整页布局。
DetailLayout — a system-level layout shell for admin/dashboard/auth pages.

## 何时使用 / When to Use

适用于Layout Detail Layout相关场景。

Use when you need a detail layout.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { DetailLayout } from "@/components/layout/detail-layout"

export function Basic() {
  return <DetailLayout />
}`} lang="tsx" />

## API / Props

### DetailLayout

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **无障碍**: 组件遵循 WAI-ARIA 最佳实践。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-layout-detail-layout--docs)。
