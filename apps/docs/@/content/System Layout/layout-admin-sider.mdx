import { AdminSider } from "@/components/layout/admin-sider";
import { CodeBlock } from "@/components/code-block";

# AdminSider Layout Admin Sider

> 系统布局 / System-level layout shells for admin pages.

Layout Admin Sider — 系统级布局外壳,用于后台/仪表盘/认证等整页布局。
AdminSider — a system-level layout shell for admin/dashboard/auth pages.

## 何时使用 / When to Use

适用于Layout Admin Sider相关场景。

Use when you need an admin sider.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { AdminSider } from "@/components/layout/admin-sider"

export function Basic() {
  return <AdminSider />
}`} lang="tsx" />

## API / Props

### AdminSider

| Prop        | Type                            | Default | 说明 / Description                                              |
| ----------- | ------------------------------- | ------- | --------------------------------------------------------------- |
| `className` | `string`                        | —       | 自定义样式类名 <br/> Custom CSS class                           |
| `items`     | `AdminSiderItem[]`              | —       | 选项/数据项列表 <br/> Items / data list                         |
| `activeKey` | `string`                        | —       |                                                                 |
| `onSelect`  | `() => void`                    | —       | 选中回调 <br/> Selection handler                                |
| ...rest     | `React.ComponentProps<"aside">` | —       | 透传 `<aside>` 原生属性 <br/> Inherits all `<aside>` attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **原生属性**: 继承所有 `<aside>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-layout-admin-sider--docs)。
