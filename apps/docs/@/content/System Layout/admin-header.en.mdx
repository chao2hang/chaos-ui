import { AdminHeader } from "@/components/layout/admin-header"
import { CodeBlock } from "@/components/code-block"

# AdminHeader

> System-level layout shells for admin pages.

AdminHeader — a system-level layout shell for admin/dashboard/auth pages.

## When to Use

Use when you need an admin header.


## Examples

### Basic Usage

<CodeBlock code={`import { AdminHeader } from "@/components/layout/admin-header"

export function Basic() {
  return <AdminHeader />
}`} lang="tsx" />


## API / Props

### AdminHeader

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `logo` | `React.ReactNode` | — |  |
| `actions` | `React.ReactNode` | — |  |
| `userMenu` | `React.ReactNode` | — |  |
| ...rest | `React.ComponentProps<"div">` | — | Inherits all `<div>` attributes |

## Notes



