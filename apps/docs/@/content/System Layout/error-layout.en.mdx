import { ErrorLayout } from "@chaos_team/chaos-ui/layout";
import { CodeBlock } from "@/components/code-block";

# ErrorLayout

> System-level layout shells for admin pages.

ErrorLayout — a system-level layout shell for admin/dashboard/auth pages.

## When to Use

Use ErrorLayout when you need: ErrorLayout — a system-level layout shell for admin/dashboard/auth pages. Use for admin shells (sider, tabs, header) to keep multi-page navigation consistent.

## Examples

### Basic Usage

<CodeBlock code={`import { ErrorLayout } from "@chaos_team/chaos-ui/layout"

export function Basic() {
  return <ErrorLayout />
}`} lang="tsx" />

## API / Props

### ErrorLayout

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **无障碍**: 组件遵循 WAI-ARIA 最佳实践。
