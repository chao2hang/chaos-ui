import { AuthLayout } from "@/components/layout/auth-layout";
import { CodeBlock } from "@/components/code-block";

# AuthLayout

> System-level layout shells for admin pages.

AuthLayout — a system-level layout shell for admin/dashboard/auth pages.

## When to Use

Use when you need an auth layout.

## Examples

### Basic Usage

<CodeBlock code={`import { AuthLayout } from "@/components/layout/auth-layout"

export function Basic() {
  return <AuthLayout />
}`} lang="tsx" />

## API / Props

### AuthLayout

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **无障碍**: 组件遵循 WAI-ARIA 最佳实践。
