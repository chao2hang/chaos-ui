import { NavigationTabsBar, AdminTabs, NavigationTabsBarTabItem, AdminTabItem } from "@/components/layout/admin-tabs"
import { CodeBlock } from "@/components/code-block"

# AdminTabs

> System-level layout shells for admin pages.

AdminTabs — a system-level layout shell for admin/dashboard/auth pages.

## When to Use

Use when you need an admin tabs.


## Examples

### Basic Usage

<CodeBlock code={`import { NavigationTabsBar, AdminTabs, NavigationTabsBarTabItem, AdminTabItem } from "@/components/layout/admin-tabs"

export function Basic() {
  return (
    <AdminTabs>
      <AdminTabs>Content</AdminTabs>
    </AdminTabs>
  )
}`} lang="tsx" />


## API / Props

### AdminTabs

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **复合组件**: 包含 2 个子组件(NavigationTabsBarTabItem, AdminTabItem),使用组合模式按需导入。


