import { PrintTemplateLayout } from "@chaos_team/chaos-ui/layout";
import { CodeBlock } from "@/components/code-block";

# PrintTemplateLayout Print Template Layout

> System-level layout shells for admin pages.

PrintTemplateLayout — a system-level layout shell for admin/dashboard/auth pages.

## When to Use

Use PrintTemplateLayout when you need: PrintTemplateLayout — a system-level layout shell for admin/dashboard/auth pages. Use for admin shells (sider, tabs, header) to keep multi-page navigation consistent.

## Examples

### Basic Usage

<CodeBlock code={`import { PrintTemplateLayout } from "@chaos_team/chaos-ui/layout"

export function Basic() {
  return <PrintTemplateLayout />
}`} lang="tsx" />

## API / Props

### PrintTemplateLayout

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| `title`     | `string`               | —       | Page title rendered as an `<h1>`. |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
