import { PrintTemplateLayout } from "@/components/layout/print-template-layout"
import { CodeBlock } from "@/components/code-block"

# PrintTemplateLayout Print Template Layout

> System-level layout shells for admin pages.

PrintTemplateLayout — a system-level layout shell for admin/dashboard/auth pages.

## When to Use

Use when you need a print template layout.


## Examples

### Basic Usage

<CodeBlock code={`import { PrintTemplateLayout } from "@/components/layout/print-template-layout"

export function Basic() {
  return <PrintTemplateLayout />
}`} lang="tsx" />


## API / Props

### PrintTemplateLayout

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `title` | `string` | — | Page title rendered as an `<h1>`. |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


