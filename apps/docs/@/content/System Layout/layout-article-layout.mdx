import { ArticleLayout } from "@/components/layout/article-layout";
import { CodeBlock } from "@/components/code-block";

# ArticleLayout Layout Article Layout

> 系统布局 / System-level layout shells for admin pages.

`tsx <ArticleLayout title={<h1>指南</h1>} toc={<Toc />}> <p>正文内容…</p> </ArticleLayout> `
ArticleLayout — layout article layout component.

## 何时使用 / When to Use

适用于Layout Article Layout相关场景。

Use when you need an article layout.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { ArticleLayout } from "@/components/layout/article-layout"

export function Basic() {
  return <ArticleLayout />
}`} lang="tsx" />

## API / Props

### ArticleLayout

| Prop        | Type                   | Default | 说明 / Description                                           |
| ----------- | ---------------------- | ------- | ------------------------------------------------------------ |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                        |
| `title`     | `React.ReactNode`      | —       | Optional article header title rendered inside an `<header>`. |
| `toc`       | `React.ReactNode`      | —       | Optional table-of-contents rendered as a sticky `<aside>`.   |
| `footer`    | `React.ReactNode`      | —       | Optional article footer.                                     |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes    |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-layout-article-layout--docs)。
