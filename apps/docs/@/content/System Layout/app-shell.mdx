import { AppShell } from "@/components/layout/app-shell"
import { CodeBlock } from "@/components/code-block"

# AppShell 应用外壳

> 系统布局 / System-level layout shells for admin pages.

应用外壳 — 系统级布局外壳,用于后台/仪表盘/认证等整页布局。
AppShell — a system-level layout shell for admin/dashboard/auth pages.

## 何时使用 / When to Use

应用外壳,提供顶部导航、侧边栏、主区域布局。

Application shell providing top nav, sidebar, and main area.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { AppShell } from "@/components/layout/app-shell"

export function Basic() {
  return <AppShell />
}`} lang="tsx" />


## API / Props

### AppShell

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| `header` | `React.ReactNode` | — |  |
| `sidebar` | `React.ReactNode` | — |  |
| `aside` | `React.ReactNode` | — |  |
| `footer` | `React.ReactNode` | — |  |
| `sidebarWidth` | `number` | `240` |  |
| `asideWidth` | `number` | `280` |  |
| `sidebarCollapsible` | `boolean` | `true` |  |
| `defaultCollapsed` | `boolean` | `false` |  |
| `variant` | `"default" \| "floating" \| "sticky"` | `default` | 样式变体 <br/> Style variant |
| ...rest | `React.ComponentProps<"div">` | — | 透传 `<div>` 原生属性 <br/> Inherits all `<div>` attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-app-shell--docs)。
