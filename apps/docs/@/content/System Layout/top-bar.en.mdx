import { TopBar, MegaMenu } from "@chaos_team/chaos-ui/layout";
import { CodeBlock } from "@/components/code-block";

# TopBar

> System-level layout shells for admin pages.

TopBar — a system-level layout shell for admin/dashboard/auth pages.

## When to Use

Use TopBar when you need: TopBar — a system-level layout shell for admin/dashboard/auth pages. Use for admin shells (sider, tabs, header) to keep multi-page navigation consistent.

## Examples

### Basic Usage

<CodeBlock code={`import { TopBar, MegaMenu } from "@chaos_team/chaos-ui/layout"

export function Basic() {
  return (
    <div className="space-y-4">
  <TopBar />
  <MegaMenu />
    </div>
  )
}`} lang="tsx" />

## API / Props

### TopBar

| Prop        | Type                                       | Default    | Description                        |
| ----------- | ------------------------------------------ | ---------- | ---------------------------------- |
| `className` | `string`                                   | —          | Custom CSS class                   |
| `logo`      | `React.ReactNode`                          | `Chaos UI` |                                    |
| `logoHref`  | `string`                                   | `/`        |                                    |
| `nav`       | `TopBarItem[]`                             | —          |                                    |
| `actions`   | `React.ReactNode`                          | —          |                                    |
| `sticky`    | `boolean`                                  | `true`     |                                    |
| `variant`   | `"default" \| "transparent" \| "bordered"` | `default`  | Style variant                      |
| ...rest     | `React.ComponentProps<"header">`           | —          | Inherits all `<header>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
