import { MasterDetailLayout } from "@/components/layout/master-detail-layout";
import { CodeBlock } from "@/components/code-block";

# MasterDetailLayout Layout Master Detail Layout

> 系统布局 / System-level layout shells for admin pages.

Layout Master Detail Layout — 系统级布局外壳,用于后台/仪表盘/认证等整页布局。
MasterDetailLayout — a system-level layout shell for admin/dashboard/auth pages.

## 何时使用 / When to Use

适用于Layout Master Detail Layout相关场景。

Use when you need a master detail layout.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { MasterDetailLayout } from "@/components/layout/master-detail-layout"

export function Basic() {
  return <MasterDetailLayout />
}`} lang="tsx" />

## API / Props

### MasterDetailLayout

| Prop                 | Type                          | Default | 说明 / Description                                          |
| -------------------- | ----------------------------- | ------- | ----------------------------------------------------------- |
| `className`          | `string`                      | —       | 自定义样式类名 <br/> Custom CSS class                       |
| `master`             | `React.ReactNode`             | —       |                                                             |
| `detail`             | `React.ReactNode`             | —       |                                                             |
| `masterWidth`        | `number`                      | —       |                                                             |
| `defaultMasterWidth` | `number`                      | `320`   |                                                             |
| `minMasterWidth`     | `number`                      | `240`   |                                                             |
| `maxMasterWidth`     | `number`                      | `480`   |                                                             |
| `resizable`          | `boolean`                     | `true`  |                                                             |
| ...rest              | `React.ComponentProps<"div">` | —       | 透传 `<div>` 原生属性 <br/> Inherits all `<div>` attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-layout-master-detail-layout--docs)。
