import { ChatLayout } from "@/components/layout/chat-layout"
import { CodeBlock } from "@/components/code-block"

# ChatLayout Chat Layout

> 系统布局

Chat Layout — 系统级布局外壳,用于后台/仪表盘/认证等整页布局。

## 何时使用

适用于Chat Layout相关场景。


## 代码示例

### 基础用法

<CodeBlock code={`import { ChatLayout } from "@/components/layout/chat-layout"

export function Basic() {
  return <ChatLayout />
}`} lang="tsx" />


## API / Props

### ChatLayout

| Prop | Type | Default | 说明 |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 |
| `sidebar` | `React.ReactNode` | — |  |
| `messagesArea` | `React.ReactNode` | — |  |
| `inputArea` | `React.ReactNode` | — |  |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/layouts-chatlayout--docs)。

