import { MasterDetailTabs } from "@chaos_team/chaos-ui/layout";
import { CodeBlock } from "@/components/code-block";

# MasterDetailTabs

> System-level layout shells for admin pages.

MasterDetailTabs — a system-level layout shell for admin/dashboard/auth pages.

## When to Use

Use MasterDetailTabs when you need: MasterDetailTabs — a system-level layout shell for admin/dashboard/auth pages. Use for admin shells (sider, tabs, header) to keep multi-page navigation consistent.

## Examples

### Basic Usage

<CodeBlock code={`import { MasterDetailTabs } from "@chaos_team/chaos-ui/layout"

export function Basic() {
  return <MasterDetailTabs />
}`} lang="tsx" />

## API / Props

### MasterDetailTabs

| Prop               | Type                          | Default | Description                     |
| ------------------ | ----------------------------- | ------- | ------------------------------- |
| `className`        | `string`                      | —       | Custom CSS class                |
| `items`            | `MasterDetailTabItem[]`       | —       | Items / data list               |
| `activeKey`        | `string`                      | —       |                                 |
| `defaultActiveKey` | `string`                      | —       |                                 |
| `onChange`         | `() => void`                  | —       | Callback when value changes     |
| ...rest            | `React.ComponentProps<"div">` | —       | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
