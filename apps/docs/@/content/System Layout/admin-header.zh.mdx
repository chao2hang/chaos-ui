import { AdminHeader } from "@/components/layout/admin-header";
import { CodeBlock } from "@/components/code-block";

# AdminHeader 管理后台头部

> 系统布局

管理后台头部 — 系统级布局外壳,用于后台/仪表盘/认证等整页布局。

## 何时使用

需要管理后台头部功能时使用本组件。

## 代码示例

### 基础用法

<CodeBlock code={`import { AdminHeader } from "@/components/layout/admin-header"

export function Basic() {
  return <AdminHeader />
}`} lang="tsx" />

## API / Props

### AdminHeader

| Prop        | Type                          | Default | 说明                  |
| ----------- | ----------------------------- | ------- | --------------------- |
| `className` | `string`                      | —       | 自定义样式类名        |
| `logo`      | `React.ReactNode`             | —       |                       |
| `actions`   | `React.ReactNode`             | —       |                       |
| `userMenu`  | `React.ReactNode`             | —       |                       |
| ...rest     | `React.ComponentProps<"div">` | —       | 透传 `<div>` 原生属性 |

## 注意事项

- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-admin-header--docs)。
