import { MasterDetailTabs } from "@chaos_team/chaos-ui/layout";
import { CodeBlock } from "@/components/code-block";

# MasterDetailTabs 主次标签布局

> 系统布局

主次标签布局 — 系统级布局外壳,用于后台/仪表盘/认证等整页布局。

## 何时使用

当你需要「主次标签布局」相关能力时使用：主次标签布局 — 系统级布局外壳,用于后台/仪表盘/认证等整页布局。用于后台壳层与系统级布局（侧栏、页签、顶栏）；保证多页应用导航一致。

## 代码示例

### 基础用法

<CodeBlock code={`import { MasterDetailTabs } from "@chaos_team/chaos-ui/layout"

export function Basic() {
  return <MasterDetailTabs />
}`} lang="tsx" />

## API / Props

### MasterDetailTabs

| Prop               | Type                          | Default | 说明                  |
| ------------------ | ----------------------------- | ------- | --------------------- |
| `className`        | `string`                      | —       | 自定义样式类名        |
| `items`            | `MasterDetailTabItem[]`       | —       | 选项/数据项列表       |
| `activeKey`        | `string`                      | —       |                       |
| `defaultActiveKey` | `string`                      | —       |                       |
| `onChange`         | `() => void`                  | —       | 值变化回调            |
| ...rest            | `React.ComponentProps<"div">` | —       | 透传 `<div>` 原生属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](http://localhost:6006/?path=/docs/layouts-masterdetailtabs--docs)。
