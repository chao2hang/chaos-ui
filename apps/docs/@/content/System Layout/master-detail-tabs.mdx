import { MasterDetailTabs } from "@chaos_team/chaos-ui/layout";
import { CodeBlock } from "@/components/code-block";

# MasterDetailTabs 主次标签布局

> 系统布局 / System-level layout shells for admin pages.

主次标签布局 — 系统级布局外壳,用于后台/仪表盘/认证等整页布局。
MasterDetailTabs — a system-level layout shell for admin/dashboard/auth pages.

## 何时使用 / When to Use

需要主次标签布局功能时使用本组件。

Use when you need a master detail tabs.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { MasterDetailTabs } from "@chaos_team/chaos-ui/layout"

export function Basic() {
  return <MasterDetailTabs />
}`} lang="tsx" />

## API / Props

### MasterDetailTabs

| Prop               | Type                          | Default | 说明 / Description                                          |
| ------------------ | ----------------------------- | ------- | ----------------------------------------------------------- |
| `className`        | `string`                      | —       | 自定义样式类名 <br/> Custom CSS class                       |
| `items`            | `MasterDetailTabItem[]`       | —       | 选项/数据项列表 <br/> Items / data list                     |
| `activeKey`        | `string`                      | —       |                                                             |
| `defaultActiveKey` | `string`                      | —       |                                                             |
| `onChange`         | `() => void`                  | —       | 值变化回调 <br/> Callback when value changes                |
| ...rest            | `React.ComponentProps<"div">` | —       | 透传 `<div>` 原生属性 <br/> Inherits all `<div>` attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/layouts-masterdetailtabs--docs)。
