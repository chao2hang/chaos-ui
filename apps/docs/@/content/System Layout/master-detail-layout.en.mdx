import { MasterDetailLayout } from "@chaos_team/chaos-ui/layout";
import { CodeBlock } from "@/components/code-block";

# MasterDetailLayout

> System-level layout shells for admin pages.

MasterDetailLayout — a system-level layout shell for admin/dashboard/auth pages.

## When to Use

Use MasterDetailLayout when you need: MasterDetailLayout — a system-level layout shell for admin/dashboard/auth pages. Use for admin shells (sider, tabs, header) to keep multi-page navigation consistent.

## Examples

### Basic Usage

<CodeBlock code={`import { MasterDetailLayout } from "@chaos_team/chaos-ui/layout"

export function Basic() {
  return <MasterDetailLayout />
}`} lang="tsx" />

## API / Props

### MasterDetailLayout

| Prop                 | Type                          | Default | Description                     |
| -------------------- | ----------------------------- | ------- | ------------------------------- |
| `className`          | `string`                      | —       | Custom CSS class                |
| `master`             | `React.ReactNode`             | —       |                                 |
| `detail`             | `React.ReactNode`             | —       |                                 |
| `masterWidth`        | `number`                      | —       |                                 |
| `defaultMasterWidth` | `number`                      | `320`   |                                 |
| `minMasterWidth`     | `number`                      | `240`   |                                 |
| `maxMasterWidth`     | `number`                      | `480`   |                                 |
| `resizable`          | `boolean`                     | `true`  |                                 |
| ...rest              | `React.ComponentProps<"div">` | —       | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
