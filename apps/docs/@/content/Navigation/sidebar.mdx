import { Sidebar } from "@/components/ui/sidebar";
import { CodeBlock } from "@/components/code-block";

# Sidebar 侧边栏

> 导航组件 / Navigation components for moving through the app.

侧边栏 — Chaos UI 的导航组件。
Sidebar — a navigation components for moving through the app. from Chaos UI.

## 何时使用 / When to Use

需要侧边栏功能时使用本组件。

Use when you need a sidebar.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { Sidebar, SidebarContent, SidebarFooter, SidebarGroup, SidebarGroupAction, SidebarGroupContent } from "@/components/ui/sidebar"

export function Basic() {
  return (
    <Sidebar>
      <SidebarContent>Content</SidebarContent>
    </Sidebar>
  )
}`} lang="tsx" />

### 变体 / Variants

<CodeBlock
  code={`{/* 展示 variant 变体 */}
<div className="flex flex-wrap items-center gap-3">
  <Sidebar variant="default">Default</Sidebar>
  <Sidebar variant="outline">Outline</Sidebar>
</div>`}
  lang="tsx"
/>

### 尺寸 / Sizes

<CodeBlock
  code={`{/* 尺寸对照 */}
<div className="flex flex-wrap items-center gap-3">
  <Sidebar size="sm">Sm</Sidebar>
  <Sidebar size="default">Default</Sidebar>
  <Sidebar size="lg">Lg</Sidebar>
</div>`}
  lang="tsx"
/>

## 样式变体 / Style Variants

**变体 / Variant**

| Variant   | 说明 / Description        | 默认 |
| --------- | ------------------------- | ---- |
| `default` | 默认样式 / Default style  | ✓    |
| `outline` | 描边样式 / Outlined style |      |

**尺寸 / Size**

| Size      | 说明 / Description      | 默认 |
| --------- | ----------------------- | ---- |
| `default` | 默认尺寸 / Default size | ✓    |
| `sm`      | 小号 / Small            |      |
| `lg`      | 大号 / Large            |      |

## API / Props

### Sidebar

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **CVA 变体**: 通过 `class-variance-authority` 管理样式变体,类型安全。变体对象可导出复用。
- **复合组件**: 包含 22 个子组件(SidebarContent, SidebarFooter, SidebarGroup, SidebarGroupAction, SidebarGroupContent…),使用组合模式按需导入。
- **Base UI**: 基于 `@base-ui/react` 无头(headless)原语构建,无障碍(a11y)开箱即用。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-sidebar--docs)。
