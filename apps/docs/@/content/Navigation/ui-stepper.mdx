import { Stepper, Step } from "@/components/ui/stepper";
import { CodeBlock } from "@/components/code-block";

# Stepper Ui Stepper

> 导航组件 / Navigation components for moving through the app.

Ui Stepper — Chaos UI 的导航组件。
Stepper — a navigation components for moving through the app. from Chaos UI.

## 何时使用 / When to Use

适用于Ui Stepper相关场景。

Use when you need a stepper.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { Stepper } from "@/components/ui/stepper"

export function Basic() {
  return <Stepper>Default</Stepper>
}`} lang="tsx" />

## API / Props

### Stepper

| Prop          | Type                         | Default      | 说明 / Description                                        |
| ------------- | ---------------------------- | ------------ | --------------------------------------------------------- |
| `className`   | `string`                     | —            | 自定义样式类名 <br/> Custom CSS class                     |
| `activeStep`  | `number`                     | `0`          |                                                           |
| `orientation` | `"horizontal" \| "vertical"` | `horizontal` | 排列方向 <br/> Orientation                                |
| ...rest       | `React.HTMLAttributes`       | —            | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **无障碍**: 组件遵循 WAI-ARIA 最佳实践。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-ui-stepper--docs)。
