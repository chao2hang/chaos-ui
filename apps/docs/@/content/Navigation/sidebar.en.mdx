import { Sidebar } from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# Sidebar

> Navigation components for moving through the app.

Sidebar — a navigation components for moving through the app. from Chaos UI.

## When to Use

Use Sidebar when you need: Sidebar — a navigation components for moving through the app. from Chaos UI. Use for moving between pages/sections; keep hierarchy clear and avoid navigation clutter.

## Examples

### Basic Usage

<CodeBlock code={`import { Sidebar, SidebarContent, SidebarFooter, SidebarGroup, SidebarGroupAction, SidebarGroupContent } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return (
    <Sidebar>
      <SidebarContent>Content</SidebarContent>
    </Sidebar>
  )
}`} lang="tsx" />

### Variants

<CodeBlock
  code={`{/* 展示 variant 变体 */}
<div className="flex flex-wrap items-center gap-3">
  <Sidebar variant="default">Default</Sidebar>
  <Sidebar variant="outline">Outline</Sidebar>
</div>`}
  lang="tsx"
/>

### Sizes

<CodeBlock
  code={`{/* 尺寸对照 */}
<div className="flex flex-wrap items-center gap-3">
  <Sidebar size="sm">Sm</Sidebar>
  <Sidebar size="default">Default</Sidebar>
  <Sidebar size="lg">Lg</Sidebar>
</div>`}
  lang="tsx"
/>

## Style Variants

**Variant**

| Variant   | Description    | 默认 |
| --------- | -------------- | ---- |
| `default` | Default style  | ✓    |
| `outline` | Outlined style |      |

**Size**

| Size      | Description  | 默认 |
| --------- | ------------ | ---- |
| `default` | Default size | ✓    |
| `sm`      | Small        |      |
| `lg`      | Large        |      |

## API / Props

### Sidebar

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **复合组件**: 包含 22 个子组件(SidebarContent, SidebarFooter, SidebarGroup, SidebarGroupAction, SidebarGroupContent…),使用组合模式按需导入。
- **Base UI**: 基于 `@base-ui/react` 无头(headless)原语构建,无障碍(a11y)开箱即用。
