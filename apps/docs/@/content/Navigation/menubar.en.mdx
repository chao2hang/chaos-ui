import { Menubar } from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# Menubar

> Navigation components for moving through the app.

Horizontal menu bar providing grouped dropdown menus.

## When to Use

Use Menubar when you need: Horizontal menu bar providing grouped dropdown menus. Use for moving between pages/sections; keep hierarchy clear and avoid navigation clutter.

## Examples

### Basic Usage

<CodeBlock code={`import { Menubar } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <Menubar />
}`} lang="tsx" />

## API / Props

### Menubar

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **Base UI**: 基于 `@base-ui/react` 无头(headless)原语构建,无障碍(a11y)开箱即用。
