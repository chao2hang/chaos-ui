import { ContextMenu } from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# ContextMenu 右键菜单

> 导航组件 / Navigation components for moving through the app.

右键触发的上下文操作菜单。
Right-click triggered context action menu.

## 何时使用 / When to Use

需要右键菜单功能时使用本组件。

Use when you need a context menu.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { ContextMenu, ContextMenuCheckboxItem, ContextMenuContent, ContextMenuGroup, ContextMenuItem, ContextMenuLabel } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return (
    <ContextMenu>
      <ContextMenuContent>Content</ContextMenuContent>
    </ContextMenu>
  )
}`} lang="tsx" />

## API / Props

### ContextMenu

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **复合组件**: 包含 13 个子组件(ContextMenuCheckboxItem, ContextMenuContent, ContextMenuGroup, ContextMenuItem, ContextMenuLabel…),使用组合模式按需导入。
- **Base UI**: 基于 `@base-ui/react` 无头(headless)原语构建,无障碍(a11y)开箱即用。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-contextmenu--docs)。
