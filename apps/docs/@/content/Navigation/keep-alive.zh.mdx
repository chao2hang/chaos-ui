import { KeepAlive } from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# KeepAlive 页面缓存

> 导航组件

页面缓存 — 页面缓存组件，跨标签/路由切换保持状态。

## 何时使用

当你需要「页面缓存」相关能力时使用：页面缓存组件，跨标签/路由切换保持状态。用于页面/模块间跳转与定位；保持信息层级清晰，避免同页过多导航入口。

## 代码示例

### 基础用法

<CodeBlock code={`import { KeepAlive } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <KeepAlive />
}`} lang="tsx" />

## API / Props

### KeepAlive

| Prop        | Type                   | Default | 说明              |
| ----------- | ---------------------- | ------- | ----------------- |
| `className` | `string`               | —       | 自定义样式类名    |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](http://localhost:6006/?path=/docs/components-keepalive--docs)。
