import { KeyboardShortcutsManager } from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# KeyboardShortcutsManager

> Navigation components for page routing and orientation.

KeyboardShortcutsManager — Global keyboard shortcuts manager with registration and conflict detection.

## When to Use

Use KeyboardShortcutsManager when you need: Global keyboard shortcuts manager with registration and conflict detection. Use for moving between pages/sections; keep hierarchy clear and avoid navigation clutter.

## Examples

### Basic Usage

<CodeBlock code={`import { KeyboardShortcutsManager } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <KeyboardShortcutsManager />
}`} lang="tsx" />

## API / Props

### KeyboardShortcutsManager

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
