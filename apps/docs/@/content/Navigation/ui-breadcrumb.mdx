import { Breadcrumb } from "@/components/ui/breadcrumb";
import { CodeBlock } from "@/components/code-block";

# Breadcrumb Ui Breadcrumb

> 导航组件 / Navigation components for moving through the app.

Ui Breadcrumb — Chaos UI 的导航组件。
Breadcrumb — a navigation components for moving through the app. from Chaos UI.

## 何时使用 / When to Use

适用于Ui Breadcrumb相关场景。

Use when you need a breadcrumb.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { Breadcrumb, BreadcrumbList, BreadcrumbItem, BreadcrumbLink, BreadcrumbPage, BreadcrumbSeparator } from "@/components/ui/breadcrumb"

export function Basic() {
  return (
    <Breadcrumb>
      <BreadcrumbList>Content</BreadcrumbList>
    </Breadcrumb>
  )
}`} lang="tsx" />

## API / Props

### Breadcrumb

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **复合组件**: 包含 6 个子组件(BreadcrumbList, BreadcrumbItem, BreadcrumbLink, BreadcrumbPage, BreadcrumbSeparator…),使用组合模式按需导入。
- **Base UI**: 基于 `@base-ui/react` 无头(headless)原语构建,无障碍(a11y)开箱即用。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-ui-breadcrumb--docs)。
