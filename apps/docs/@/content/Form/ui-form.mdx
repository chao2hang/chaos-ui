import { Form, FormItem, FormField } from "@/components/ui/form";
import { CodeBlock } from "@/components/code-block";

# Form Ui Form

> 表单控件 / Form controls for user input and data submission.

Ui Form — Chaos UI 的表单控件。
Form — a form controls for user input and data submission. from Chaos UI.

## 何时使用 / When to Use

适用于Ui Form相关场景。

Use when you need a form.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { Form, FormItem, FormField } from "@/components/ui/form"

export function Basic() {
  return (
    <Form>
      <FormItem>Content</FormItem>
    </Form>
  )
}`} lang="tsx" />

## 样式变体 / Style Variants

**layout / layout**

| layout       | 说明 / Description | 默认 |
| ------------ | ------------------ | ---- |
| `vertical`   |                    | ✓    |
| `horizontal` |                    |      |
| `inline`     |                    |      |

## API / Props

### Form

| Prop        | Type                           | Default | 说明 / Description                                            |
| ----------- | ------------------------------ | ------- | ------------------------------------------------------------- |
| `className` | `string`                       | —       | 自定义样式类名 <br/> Custom CSS class                         |
| `onSubmit`  | `() => void`                   | —       | Called on submit with form data                               |
| `errors`    | `Record<string, string>`       | —       | Validation errors map (field name -> error message)           |
| ...rest     | `React.ComponentProps<"form">` | —       | 透传 `<form>` 原生属性 <br/> Inherits all `<form>` attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **CVA 变体**: 通过 `class-variance-authority` 管理样式变体,类型安全。变体对象可导出复用。
- **复合组件**: 包含 2 个子组件(FormItem, FormField),使用组合模式按需导入。
- **原生属性**: 继承所有 `<form>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-ui-form--docs)。
