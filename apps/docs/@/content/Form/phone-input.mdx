import { PhoneInput } from "@/components/ui/phone-input"
import { CodeBlock } from "@/components/code-block"

# PhoneInput 电话输入

> 表单组件 / Form components for data entry and collection.

电话输入 — 国际电话号码输入，支持区号选择。
PhoneInput — International phone number input with country code selector.

## 何时使用 / When to Use

需要电话输入功能时使用本组件。

Use when you need a phoneinput.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { PhoneInput } from "@/components/ui/phone-input"

export function Basic() {
  return <PhoneInput />
}`} lang="tsx" />


## API / Props

### PhoneInput

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-phoneinput--docs)。
