import { PasswordInput } from "@/components/ui/password-input"
import { CodeBlock } from "@/components/code-block"

# PasswordInput 密码输入

> 表单组件 / Form components for data entry and collection.

密码输入 — 密码输入框，支持显示/隐藏切换与密码强度指示器。
PasswordInput — Password input with show/hide toggle and optional strength meter.

## 何时使用 / When to Use

需要密码输入功能时使用本组件。

Use when you need a passwordinput.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { PasswordInput } from "@/components/ui/password-input"

export function Basic() {
  return <PasswordInput />
}`} lang="tsx" />


## API / Props

### PasswordInput

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-passwordinput--docs)。
