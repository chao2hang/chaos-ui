import { TimePicker } from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# TimePicker 时间选择器

> 表单控件 / Form controls for user input and data submission.

时间选择器 — Chaos UI 的表单控件。
TimePicker — a form controls for user input and data submission. from Chaos UI.

## 何时使用 / When to Use

当你需要「时间选择器」相关能力时使用：时间选择器 — Chaos UI 的表单控件。用于采集或编辑业务数据；需要校验、提交、草稿时优先用表单控件族。

Use TimePicker when you need: TimePicker — a form controls for user input and data submission. from Chaos UI. Use to collect or edit data with validation and submit flows.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { TimePicker } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <TimePicker />
}`} lang="tsx" />

### 状态 / States

<CodeBlock
  code={`<TimePicker>Normal</TimePicker>
<TimePicker disabled>Disabled</TimePicker>`}
  lang="tsx"
/>

## API / Props

### TimePicker

| Prop          | Type                             | Default    | 说明 / Description                                        |
| ------------- | -------------------------------- | ---------- | --------------------------------------------------------- |
| `className`   | `string`                         | —          | 自定义样式类名 <br/> Custom CSS class                     |
| `value`       | `string`                         | —          | 当前值(受控) <br/> Current value (controlled)             |
| `onChange`    | `() => void`                     | —          | 值变化回调 <br/> Callback when value changes              |
| `step`        | `1 \| 5 \| 10 \| 15 \| 30 \| 60` | `1`        | 步长 <br/> Step value                                     |
| `format`      | `"12h" \| "24h"`                 | `24h`      |                                                           |
| `disabled`    | `boolean`                        | —          | 是否禁用 <br/> Whether disabled                           |
| `placeholder` | `string`                         | `选择时间` | 占位文本 <br/> Placeholder text                           |
| ...rest       | `React.HTMLAttributes`           | —          | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](http://localhost:6006/?path=/docs/business-timepicker--docs)。
