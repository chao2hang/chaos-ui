import { AmountInput } from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# AmountInput

> Form components for data entry and collection.

AmountInput — Currency amount input with thousand separators and precision control.

## When to Use

Use AmountInput when you need: Currency amount input with thousand separators and precision control. Use to collect or edit data with validation and submit flows.

## Examples

### Basic Usage

<CodeBlock code={`import { AmountInput } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <AmountInput />
}`} lang="tsx" />

## API / Props

### AmountInput

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
