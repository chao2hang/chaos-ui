import { Slider } from "@/components/ui/slider";
import { CodeBlock } from "@/components/code-block";

# Slider Ui Slider

> 表单控件 / Form controls for user input and data submission.

Ui Slider — Chaos UI 的表单控件。
Slider — a form controls for user input and data submission. from Chaos UI.

## 何时使用 / When to Use

适用于Ui Slider相关场景。

Use when you need a slider.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { Slider, SliderControl, SliderTrack, SliderIndicator, SliderThumb } from "@/components/ui/slider"

export function Basic() {
  return (
    <Slider>
      <SliderControl>Content</SliderControl>
    </Slider>
  )
}`} lang="tsx" />

## API / Props

### Slider

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **复合组件**: 包含 4 个子组件(SliderControl, SliderTrack, SliderIndicator, SliderThumb),使用组合模式按需导入。
- **Base UI**: 基于 `@base-ui/react` 无头(headless)原语构建,无障碍(a11y)开箱即用。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-ui-slider--docs)。
