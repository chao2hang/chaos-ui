import { PasswordPolicyValidator } from "@chaos_team/chaos-ui/ui"
import { CodeBlock } from "@/components/code-block"

# PasswordPolicyValidator 密码策略校验

> 表单组件 / Form components for data entry and collection.

密码策略校验 — 实时密码策略校验器，带清单 UI 与强度条。
PasswordPolicyValidator — Real-time password policy checker with checklist UI and score bar.

## 何时使用 / When to Use

需要密码策略校验功能时使用本组件。

Use when you need a passwordpolicyvalidator.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { PasswordPolicyValidator } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <PasswordPolicyValidator />
}`} lang="tsx" />


## API / Props

### PasswordPolicyValidator

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-passwordpolicyvalidator--docs)。
