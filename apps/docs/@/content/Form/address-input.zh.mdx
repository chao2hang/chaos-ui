import { AddressInput } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# AddressInput 结构化地址

> 表单组件

结构化地址 — 结构化地址输入，省/市/区+街道+详细+经纬度。

## 何时使用

当你需要「结构化地址」相关能力时使用：结构化地址输入，省/市/区+街道+详细+经纬度。用于采集或编辑业务数据；需要校验、提交、草稿时优先用表单控件族。

## 代码示例

### 基础用法

<CodeBlock code={`import { AddressInput } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <AddressInput />
}`} lang="tsx" />

## API / Props

### AddressInput

| Prop        | Type                   | Default | 说明              |
| ----------- | ---------------------- | ------- | ----------------- |
| `className` | `string`               | —       | 自定义样式类名    |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](http://localhost:6006/?path=/docs/components-addressinput--docs)。
