import { RangeSlider } from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# RangeSlider

> Form components for data entry and collection.

RangeSlider — Dual-handle range slider for selecting min and max values.

## When to Use

Use RangeSlider when you need: Dual-handle range slider for selecting min and max values. Use to collect or edit data with validation and submit flows.

## Examples

### Basic Usage

<CodeBlock code={`import { RangeSlider } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <RangeSlider />
}`} lang="tsx" />

## API / Props

### RangeSlider

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
