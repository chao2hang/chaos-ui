import { DateRangePicker } from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# DateRangePicker 日期范围选择

> 表单组件 / Form components for data entry and collection.

日期范围选择 — 日期范围选择器，支持起止日期选择。
DateRangePicker — Date range picker with start and end date selection.

## 何时使用 / When to Use

需要日期范围选择器时使用本组件。用于 ERP/业务域场景（单据、选择器、审批、对账等）；按业务语义接入而不是当纯 UI 积木。

Use DateRangePicker when you need: DateRangePicker: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { DateRangePicker } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <DateRangePicker />
}`} lang="tsx" />

## API / Props

### DateRangePicker

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](http://localhost:6006/?path=/docs/components-daterangepicker--docs)。
