import { FormSection } from "@/components/ui/form-section";
import { CodeBlock } from "@/components/code-block";

# FormSection 表单分区

> 表单控件 / Form controls for user input and data submission.

表单分区 — Chaos UI 的表单控件。
FormSection — a form controls for user input and data submission. from Chaos UI.

## 何时使用 / When to Use

需要表单分区功能时使用本组件。

Use when you need a form section.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { FormSection } from "@/components/ui/form-section"

export function Basic() {
  return <FormSection />
}`} lang="tsx" />

### 变体 / Variants

<CodeBlock
  code={`{/* 展示 variant 变体 */}
<div className="flex flex-wrap items-center gap-3">
  <FormSection variant="default">Default</FormSection>
  <FormSection variant="elevated">Elevated</FormSection>
  <FormSection variant="flat">Flat</FormSection>
</div>`}
  lang="tsx"
/>

## 样式变体 / Style Variants

**变体 / Variant**

| Variant    | 说明 / Description       | 默认 |
| ---------- | ------------------------ | ---- |
| `default`  | 默认样式 / Default style | ✓    |
| `elevated` |                          |      |
| `flat`     |                          |      |

## API / Props

### FormSection

| Prop               | Type                   | Default | 说明 / Description                                        |
| ------------------ | ---------------------- | ------- | --------------------------------------------------------- |
| `className`        | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `title`            | `string`               | —       | 标题 <br/> Title                                          |
| `description`      | `string`               | —       | 描述文本 <br/> Description text                           |
| `collapsible`      | `boolean`              | `false` |                                                           |
| `defaultCollapsed` | `boolean`              | `false` |                                                           |
| `required`         | `boolean`              | —       | 是否必填 <br/> Whether required                           |
| `extra`            | `React.ReactNode`      | —       |                                                           |
| ...rest            | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **CVA 变体**: 通过 `class-variance-authority` 管理样式变体,类型安全。变体对象可导出复用。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-form-section--docs)。
