import { PasswordPolicyValidator } from "@/components/ui/password-policy-validator"
import { CodeBlock } from "@/components/code-block"

# PasswordPolicyValidator

> Form components for data entry and collection.

PasswordPolicyValidator — Real-time password policy checker with checklist UI and score bar.

## When to Use

Use when you need a passwordpolicyvalidator.


## Examples

### Basic Usage

<CodeBlock code={`import { PasswordPolicyValidator } from "@/components/ui/password-policy-validator"

export function Basic() {
  return <PasswordPolicyValidator />
}`} lang="tsx" />


## API / Props

### PasswordPolicyValidator

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


