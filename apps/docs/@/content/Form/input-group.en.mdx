import { InputGroup } from "@chaos_team/chaos-ui/ui"
import { CodeBlock } from "@/components/code-block"

# InputGroup

> Form controls for user input and data submission.

InputGroup — a form controls for user input and data submission. from Chaos UI.

## When to Use

Use when you need an input group.


## Examples

### Basic Usage

<CodeBlock code={`import { InputGroup, InputGroupAddon, InputGroupButton, InputGroupText, InputGroupInput, InputGroupTextarea } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return (
    <InputGroup>
      <InputGroupAddon>Content</InputGroupAddon>
    </InputGroup>
  )
}`} lang="tsx" />

### Sizes

<CodeBlock code={`{/* 尺寸对照 */}
<div className="flex flex-wrap items-center gap-3">
  <InputGroup size="xs">Xs</InputGroup>
  <InputGroup size="sm">Sm</InputGroup>
</div>`} lang="tsx" />

### States

<CodeBlock code={`<InputGroup>Normal</InputGroup>
<InputGroup disabled>Disabled</InputGroup>`} lang="tsx" />


## Style Variants

**align**

| align | Description | 默认 |
|---|---|---|
| `inline-start` |  | ✓ |
| `inline-end` |  |  |
| `block-start` |  |  |
| `block-end` |  |  |

**Size**

| Size | Description | 默认 |
|---|---|---|
| `xs` | Extra small | ✓ |
| `sm` | Small |  |
| `icon-xs` | Extra small icon |  |
| `icon-sm` | Small icon |  |

## API / Props

### InputGroup

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **复合组件**: 包含 5 个子组件(InputGroupAddon, InputGroupButton, InputGroupText, InputGroupInput, InputGroupTextarea),使用组合模式按需导入。


