import { FormGrid, FormGridItem } from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# FormGrid 表单栅格

> 表单控件 / Form controls for user input and data submission.

表单栅格 — Chaos UI 的表单控件。
FormGrid — a form controls for user input and data submission. from Chaos UI.

## 何时使用 / When to Use

当你需要「表单栅格」相关能力时使用：表单栅格 — Chaos UI 的表单控件。用于采集或编辑业务数据；需要校验、提交、草稿时优先用表单控件族。

Use FormGrid when you need: FormGrid — a form controls for user input and data submission. from Chaos UI. Use to collect or edit data with validation and submit flows.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { FormGrid, FormGridItem } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return (
    <FormGrid>
      <FormGridItem>Content</FormGridItem>
    </FormGrid>
  )
}`} lang="tsx" />

## 样式变体 / Style Variants

**columns / columns**

| columns | 说明 / Description | 默认 |
| ------- | ------------------ | ---- |
| `1`     |                    | ✓    |
| `2`     |                    |      |
| `3`     |                    |      |
| `4`     |                    |      |

**gap / gap**

| gap       | 说明 / Description | 默认 |
| --------- | ------------------ | ---- |
| `sm`      |                    |      |
| `default` |                    | ✓    |
| `lg`      |                    |      |

## API / Props

### FormGrid

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **CVA 变体**: 通过 `class-variance-authority` 管理样式变体,类型安全。变体对象可导出复用。
- **复合组件**: 包含 1 个子组件(FormGridItem),使用组合模式按需导入。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](http://localhost:6006/?path=/docs/components-formgrid--docs)。
