import { FormGrid, FormGridItem } from "@/components/ui/form-grid"
import { CodeBlock } from "@/components/code-block"

# FormGrid 表单栅格

> 表单控件 / Form controls for user input and data submission.

表单栅格 — Chaos UI 的表单控件。
FormGrid — a form controls for user input and data submission. from Chaos UI.

## 何时使用 / When to Use

需要表单栅格功能时使用本组件。

Use when you need a form grid.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { FormGrid, FormGridItem } from "@/components/ui/form-grid"

export function Basic() {
  return (
    <FormGrid>
      <FormGridItem>Content</FormGridItem>
    </FormGrid>
  )
}`} lang="tsx" />


## 样式变体 / Style Variants

**columns / columns**

| columns | 说明 / Description | 默认 |
|---|---|---|
| `1` |  | ✓ |
| `2` |  |  |
| `3` |  |  |
| `4` |  |  |

**gap / gap**

| gap | 说明 / Description | 默认 |
|---|---|---|
| `sm` |  |  |
| `default` |  | ✓ |
| `lg` |  |  |

## API / Props

### FormGrid

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **CVA 变体**: 通过 `class-variance-authority` 管理样式变体,类型安全。变体对象可导出复用。
- **复合组件**: 包含 1 个子组件(FormGridItem),使用组合模式按需导入。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-form-grid--docs)。
