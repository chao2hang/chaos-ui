import { ColorPicker } from "@/components/ui/color-picker";
import { CodeBlock } from "@/components/code-block";

# ColorPicker Ui Color Picker

> 表单控件 / Form controls for user input and data submission.

Ui Color Picker — Chaos UI 的表单控件。
ColorPicker — a form controls for user input and data submission. from Chaos UI.

## 何时使用 / When to Use

适用于Ui Color Picker相关场景。

Use when you need a color picker.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { ColorPicker } from "@/components/ui/color-picker"

export function Basic() {
  return <ColorPicker />
}`} lang="tsx" />

## API / Props

### ColorPicker

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-ui-color-picker--docs)。
