import { ToggleGroup, ToggleGroupItem } from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# ToggleGroup 开关组

> 表单控件 / Form controls for user input and data submission.

开关组 — Chaos UI 的表单控件。
ToggleGroup — a form controls for user input and data submission. from Chaos UI.

## 何时使用 / When to Use

当你需要「开关组」相关能力时使用：开关组 — Chaos UI 的表单控件。用于采集或编辑业务数据；需要校验、提交、草稿时优先用表单控件族。

Use ToggleGroup when you need: ToggleGroup — a form controls for user input and data submission. from Chaos UI. Use to collect or edit data with validation and submit flows.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { ToggleGroup, ToggleGroupItem } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return (
    <ToggleGroup>
      <ToggleGroupItem>Content</ToggleGroupItem>
    </ToggleGroup>
  )
}`} lang="tsx" />

### 状态 / States

<CodeBlock
  code={`<ToggleGroup>Normal</ToggleGroup>
<ToggleGroup disabled>Disabled</ToggleGroup>`}
  lang="tsx"
/>

## API / Props

### ToggleGroup

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **复合组件**: 包含 1 个子组件(ToggleGroupItem),使用组合模式按需导入。
- **Base UI**: 基于 `@base-ui/react` 无头(headless)原语构建,无障碍(a11y)开箱即用。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](http://localhost:6006/?path=/docs/components-togglegroup--docs)。
