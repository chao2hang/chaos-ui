import { Input } from "@/components/ui/input";
import { CodeBlock } from "@/components/code-block";

# Input 输入框

> 表单控件

通过键盘输入内容,是最基础的表单字段组件。支持前缀/后缀、尺寸、状态。

## 何时使用

需要用户通过键盘输入文本内容(用户名、搜索词、单行描述等)。

## 代码示例

### 基础用法

<CodeBlock code={`import { Input } from "@/components/ui/input"

export function Basic() {
  return <Input />
}`} lang="tsx" />

## API / Props

### Input

| Prop        | Type                   | Default | 说明              |
| ----------- | ---------------------- | ------- | ----------------- |
| `className` | `string`               | —       | 自定义样式类名    |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 |

## 注意事项

- **Base UI**: 基于 `@base-ui/react` 无头(headless)原语构建,无障碍(a11y)开箱即用。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-input--docs)。
