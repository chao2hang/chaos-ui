import { Calendar, CalendarDayButton } from "@/components/ui/calendar";
import { CodeBlock } from "@/components/code-block";

# Calendar Ui Calendar

> 表单控件 / Form controls for user input and data submission.

Ui Calendar — Chaos UI 的表单控件。
Calendar — a form controls for user input and data submission. from Chaos UI.

## 何时使用 / When to Use

适用于Ui Calendar相关场景。

Use when you need a calendar.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { Calendar, CalendarDayButton } from "@/components/ui/calendar"

export function Basic() {
  return (
    <Calendar>
      <CalendarDayButton>Content</CalendarDayButton>
    </Calendar>
  )
}`} lang="tsx" />

## API / Props

### Calendar

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **复合组件**: 包含 1 个子组件(CalendarDayButton),使用组合模式按需导入。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-ui-calendar--docs)。
