import { FormSection } from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# FormSection

> Form controls for user input and data submission.

FormSection — a form controls for user input and data submission. from Chaos UI.

## When to Use

Use when you need a form section.

## Examples

### Basic Usage

<CodeBlock code={`import { FormSection } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <FormSection />
}`} lang="tsx" />

### Variants

<CodeBlock
  code={`{/* 展示 variant 变体 */}
<div className="flex flex-wrap items-center gap-3">
  <FormSection variant="default">Default</FormSection>
  <FormSection variant="elevated">Elevated</FormSection>
  <FormSection variant="flat">Flat</FormSection>
</div>`}
  lang="tsx"
/>

## Style Variants

**Variant**

| Variant    | Description   | 默认 |
| ---------- | ------------- | ---- |
| `default`  | Default style | ✓    |
| `elevated` |               |      |
| `flat`     |               |      |

## API / Props

### FormSection

| Prop               | Type                   | Default | Description                       |
| ------------------ | ---------------------- | ------- | --------------------------------- |
| `className`        | `string`               | —       | Custom CSS class                  |
| `title`            | `string`               | —       | Title                             |
| `description`      | `string`               | —       | Description text                  |
| `collapsible`      | `boolean`              | `false` |                                   |
| `defaultCollapsed` | `boolean`              | `false` |                                   |
| `required`         | `boolean`              | —       | Whether required                  |
| `extra`            | `React.ReactNode`      | —       |                                   |
| ...rest            | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
