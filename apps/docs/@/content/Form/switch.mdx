import { Switch } from "@/components/ui/switch"
import { CodeBlock } from "@/components/code-block"

# Switch 开关

> 表单控件 / Form controls for user input and data submission.

开关选择器,代表两种状态之间的切换,比 Checkbox 更直观的开关形态。
Toggle switch representing binary state — more visually intuitive than a checkbox.

## 何时使用 / When to Use

两种状态之间的切换,典型用于开/关、启用/禁用。

Toggle between two states, typically on/off or enabled/disabled.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { Switch } from "@/components/ui/switch"

export function Basic() {
  return <Switch />
}`} lang="tsx" />

### 状态 / States

<CodeBlock code={`<Switch>Normal</Switch>
<Switch disabled>Disabled</Switch>`} lang="tsx" />


## API / Props

### Switch

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **Base UI**: 基于 `@base-ui/react` 无头(headless)原语构建,无障碍(a11y)开箱即用。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-switch--docs)。
