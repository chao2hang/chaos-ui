import { RadioGroup, RadioGroupItem } from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# RadioGroup 单选组

> 表单控件 / Form controls for user input and data submission.

单选组 — Chaos UI 的表单控件。
RadioGroup — a form controls for user input and data submission. from Chaos UI.

## 何时使用 / When to Use

在多个互斥选项中单选时使用（如结算方式、发票类型）。

Use RadioGroup when you need: RadioGroup — a form controls for user input and data submission. from Chaos UI. Use to collect or edit data with validation and submit flows.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { RadioGroup, RadioGroupItem } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return (
    <RadioGroup>
      <RadioGroupItem>Content</RadioGroupItem>
    </RadioGroup>
  )
}`} lang="tsx" />

### 状态 / States

<CodeBlock
  code={`<RadioGroup>Normal</RadioGroup>
<RadioGroup disabled>Disabled</RadioGroup>`}
  lang="tsx"
/>

## API / Props

### RadioGroup

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **复合组件**: 包含 1 个子组件(RadioGroupItem),使用组合模式按需导入。
- **Base UI**: 基于 `@base-ui/react` 无头(headless)原语构建,无障碍(a11y)开箱即用。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](http://localhost:6006/?path=/docs/components-radiogroup--docs)。
