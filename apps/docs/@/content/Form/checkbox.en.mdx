import { Checkbox } from "@/components/ui/checkbox";
import { CodeBlock } from "@/components/code-block";

# Checkbox

> Form controls for user input and data submission.

Multi-select from a group, or standalone toggle for binary state.

## When to Use

Select multiple items from a group, or toggle a single binary state.

## Examples

### Basic Usage

<CodeBlock code={`import { Checkbox } from "@/components/ui/checkbox"

export function Basic() {
  return <Checkbox />
}`} lang="tsx" />

### States

<CodeBlock
  code={`<Checkbox>Normal</Checkbox>
<Checkbox disabled>Disabled</Checkbox>`}
  lang="tsx"
/>

## API / Props

### Checkbox

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **Base UI**: 基于 `@base-ui/react` 无头(headless)原语构建,无障碍(a11y)开箱即用。
