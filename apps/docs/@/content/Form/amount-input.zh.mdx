import { AmountInput } from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# AmountInput 金额输入

> 表单组件

金额输入 — 金额输入框，支持千分位分隔与精度控制。

## 何时使用

需要金额输入功能时使用本组件。

## 代码示例

### 基础用法

<CodeBlock code={`import { AmountInput } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <AmountInput />
}`} lang="tsx" />

## API / Props

### AmountInput

| Prop        | Type                   | Default | 说明              |
| ----------- | ---------------------- | ------- | ----------------- |
| `className` | `string`               | —       | 自定义样式类名    |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-amountinput--docs)。
