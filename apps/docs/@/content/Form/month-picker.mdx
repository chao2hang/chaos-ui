import { MonthPicker } from "@/components/ui/month-picker"
import { CodeBlock } from "@/components/code-block"

# MonthPicker 月份选择

> 表单组件 / Form components for data entry and collection.

月份选择 — 月份选择器，支持年份导航与月份网格。
MonthPicker — Month-only picker with year navigation and month grid.

## 何时使用 / When to Use

需要月份选择功能时使用本组件。

Use when you need a monthpicker.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { MonthPicker } from "@/components/ui/month-picker"

export function Basic() {
  return <MonthPicker />
}`} lang="tsx" />


## API / Props

### MonthPicker

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-monthpicker--docs)。
