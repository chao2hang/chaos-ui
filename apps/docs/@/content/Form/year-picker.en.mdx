import { YearPicker } from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# YearPicker

> Form components for data entry and collection.

YearPicker — Year-only picker with decade navigation grid.

## When to Use

Use YearPicker when you need: Year-only picker with decade navigation grid. Use to collect or edit data with validation and submit flows.

## Examples

### Basic Usage

<CodeBlock code={`import { YearPicker } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <YearPicker />
}`} lang="tsx" />

## API / Props

### YearPicker

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
