import { TreeSelect } from "@/components/ui/tree-select";
import { CodeBlock } from "@/components/code-block";

# TreeSelect Ui Tree Select

> 表单控件 / Form controls for user input and data submission.

Ui Tree Select — Chaos UI 的表单控件。
TreeSelect — a form controls for user input and data submission. from Chaos UI.

## 何时使用 / When to Use

适用于Ui Tree Select相关场景。

Use when you need a tree select.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { TreeSelect } from "@/components/ui/tree-select"

export function Basic() {
  return <TreeSelect />
}`} lang="tsx" />

## API / Props

### TreeSelect

| Prop           | Type                   | Default     | 说明 / Description                                        |
| -------------- | ---------------------- | ----------- | --------------------------------------------------------- |
| `className`    | `string`               | —           | 自定义样式类名 <br/> Custom CSS class                     |
| `value`        | `string \| string[]`   | —           | 当前值(受控) <br/> Current value (controlled)             |
| `defaultValue` | `string \| string[]`   | —           | 默认值(非受控) <br/> Default value (uncontrolled)         |
| `placeholder`  | `string`               | `Select...` | 占位文本 <br/> Placeholder text                           |
| `disabled`     | `boolean`              | —           | 是否禁用 <br/> Whether disabled                           |
| `multiple`     | `boolean`              | `false`     | 是否多选 <br/> Multiple selection                         |
| `maxCount`     | `number`               | —           |                                                           |
| `data`         | `TreeNode[]`           | —           | 数据源 <br/> Data source                                  |
| `onChange`     | `() => void`           | —           | 值变化回调 <br/> Callback when value changes              |
| ...rest        | `React.HTMLAttributes` | —           | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-ui-tree-select--docs)。
