import { PasswordInput } from "@chaos_team/chaos-ui/ui"
import { CodeBlock } from "@/components/code-block"

# PasswordInput

> Form components for data entry and collection.

PasswordInput — Password input with show/hide toggle and optional strength meter.

## When to Use

Use when you need a passwordinput.


## Examples

### Basic Usage

<CodeBlock code={`import { PasswordInput } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <PasswordInput />
}`} lang="tsx" />


## API / Props

### PasswordInput

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


