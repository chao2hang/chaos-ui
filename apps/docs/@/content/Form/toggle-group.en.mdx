import { ToggleGroup, ToggleGroupItem } from "@/components/ui/toggle-group"
import { CodeBlock } from "@/components/code-block"

# ToggleGroup

> Form controls for user input and data submission.

ToggleGroup — a form controls for user input and data submission. from Chaos UI.

## When to Use

Use when you need a toggle group.


## Examples

### Basic Usage

<CodeBlock code={`import { ToggleGroup, ToggleGroupItem } from "@/components/ui/toggle-group"

export function Basic() {
  return (
    <ToggleGroup>
      <ToggleGroupItem>Content</ToggleGroupItem>
    </ToggleGroup>
  )
}`} lang="tsx" />

### States

<CodeBlock code={`<ToggleGroup>Normal</ToggleGroup>
<ToggleGroup disabled>Disabled</ToggleGroup>`} lang="tsx" />


## API / Props

### ToggleGroup

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **复合组件**: 包含 1 个子组件(ToggleGroupItem),使用组合模式按需导入。
- **Base UI**: 基于 `@base-ui/react` 无头(headless)原语构建,无障碍(a11y)开箱即用。


