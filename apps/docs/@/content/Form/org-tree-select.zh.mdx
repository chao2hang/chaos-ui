import { OrgTreeSelect } from "@chaos_team/chaos-ui/business"
import { CodeBlock } from "@/components/code-block"

# OrgTreeSelect 组织树选择

> 表单组件

组织树选择 — 组织树选择器，支持 Popover、搜索与单/多选。

## 何时使用

需要组织树选择功能时使用本组件。


## 代码示例

### 基础用法

<CodeBlock code={`import { OrgTreeSelect } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <OrgTreeSelect />
}`} lang="tsx" />


## API / Props

### OrgTreeSelect

| Prop | Type | Default | 说明 |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-orgtreeselect--docs)。

