import { Cascader } from "@/components/ui/cascader"
import { CodeBlock } from "@/components/code-block"

# Cascader 级联选择

> 表单控件

从多级层级关联数据中进行选择,如省/市/区。

## 何时使用

需要级联选择功能时使用本组件。


## 代码示例

### 基础用法

<CodeBlock code={`import { Cascader } from "@/components/ui/cascader"

export function Basic() {
  return <Cascader />
}`} lang="tsx" />

### 变体

<CodeBlock code={`{/* 展示 variant 变体 */}
<div className="flex flex-wrap items-center gap-3">
  <Cascader variant="default">Default</Cascader>
  <Cascader variant="outline">Outline</Cascader>
</div>`} lang="tsx" />

### 尺寸

<CodeBlock code={`{/* 尺寸对照 */}
<div className="flex flex-wrap items-center gap-3">
  <Cascader size="sm">Sm</Cascader>
  <Cascader size="default">Default</Cascader>
  <Cascader size="lg">Lg</Cascader>
</div>`} lang="tsx" />


## 样式变体

**变体**

| Variant | 说明 | 默认 |
|---|---|---|
| `default` | 默认样式 | ✓ |
| `outline` | 描边样式 |  |

**尺寸**

| Size | 说明 | 默认 |
|---|---|---|
| `default` | 默认尺寸 | ✓ |
| `sm` | 小号 |  |
| `lg` | 大号 |  |

## API / Props

### Cascader

| Prop | Type | Default | 说明 |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 |
| `options` | `CascaderOption[]` | — | 可选项列表 |
| `value` | `string[]` | — | 当前值(受控) |
| `onChange` | `() => void` | — | 值变化回调 |
| `placeholder` | `string` | `Please select` | 占位文本 |
| `disabled` | `boolean` | — | 是否禁用 |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **CVA 变体**: 通过 `class-variance-authority` 管理样式变体,类型安全。变体对象可导出复用。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-cascader--docs)。

