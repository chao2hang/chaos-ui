import { FullscreenToggle } from "@/components/ui/fullscreen-toggle"
import { CodeBlock } from "@/components/code-block"

# FullscreenToggle 全屏切换

> 表单控件 / Form controls for user input and data submission.

全屏切换 — Chaos UI 的表单控件。
FullscreenToggle — a form controls for user input and data submission. from Chaos UI.

## 何时使用 / When to Use

需要全屏切换功能时使用本组件。

Use when you need a fullscreen toggle.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { FullscreenToggle } from "@/components/ui/fullscreen-toggle"

export function Basic() {
  return <FullscreenToggle />
}`} lang="tsx" />

### 变体 / Variants

<CodeBlock code={`{/* 展示 variant 变体 */}
<div className="flex flex-wrap items-center gap-3">
  <FullscreenToggle variant="default">Default</FullscreenToggle>
  <FullscreenToggle variant="outline">Outline</FullscreenToggle>
  <FullscreenToggle variant="ghost">Ghost</FullscreenToggle>
</div>`} lang="tsx" />

### 尺寸 / Sizes

<CodeBlock code={`{/* 尺寸对照 */}
<div className="flex flex-wrap items-center gap-3">
  <FullscreenToggle size="sm">Sm</FullscreenToggle>
  <FullscreenToggle size="default">Default</FullscreenToggle>
  <FullscreenToggle size="lg">Lg</FullscreenToggle>
</div>`} lang="tsx" />


## 样式变体 / Style Variants

**变体 / Variant**

| Variant | 说明 / Description | 默认 |
|---|---|---|
| `default` | 默认样式 / Default style |  |
| `outline` | 描边样式 / Outlined style |  |
| `ghost` | 幽灵(无背景)样式 / Ghost (no background) style | ✓ |

**尺寸 / Size**

| Size | 说明 / Description | 默认 |
|---|---|---|
| `default` | 默认尺寸 / Default size |  |
| `sm` | 小号 / Small |  |
| `lg` | 大号 / Large |  |
| `icon` | 图标正方形 / Icon-only square | ✓ |

## API / Props

### FullscreenToggle

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| `target` | `React.RefObject<HTMLElement>` | — | Target element to fullscreen. Defaults to document.documentElement |
| ...rest | `React.ComponentProps<"button">` | — | 透传 `<button>` 原生属性 <br/> Inherits all `<button>` attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **CVA 变体**: 通过 `class-variance-authority` 管理样式变体,类型安全。变体对象可导出复用。
- **原生属性**: 继承所有 `<button>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-fullscreentoggle--docs)。
