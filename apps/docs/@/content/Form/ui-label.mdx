import { Label } from "@/components/ui/label";
import { CodeBlock } from "@/components/code-block";

# Label Ui Label

> 表单控件 / Form controls for user input and data submission.

Ui Label — Chaos UI 的表单控件。
Label — a form controls for user input and data submission. from Chaos UI.

## 何时使用 / When to Use

适用于Ui Label相关场景。

Use when you need a label.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { Label } from "@/components/ui/label"

export function Basic() {
  return <Label />
}`} lang="tsx" />

## API / Props

### Label

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-ui-label--docs)。
