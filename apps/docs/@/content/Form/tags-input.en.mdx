import { TagsInput } from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# TagsInput

> Form controls for user input and data submission.

TagsInput — a form controls for user input and data submission. from Chaos UI.

## When to Use

Use TagsInput when you need: TagsInput — a form controls for user input and data submission. from Chaos UI. Use to collect or edit data with validation and submit flows.

## Examples

### Basic Usage

<CodeBlock code={`import { TagsInput } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <TagsInput />
}`} lang="tsx" />

## API / Props

### TagsInput

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
