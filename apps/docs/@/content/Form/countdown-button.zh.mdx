import { CountdownButton } from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# CountdownButton 倒计时按钮

> 表单组件

倒计时按钮 — 倒计时按钮，用于发送验证码后禁用倒计时。

## 何时使用

当你需要「倒计时按钮」相关能力时使用：倒计时按钮，用于发送验证码后禁用倒计时。用于采集或编辑业务数据；需要校验、提交、草稿时优先用表单控件族。

## 代码示例

### 基础用法

<CodeBlock code={`import { CountdownButton } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <CountdownButton />
}`} lang="tsx" />

## API / Props

### CountdownButton

| Prop        | Type                   | Default | 说明              |
| ----------- | ---------------------- | ------- | ----------------- |
| `className` | `string`               | —       | 自定义样式类名    |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](http://localhost:6006/?path=/docs/components-countdownbutton--docs)。
