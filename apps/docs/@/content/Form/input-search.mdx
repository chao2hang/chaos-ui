import { InputSearch } from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# InputSearch 搜索输入

> 表单控件 / Form controls for user input and data submission.

搜索输入 — Chaos UI 的表单控件。
InputSearch — a form controls for user input and data submission. from Chaos UI.

## 何时使用 / When to Use

当你需要「搜索输入」相关能力时使用：搜索输入 — Chaos UI 的表单控件。用于采集或编辑业务数据；需要校验、提交、草稿时优先用表单控件族。

Use InputSearch when you need: InputSearch — a form controls for user input and data submission. from Chaos UI. Use to collect or edit data with validation and submit flows.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { InputSearch } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <InputSearch />
}`} lang="tsx" />

### 尺寸 / Sizes

<CodeBlock
  code={`{/* 尺寸对照 */}
<div className="flex flex-wrap items-center gap-3">
  <InputSearch size="sm">Sm</InputSearch>
  <InputSearch size="default">Default</InputSearch>
  <InputSearch size="lg">Lg</InputSearch>
</div>`}
  lang="tsx"
/>

### 状态 / States

<CodeBlock
  code={`<InputSearch>Normal</InputSearch>
<InputSearch disabled>Disabled</InputSearch>`}
  lang="tsx"
/>

## 样式变体 / Style Variants

**尺寸 / Size**

| Size      | 说明 / Description      | 默认 |
| --------- | ----------------------- | ---- |
| `default` | 默认尺寸 / Default size | ✓    |
| `sm`      | 小号 / Small            |      |
| `lg`      | 大号 / Large            |      |

## API / Props

### InputSearch

| Prop            | Type                   | Default | 说明 / Description                                        |
| --------------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className`     | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `value`         | `string`               | —       | Controlled value                                          |
| `onValueChange` | `() => void`           | —       | Change handler                                            |
| `onSearch`      | `() => void`           | —       | Called when search is submitted                           |
| `loading`       | `boolean`              | `false` | Whether to show loading spinner                           |
| ...rest         | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **CVA 变体**: 通过 `class-variance-authority` 管理样式变体,类型安全。变体对象可导出复用。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](http://localhost:6006/?path=/docs/components-inputsearch--docs)。
