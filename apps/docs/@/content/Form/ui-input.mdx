import { Input } from "@/components/ui/input";
import { CodeBlock } from "@/components/code-block";

# Input Ui Input

> 表单控件 / Form controls for user input and data submission.

Ui Input — Chaos UI 的表单控件。
Input — a form controls for user input and data submission. from Chaos UI.

## 何时使用 / When to Use

适用于Ui Input相关场景。

Use when you need an input.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { Input } from "@/components/ui/input"

export function Basic() {
  return <Input />
}`} lang="tsx" />

## API / Props

### Input

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Base UI**: 基于 `@base-ui/react` 无头(headless)原语构建,无障碍(a11y)开箱即用。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-ui-input--docs)。
