import { FormList } from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# FormList 表单列表

> 表单控件 / Form controls for user input and data submission.

表单列表 — Chaos UI 的表单控件。
FormList — a form controls for user input and data submission. from Chaos UI.

## 何时使用 / When to Use

需要表单列表功能时使用本组件。

Use when you need a form list.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { FormList } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <FormList />
}`} lang="tsx" />

## API / Props

### FormList

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-formlist--docs)。
