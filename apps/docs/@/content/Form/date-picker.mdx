import { DatePicker } from "@/components/ui/date-picker"
import { CodeBlock } from "@/components/code-block"

# DatePicker 日期选择器

> 表单控件 / Form controls for user input and data submission.

选择单个日期、日期范围或多个日期。
Select single date, date ranges, or multiple dates.

## 何时使用 / When to Use

选择单个日期。

Select a single date.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { DatePicker } from "@/components/ui/date-picker"

export function Basic() {
  return <DatePicker />
}`} lang="tsx" />

### 变体 / Variants

<CodeBlock code={`{/* 展示 variant 变体 */}
<div className="flex flex-wrap items-center gap-3">
  <DatePicker variant="default">Default</DatePicker>
  <DatePicker variant="outline">Outline</DatePicker>
</div>`} lang="tsx" />

### 尺寸 / Sizes

<CodeBlock code={`{/* 尺寸对照 */}
<div className="flex flex-wrap items-center gap-3">
  <DatePicker size="sm">Sm</DatePicker>
  <DatePicker size="default">Default</DatePicker>
  <DatePicker size="lg">Lg</DatePicker>
</div>`} lang="tsx" />

### 状态 / States

<CodeBlock code={`<DatePicker>Normal</DatePicker>
<DatePicker disabled>Disabled</DatePicker>`} lang="tsx" />


## 样式变体 / Style Variants

**变体 / Variant**

| Variant | 说明 / Description | 默认 |
|---|---|---|
| `default` | 默认样式 / Default style | ✓ |
| `outline` | 描边样式 / Outlined style |  |

**尺寸 / Size**

| Size | 说明 / Description | 默认 |
|---|---|---|
| `default` | 默认尺寸 / Default size | ✓ |
| `sm` | 小号 / Small |  |
| `lg` | 大号 / Large |  |

## API / Props

### DatePicker

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| `value` | `Date` | — | Selected date |
| `onChange` | `() => void` | — | Change handler |
| `placeholder` | `string` | `Pick a date` | Placeholder text |
| `format` | `string` | — | Date format for display |
| `minDate` | `Date` | — | Min selectable date |
| `maxDate` | `Date` | — | Max selectable date |
| `disabled` | `boolean` | — | 是否禁用 <br/> Whether disabled |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **CVA 变体**: 通过 `class-variance-authority` 管理样式变体,类型安全。变体对象可导出复用。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-date-picker--docs)。
