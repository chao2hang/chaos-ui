import { Textarea } from "@/components/ui/textarea";
import { CodeBlock } from "@/components/code-block";

# Textarea Ui Textarea

> 表单控件 / Form controls for user input and data submission.

Ui Textarea — Chaos UI 的表单控件。
Textarea — a form controls for user input and data submission. from Chaos UI.

## 何时使用 / When to Use

适用于Ui Textarea相关场景。

Use when you need a textarea.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { Textarea } from "@/components/ui/textarea"

export function Basic() {
  return <Textarea />
}`} lang="tsx" />

## API / Props

### Textarea

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **无障碍**: 组件遵循 WAI-ARIA 最佳实践。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-ui-textarea--docs)。
