import { ErrorBoundaryUI } from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# ErrorBoundaryUI

> Feedback components for user interaction and status.

ErrorBoundaryUI — React error boundary UI with fallback render and reset.

## When to Use

Use ErrorBoundaryUI when you need: React error boundary UI with fallback render and reset. Use for loading, confirmation, warnings, and notifications—never fail silently.

## Examples

### Basic Usage

<CodeBlock code={`import { ErrorBoundaryUI } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <ErrorBoundaryUI />
}`} lang="tsx" />

## API / Props

### ErrorBoundaryUI

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
