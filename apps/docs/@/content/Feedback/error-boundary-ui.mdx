import { ErrorBoundaryUI } from "@chaos_team/chaos-ui/ui"
import { CodeBlock } from "@/components/code-block"

# ErrorBoundaryUI 错误边界

> 反馈组件 / Feedback components for user interaction and status.

错误边界 — React 错误边界，支持降级渲染与重置。
ErrorBoundaryUI — React error boundary UI with fallback render and reset.

## 何时使用 / When to Use

需要错误边界功能时使用本组件。

Use when you need a errorboundaryui.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { ErrorBoundaryUI } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <ErrorBoundaryUI />
}`} lang="tsx" />


## API / Props

### ErrorBoundaryUI

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-errorboundaryui--docs)。
