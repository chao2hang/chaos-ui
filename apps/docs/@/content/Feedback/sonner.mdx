import { Toaster } from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# Sonner 通知提示

> 反馈组件 / Feedback and status communication to the user.

基于 sonner 的 opinionated 通知组件,堆叠展示在角落。
Opinionated notification component (wraps sonner) stacked in a corner.

## 何时使用 / When to Use

需要通知提示功能时使用本组件。

Use when you need a sonner.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { Toaster } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <Toaster />
}`} lang="tsx" />

## API / Props

### Toaster

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-sonner--docs)。
