import { Toaster } from "@/components/ui/sonner";
import { CodeBlock } from "@/components/code-block";

# Sonner Ui Sonner

> 反馈组件 / Feedback and status communication to the user.

Ui Sonner — Chaos UI 的反馈组件。
Sonner — a feedback and status communication to the user. from Chaos UI.

## 何时使用 / When to Use

适用于Ui Sonner相关场景。

Use when you need a sonner.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { Toaster } from "@/components/ui/sonner"

export function Basic() {
  return <Toaster />
}`} lang="tsx" />

## API / Props

### Toaster

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-ui-sonner--docs)。
