import { Result } from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# Result 结果页

> 反馈组件

向用户反馈操作结果(成功/失败/警告/404/403),常为整页。

## 何时使用

当你需要「结果页」相关能力时使用：向用户反馈操作结果(成功/失败/警告/404/403),常为整页。用于过程反馈与结果提示（加载、确认、警告、通知）；避免静默失败。

## 代码示例

### 基础用法

<CodeBlock code={`import { Result } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <Result>Default</Result>
}`} lang="tsx" />

### 尺寸

<CodeBlock
  code={`{/* 尺寸对照 */}
<div className="flex flex-wrap items-center gap-3">
  <Result size="sm">Sm</Result>
  <Result size="default">Default</Result>
  <Result size="lg">Lg</Result>
</div>`}
  lang="tsx"
/>

## 样式变体

**状态**

| status    | 说明 | 默认 |
| --------- | ---- | ---- |
| `success` |      | ✓    |
| `error`   |      |      |
| `info`    |      |      |
| `warning` |      |      |

**尺寸**

| Size      | 说明     | 默认 |
| --------- | -------- | ---- |
| `default` | 默认尺寸 | ✓    |
| `sm`      | 小号     |      |
| `lg`      | 大号     |      |

## API / Props

### Result

| Prop        | Type                                       | Default | 说明                                     |
| ----------- | ------------------------------------------ | ------- | ---------------------------------------- |
| `className` | `string`                                   | —       | 自定义样式类名                           |
| `status`    | `"success" \| "error" \| "info" \| "wa...` | `info`  | Status determines default icon and color |
| `title`     | `React.ReactNode`                          | —       | Title text                               |
| `subtitle`  | `React.ReactNode`                          | —       | Subtitle / description                   |
| `icon`      | `React.ReactNode`                          | —       | Custom icon override                     |
| `extra`     | `React.ReactNode`                          | —       | Extra content (e.g., action buttons)     |
| ...rest     | `React.ComponentProps<"div">`              | —       | 透传 `<div>` 原生属性                    |

## 注意事项

- **CVA 变体**: 通过 `class-variance-authority` 管理样式变体,类型安全。变体对象可导出复用。
- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](http://localhost:6006/?path=/docs/components-result--docs)。
