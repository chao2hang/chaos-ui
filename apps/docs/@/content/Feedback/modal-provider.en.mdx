import { ModalProvider, Modal, ModalContext } from "@chaos_team/chaos-ui/ui"
import { CodeBlock } from "@/components/code-block"

# ModalProvider

> Feedback and status communication to the user.

ModalProvider — a feedback and status communication to the user. from Chaos UI.

## When to Use

Use when you need a modal provider.


## Examples

### Basic Usage

<CodeBlock code={`import { ModalProvider, Modal, ModalContext } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return (
    <div className="space-y-4">
  <ModalProvider />
  <Modal />
  <ModalContext />
    </div>
  )
}`} lang="tsx" />


## API / Props

### ModalProvider

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| ...rest | `React.ComponentProps<"div">` | — | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


