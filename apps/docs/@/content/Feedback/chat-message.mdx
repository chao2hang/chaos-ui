import { ChatMessage } from "@/components/ui/chat-message"
import { CodeBlock } from "@/components/code-block"

# ChatMessage Chat Message

> 反馈组件 / Feedback and status communication to the user.

Chat Message — Chaos UI 的反馈组件。
ChatMessage — a feedback and status communication to the user. from Chaos UI.

## 何时使用 / When to Use

适用于Chat Message相关场景。

Use when you need a chat message.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { ChatMessage } from "@/components/ui/chat-message"

export function Basic() {
  return <ChatMessage />
}`} lang="tsx" />


## API / Props

### ChatMessage

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| `role` | `"user" \| "assistant" \| "system"` | `assistant` | 角色 / message role |
| `content` | `string` | — | 消息内容 / message content |
| `avatar` | `React.ReactNode` | — | 头像节点 / avatar node |
| `name` | `string` | — | 发送者名称 / sender name |
| `time` | `string` | — | 时间戳 / timestamp |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-chatmessage--docs)。
