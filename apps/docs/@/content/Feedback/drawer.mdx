import { Drawer } from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# Drawer 抽屉

> 反馈组件 / Feedback and status communication to the user.

从屏幕边缘滑出的浮层面板,用于承载详情/表单等次要内容。
Panel that slides out from an edge, used for secondary content like details or forms.

## 何时使用 / When to Use

从屏幕边缘滑出的面板,用于承载详情、表单或配置,不打断主流程。

A panel sliding from the screen edge for details, forms, or configurations without interrupting the main flow.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { Drawer, DrawerTrigger, DrawerContent, DrawerHeader, DrawerTitle, DrawerDescription } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return (
    <Drawer>
      <DrawerTrigger>Open</DrawerTrigger>
      <DrawerContent>
      <DrawerHeader>
        <DrawerTitle>Title</DrawerTitle>
        <DrawerDescription>Description.</DrawerDescription>
      </DrawerHeader>
      </DrawerContent>
    </Drawer>
  )
}`} lang="tsx" />

## API / Props

### Drawer

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **复合组件**: 包含 9 个子组件(DrawerPortal, DrawerOverlay, DrawerTrigger, DrawerClose, DrawerContent…),使用组合模式按需导入。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-drawer--docs)。
