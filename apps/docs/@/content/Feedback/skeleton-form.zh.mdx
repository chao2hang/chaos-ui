import { SkeletonForm } from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# SkeletonForm 表单骨架屏

> 反馈组件

表单骨架屏 — 表单骨架屏占位，按字段精确占位。

## 何时使用

当你需要「表单骨架屏」相关能力时使用：表单骨架屏占位，按字段精确占位。用于过程反馈与结果提示（加载、确认、警告、通知）；避免静默失败。

## 代码示例

### 基础用法

<CodeBlock code={`import { SkeletonForm } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <SkeletonForm />
}`} lang="tsx" />

## API / Props

### SkeletonForm

| Prop        | Type                   | Default | 说明              |
| ----------- | ---------------------- | ------- | ----------------- |
| `className` | `string`               | —       | 自定义样式类名    |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](http://localhost:6006/?path=/docs/components-skeletonform--docs)。
