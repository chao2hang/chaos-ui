import { SkeletonForm } from "@/components/ui/skeleton-form"
import { CodeBlock } from "@/components/code-block"

# SkeletonForm 表单骨架屏

> 反馈组件

表单骨架屏 — 表单骨架屏占位，按字段精确占位。

## 何时使用

需要表单骨架屏功能时使用本组件。


## 代码示例

### 基础用法

<CodeBlock code={`import { SkeletonForm } from "@/components/ui/skeleton-form"

export function Basic() {
  return <SkeletonForm />
}`} lang="tsx" />


## API / Props

### SkeletonForm

| Prop | Type | Default | 说明 |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-skeletonform--docs)。

