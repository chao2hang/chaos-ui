import { SkeletonTable } from "@/components/ui/skeleton-table"
import { CodeBlock } from "@/components/code-block"

# SkeletonTable 表格骨架屏

> 反馈组件 / Feedback components for user interaction and status.

表格骨架屏 — 表格骨架屏占位，按列精确占位。
SkeletonTable — Table skeleton placeholder with column-aware shimmer.

## 何时使用 / When to Use

需要表格骨架屏功能时使用本组件。

Use when you need a skeletontable.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { SkeletonTable } from "@/components/ui/skeleton-table"

export function Basic() {
  return <SkeletonTable />
}`} lang="tsx" />


## API / Props

### SkeletonTable

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-skeletontable--docs)。
