import { KeyboardShortcutDialog } from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# KeyboardShortcutDialog

> Feedback and status communication to the user.

KeyboardShortcutDialog — a feedback and status communication to the user. from Chaos UI.

## When to Use

Use when you need a keyboard shortcut dialog.

## Examples

### Basic Usage

<CodeBlock code={`import { KeyboardShortcutDialog } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <KeyboardShortcutDialog />
}`} lang="tsx" />

### Sizes

<CodeBlock
  code={`{/* 尺寸对照 */}
<div className="flex flex-wrap items-center gap-3">
  <KeyboardShortcutDialog size="sm">Sm</KeyboardShortcutDialog>
  <KeyboardShortcutDialog size="default">Default</KeyboardShortcutDialog>
  <KeyboardShortcutDialog size="lg">Lg</KeyboardShortcutDialog>
</div>`}
  lang="tsx"
/>

## Style Variants

**Size**

| Size      | Description  | 默认 |
| --------- | ------------ | ---- |
| `default` | Default size | ✓    |
| `sm`      | Small        |      |
| `lg`      | Large        |      |

## API / Props

### KeyboardShortcutDialog

| Prop        | Type                          | Default | Description                         |
| ----------- | ----------------------------- | ------- | ----------------------------------- |
| `className` | `string`                      | —       | Custom CSS class                    |
| `open`      | `boolean`                     | `false` | Whether the dialog is open          |
| `onClose`   | `() => void`                  | —       | Called when the dialog should close |
| `groups`    | `ShortcutGroup[]`             | —       | Shortcut groups to display          |
| ...rest     | `React.ComponentProps<"div">` | —       | Inherits all `<div>` attributes     |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
