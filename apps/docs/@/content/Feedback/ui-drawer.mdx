import { Drawer } from "@/components/ui/drawer";
import { CodeBlock } from "@/components/code-block";

# Drawer Ui Drawer

> 反馈组件 / Feedback and status communication to the user.

Ui Drawer — Chaos UI 的反馈组件。
Drawer — a feedback and status communication to the user. from Chaos UI.

## 何时使用 / When to Use

适用于Ui Drawer相关场景。

Use when you need a drawer.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { Drawer, DrawerTrigger, DrawerContent, DrawerHeader, DrawerTitle, DrawerDescription } from "@/components/ui/drawer"

export function Basic() {
  return (
    <Drawer>
      <DrawerTrigger>Open</DrawerTrigger>
      <DrawerContent>
      <DrawerHeader>
        <DrawerTitle>Title</DrawerTitle>
        <DrawerDescription>Description.</DrawerDescription>
      </DrawerHeader>
      </DrawerContent>
    </Drawer>
  )
}`} lang="tsx" />

## API / Props

### Drawer

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **复合组件**: 包含 9 个子组件(DrawerPortal, DrawerOverlay, DrawerTrigger, DrawerClose, DrawerContent…),使用组合模式按需导入。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-ui-drawer--docs)。
