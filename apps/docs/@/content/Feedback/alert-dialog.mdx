import { AlertDialog } from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# AlertDialog 警告对话框

> 反馈组件 / Feedback and status communication to the user.

需要用户确认的危险操作对话框,阻断式,含警告样式。
Blocking confirmation dialog for destructive actions, styled for emphasis.

## 何时使用 / When to Use

当你需要「警告对话框」相关能力时使用：需要用户确认的危险操作对话框,阻断式,含警告样式。用于过程反馈与结果提示（加载、确认、警告、通知）；避免静默失败。

Use AlertDialog when you need: Blocking confirmation dialog for destructive actions, styled for emphasis. Use for loading, confirmation, warnings, and notifications—never fail silently.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { AlertDialog, AlertDialogTrigger, AlertDialogContent, AlertDialogHeader, AlertDialogTitle, AlertDialogDescription } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return (
    <AlertDialog>
      <AlertDialogTrigger>Open</AlertDialogTrigger>
      <AlertDialogContent>
      <AlertDialogHeader>
        <AlertDialogTitle>Title</AlertDialogTitle>
        <AlertDialogDescription>Description.</AlertDialogDescription>
      </AlertDialogHeader>
      </AlertDialogContent>
    </AlertDialog>
  )
}`} lang="tsx" />

## API / Props

### AlertDialog

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **复合组件**: 包含 8 个子组件(AlertDialogTrigger, AlertDialogContent, AlertDialogHeader, AlertDialogTitle, AlertDialogDescription…),使用组合模式按需导入。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](http://localhost:6006/?path=/docs/components-alertdialog--docs)。
