import { MessageProvider, Message, MessageContext } from "@/components/ui/message-provider"
import { CodeBlock } from "@/components/code-block"

# MessageProvider 消息提供者

> 反馈组件 / Feedback and status communication to the user.

消息提供者 — Chaos UI 的反馈组件。
MessageProvider — a feedback and status communication to the user. from Chaos UI.

## 何时使用 / When to Use

需要消息提供者功能时使用本组件。

Use when you need a message provider.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { MessageProvider, Message, MessageContext } from "@/components/ui/message-provider"

export function Basic() {
  return (
    <div className="space-y-4">
  <MessageProvider />
  <Message />
  <MessageContext />
    </div>
  )
}`} lang="tsx" />


## API / Props

### MessageProvider

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| `max` | `number` | `5` | Max messages visible at once |
| ...rest | `React.ComponentProps<"div">` | — | 透传 `<div>` 原生属性 <br/> Inherits all `<div>` attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-messageprovider--docs)。
