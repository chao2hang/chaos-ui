import { NotificationDropdown } from "@chaos_team/chaos-ui/ui"
import { CodeBlock } from "@/components/code-block"

# NotificationDropdown 通知下拉

> 反馈组件

通知下拉 — Header 通知下拉面板，支持已读/未读与全部清除。

## 何时使用

需要通知下拉功能时使用本组件。


## 代码示例

### 基础用法

<CodeBlock code={`import { NotificationDropdown } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <NotificationDropdown />
}`} lang="tsx" />


## API / Props

### NotificationDropdown

| Prop | Type | Default | 说明 |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-notificationdropdown--docs)。

