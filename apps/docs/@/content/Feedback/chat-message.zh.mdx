import { ChatMessage } from "@/components/ui/chat-message"
import { CodeBlock } from "@/components/code-block"

# ChatMessage Chat Message

> 反馈组件

Chat Message — Chaos UI 的反馈组件。

## 何时使用

适用于Chat Message相关场景。


## 代码示例

### 基础用法

<CodeBlock code={`import { ChatMessage } from "@/components/ui/chat-message"

export function Basic() {
  return <ChatMessage />
}`} lang="tsx" />


## API / Props

### ChatMessage

| Prop | Type | Default | 说明 |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 |
| `role` | `"user" \| "assistant" \| "system"` | `assistant` | 角色 |
| `content` | `string` | — | 消息内容 |
| `avatar` | `React.ReactNode` | — | 头像节点 |
| `name` | `string` | — | 发送者名称 |
| `time` | `string` | — | 时间戳 |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-chatmessage--docs)。

