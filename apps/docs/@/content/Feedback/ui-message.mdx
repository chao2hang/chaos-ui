import { Message } from "@/components/ui/message";
import { CodeBlock } from "@/components/code-block";

# Message Ui Message

> 反馈组件 / Feedback and status communication to the user.

Ui Message — Chaos UI 的反馈组件。
Message — a feedback and status communication to the user. from Chaos UI.

## 何时使用 / When to Use

适用于Ui Message相关场景。

Use when you need a message.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { Message } from "@/components/ui/message"

export function Basic() {
  return <Message />
}`} lang="tsx" />

## API / Props

### Message

| Prop        | Type                          | Default | 说明 / Description                                          |
| ----------- | ----------------------------- | ------- | ----------------------------------------------------------- |
| `className` | `string`                      | —       | 自定义样式类名 <br/> Custom CSS class                       |
| `variant`   | `MessageVariant`              | `info`  | Message variant / 消息变体                                  |
| `icon`      | `React.ReactNode`             | —       | Custom icon (overrides variant default) / 自定义图标        |
| `action`    | `React.ReactNode`             | —       | Optional action element (button, link) / 可选操作元素       |
| ...rest     | `React.ComponentProps<"div">` | —       | 透传 `<div>` 原生属性 <br/> Inherits all `<div>` attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-ui-message--docs)。
