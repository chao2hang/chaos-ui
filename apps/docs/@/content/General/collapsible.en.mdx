import {
  Collapsible,
  CollapsibleTrigger,
  CollapsibleContent,
} from "@/components/ui/collapsible";
import { CodeBlock } from "@/components/code-block";

# Collapsible

> General-purpose primitives used across the app.

Collapsible — a general-purpose primitives used across the app. from Chaos UI.

## When to Use

Use when you need a collapsible.

## Examples

### Basic Usage

<CodeBlock code={`import { Collapsible, CollapsibleTrigger, CollapsibleContent } from "@/components/ui/collapsible"

export function Basic() {
  return (
    <Collapsible>
      <CollapsibleContent>Content</CollapsibleContent>
    </Collapsible>
  )
}`} lang="tsx" />

## API / Props

### Collapsible

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **复合组件**: 包含 2 个子组件(CollapsibleTrigger, CollapsibleContent),使用组合模式按需导入。
- **Base UI**: 基于 `@base-ui/react` 无头(headless)原语构建,无障碍(a11y)开箱即用。
