import { I18nProvider } from "@chaos_team/chaos-ui/ui"
import { CodeBlock } from "@/components/code-block"

# I18nProvider 国际化Provider

> 通用组件

国际化Provider — 全局国际化 Provider，支持语言切换与消息打包。

## 何时使用

需要国际化Provider功能时使用本组件。


## 代码示例

### 基础用法

<CodeBlock code={`import { I18nProvider } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <I18nProvider />
}`} lang="tsx" />


## API / Props

### I18nProvider

| Prop | Type | Default | 说明 |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-i18nprovider--docs)。

