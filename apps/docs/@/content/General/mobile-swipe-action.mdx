import { MobileSwipeAction } from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# MobileSwipeAction 滑动操作

> 移动端滑动操作行 / Swipe action row for mobile.

移动端滑动操作行，左右拖拽时揭示操作按钮。
Swipe action row for mobile; reveals left/right action buttons on drag.

## 何时使用 / When to Use

当你需要「Mobile Swipe Action（MobileSwipeAction）」解决具体交互/展示问题时使用；优先看下方示例与 API，按业务字段接入。

Use when you need MobileSwipeAction for a concrete interaction; follow the examples and API below.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { MobileSwipeAction } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return (
    <MobileSwipeAction
      rightActions={[
        { key: "delete", label: "删除", color: "destructive", onClick: () => {} },
      ]}
    >
      <div className="p-4">Swipe left to reveal actions</div>
    </MobileSwipeAction>
  )
}`} lang="tsx" />

## API / Props

### MobileSwipeAction

| Prop           | Type               | Default | 说明 / Description                  |
| -------------- | ------------------ | ------- | ----------------------------------- |
| `leftActions`  | `SwipeActionDef[]` | —       | Left action buttons / 左侧操作按钮  |
| `rightActions` | `SwipeActionDef[]` | —       | Right action buttons / 右侧操作按钮 |
| `children`     | `React.ReactNode`  | —       | Row content / 行内容                |

### SwipeActionDef

| Prop      | Type                                      | 说明 / Description                  |
| --------- | ----------------------------------------- | ----------------------------------- |
| `key`     | `string`                                  | Unique key / 唯一键 (required)      |
| `label`   | `string`                                  | Button label / 按钮文本 (required)  |
| `onClick` | `() => void`                              | Click handler / 点击回调 (required) |
| `color`   | `"default" \| "primary" \| "destructive"` | Button color / 按钮颜色             |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **移动端专用**: 基于触摸事件，适用于移动端场景。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](http://localhost:6006/?path=/docs/components-mobileswipeaction--docs)。
