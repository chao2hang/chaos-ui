import { SegmentedControl } from "@/components/ui/segmented-control";
import { CodeBlock } from "@/components/code-block";

# SegmentedControl Ui Segmented Control

> 通用基础组件 / General-purpose primitives used across the app.

Ui Segmented Control — Chaos UI 的通用基础组件。
SegmentedControl — a general-purpose primitives used across the app. from Chaos UI.

## 何时使用 / When to Use

适用于Ui Segmented Control相关场景。

Use when you need a segmented control.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { SegmentedControl } from "@/components/ui/segmented-control"

export function Basic() {
  return <SegmentedControl />
}`} lang="tsx" />

### 尺寸 / Sizes

<CodeBlock
  code={`{/* 尺寸对照 */}
<div className="flex flex-wrap items-center gap-3">
  <SegmentedControl size="sm">Sm</SegmentedControl>
  <SegmentedControl size="default">Default</SegmentedControl>
  <SegmentedControl size="lg">Lg</SegmentedControl>
</div>`}
  lang="tsx"
/>

## 样式变体 / Style Variants

**尺寸 / Size**

| Size      | 说明 / Description      | 默认 |
| --------- | ----------------------- | ---- |
| `default` | 默认尺寸 / Default size | ✓    |
| `sm`      | 小号 / Small            |      |
| `lg`      | 大号 / Large            |      |

**全宽 / fullWidth**

| fullWidth | 说明 / Description | 默认 |
| --------- | ------------------ | ---- |
| `true`    |                    | ✓    |
| `false`   |                    |      |

## API / Props

### SegmentedControl

| Prop           | Type                       | Default | 说明 / Description                                        |
| -------------- | -------------------------- | ------- | --------------------------------------------------------- |
| `className`    | `string`                   | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `options`      | `SegmentedControlOption[]` | —       | 可选项列表 <br/> Available options                        |
| `value`        | `string`                   | —       | 当前值(受控) <br/> Current value (controlled)             |
| `onChange`     | `() => void`               | —       | 值变化回调 <br/> Callback when value changes              |
| `defaultValue` | `string`                   | —       | 默认值(非受控) <br/> Default value (uncontrolled)         |
| ...rest        | `React.HTMLAttributes`     | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **CVA 变体**: 通过 `class-variance-authority` 管理样式变体,类型安全。变体对象可导出复用。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-ui-segmented-control--docs)。
