import { SortableList } from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# SortableList 拖拽排序列表

> 通用组件 / General-purpose utility components.

拖拽排序列表 — 通用拖拽排序列表，支持列表项重新排序。
SortableList — Generic drag-and-drop sortable list with reordering support.

## 何时使用 / When to Use

当你需要「拖拽排序列表」相关能力时使用：通用拖拽排序列表，支持列表项重新排序。作为页面中的通用基础能力使用；优先复用而非在业务里再造一套。

Use SortableList when you need: Generic drag-and-drop sortable list with reordering support. Use as a shared primitive across the app; prefer reuse over reinventing in feature code.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { SortableList } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <SortableList />
}`} lang="tsx" />

## API / Props

### SortableList

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](http://localhost:6006/?path=/docs/components-sortablelist--docs)。
