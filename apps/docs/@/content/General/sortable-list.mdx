import { SortableList } from "@chaos_team/chaos-ui/ui"
import { CodeBlock } from "@/components/code-block"

# SortableList 拖拽排序列表

> 通用组件 / General-purpose utility components.

拖拽排序列表 — 通用拖拽排序列表，支持列表项重新排序。
SortableList — Generic drag-and-drop sortable list with reordering support.

## 何时使用 / When to Use

需要拖拽排序列表功能时使用本组件。

Use when you need a sortablelist.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { SortableList } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <SortableList />
}`} lang="tsx" />


## API / Props

### SortableList

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-sortablelist--docs)。
