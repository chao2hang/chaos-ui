import { SignaturePad } from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# SignaturePad 签名板

> 签名板 / Canvas-based signature pad.

基于画布的签名板，支持撤销、清除和导出为 data URL/Blob。
Canvas-based signature pad with undo, clear, and export to data URL/Blob.

## 何时使用 / When to Use

当你需要「签名板」相关能力时使用：基于画布的签名板，支持撤销、清除和导出为 data URL/Blob。作为页面中的通用基础能力使用；优先复用而非在业务里再造一套。

Use SignaturePad when you need: Canvas-based signature pad with undo, clear, and export to data URL/Blob. Use as a shared primitive across the app; prefer reuse over reinventing in feature code.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { SignaturePad } from "@chaos_team/chaos-ui/ui"
import { useRef } from "react"

export function Basic() {
  const ref = useRef(null)
  return (
    <div className="flex flex-col gap-2">
      <SignaturePad ref={ref} />
      <button onClick={() => ref.current?.clear()}>Clear</button>
      <button onClick={() => console.log(ref.current?.toDataURL())}>Export</button>
    </div>
  )
}`} lang="tsx" />

## API / Props

### SignaturePad

| Prop        | Type         | Default | 说明 / Description                    |
| ----------- | ------------ | ------- | ------------------------------------- |
| `className` | `string`     | —       | 自定义样式类名 <br/> Custom CSS class |
| `onEnd`     | `() => void` | —       | Stroke end callback / 笔画结束回调    |
| `onBegin`   | `() => void` | —       | Stroke begin callback / 笔画开始回调  |

### Imperative Handle (ref)

| Method             | Type                                | 说明 / Description                       |
| ------------------ | ----------------------------------- | ---------------------------------------- |
| `clear()`          | `() => void`                        | Clear the canvas / 清空画布              |
| `isEmpty()`        | `() => boolean`                     | Check if canvas has ink / 检查是否有签名 |
| `toDataURL(type?)` | `(type?: string) => string \| null` | Export as data URL / 导出为 data URL     |
| `toBlob()`         | `() => Promise<Blob \| null>`       | Export as Blob / 导出为 Blob             |
| `undo()`           | `() => void`                        | Undo last stroke / 撤销上一笔            |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **Canvas API**: 基于 HTML5 Canvas 实现，需浏览器支持。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](http://localhost:6006/?path=/docs/components-signaturepad--docs)。
