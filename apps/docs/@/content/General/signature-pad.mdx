import { SignaturePad } from "@/components/ui/signature-pad";
import { CodeBlock } from "@/components/code-block";

# SignaturePad 签名板

> 通用基础组件 / General-purpose primitives used across the app.

签名板 — Chaos UI 的通用基础组件。
SignaturePad — a general-purpose primitives used across the app. from Chaos UI.

## 何时使用 / When to Use

需要签名板功能时使用本组件。

Use when you need a signature pad.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { SignaturePad } from "@/components/ui/signature-pad"

export function Basic() {
  return <SignaturePad />
}`} lang="tsx" />

### 尺寸 / Sizes

<CodeBlock
  code={`{/* 尺寸对照 */}
<div className="flex flex-wrap items-center gap-3">
  <SignaturePad size="sm">Sm</SignaturePad>
  <SignaturePad size="default">Default</SignaturePad>
  <SignaturePad size="lg">Lg</SignaturePad>
</div>`}
  lang="tsx"
/>

## 样式变体 / Style Variants

**尺寸 / Size**

| Size      | 说明 / Description      | 默认 |
| --------- | ----------------------- | ---- |
| `default` | 默认尺寸 / Default size | ✓    |
| `sm`      | 小号 / Small            |      |
| `lg`      | 大号 / Large            |      |

## API / Props

### SignaturePad

| Prop              | Type                          | Default   | 说明 / Description                                          |
| ----------------- | ----------------------------- | --------- | ----------------------------------------------------------- |
| `className`       | `string`                      | —         | 自定义样式类名 <br/> Custom CSS class                       |
| `onSave`          | `() => void`                  | —         | Called when signature data URL changes                      |
| `penColor`        | `string`                      | `#000000` | Pen color                                                   |
| `penWidth`        | `number`                      | `2`       | Pen width                                                   |
| `backgroundColor` | `string`                      | `#ffffff` | Background color                                            |
| `readOnly`        | `boolean`                     | `false`   | Whether the pad is read-only                                |
| `onClear`         | `() => void`                  | —         | Clear signature                                             |
| ...rest           | `React.ComponentProps<"div">` | —         | 透传 `<div>` 原生属性 <br/> Inherits all `<div>` attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **CVA 变体**: 通过 `class-variance-authority` 管理样式变体,类型安全。变体对象可导出复用。
- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-signature-pad--docs)。
