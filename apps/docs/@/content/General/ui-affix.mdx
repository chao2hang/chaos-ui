import { Affix } from "@/components/ui/affix";
import { CodeBlock } from "@/components/code-block";

# Affix Ui Affix

> 通用基础组件 / General-purpose primitives used across the app.

Ui Affix — Chaos UI 的通用基础组件。
Affix — a general-purpose primitives used across the app. from Chaos UI.

## 何时使用 / When to Use

适用于Ui Affix相关场景。

Use when you need an affix.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { Affix } from "@/components/ui/affix"

export function Basic() {
  return <Affix />
}`} lang="tsx" />

## 样式变体 / Style Variants

**position / position**

| position       | 说明 / Description | 默认 |
| -------------- | ------------------ | ---- |
| `top`          |                    |      |
| `bottom`       |                    |      |
| `top-start`    |                    |      |
| `top-end`      |                    |      |
| `bottom-start` |                    |      |
| `bottom-end`   |                    | ✓    |

## API / Props

### Affix

| Prop        | Type                          | Default | 说明 / Description                                          |
| ----------- | ----------------------------- | ------- | ----------------------------------------------------------- |
| `className` | `string`                      | —       | 自定义样式类名 <br/> Custom CSS class                       |
| `offset`    | `number`                      | `0`     | Offset from the target position in pixels                   |
| `target`    | `(...args) => unknown`        | —       | Target container ref to observe scroll                      |
| ...rest     | `React.ComponentProps<"div">` | —       | 透传 `<div>` 原生属性 <br/> Inherits all `<div>` attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **CVA 变体**: 通过 `class-variance-authority` 管理样式变体,类型安全。变体对象可导出复用。
- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-ui-affix--docs)。
