import {
  ConfigProvider,
  ConfigContext,
  Config,
} from "@/components/ui/config-provider";
import { CodeBlock } from "@/components/code-block";

# ConfigProvider Ui Config Provider

> 通用基础组件 / General-purpose primitives used across the app.

Ui Config Provider — Chaos UI 的通用基础组件。
ConfigProvider — a general-purpose primitives used across the app. from Chaos UI.

## 何时使用 / When to Use

适用于Ui Config Provider相关场景。

Use when you need a config provider.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { ConfigProvider, ConfigContext, Config } from "@/components/ui/config-provider"

export function Basic() {
  return (
    <div className="space-y-4">
  <ConfigProvider />
  <ConfigContext />
  <Config />
    </div>
  )
}`} lang="tsx" />

## API / Props

### ConfigProvider

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-ui-config-provider--docs)。
