import {
  Collapsible,
  CollapsibleTrigger,
  CollapsibleContent,
} from "@/components/ui/collapsible";
import { CodeBlock } from "@/components/code-block";

# Collapsible 折叠面板

> 通用基础组件 / General-purpose primitives used across the app.

折叠面板 — Chaos UI 的通用基础组件。
Collapsible — a general-purpose primitives used across the app. from Chaos UI.

## 何时使用 / When to Use

需要折叠面板功能时使用本组件。

Use when you need a collapsible.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { Collapsible, CollapsibleTrigger, CollapsibleContent } from "@/components/ui/collapsible"

export function Basic() {
  return (
    <Collapsible>
      <CollapsibleContent>Content</CollapsibleContent>
    </Collapsible>
  )
}`} lang="tsx" />

## API / Props

### Collapsible

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **复合组件**: 包含 2 个子组件(CollapsibleTrigger, CollapsibleContent),使用组合模式按需导入。
- **Base UI**: 基于 `@base-ui/react` 无头(headless)原语构建,无障碍(a11y)开箱即用。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-collapsible--docs)。
