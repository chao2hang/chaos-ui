import { I18nProvider } from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# I18nProvider

> General-purpose utility components.

I18nProvider — Global i18n provider with locale switching and message bundling.

## When to Use

Use I18nProvider when you need: Global i18n provider with locale switching and message bundling. Use as a shared primitive across the app; prefer reuse over reinventing in feature code.

## Examples

### Basic Usage

<CodeBlock code={`import { I18nProvider } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <I18nProvider />
}`} lang="tsx" />

## API / Props

### I18nProvider

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
