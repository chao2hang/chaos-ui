import { I18nProvider } from "@/components/ui/i18n-provider"
import { CodeBlock } from "@/components/code-block"

# I18nProvider

> General-purpose utility components.

I18nProvider — Global i18n provider with locale switching and message bundling.

## When to Use

Use when you need a i18nprovider.


## Examples

### Basic Usage

<CodeBlock code={`import { I18nProvider } from "@/components/ui/i18n-provider"

export function Basic() {
  return <I18nProvider />
}`} lang="tsx" />


## API / Props

### I18nProvider

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


