import { RTLProvider } from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# RTLProvider RTL Provider

> General-purpose utility components.

RTLProvider — Right-to-left layout provider for Arabic and Hebrew locales.

## When to Use

Use RTLProvider when you need: Right-to-left layout provider for Arabic and Hebrew locales. Use as a shared primitive across the app; prefer reuse over reinventing in feature code.

## Examples

### Basic Usage

<CodeBlock code={`import { RTLProvider } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <RTLProvider />
}`} lang="tsx" />

## API / Props

### RTLProvider

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
