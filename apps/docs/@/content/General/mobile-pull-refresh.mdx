import { MobilePullRefresh } from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# MobilePullRefresh 下拉刷新

> 移动端下拉刷新 / Pull-to-refresh container for mobile.

移动端下拉刷新容器，触摸下拉超过阈值时触发 onRefresh。
Pull-to-refresh container for mobile; triggers onRefresh when touch pull exceeds threshold.

## 何时使用 / When to Use

当你需要「Mobile Pull Refresh（MobilePullRefresh）」解决具体交互/展示问题时使用；优先看下方示例与 API，按业务字段接入。

Use when you need MobilePullRefresh for a concrete interaction; follow the examples and API below.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { MobilePullRefresh } from "@chaos_team/chaos-ui/ui"
import { useState } from "react"

export function Basic() {
  const [refreshing, setRefreshing] = useState(false)
  const onRefresh = async () => {
    setRefreshing(true)
    await fetchData()
    setRefreshing(false)
  }
  return (
    <MobilePullRefresh refreshing={refreshing} onRefresh={onRefresh}>
      <div>List items...</div>
    </MobilePullRefresh>
  )
}`} lang="tsx" />

## API / Props

### MobilePullRefresh

| Prop         | Type                          | Default | 说明 / Description                      |
| ------------ | ----------------------------- | ------- | --------------------------------------- |
| `onRefresh`  | `() => Promise<void> \| void` | —       | Refresh callback / 刷新回调             |
| `refreshing` | `boolean`                     | `false` | Refreshing state / 刷新状态             |
| `children`   | `React.ReactNode`             | —       | Content / 子内容                        |
| `threshold`  | `number`                      | `60`    | Pull threshold in px / 触发阈值（像素） |
| `className`  | `string`                      | —       | 自定义样式类名 <br/> Custom CSS class   |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **移动端专用**: 基于触摸事件，适用于移动端场景。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](http://localhost:6006/?path=/docs/components-mobilepullrefresh--docs)。
