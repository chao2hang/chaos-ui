import { RowContextMenu } from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# RowContextMenu 行右键菜单

> 通用组件 / General-purpose utility components.

行右键菜单 — 表格/列表行右键菜单，支持图标、危险项与分隔线。
RowContextMenu — Table/list row right-click context menu with icons, danger items and separators.

## 何时使用 / When to Use

当你需要「行右键菜单」相关能力时使用：表格/列表行右键菜单，支持图标、危险项与分隔线。作为页面中的通用基础能力使用；优先复用而非在业务里再造一套。

Use RowContextMenu when you need: Table/list row right-click context menu with icons, danger items and separators. Use as a shared primitive across the app; prefer reuse over reinventing in feature code.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { RowContextMenu } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <RowContextMenu />
}`} lang="tsx" />

## API / Props

### RowContextMenu

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](http://localhost:6006/?path=/docs/components-rowcontextmenu--docs)。
