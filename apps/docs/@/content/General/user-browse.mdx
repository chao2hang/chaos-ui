import { UserBrowse } from "@/components/ui/user-browse";
import { CodeBlock } from "@/components/code-block";

# UserBrowse 用户选择

> 用户选择对话框 / User picker dialog with search and multi-select.

用户选择对话框，支持搜索、头像展示和多选。
User picker dialog with search, avatar display, and multi-select support.

## 何时使用 / When to Use

需要选择一个或多个用户时使用，如任务分配、权限授予、审批人选择。
Use when you need to select one or more users, such as task assignment, permission grants, or approver selection.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { UserBrowse } from "@/components/ui/user-browse"
import { useState } from "react"

export function Basic() {
  const [value, setValue] = useState(null)
  return <UserBrowse value={value} onChange={setValue} />
}`} lang="tsx" />

## API / Props

### UserBrowse

| Prop           | Type                              | Default | 说明 / Description           |
| -------------- | --------------------------------- | ------- | ---------------------------- |
| `value`        | `User \| User[]`                  | —       | Selected user(s) / 已选用户  |
| `defaultValue` | `User \| User[]`                  | —       | Default selection / 默认选择 |
| `onChange`     | `(value: User \| User[]) => void` | —       | Change callback / 变化回调   |
| `multiple`     | `boolean`                         | `false` | Multi-select / 多选          |
| `placeholder`  | `string`                          | —       | Placeholder / 占位文本       |

### User

| Prop         | Type     | 说明 / Description             |
| ------------ | -------- | ------------------------------ |
| `id`         | `string` | Unique ID / 唯一 ID (required) |
| `name`       | `string` | User name / 用户名 (required)  |
| `email`      | `string` | Email / 邮箱                   |
| `avatar`     | `string` | Avatar URL / 头像地址          |
| `department` | `string` | Department / 部门              |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **基于 Dialog**: 内部使用 Dialog 组件作为选择面板容器。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-userbrowse--docs)。
