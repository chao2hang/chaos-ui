import { RowContextMenu } from "@chaos_team/chaos-ui/ui"
import { CodeBlock } from "@/components/code-block"

# RowContextMenu

> General-purpose utility components.

RowContextMenu — Table/list row right-click context menu with icons, danger items and separators.

## When to Use

Use when you need a rowcontextmenu.


## Examples

### Basic Usage

<CodeBlock code={`import { RowContextMenu } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <RowContextMenu />
}`} lang="tsx" />


## API / Props

### RowContextMenu

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


