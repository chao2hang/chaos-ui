import { CopyButton } from "@chaos_team/chaos-ui/ui"
import { CodeBlock } from "@/components/code-block"

# CopyButton

> General-purpose utility components.

CopyButton — One-click copy button with success feedback.

## When to Use

Use when you need a copybutton.


## Examples

### Basic Usage

<CodeBlock code={`import { CopyButton } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <CopyButton />
}`} lang="tsx" />


## API / Props

### CopyButton

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


