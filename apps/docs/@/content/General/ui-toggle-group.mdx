import { ToggleGroup, ToggleGroupItem } from "@/components/ui/toggle-group";
import { CodeBlock } from "@/components/code-block";

# ToggleGroup Ui Toggle Group

> 通用基础组件 / General-purpose primitives used across the app.

Ui Toggle Group — Chaos UI 的通用基础组件。
ToggleGroup — a general-purpose primitives used across the app. from Chaos UI.

## 何时使用 / When to Use

适用于Ui Toggle Group相关场景。

Use when you need a toggle group.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { ToggleGroup, ToggleGroupItem } from "@/components/ui/toggle-group"

export function Basic() {
  return (
    <ToggleGroup>
      <ToggleGroupItem>Content</ToggleGroupItem>
    </ToggleGroup>
  )
}`} lang="tsx" />

## API / Props

### ToggleGroup

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **复合组件**: 包含 1 个子组件(ToggleGroupItem),使用组合模式按需导入。
- **Base UI**: 基于 `@base-ui/react` 无头(headless)原语构建,无障碍(a11y)开箱即用。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-ui-toggle-group--docs)。
