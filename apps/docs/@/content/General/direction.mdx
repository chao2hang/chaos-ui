import { Direction } from "@/components/ui/direction";
import { CodeBlock } from "@/components/code-block";

# Direction

> 通用基础组件 / General-purpose primitives used across the app.

Direction — Chaos UI 的通用基础组件。
Direction — a general-purpose primitives used across the app. from Chaos UI.

## 何时使用 / When to Use

适用于Direction相关场景。

Use when you need a direction.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { Direction } from "@/components/ui/direction"

export function Basic() {
  return <Direction />
}`} lang="tsx" />

## API / Props

### Direction

| Prop        | Type                          | Default | 说明 / Description                                          |
| ----------- | ----------------------------- | ------- | ----------------------------------------------------------- |
| `className` | `string`                      | —       | 自定义样式类名 <br/> Custom CSS class                       |
| `dir`       | `DirectionValue`              | `ltr`   | Text direction / 文本方向                                   |
| ...rest     | `React.ComponentProps<"div">` | —       | 透传 `<div>` 原生属性 <br/> Inherits all `<div>` attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-direction--docs)。
