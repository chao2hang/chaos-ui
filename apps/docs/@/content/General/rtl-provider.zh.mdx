import { RTLProvider } from "@chaos_team/chaos-ui/ui"
import { CodeBlock } from "@/components/code-block"

# RTLProvider RTL Provider

> 通用组件

RTL Provider — 从右到左布局 Provider，支持阿拉伯语和希伯来语。

## 何时使用

需要RTL Provider功能时使用本组件。


## 代码示例

### 基础用法

<CodeBlock code={`import { RTLProvider } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <RTLProvider />
}`} lang="tsx" />


## API / Props

### RTLProvider

| Prop | Type | Default | 说明 |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-rtlprovider--docs)。

