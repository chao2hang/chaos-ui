import { UserBrowse } from "@/components/ui/user-browse";
import { CodeBlock } from "@/components/code-block";

# UserBrowse Ui User Browse

> 通用基础组件 / General-purpose primitives used across the app.

Ui User Browse — Chaos UI 的通用基础组件。
UserBrowse — a general-purpose primitives used across the app. from Chaos UI.

## 何时使用 / When to Use

适用于Ui User Browse相关场景。

Use when you need an user browse.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { UserBrowse } from "@/components/ui/user-browse"

export function Basic() {
  return <UserBrowse />
}`} lang="tsx" />

## API / Props

### UserBrowse

| Prop           | Type                   | Default          | 说明 / Description                                        |
| -------------- | ---------------------- | ---------------- | --------------------------------------------------------- |
| `className`    | `string`               | —                | 自定义样式类名 <br/> Custom CSS class                     |
| `value`        | `User \| User[]`       | —                | 当前值(受控) <br/> Current value (controlled)             |
| `defaultValue` | `User \| User[]`       | —                | 默认值(非受控) <br/> Default value (uncontrolled)         |
| `placeholder`  | `string`               | `Select user...` | 占位文本 <br/> Placeholder text                           |
| `disabled`     | `boolean`              | —                | 是否禁用 <br/> Whether disabled                           |
| `multiple`     | `boolean`              | `false`          | 是否多选 <br/> Multiple selection                         |
| `maxCount`     | `number`               | —                |                                                           |
| `users`        | `User[]`               | —                |                                                           |
| `onChange`     | `() => void`           | —                | 值变化回调 <br/> Callback when value changes              |
| `onBrowse`     | `() => void`           | —                |                                                           |
| ...rest        | `React.HTMLAttributes` | —                | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-ui-user-browse--docs)。
