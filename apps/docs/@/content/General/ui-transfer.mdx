import { Transfer } from "@/components/ui/transfer";
import { CodeBlock } from "@/components/code-block";

# Transfer Ui Transfer

> 通用基础组件 / General-purpose primitives used across the app.

Ui Transfer — Chaos UI 的通用基础组件。
Transfer — a general-purpose primitives used across the app. from Chaos UI.

## 何时使用 / When to Use

适用于Ui Transfer相关场景。

Use when you need a transfer.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { Transfer } from "@/components/ui/transfer"

export function Basic() {
  return <Transfer />
}`} lang="tsx" />

## API / Props

### Transfer

| Prop           | Type                   | Default | 说明 / Description                                        |
| -------------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className`    | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `dataSource`   | `TransferItem[]`       | —       |                                                           |
| `targetKeys`   | `string[]`             | —       |                                                           |
| `onChange`     | `() => void`           | —       | 值变化回调 <br/> Callback when value changes              |
| `titles`       | `[string, string]`     | —       |                                                           |
| `searchable`   | `boolean`              | `true`  |                                                           |
| `filterOption` | `(...args) => unknown` | —       |                                                           |
| `disabled`     | `boolean`              | —       | 是否禁用 <br/> Whether disabled                           |
| `oneWay`       | `boolean`              | `false` |                                                           |
| ...rest        | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-ui-transfer--docs)。
