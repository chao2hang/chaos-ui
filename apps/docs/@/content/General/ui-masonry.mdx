import { Masonry } from "@/components/ui/masonry";
import { CodeBlock } from "@/components/code-block";

# Masonry Ui Masonry

> 通用基础组件 / General-purpose primitives used across the app.

Ui Masonry — Chaos UI 的通用基础组件。
Masonry — a general-purpose primitives used across the app. from Chaos UI.

## 何时使用 / When to Use

适用于Ui Masonry相关场景。

Use when you need a masonry.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { Masonry } from "@/components/ui/masonry"

export function Basic() {
  return <Masonry />
}`} lang="tsx" />

## API / Props

### Masonry

| Prop        | Type                          | Default | 说明 / Description                                                       |
| ----------- | ----------------------------- | ------- | ------------------------------------------------------------------------ |
| `className` | `string`                      | —       | 自定义样式类名 <br/> Custom CSS class                                    |
| `columns`   | `ColumnConfig`                | `3`     | Number of columns or responsive breakpoint config / 列数或响应式断点配置 |
| `gap`       | `number`                      | `16`    | Gap between items in pixels / 项目间距（像素）                           |
| ...rest     | `React.ComponentProps<"div">` | —       | 透传 `<div>` 原生属性 <br/> Inherits all `<div>` attributes              |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-ui-masonry--docs)。
