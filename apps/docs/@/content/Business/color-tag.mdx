import { ColorTag } from "@/components/business/color-tag"
import { CodeBlock } from "@/components/code-block"

# ColorTag 颜色标签

> 业务组件 / Domain-specific business components for ERP scenarios.

颜色标签 — 面向 ERP/企业场景的业务组件。
ColorTag — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

需要颜色标签功能时使用本组件。

Use when you need a color tag.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { ColorTag } from "@/components/business/color-tag"

export function Basic() {
  return <ColorTag />
}`} lang="tsx" />


## 样式变体 / Style Variants

**color / color**

| color | 说明 / Description | 默认 |
|---|---|---|
| `red` |  |  |
| `orange` |  |  |
| `yellow` |  |  |
| `green` |  |  |
| `blue` |  |  |
| `purple` |  |  |
| `pink` |  |  |
| `gray` |  | ✓ |

## API / Props

### ColorTag

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| `label` | `string` | — | 标签文本 <br/> Label text |
| `dot` | `boolean` | `true` |  |
| ...rest | `React.ComponentProps<"span">` | — | 透传 `<span>` 原生属性 <br/> Inherits all `<span>` attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **CVA 变体**: 通过 `class-variance-authority` 管理样式变体,类型安全。变体对象可导出复用。
- **原生属性**: 继承所有 `<span>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-colortag--docs)。
