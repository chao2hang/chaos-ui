import { HeatmapChart } from "@/components/business/charts/heatmap-chart";
import { CodeBlock } from "@/components/code-block";

# HeatmapChart 热力图

> 业务组件

热力图 — 面向 ERP/企业场景的业务组件。

## 何时使用

需要热力图功能时使用本组件。


## 代码示例

### 基础用法

<CodeBlock code={`import { HeatmapChart } from "@/components/business/charts/heatmap-chart"

export function Basic() {
  return <HeatmapChart />
}`} lang="tsx" />

## API / Props

### HeatmapChart

| Prop | Type | Default | 说明 |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string` | — | 自定义样式类名 |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-charts-heatmap-chart--docs)。

