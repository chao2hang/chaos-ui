import { CheckoutBar } from "@/components/business/checkout-bar";
import { CodeBlock } from "@/components/code-block";

# CheckoutBar Business Checkout Bar

> 业务组件 / Domain-specific business components for ERP scenarios.

Business Checkout Bar — 面向 ERP/企业场景的业务组件。
CheckoutBar — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Business Checkout Bar相关场景。

Use when you need a checkout bar.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { CheckoutBar } from "@/components/business/checkout-bar"

export function Basic() {
  return <CheckoutBar />
}`} lang="tsx" />

## API / Props

### CheckoutBar

| Prop        | Type                   | Default   | 说明 / Description                                        |
| ----------- | ---------------------- | --------- | --------------------------------------------------------- |
| `className` | `string`               | —         | 自定义样式类名 <br/> Custom CSS class                     |
| `total`     | `string \| number`     | —         | Total amount to display.                                  |
| `currency`  | `string`               | `¥`       | Currency symbol or code.                                  |
| `payLabel`  | `string`               | `Pay Now` | Checkout button label. Default "Pay Now".                 |
| `isLoading` | `boolean`              | —         | Whether the checkout is in a loading/processing state.    |
| `onPay`     | `() => void`           | —         | Called when the user clicks the pay button.               |
| ...rest     | `React.HTMLAttributes` | —         | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-business-checkout-bar--docs)。
