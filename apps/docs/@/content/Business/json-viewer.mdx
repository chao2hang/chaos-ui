import { JsonViewer } from "@/components/business/json-viewer"
import { CodeBlock } from "@/components/code-block"

# JsonViewer JSON 查看器

> 业务组件 / Domain-specific business components for ERP scenarios.

JSON 查看器 — 面向 ERP/企业场景的业务组件。
JsonViewer — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于JSON 查看器相关场景。

Use when you need a json viewer.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { JsonViewer } from "@/components/business/json-viewer"

export function Basic() {
  return <JsonViewer />
}`} lang="tsx" />


## API / Props

### JsonViewer

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| `data` | `unknown` | — | 数据源 <br/> Data source |
| `defaultCollapsedDepth` | `number` | `3` |  |
| `rootClassName` | `string` | — |  |
| `showCopy` | `boolean` | `true` |  |
| `indentSize` | `number` | `2` |  |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-json-viewer--docs)。
