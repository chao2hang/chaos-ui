import { LoadingPage, FullPageLoader } from "@chaos_team/chaos-ui/business"
import { CodeBlock } from "@/components/code-block"

# LoadingPage 加载页

> 业务组件 / Domain-specific business components for ERP scenarios.

加载页 — 面向 ERP/企业场景的业务组件。
LoadingPage — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

需要加载页功能时使用本组件。

Use when you need a loading page.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { LoadingPage, FullPageLoader } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return (
    <div className="space-y-4">
  <LoadingPage />
  <FullPageLoader />
    </div>
  )
}`} lang="tsx" />


## API / Props

### LoadingPage

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| `title` | `string` | `加载中...` | 标题 <br/> Title |
| `description` | `string` | — | 描述文本 <br/> Description text |
| `icon` | `React.ReactNode` | — | 图标元素 <br/> Icon element |
| `variant` | `"spinner" \| "dots" \| "pulse"` | `spinner` | 样式变体 <br/> Style variant |
| ...rest | `React.ComponentProps<"div">` | — | 透传 `<div>` 原生属性 <br/> Inherits all `<div>` attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-loadingpage--docs)。
