import { ChatVoiceMessage } from "@/components/business/chat-voice-message"
import { CodeBlock } from "@/components/code-block"

# ChatVoiceMessage Chat Voice Message

> 业务组件 / Domain-specific business components for ERP scenarios.

Chat Voice Message — 面向 ERP/企业场景的业务组件。
ChatVoiceMessage — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Chat Voice Message相关场景。

Use when you need a chat voice message.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { ChatVoiceMessage } from "@/components/business/chat-voice-message"

export function Basic() {
  return <ChatVoiceMessage />
}`} lang="tsx" />


## API / Props

### ChatVoiceMessage

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| `duration` | `number` | — | 持续时间/展示时长(毫秒) <br/> Duration in milliseconds |
| `src` | `string` | — | 资源地址 <br/> Source URL |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-chatvoicemessage--docs)。
