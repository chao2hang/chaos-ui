import { ChartSuite } from "@/components/business/chart-suite";
import { CodeBlock } from "@/components/code-block";

# ChartSuite Business Chart Suite

> 业务组件 / Domain-specific business components for ERP scenarios.

Business Chart Suite — 面向 ERP/企业场景的业务组件。
ChartSuite — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Business Chart Suite相关场景。

Use when you need a chart suite.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { ChartSuite } from "@/components/business/chart-suite"

export function Basic() {
  return <ChartSuite />
}`} lang="tsx" />

## API / Props

### ChartSuite

| Prop        | Type                                       | Default | 说明 / Description                                        |
| ----------- | ------------------------------------------ | ------- | --------------------------------------------------------- |
| `className` | `string`                                   | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `type`      | `"line" \| "bar" \| "pie" \| "radar" \...` | —       | 类型 <br/> Type                                           |
| `data`      | `unknown[]`                                | —       | 数据源 <br/> Data source                                  |
| `xField`    | `string`                                   | `x`     |                                                           |
| `yField`    | `string`                                   | `y`     |                                                           |
| `series`    | `string`                                   | —       |                                                           |
| `height`    | `number`                                   | `200`   | 组件高度 <br/> Component height                           |
| ...rest     | `React.HTMLAttributes`                     | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-business-chart-suite--docs)。
