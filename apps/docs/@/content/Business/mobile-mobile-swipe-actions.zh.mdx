import { SwipeActions } from "@chaos_team/chaos-ui/mobile";
import { CodeBlock } from "@/components/code-block";

# MobileSwipeActions Mobile Mobile Swipe Actions

> 业务组件

Mobile Mobile Swipe Actions — 面向 ERP/企业场景的业务组件。

## 何时使用

在手机/窄屏流程中使用，优先触控与单手操作。典型用于「SwipeActions」场景。面向 ERP/业务域（单据、选择器、审批、结算等），按业务语义接入。

## 代码示例

### 基础用法

<CodeBlock code={`import { SwipeActions } from "@chaos_team/chaos-ui/mobile"

export function Basic() {
  return <SwipeActions />
}`} lang="tsx" />

## API / Props

### SwipeActions

| Prop           | Type                          | Default | 说明                  |
| -------------- | ----------------------------- | ------- | --------------------- |
| `className`    | `string`                      | —       | 自定义样式类名        |
| `onSwipeLeft`  | `() => void`                  | —       |                       |
| `onSwipeRight` | `() => void`                  | —       |                       |
| `onSwipeUp`    | `() => void`                  | —       |                       |
| `onSwipeDown`  | `() => void`                  | —       |                       |
| `threshold`    | `number`                      | `80`    |                       |
| `disabled`     | `boolean`                     | —       | 是否禁用              |
| `leftAction`   | `{`                           | —       |                       |
| `label`        | `string`                      | —       | 标签文本              |
| `color`        | `string`                      | —       | 颜色主题              |
| `onClick`      | `() => void`                  | —       | 点击回调              |
| `icon`         | `React.ReactNode`             | —       | 图标元素              |
| `rightAction`  | `{`                           | —       |                       |
| ...rest        | `React.ComponentProps<"div">` | —       | 透传 `<div>` 原生属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](http://localhost:6006/?path=/docs/components-mobile-mobile-swipe-actions--docs)。
