import { MobileFilterBuilder } from "@chaos_team/chaos-ui/mobile";
import { CodeBlock } from "@/components/code-block";

# MobileFilterBuilder Mobile Mobile Filter Builder

> Domain-specific business components for ERP scenarios.

MobileFilterBuilder — a business component for ERP / enterprise scenarios.

## When to Use

Use MobileMobileFilterBuilder when this capability is required. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { MobileFilterBuilder } from "@chaos_team/chaos-ui/mobile"

export function Basic() {
  return <MobileFilterBuilder />
}`} lang="tsx" />

## API / Props

### MobileFilterBuilder

| Prop        | Type                               | Default | Description                       |
| ----------- | ---------------------------------- | ------- | --------------------------------- |
| `className` | `string`                           | —       | Custom CSS class                  |
| `fields`    | `{ key: string; label: string }[]` | —       |                                   |
| `onChange`  | `() => void`                       | —       | Callback when value changes       |
| ...rest     | `React.HTMLAttributes`             | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
