import { Transfer } from "@/components/business/transfer";
import { CodeBlock } from "@/components/code-block";

# Transfer Business Transfer

> 业务组件 / Domain-specific business components for ERP scenarios.

Business Transfer — 面向 ERP/企业场景的业务组件。
Transfer — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Business Transfer相关场景。

Use when you need a transfer.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { Transfer } from "@/components/business/transfer"

export function Basic() {
  return <Transfer />
}`} lang="tsx" />

## API / Props

### Transfer

| Prop           | Type                   | Default | 说明 / Description                                        |
| -------------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className`    | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `dataSource`   | `TransferItem[]`       | —       |                                                           |
| `targetKeys`   | `string[]`             | —       |                                                           |
| `onChange`     | `() => void`           | —       | 值变化回调 <br/> Callback when value changes              |
| `titles`       | `[string, string]`     | —       |                                                           |
| `searchable`   | `boolean`              | `true`  |                                                           |
| `filterOption` | `(...args) => unknown` | —       |                                                           |
| `disabled`     | `boolean`              | —       | 是否禁用 <br/> Whether disabled                           |
| `oneWay`       | `boolean`              | `false` |                                                           |
| ...rest        | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-business-transfer--docs)。
