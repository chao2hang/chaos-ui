import { BillTodoList } from "@/components/business/bill-todo-list"
import { CodeBlock } from "@/components/code-block"

# BillTodoList Bill Todo List

> 业务组件 / Domain-specific business components for ERP scenarios.

Bill Todo List — 面向 ERP/企业场景的业务组件。
BillTodoList — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Bill Todo List相关场景。

Use when you need a bill todo list.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { BillTodoList } from "@/components/business/bill-todo-list"

export function Basic() {
  return <BillTodoList />
}`} lang="tsx" />


## API / Props

### BillTodoList

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| `items` | `Array<T>` | — | 选项/数据项列表 <br/> Items / data list |
| `onItemClick` | `() => void` | — |  |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-billtodolist--docs)。
