import { LineChart } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# LineChart Line Chart

> 业务组件 / Domain-specific business components for ERP scenarios.

折线图(多线+标注)
LineChart — line chart component.

## 何时使用 / When to Use

适用于Line Chart相关场景。

Use when you need a line chart.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { LineChart } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <LineChart />
}`} lang="tsx" />

## API / Props

### LineChart

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `series`    | `Array<T>`             | —       | One or more named series.                                 |
| `labels`    | `string[]`             | —       | X-axis labels aligned 1:1 with each series values.        |
| `height`    | `number`               | —       | SVG height in px.                                         |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-linechart--docs)。
