import { ChatMessageList, ChatComposer } from "@/components/business/chat";
import { CodeBlock } from "@/components/code-block";

# Chat 聊天

> 业务组件

聊天 — 面向 ERP/企业场景的业务组件。

## 何时使用

需要聊天功能时使用本组件。


## 代码示例

### 基础用法

<CodeBlock code={`import { ChatMessageList, ChatComposer } from "@/components/business/chat"

export function Basic() {
  return (
    <div className="space-y-4">
  <ChatMessageList />
  <ChatComposer />
    </div>
  )
}`} lang="tsx" />

## API / Props

### ChatMessageList

| Prop | Type | Default | 说明 |
| --------------- | ----------------------------- | ------- | ----------------------------------------------------------- |
| `className` | `string` | — | 自定义样式类名 |
| `messages`      | `ChatMessage[]`               | —       |                                                             |
| `currentUserId` | `string`                      | —       |                                                             |
| ...rest | `React.ComponentProps<"div">` | — | 透传 `<div>` 原生属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-chat--docs)。

