import { TreeTable } from "@/components/business/tree-table";
import { CodeBlock } from "@/components/code-block";

# TreeTable

> Domain-specific business components for ERP scenarios.

TreeTable — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a tree table.

## Examples

### Basic Usage

<CodeBlock code={`import { TreeTable } from "@/components/business/tree-table"

export function Basic() {
  return <TreeTable />
}`} lang="tsx" />

## API / Props

### TreeTable

| Prop              | Type                          | Default | Description                     |
| ----------------- | ----------------------------- | ------- | ------------------------------- |
| `className`       | `string`                      | —       | Custom CSS class                |
| `data`            | `TreeNode[]`                  | —       | Data source                     |
| `columns`         | `Array<{`                     | —       | Column configuration            |
| `key`             | `string`                      | —       |                                 |
| `header`          | `string`                      | —       |                                 |
| `defaultExpanded` | `boolean`                     | `false` |                                 |
| ...rest           | `React.ComponentProps<"div">` | —       | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
