import { MobileFormField } from "@/components/mobile/mobile-form-field";
import { CodeBlock } from "@/components/code-block";

# MobileFormField 移动端表单字段

> 业务组件

移动端表单字段 — 面向 ERP/企业场景的业务组件。

## 何时使用

需要移动端表单字段功能时使用本组件。

## 代码示例

### 基础用法

<CodeBlock code={`import { MobileFormField } from "@/components/mobile/mobile-form-field"

export function Basic() {
  return <MobileFormField />
}`} lang="tsx" />

## API / Props

### MobileFormField

| Prop          | Type                   | Default | 说明              |
| ------------- | ---------------------- | ------- | ----------------- |
| `className`   | `string`               | —       | 自定义样式类名    |
| `label`       | `string`               | —       | 标签文本          |
| `description` | `string`               | —       | 描述文本          |
| `error`       | `string`               | —       |                   |
| `required`    | `boolean`              | —       | 是否必填          |
| ...rest       | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-mobileformfield--docs)。
