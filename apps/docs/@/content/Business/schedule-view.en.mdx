import { ScheduleView } from "@/components/business/schedule-view";
import { CodeBlock } from "@/components/code-block";

# ScheduleView

> Domain-specific business components for ERP scenarios.

ScheduleView — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a schedule view.


## Examples

### Basic Usage

<CodeBlock code={`import { ScheduleView } from "@/components/business/schedule-view"

export function Basic() {
  return <ScheduleView />
}`} lang="tsx" />

## API / Props

### ScheduleView

| Prop | Type | Default | Description |
| -------------- | ----------------------------- | ------- | ----------------------------------------------------------- |
| `className` | `string` | — | Custom CSS class |
| `events`       | `ScheduleEvent[]`             | —       |                                                             |
| `onEventClick` | `() => void`                  | —       |                                                             |
| `date`         | `Date`                        | —       |                                                             |
| ...rest | `React.ComponentProps<"div">` | — | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


