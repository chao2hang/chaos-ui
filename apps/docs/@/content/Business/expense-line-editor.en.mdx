import { ExpenseLineEditor } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# ExpenseLineEditor

> Domain-specific business components for ERP scenarios.

ExpenseLineEditor — a business component for ERP / enterprise scenarios.

## When to Use

Use ExpenseLineEditor when you need: ExpenseLineEditor: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { ExpenseLineEditor } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <ExpenseLineEditor />
}`} lang="tsx" />

## API / Props

### ExpenseLineEditor

| Prop         | Type                          | Default | Description                     |
| ------------ | ----------------------------- | ------- | ------------------------------- |
| `className`  | `string`                      | —       | Custom CSS class                |
| `lines`      | `ExpenseLine[]`               | —       |                                 |
| `onChange`   | `() => void`                  | —       | Callback when value changes     |
| `categories` | `string[]`                    | —       |                                 |
| ...rest      | `React.ComponentProps<"div">` | —       | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
