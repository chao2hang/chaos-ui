import { PromotionRuleEditor } from "@/components/business/promotion-rule-editor";
import { CodeBlock } from "@/components/code-block";

# PromotionRuleEditor Promotion Rule Editor

> Domain-specific business components for ERP scenarios.

PromotionRuleEditor — promotion rule editor component.

## When to Use

Use when you need a promotion rule editor.

## Examples

### Basic Usage

<CodeBlock code={`import { PromotionRuleEditor } from "@/components/business/promotion-rule-editor"

export function Basic() {
  return <PromotionRuleEditor />
}`} lang="tsx" />

## API / Props

### PromotionRuleEditor

| Prop        | Type                   | Default | Description                                             |
| ----------- | ---------------------- | ------- | ------------------------------------------------------- |
| `className` | `string`               | —       | Custom CSS class                                        |
| `rules`     | `PromotionRule[]`      | —       | Initial rules. Defaults to a single empty starter rule. |
| `onChange`  | `() => void`           | —       | Called with the current rules whenever they change.     |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes                       |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
