import { AttachmentPreview } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# AttachmentPreview Attachment Preview

> Domain-specific business components for ERP scenarios.

AttachmentPreview — a business component for ERP / enterprise scenarios.

## When to Use

Use AttachmentPreview when you need: AttachmentPreview: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { AttachmentPreview } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <AttachmentPreview />
}`} lang="tsx" />

## API / Props

### AttachmentPreview

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| `url`       | `string`               | —       |                                   |
| `type`      | `string`               | —       | Type                              |
| `name`      | `string`               | —       | Form field name                   |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
