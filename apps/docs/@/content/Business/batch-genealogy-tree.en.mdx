import { BatchGenealogyTree } from "@/components/business/batch-genealogy-tree"
import { CodeBlock } from "@/components/code-block"

# BatchGenealogyTree Batch Genealogy Tree

> Domain-specific business components for ERP scenarios.

BatchGenealogyTree — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a batch genealogy tree.


## Examples

### Basic Usage

<CodeBlock code={`import { BatchGenealogyTree } from "@/components/business/batch-genealogy-tree"

export function Basic() {
  return <BatchGenealogyTree />
}`} lang="tsx" />


## API / Props

### BatchGenealogyTree

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `root` | `BatchNode` | — | Root batch node. |
| `direction` | `"forward" \| "backward"` | `backward` | Traceability direction. |
| `onBatchClick` | `() => void` | — | Batch click handler. |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


