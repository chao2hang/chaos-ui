import { DeltaBar } from "@/components/business/delta-bar"
import { CodeBlock } from "@/components/code-block"

# DeltaBar Delta Bar

> Domain-specific business components for ERP scenarios.

DeltaBar — delta bar component.

## When to Use

Use when you need a delta bar.


## Examples

### Basic Usage

<CodeBlock code={`import { DeltaBar } from "@/components/business/delta-bar"

export function Basic() {
  return <DeltaBar />
}`} lang="tsx" />


## API / Props

### DeltaBar

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `value` | `number` | — | Current value (controlled) |
| `maxValue` | `number` | — |  |
| `label` | `string` | — | Label text |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **无障碍**: 组件遵循 WAI-ARIA 最佳实践。


