import { DictSelect } from "@/components/business/dict-select";
import { CodeBlock } from "@/components/code-block";

# DictSelect Business Dict Select

> 业务组件 / Domain-specific business components for ERP scenarios.

Business Dict Select — 面向 ERP/企业场景的业务组件。
DictSelect — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Business Dict Select相关场景。

Use when you need a dict select.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { DictSelect } from "@/components/business/dict-select"

export function Basic() {
  return <DictSelect />
}`} lang="tsx" />

## API / Props

### DictSelect

| Prop            | Type                   | Default  | 说明 / Description                                        |
| --------------- | ---------------------- | -------- | --------------------------------------------------------- |
| `className`     | `string`               | —        | 自定义样式类名 <br/> Custom CSS class                     |
| `dictCode`      | `string`               | —        |                                                           |
| `options`       | `DictOption[]`         | —        | 可选项列表 <br/> Available options                        |
| `value`         | `string`               | —        | 当前值(受控) <br/> Current value (controlled)             |
| `onValueChange` | `() => void`           | —        |                                                           |
| `placeholder`   | `string`               | `请选择` | 占位文本 <br/> Placeholder text                           |
| ...rest         | `React.HTMLAttributes` | —        | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-business-dict-select--docs)。
