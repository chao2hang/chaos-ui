import { LineChart } from "@/components/business/charts/line-chart";
import { CodeBlock } from "@/components/code-block";

# LineChart

> Domain-specific business components for ERP scenarios.

LineChart — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a line chart.


## Examples

### Basic Usage

<CodeBlock code={`import { LineChart } from "@/components/business/charts/line-chart"

export function Basic() {
  return <LineChart />
}`} lang="tsx" />

## API / Props

### LineChart

| Prop | Type | Default | Description |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string` | — | Custom CSS class |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


