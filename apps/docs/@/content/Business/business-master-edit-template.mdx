import { MasterEditTemplate } from "@/components/business/master-edit-template";
import { CodeBlock } from "@/components/code-block";

# MasterEditTemplate Business Master Edit Template

> 业务组件 / Domain-specific business components for ERP scenarios.

<MasterEditTemplate title="编辑供应商" onSave={() => {}} onCancel={() => {}}>
  {" "}
  <input />{" "}
</MasterEditTemplate>
MasterEditTemplate — business master edit template component.

## 何时使用 / When to Use

适用于Business Master Edit Template相关场景。

Use when you need a master edit template.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { MasterEditTemplate } from "@/components/business/master-edit-template"

export function Basic() {
  return <MasterEditTemplate />
}`} lang="tsx" />

## API / Props

### MasterEditTemplate

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `title`     | `string`               | `编辑`  | 标题 <br/> Title                                          |
| `onBack`    | `() => void`           | —       |                                                           |
| `onSave`    | `() => void`           | —       |                                                           |
| `onCancel`  | `() => void`           | —       |                                                           |
| `loading`   | `boolean`              | `false` | 是否加载中 <br/> Loading state                            |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-business-master-edit-template--docs)。
