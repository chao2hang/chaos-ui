import { TaskProgress } from "@chaos_team/chaos-ui/business"
import { CodeBlock } from "@/components/code-block"

# TaskProgress Task Progress

> 业务组件

<TaskProgress percent={75} status="running" message="正在生成报表…" />

## 何时使用

适用于Task Progress相关场景。


## 代码示例

### 基础用法

<CodeBlock code={`import { TaskProgress } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <TaskProgress />
}`} lang="tsx" />


## API / Props

### TaskProgress

| Prop | Type | Default | 说明 |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 |
| `percent` | `number` | — |  |
| `status` | `string` | — | 状态 |
| `message` | `string` | — | 提示消息内容 |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-taskprogress--docs)。

