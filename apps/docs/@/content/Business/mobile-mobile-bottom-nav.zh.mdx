import { MobileBottomNav } from "@chaos_team/chaos-ui/mobile";
import { CodeBlock } from "@/components/code-block";

# MobileBottomNav Mobile Mobile Bottom Nav

> 业务组件

Mobile Mobile Bottom Nav — 面向 ERP/企业场景的业务组件。

## 何时使用

适用于Mobile Mobile Bottom Nav相关场景。


## 代码示例

### 基础用法

<CodeBlock code={`import { MobileBottomNav } from "@chaos_team/chaos-ui/mobile"

export function Basic() {
  return <MobileBottomNav />
}`} lang="tsx" />

## API / Props

### MobileBottomNav

| Prop | Type | Default | 说明 |
| ----------- | ------------------------- | --------- | --------------------------------------------------------- |
| `className` | `string` | — | 自定义样式类名 |
| `items` | `MobileNavItem[]` | — | 选项/数据项列表 |
| `onChange` | `() => void` | — | 值变化回调 |
| `variant` | `"default" \| "floating"` | `default` | 样式变体 |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-mobile-mobile-bottom-nav--docs)。

