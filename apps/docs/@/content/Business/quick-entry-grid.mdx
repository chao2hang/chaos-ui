import { QuickEntryGrid } from "@/components/business/quick-entry-grid";
import { CodeBlock } from "@/components/code-block";

# QuickEntryGrid Quick Entry Grid

> 业务组件 / Domain-specific business components for ERP scenarios.

<QuickEntryGrid
  entries={[{ id: "bill", label: "新建账单", onClick: () => {} }]}
/>
QuickEntryGrid — quick entry grid component.

## 何时使用 / When to Use

适用于Quick Entry Grid相关场景。

Use when you need a quick entry grid.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { QuickEntryGrid } from "@/components/business/quick-entry-grid"

export function Basic() {
  return <QuickEntryGrid />
}`} lang="tsx" />

## API / Props

### QuickEntryGrid

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `entries`   | `Array<{`              | —       |                                                           |
| `id`        | `string`               | —       | DOM 元素 id <br/> DOM element id                          |
| `icon`      | `React.ReactNode`      | —       | 图标元素 <br/> Icon element                               |
| `label`     | `string`               | —       | 标签文本 <br/> Label text                                 |
| `onClick`   | `() => void`           | —       | 点击回调 <br/> Click handler                              |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-quick-entry-grid--docs)。
