import { MobileInfiniteScroll } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# MobileInfiniteScroll Mobile Infinite Scroll

> Domain-specific business components for ERP scenarios.

MobileInfiniteScroll — a business component for ERP / enterprise scenarios.

## When to Use

Use MobileInfiniteScroll when you need: MobileInfiniteScroll: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { MobileInfiniteScroll } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <MobileInfiniteScroll />
}`} lang="tsx" />

## API / Props

### MobileInfiniteScroll

| Prop         | Type                   | Default | Description                       |
| ------------ | ---------------------- | ------- | --------------------------------- |
| `className`  | `string`               | —       | Custom CSS class                  |
| `onLoadMore` | `() => void`           | —       |                                   |
| `hasMore`    | `boolean`              | `true`  |                                   |
| `loading`    | `boolean`              | `false` | Loading state                     |
| ...rest      | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
