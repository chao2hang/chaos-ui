import { MobileDialog } from "@/components/mobile/mobile-dialog";
import { CodeBlock } from "@/components/code-block";

# MobileDialog Mobile Mobile Dialog

> Domain-specific business components for ERP scenarios.

MobileDialog — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a mobile dialog.


## Examples

### Basic Usage

<CodeBlock code={`import { MobileDialog } from "@/components/mobile/mobile-dialog"

export function Basic() {
  return <MobileDialog />
}`} lang="tsx" />

## API / Props

### MobileDialog

| Prop | Type | Default | Description |
| -------------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string` | — | Custom CSS class |
| `title` | `string` | — | Title |
| `description` | `string` | — | Description text |
| `trigger`      | `React.ReactElement`   | —       |                                                           |
| `actions`      | `React.ReactNode`      | —       |                                                           |
| `open` | `boolean` | — | Open state (controlled) |
| `onOpenChange` | `() => void` | — | Callback when open state changes |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


