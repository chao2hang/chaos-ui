import { CodeBlock } from "@/components/code-block";

# PresenceIndicators

> Domain-specific business components for ERP scenarios.

PresenceIndicators — a business component for ERP / enterprise scenarios.

## When to Use

Use PresenceIndicators when this capability is required. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock
  code={`export function Basic() {
  return <PresenceIndicators />
}`}
  lang="tsx"
/>

## API / Props

### PresenceIndicators

| Prop        | Type                          | Default | Description                     |
| ----------- | ----------------------------- | ------- | ------------------------------- |
| `className` | `string`                      | —       | Custom CSS class                |
| `users`     | `PresenceUser[]`              | —       |                                 |
| `max`       | `number`                      | `5`     | Maximum value                   |
| ...rest     | `React.ComponentProps<"div">` | —       | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
