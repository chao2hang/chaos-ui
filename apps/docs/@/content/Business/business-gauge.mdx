import { Gauge, RadialProgress } from "@/components/business/gauge";
import { CodeBlock } from "@/components/code-block";

# Gauge Business Gauge

> 业务组件 / Domain-specific business components for ERP scenarios.

Business Gauge — 面向 ERP/企业场景的业务组件。
Gauge — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Business Gauge相关场景。

Use when you need a gauge.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { Gauge, RadialProgress } from "@/components/business/gauge"

export function Basic() {
  return (
    <div className="space-y-4">
  <Gauge />
  <RadialProgress />
    </div>
  )
}`} lang="tsx" />

## API / Props

### Gauge

| Prop          | Type                                       | Default   | 说明 / Description                                          |
| ------------- | ------------------------------------------ | --------- | ----------------------------------------------------------- |
| `className`   | `string`                                   | —         | 自定义样式类名 <br/> Custom CSS class                       |
| `value`       | `number`                                   | —         | 当前值(受控) <br/> Current value (controlled)               |
| `min`         | `number`                                   | `0`       | 最小值 <br/> Minimum value                                  |
| `max`         | `number`                                   | `100`     | 最大值 <br/> Maximum value                                  |
| `size`        | `number`                                   | `160`     | 组件尺寸 <br/> Component size                               |
| `strokeWidth` | `number`                                   | `12`      |                                                             |
| `label`       | `React.ReactNode`                          | —         | 标签文本 <br/> Label text                                   |
| `showValue`   | `boolean`                                  | `true`    |                                                             |
| `formatValue` | `(...args) => unknown`                     | —         |                                                             |
| `variant`     | `"default" \| "success" \| "warning" \...` | `default` | 样式变体 <br/> Style variant                                |
| ...rest       | `React.ComponentProps<"div">`              | —         | 透传 `<div>` 原生属性 <br/> Inherits all `<div>` attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-business-gauge--docs)。
