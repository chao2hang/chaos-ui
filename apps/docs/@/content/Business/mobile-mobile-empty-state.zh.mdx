import { MobileEmptyState } from "@/components/mobile/mobile-empty-state";
import { CodeBlock } from "@/components/code-block";

# MobileEmptyState Mobile Mobile Empty State

> 业务组件

Mobile Mobile Empty State — 面向 ERP/企业场景的业务组件。

## 何时使用

适用于Mobile Mobile Empty State相关场景。


## 代码示例

### 基础用法

<CodeBlock code={`import { MobileEmptyState } from "@/components/mobile/mobile-empty-state"

export function Basic() {
  return <MobileEmptyState />
}`} lang="tsx" />

## API / Props

### MobileEmptyState

| Prop | Type | Default | 说明 |
| ------------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string` | — | 自定义样式类名 |
| `variant` | `string` | — | 样式变体 |
| `icon` | `React.ElementType` | — | 图标元素 |
| `title` | `string` | — | 标题 |
| `description` | `string` | — | 描述文本 |
| `action`      | `React.ReactNode`      | —       |                                                           |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-mobile-mobile-empty-state--docs)。

