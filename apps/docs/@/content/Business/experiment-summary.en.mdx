import { ExperimentSummary } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# ExperimentSummary

> Domain-specific business components for ERP scenarios.

ExperimentSummary — a business component for ERP / enterprise scenarios.

## When to Use

Use ExperimentSummary when you need: ExperimentSummary: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { ExperimentSummary } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <ExperimentSummary />
}`} lang="tsx" />

## API / Props

### ExperimentSummary

| Prop          | Type                                    | Default   | Description                       |
| ------------- | --------------------------------------- | --------- | --------------------------------- |
| `className`   | `string`                                | —         | Custom CSS class                  |
| `name`        | `string`                                | —         | Form field name                   |
| `controlRate` | `number`                                | —         |                                   |
| `variantRate` | `number`                                | —         |                                   |
| `metric`      | `string`                                | `转化率`  |                                   |
| `confidence`  | `number`                                | `95`      |                                   |
| `status`      | `"running" \| "completed" \| "stopped"` | `running` | Status                            |
| ...rest       | `React.HTMLAttributes`                  | —         | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
