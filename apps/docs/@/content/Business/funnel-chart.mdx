import { FunnelChart } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# FunnelChart Funnel Chart

> 业务组件 / Domain-specific business components for ERP scenarios.

conversion percentages. <FunnelChart data={[{label:"访问",value:1000},{label:"下单",value:300}]} />
FunnelChart — funnel chart component.

## 何时使用 / When to Use

适用于Funnel Chart相关场景。

Use when you need a funnel chart.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { FunnelChart } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <FunnelChart />
}`} lang="tsx" />

## API / Props

### FunnelChart

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `data`      | `Array<T>`             | —       | 数据源 <br/> Data source                                  |
| `height`    | `number`               | `44`    | 组件高度 <br/> Component height                           |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-funnelchart--docs)。
