import { OrgPicker } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# OrgPicker 组织选择器

> 业务组件

组织选择器 — 组织选择器对话框，支持部门树与成员多选。

## 何时使用

需要组织选择器功能时使用本组件。

## 代码示例

### 基础用法

<CodeBlock code={`import { OrgPicker } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <OrgPicker />
}`} lang="tsx" />

## API / Props

### OrgPicker

| Prop        | Type                   | Default | 说明              |
| ----------- | ---------------------- | ------- | ----------------- |
| `className` | `string`               | —       | 自定义样式类名    |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-orgpicker--docs)。
