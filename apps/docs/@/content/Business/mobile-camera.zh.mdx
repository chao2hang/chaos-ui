import { MobileCamera } from "@/components/business/mobile-camera";
import { CodeBlock } from "@/components/code-block";

# MobileCamera Mobile Camera

> 业务组件

Mobile Camera — 面向 ERP/企业场景的业务组件。

## 何时使用

适用于Mobile Camera相关场景。

## 代码示例

### 基础用法

<CodeBlock code={`import { MobileCamera } from "@/components/business/mobile-camera"

export function Basic() {
  return <MobileCamera />
}`} lang="tsx" />

## API / Props

### MobileCamera

| Prop        | Type                   | Default | 说明              |
| ----------- | ---------------------- | ------- | ----------------- |
| `className` | `string`               | —       | 自定义样式类名    |
| `onCapture` | `() => void`           | —       |                   |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-mobilecamera--docs)。
