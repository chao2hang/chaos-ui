import { SankeyChart } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# SankeyChart Sankey Chart

> 业务组件 / Domain-specific business components for ERP scenarios.

with weighted connecting bands (pure SVG, no deps). 桑基图(流量流转)
SankeyChart — sankey chart component.

## 何时使用 / When to Use

适用于Sankey Chart相关场景。

Use when you need a sankey chart.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { SankeyChart } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <SankeyChart />
}`} lang="tsx" />

## API / Props

### SankeyChart

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `flows`     | `Array<T>`             | —       | Source → target flows.                                    |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-sankeychart--docs)。
