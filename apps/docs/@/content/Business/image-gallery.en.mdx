import { ImageGallery } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# ImageGallery

> Domain-specific business components for ERP scenarios.

ImageGallery — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need an image gallery.

## Examples

### Basic Usage

<CodeBlock code={`import { ImageGallery } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <ImageGallery />
}`} lang="tsx" />

## API / Props

### ImageGallery

| Prop             | Type                          | Default | Description                     |
| ---------------- | ----------------------------- | ------- | ------------------------------- |
| `className`      | `string`                      | —       | Custom CSS class                |
| `images`         | `GalleryImage[]`              | —       |                                 |
| `columns`        | `2 \| 3 \| 4 \| 5`            | `3`     | Column configuration            |
| `enableLightbox` | `boolean`                     | `true`  |                                 |
| ...rest          | `React.ComponentProps<"div">` | —       | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
