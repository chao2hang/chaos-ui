import { PaymentSchedule } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# PaymentSchedule Payment Schedule

> 业务组件

Payment Schedule — 面向 ERP/企业场景的业务组件。

## 何时使用

在需要Payment Schedule时使用：Payment Schedule — 组件库业务组件，详见 Storybook 与源码。面向 ERP/业务域（单据、选择器、审批、结算等），按业务语义接入。

## 代码示例

### 基础用法

<CodeBlock code={`import { PaymentSchedule } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <PaymentSchedule />
}`} lang="tsx" />

## API / Props

### PaymentSchedule

| Prop        | Type                   | Default | 说明              |
| ----------- | ---------------------- | ------- | ----------------- |
| `className` | `string`               | —       | 自定义样式类名    |
| `schedule`  | `Array<T>`             | —       |                   |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](http://localhost:6006/?path=/docs/business-paymentschedule--docs)。
