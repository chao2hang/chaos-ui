import { EventDetail } from "@/components/business/calendar/event-detail"
import { CodeBlock } from "@/components/code-block"

# EventDetail 日历事件详情

> 业务组件 / Domain-specific business components for ERP scenarios.

日历事件详情 — 面向 ERP/企业场景的业务组件。
EventDetail — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

需要日历事件详情功能时使用本组件。

Use when you need an event detail.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { EventDetail } from "@/components/business/calendar/event-detail"

export function Basic() {
  return <EventDetail />
}`} lang="tsx" />

### 状态 / States

<CodeBlock code={`<EventDetail>Normal</EventDetail>
<EventDetail disabled>Disabled</EventDetail>`} lang="tsx" />


## API / Props

### EventDetail

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| `event` | `EventDetailData` | — |  |
| `onRsvp` | `() => void` | — |  |
| ...rest | `React.ComponentProps<"div">` | — | 透传 `<div>` 原生属性 <br/> Inherits all `<div>` attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-calendar-event-detail--docs)。
