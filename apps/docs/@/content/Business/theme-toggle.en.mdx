import { ThemeToggle } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# ThemeToggle

> Domain-specific business components for ERP scenarios.

ThemeToggle — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a theme toggle.

## Examples

### Basic Usage

<CodeBlock code={`import { ThemeToggle } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <ThemeToggle />
}`} lang="tsx" />

## API / Props

### ThemeToggle

| Prop         | Type                           | Default | Description                       |
| ------------ | ------------------------------ | ------- | --------------------------------- |
| `className`  | `string`                       | —       | Custom CSS class                  |
| `showSystem` | `boolean`                      | `true`  |                                   |
| `align`      | `"start" \| "center" \| "end"` | `end`   | Alignment                         |
| ...rest      | `React.HTMLAttributes`         | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
