import { GanttChart } from "@/components/business/gantt-chart"
import { CodeBlock } from "@/components/code-block"

# GanttChart Gantt Chart

> 业务组件

<GanttChart tasks={[{id:"1",name:"设计",start:"2026-06-01",end:"2026-06-05"}]} />

## 何时使用

适用于Gantt Chart相关场景。


## 代码示例

### 基础用法

<CodeBlock code={`import { GanttChart } from "@/components/business/gantt-chart"

export function Basic() {
  return <GanttChart />
}`} lang="tsx" />


## API / Props

### GanttChart

| Prop | Type | Default | 说明 |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 |
| `tasks` | `Array<{` | — |  |
| `id` | `string` | — | DOM 元素 id |
| `name` | `string` | — | 表单字段名 |
| `start` | `string` | — |  |
| `end` | `string` | — |  |
| `progress` | `number` | — |  |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-ganttchart--docs)。

