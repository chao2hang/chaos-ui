import { SalesTargetEditor } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# SalesTargetEditor Sales Target Editor

> 业务组件 / Domain-specific business components for ERP scenarios.

<SalesTargetEditor
  rows={[
    {
      id: "1",
      year: 2026,
      region: "华东",
      q1: 100,
      q2: 120,
      q3: 130,
      q4: 150,
      annual: 500,
    },
  ]}
/>
SalesTargetEditor — sales target editor component.

## 何时使用 / When to Use

需要Sales Target Editor时使用本组件。用于 ERP/业务域场景（单据、选择器、审批、对账等）；按业务语义接入而不是当纯 UI 积木。

Use SalesTargetEditor when this capability is required. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { SalesTargetEditor } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <SalesTargetEditor />
}`} lang="tsx" />

## API / Props

### SalesTargetEditor

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `rows`      | `SalesTargetRow[]`     | —       | 行数据 <br/> Row data                                     |
| `onChange`  | `() => void`           | —       | 值变化回调 <br/> Callback when value changes              |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](http://localhost:6006/?path=/docs/business-salestargeteditor--docs)。
