import { BarListCard } from "@/components/business/bar-list-card"
import { CodeBlock } from "@/components/code-block"

# BarListCard Bar List Card

> 业务组件

 排名列表卡

## 何时使用

适用于Bar List Card相关场景。


## 代码示例

### 基础用法

<CodeBlock code={`import { BarListCard } from "@/components/business/bar-list-card"

export function Basic() {
  return <BarListCard />
}`} lang="tsx" />


## API / Props

### BarListCard

| Prop | Type | Default | 说明 |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 |
| `title` | `string` | `排名` | Card heading. |
| `data` | `Array<T>` | — | Ranked entries. |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-barlistcard--docs)。

