import { AudioRecorder } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# AudioRecorder 录音组件

> 业务组件 / Domain-specific business components for ERP scenarios.

录音组件 — 录音组件，支持波形动画与计时器。
AudioRecorder — Audio recording component with waveform animation and timer.

## 何时使用 / When to Use

需要录音组件功能时使用本组件。

Use when you need a audiorecorder.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { AudioRecorder } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <AudioRecorder />
}`} lang="tsx" />

## API / Props

### AudioRecorder

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-audiorecorder--docs)。
