import { OrgChart } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# OrgChart

> Domain-specific business components for ERP scenarios.

OrgChart — a business component for ERP / enterprise scenarios.

## When to Use

Use OrgChart when you need: OrgChart: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { OrgChart } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <OrgChart />
}`} lang="tsx" />

## API / Props

### OrgChart

| Prop          | Type                          | Default | Description                     |
| ------------- | ----------------------------- | ------- | ------------------------------- |
| `className`   | `string`                      | —       | Custom CSS class                |
| `nodes`       | `OrgNode[]`                   | —       |                                 |
| `edges`       | `OrgEdge[]`                   | —       |                                 |
| `onNodeClick` | `() => void`                  | —       |                                 |
| ...rest       | `React.ComponentProps<"div">` | —       | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
