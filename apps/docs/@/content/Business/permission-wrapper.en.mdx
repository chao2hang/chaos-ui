import { PermissionWrapper } from "@/components/business/permission-wrapper"
import { CodeBlock } from "@/components/code-block"

# PermissionWrapper

> Domain-specific business components for ERP scenarios.

PermissionWrapper — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a permission wrapper.


## Examples

### Basic Usage

<CodeBlock code={`import { PermissionWrapper } from "@/components/business/permission-wrapper"

export function Basic() {
  return <PermissionWrapper />
}`} lang="tsx" />


## API / Props

### PermissionWrapper

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `permission` | `string \| string[]` | — |  |
| `permissions` | `string[]` | — |  |
| `mode` | `"all" \| "any"` | `any` |  |
| `fallback` | `React.ReactNode` | — |  |
| `userPermissions` | `string[]` | — |  |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


