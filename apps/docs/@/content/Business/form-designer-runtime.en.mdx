import { FormDesignerRuntime } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# FormDesignerRuntime Form Designer Runtime

> Domain-specific business components for ERP scenarios.

<FormDesignerRuntime
  schema={[{ id: "name", type: "text", label: "姓名", required: true }]}
  value={{ name: "张三" }}
/>
FormDesignerRuntime — form designer runtime component.

## When to Use

Use FormDesignerRuntime when this capability is required. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { FormDesignerRuntime } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <FormDesignerRuntime />
}`} lang="tsx" />

## API / Props

### FormDesignerRuntime

| Prop        | Type                      | Default | Description                       |
| ----------- | ------------------------- | ------- | --------------------------------- |
| `className` | `string`                  | —       | Custom CSS class                  |
| `schema`    | `unknown`                 | —       |                                   |
| `value`     | `Record<string, unknown>` | —       | Current value (controlled)        |
| `onChange`  | `() => void`              | —       | Callback when value changes       |
| ...rest     | `React.HTMLAttributes`    | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
