import { CustomerBrowse } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# CustomerBrowse Customer Browse

> Domain-specific business components for ERP scenarios.

CustomerBrowse — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a customer browse.

## Examples

### Basic Usage

<CodeBlock code={`import { CustomerBrowse } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <CustomerBrowse />
}`} lang="tsx" />

## API / Props

### CustomerBrowse

| Prop                | Type                   | Default | Description                                                                    |
| ------------------- | ---------------------- | ------- | ------------------------------------------------------------------------------ |
| `className`         | `string`               | —       | Custom CSS class                                                               |
| `open`              | `boolean`              | —       | Open state (controlled)                                                        |
| `onOpenChange`      | `() => void`           | —       | Callback when open state changes                                               |
| `onSelect`          | `() => void`           | —       | Single-select emits the chosen item; multi-select emits the last toggled item. |
| `multiple`          | `boolean`              | `false` | Enable multi-select (default false). / 多选                                    |
| `items`             | `CustomerBrowseItem[]` | —       | Items / data list                                                              |
| `title`             | `string`               | —       | Title                                                                          |
| `searchPlaceholder` | `string`               | —       |                                                                                |
| `emptyText`         | `string`               | —       |                                                                                |
| ...rest             | `React.HTMLAttributes` | —       | Inherits standard HTML attributes                                              |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
