import { ChatMessageBubble } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# ChatMessageBubble Chat Message Bubble

> 业务组件

Chat Message Bubble — 面向 ERP/企业场景的业务组件。

## 何时使用

在需要Chat Message Bubble时使用：Chat Message Bubble — 组件库业务组件，详见 Storybook 与源码。面向 ERP/业务域（单据、选择器、审批、结算等），按业务语义接入。

## 代码示例

### 基础用法

<CodeBlock code={`import { ChatMessageBubble } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <ChatMessageBubble />
}`} lang="tsx" />

## API / Props

### ChatMessageBubble

| Prop        | Type                                | Default | 说明              |
| ----------- | ----------------------------------- | ------- | ----------------- |
| `className` | `string`                            | —       | 自定义样式类名    |
| `role`      | `"user" \| "assistant" \| "system"` | —       |                   |
| `content`   | `string`                            | —       | 内容              |
| `avatar`    | `React.ReactNode`                   | —       |                   |
| `name`      | `string`                            | —       | 表单字段名        |
| `time`      | `string`                            | —       |                   |
| `status`    | `string`                            | —       | 状态              |
| ...rest     | `React.HTMLAttributes`              | —       | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](http://localhost:6006/?path=/docs/business-chatmessagebubble--docs)。
