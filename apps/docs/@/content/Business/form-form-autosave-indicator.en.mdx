import { FormAutosaveIndicator } from "@/components/business/form/form-autosave-indicator";
import { CodeBlock } from "@/components/code-block";

# FormAutosaveIndicator

> Domain-specific business components for ERP scenarios.

FormAutosaveIndicator — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a form autosave indicator.


## Examples

### Basic Usage

<CodeBlock code={`import { FormAutosaveIndicator } from "@/components/business/form/form-autosave-indicator"

export function Basic() {
  return <FormAutosaveIndicator />
}`} lang="tsx" />

## API / Props

### FormAutosaveIndicator

| Prop | Type | Default | Description |
| ----------- | ----------------------------- | ------- | ----------------------------------------------------------- |
| `className` | `string` | — | Custom CSS class |
| `status` | `AutosaveStatus` | — | Status |
| `lastSaved` | `number \| null`              | —       |                                                             |
| `error`     | `Error \| null`               | —       |                                                             |
| ...rest | `React.ComponentProps<"div">` | — | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


