import { ChatHeader } from "@/components/business/chat-header";
import { CodeBlock } from "@/components/code-block";

# ChatHeader Chat Header

> 业务组件 / Domain-specific business components for ERP scenarios.

Chat Header — 面向 ERP/企业场景的业务组件。
ChatHeader — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Chat Header相关场景。

Use when you need a chat header.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { ChatHeader } from "@/components/business/chat-header"

export function Basic() {
  return <ChatHeader />
}`} lang="tsx" />

## API / Props

### ChatHeader

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `title`     | `string`               | —       | 标题 <br/> Title                                          |
| `subtitle`  | `string`               | —       |                                                           |
| `status`    | `string`               | —       | 状态 <br/> Status                                         |
| `actions`   | `React.ReactNode`      | —       |                                                           |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-chat-header--docs)。
