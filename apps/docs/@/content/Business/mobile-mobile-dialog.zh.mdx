import { MobileDialog } from "@/components/mobile/mobile-dialog";
import { CodeBlock } from "@/components/code-block";

# MobileDialog Mobile Mobile Dialog

> 业务组件

Mobile Mobile Dialog — 面向 ERP/企业场景的业务组件。

## 何时使用

适用于Mobile Mobile Dialog相关场景。

## 代码示例

### 基础用法

<CodeBlock code={`import { MobileDialog } from "@/components/mobile/mobile-dialog"

export function Basic() {
  return <MobileDialog />
}`} lang="tsx" />

## API / Props

### MobileDialog

| Prop           | Type                   | Default | 说明              |
| -------------- | ---------------------- | ------- | ----------------- |
| `className`    | `string`               | —       | 自定义样式类名    |
| `title`        | `string`               | —       | 标题              |
| `description`  | `string`               | —       | 描述文本          |
| `trigger`      | `React.ReactElement`   | —       |                   |
| `actions`      | `React.ReactNode`      | —       |                   |
| `open`         | `boolean`              | —       | 是否打开(受控)    |
| `onOpenChange` | `() => void`           | —       | 打开状态变化回调  |
| ...rest        | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-mobile-mobile-dialog--docs)。
