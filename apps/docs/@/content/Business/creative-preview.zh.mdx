import { CreativePreview } from "@/components/business/creative-preview"
import { CodeBlock } from "@/components/code-block"

# CreativePreview 创意预览

> 业务组件

创意预览 — 面向 ERP/企业场景的业务组件。

## 何时使用

需要创意预览功能时使用本组件。


## 代码示例

### 基础用法

<CodeBlock code={`import { CreativePreview } from "@/components/business/creative-preview"

export function Basic() {
  return <CreativePreview />
}`} lang="tsx" />


## API / Props

### CreativePreview

| Prop | Type | Default | 说明 |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 |
| `title` | `string` | `创意素材` | 标题 |
| `type` | `"image" \| "video" \| "text"` | `image` | 类型 |
| `src` | `string` | — | 资源地址 |
| `description` | `string` | — | 描述文本 |
| `size` | `string` | — | 组件尺寸 |
| `status` | `string` | — | 状态 |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-creativepreview--docs)。

