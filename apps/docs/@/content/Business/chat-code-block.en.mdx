import { ChatCodeBlock } from "@/components/business/chat-code-block";
import { CodeBlock } from "@/components/code-block";

# ChatCodeBlock Chat Code Block

> Domain-specific business components for ERP scenarios.

ChatCodeBlock — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a chat code block.

## Examples

### Basic Usage

<CodeBlock code={`import { ChatCodeBlock } from "@/components/business/chat-code-block"

export function Basic() {
  return <ChatCodeBlock />
}`} lang="tsx" />

## API / Props

### ChatCodeBlock

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| `code`      | `string`               | —       |                                   |
| `language`  | `string`               | —       |                                   |
| `filename`  | `string`               | —       |                                   |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
