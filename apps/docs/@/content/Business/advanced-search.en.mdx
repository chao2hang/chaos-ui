import { AdvancedSearch } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# AdvancedSearch

> Domain-specific business components for ERP scenarios.

AdvancedSearch — a business component for ERP / enterprise scenarios.

## When to Use

Use AdvancedSearch when you need: AdvancedSearch: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { AdvancedSearch } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <AdvancedSearch />
}`} lang="tsx" />

## API / Props

### AdvancedSearch

| Prop        | Type                          | Default | Description                     |
| ----------- | ----------------------------- | ------- | ------------------------------- |
| `className` | `string`                      | —       | Custom CSS class                |
| `fields`    | `SearchField[]`               | —       |                                 |
| `onSearch`  | `() => void`                  | —       |                                 |
| `onReset`   | `() => void`                  | —       |                                 |
| `loading`   | `boolean`                     | `false` | Loading state                   |
| `compact`   | `boolean`                     | `false` |                                 |
| ...rest     | `React.ComponentProps<"div">` | —       | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
