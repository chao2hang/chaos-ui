import { HeatmapChart } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# HeatmapChart

> Domain-specific business components for ERP scenarios.

HeatmapChart — a business component for ERP / enterprise scenarios.

## When to Use

Use ChartsHeatmapChart when this capability is required. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { HeatmapChart } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <HeatmapChart />
}`} lang="tsx" />

## API / Props

### HeatmapChart

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
