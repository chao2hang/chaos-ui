import {
  FeatureGate,
  FeatureEnabled,
  FeatureFlag,
  FeatureFlags,
} from "@/components/business/feature-gate";
import { CodeBlock } from "@/components/code-block";

# FeatureGate 功能开关

> 业务组件 / Domain-specific business components for ERP scenarios.

功能开关 — 面向 ERP/企业场景的业务组件。
FeatureGate — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

需要功能开关功能时使用本组件。

Use when you need a feature gate.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { FeatureGate, FeatureEnabled, FeatureFlag } from "@/components/business/feature-gate"

export function Basic() {
  return (
    <div className="space-y-4">
  <FeatureGate />
  <FeatureEnabled />
  <FeatureFlag />
    </div>
  )
}`} lang="tsx" />

## API / Props

### FeatureGate

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-feature-gate--docs)。
