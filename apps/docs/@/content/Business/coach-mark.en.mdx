import { CodeBlock } from "@/components/code-block";

# CoachMark

> Domain-specific business components for ERP scenarios.

CoachMark — a business component for ERP / enterprise scenarios.

## When to Use

Use CoachMark when this capability is required. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock
  code={`export function Basic() {
  return <CoachMark />
}`}
  lang="tsx"
/>

## API / Props

### CoachMark

| Prop           | Type                                     | Default  | Description                       |
| -------------- | ---------------------------------------- | -------- | --------------------------------- |
| `className`    | `string`                                 | —        | Custom CSS class                  |
| `open`         | `boolean`                                | —        | Open state (controlled)           |
| `onOpenChange` | `() => void`                             | —        | Callback when open state changes  |
| `target`       | `HTMLElement \| null`                    | —        | Link target                       |
| `title`        | `string`                                 | —        | Title                             |
| `description`  | `React.ReactNode`                        | —        | Description text                  |
| `placement`    | `"top" \| "bottom" \| "left" \| "right"` | `bottom` | Placement position                |
| `onNext`       | `() => void`                             | —        |                                   |
| `onSkip`       | `() => void`                             | —        |                                   |
| `nextLabel`    | `string`                                 | `下一步` |                                   |
| `skipLabel`    | `string`                                 | `跳过`   |                                   |
| `showSkip`     | `boolean`                                | `true`   |                                   |
| ...rest        | `React.HTMLAttributes`                   | —        | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
