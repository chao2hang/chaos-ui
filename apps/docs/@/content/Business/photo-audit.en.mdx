import { PhotoAudit } from "@/components/business/photo-audit"
import { CodeBlock } from "@/components/code-block"

# PhotoAudit Photo Audit

> Domain-specific business components for ERP scenarios.

<PhotoAudit photos={[{ src: "/a.jpg", alt: "收据" }]} />
PhotoAudit — photo audit component.

## When to Use

Use when you need a photo audit.


## Examples

### Basic Usage

<CodeBlock code={`import { PhotoAudit } from "@/components/business/photo-audit"

export function Basic() {
  return <PhotoAudit />
}`} lang="tsx" />


## API / Props

### PhotoAudit

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `photos` | `Array<T>` | — |  |
| `onApprove` | `() => void` | — |  |
| `onReject` | `() => void` | — |  |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


