import { ImageCropper } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# ImageCropper

> Domain-specific business components for ERP scenarios.

ImageCropper — Image cropper with drag-to-select crop region and preview.

## When to Use

Use ImageCropper when you need: Image cropper with drag-to-select crop region and preview. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { ImageCropper } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <ImageCropper />
}`} lang="tsx" />

## API / Props

### ImageCropper

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
