import { BatchPrintDialog } from "@/components/business/batch-print-dialog";
import { CodeBlock } from "@/components/code-block";

# BatchPrintDialog Batch Print Dialog

> Domain-specific business components for ERP scenarios.

BatchPrintDialog — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a batch print dialog.

## Examples

### Basic Usage

<CodeBlock code={`import { BatchPrintDialog } from "@/components/business/batch-print-dialog"

export function Basic() {
  return <BatchPrintDialog />
}`} lang="tsx" />

## API / Props

### BatchPrintDialog

| Prop           | Type                   | Default | Description                       |
| -------------- | ---------------------- | ------- | --------------------------------- |
| `className`    | `string`               | —       | Custom CSS class                  |
| `open`         | `boolean`              | —       | Open state (controlled)           |
| `onOpenChange` | `() => void`           | —       | Callback when open state changes  |
| `items`        | `Array<T>`             | —       | Items / data list                 |
| `onPrint`      | `() => void`           | —       |                                   |
| ...rest        | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
