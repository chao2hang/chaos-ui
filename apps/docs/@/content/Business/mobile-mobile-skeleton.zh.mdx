import {
  MobileListItemSkeleton,
  MobileCardSkeleton,
  MobileDetailSkeleton,
} from "@/components/mobile/mobile-skeleton";
import { CodeBlock } from "@/components/code-block";

# MobileSkeleton Mobile Mobile Skeleton

> 业务组件

Mobile Mobile Skeleton — 面向 ERP/企业场景的业务组件。

## 何时使用

适用于Mobile Mobile Skeleton相关场景。


## 代码示例

### 基础用法

<CodeBlock code={`import { MobileListItemSkeleton } from "@/components/mobile/mobile-skeleton"

export function Basic() {
  return <MobileListItemSkeleton>Default</MobileListItemSkeleton>
}`} lang="tsx" />

## API / Props

### MobileListItemSkeleton

| Prop | Type | Default | 说明 |
| ----------- | ----------------------------- | ------- | ----------------------------------------------------------- |
| `className` | `string` | — | 自定义样式类名 |
| ...rest | `React.ComponentProps<"div">` | — | 透传 `<div>` 原生属性 |

## 注意事项

- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-mobile-mobile-skeleton--docs)。

