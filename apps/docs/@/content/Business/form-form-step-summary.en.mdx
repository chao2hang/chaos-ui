import { FormStepSummary } from "@/components/business/form/form-step-summary";
import { CodeBlock } from "@/components/code-block";

# FormStepSummary

> Domain-specific business components for ERP scenarios.

FormStepSummary — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a form step summary.


## Examples

### Basic Usage

<CodeBlock code={`import { FormStepSummary } from "@/components/business/form/form-step-summary"

export function Basic() {
  return <FormStepSummary />
}`} lang="tsx" />

## API / Props

### FormStepSummary

| Prop | Type | Default | Description |
| ----------- | ---------------------------- | ------- | --------------------------------------------------------- |
| `className` | `string` | — | Custom CSS class |
| `steps`     | `FormStep[]`                 | —       |                                                           |
| `values`    | `Record<string, unknown>`    | —       |                                                           |
| `onJumpTo`  | `() => void`                 | —       |                                                           |
| ...rest | `React.ComponentProps<"ol">` | — | Inherits all `<ol>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


