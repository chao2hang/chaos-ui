import { ChatInputToolbar } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# ChatInputToolbar Chat Input Toolbar

> Domain-specific business components for ERP scenarios.

ChatInputToolbar — a business component for ERP / enterprise scenarios.

## When to Use

Use ChatInputToolbar when you need: ChatInputToolbar: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { ChatInputToolbar } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <ChatInputToolbar />
}`} lang="tsx" />

## API / Props

### ChatInputToolbar

| Prop          | Type                   | Default | Description                       |
| ------------- | ---------------------- | ------- | --------------------------------- |
| `className`   | `string`               | —       | Custom CSS class                  |
| `tools`       | `Array<T>`             | —       |                                   |
| `onToolClick` | `() => void`           | —       |                                   |
| ...rest       | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
