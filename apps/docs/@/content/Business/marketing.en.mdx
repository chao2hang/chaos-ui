import {
  PricingCard,
  PricingTable,
  TestimonialCard,
  FAQSection,
} from "@/components/business/marketing";
import { CodeBlock } from "@/components/code-block";

# Marketing

> Domain-specific business components for ERP scenarios.

Marketing — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a marketing.

## Examples

### Basic Usage

<CodeBlock code={`import { PricingCard, PricingTable, TestimonialCard } from "@/components/business/marketing"

export function Basic() {
  return (
    <div className="space-y-4">
  <PricingCard />
  <PricingTable />
  <TestimonialCard />
    </div>
  )
}`} lang="tsx" />

## API / Props

### PricingCard

| Prop        | Type                          | Default | Description                     |
| ----------- | ----------------------------- | ------- | ------------------------------- |
| `className` | `string`                      | —       | Custom CSS class                |
| `tier`      | `PricingTier`                 | —       |                                 |
| `onCta`     | `() => void`                  | —       |                                 |
| ...rest     | `React.ComponentProps<"div">` | —       | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
