import { CodeBlock } from "@/components/code-block";

# Marketing

> Domain-specific business components for ERP scenarios.

Marketing — a business component for ERP / enterprise scenarios.

## When to Use

Use Marketing when this capability is required. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { CampaignCard } from "@chaos_team/chaos-ui/business";

export function Basic() {
  return (
    <CampaignCard
      name="春季活动"
      status="active"
      dateRange="2026-03-01 ~ 2026-03-31"
      budget={50000}
      spent={21000}
      channels={[]}
      metrics={[{ label: "触达", value: 120000 }]}
    />
  );
}`} lang="tsx" />

## API / Props

### PricingCard

| Prop        | Type                          | Default | Description                     |
| ----------- | ----------------------------- | ------- | ------------------------------- |
| `className` | `string`                      | —       | Custom CSS class                |
| `tier`      | `PricingTier`                 | —       |                                 |
| `onCta`     | `() => void`                  | —       |                                 |
| ...rest     | `React.ComponentProps<"div">` | —       | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
