import { MarketingActivityForm } from "@/components/business/marketing-activity-form";
import { CodeBlock } from "@/components/code-block";

# MarketingActivityForm Marketing Activity Form

> 业务组件 / Domain-specific business components for ERP scenarios.

Marketing Activity Form — 面向 ERP/企业场景的业务组件。
MarketingActivityForm — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Marketing Activity Form相关场景。

Use when you need a marketing activity form.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { MarketingActivityForm } from "@/components/business/marketing-activity-form"

export function Basic() {
  return <MarketingActivityForm />
}`} lang="tsx" />

## API / Props

### MarketingActivityForm

| Prop        | Type                      | Default | 说明 / Description                                        |
| ----------- | ------------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`                  | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `initial`   | `Record<string, unknown>` | —       |                                                           |
| `onSubmit`  | `() => void`              | —       | 提交回调 <br/> Submit handler                             |
| ...rest     | `React.HTMLAttributes`    | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-marketing-activity-form--docs)。
