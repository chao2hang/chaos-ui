import { Rating } from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# Rating 评分

> 业务组件

评分 — 面向 ERP/企业场景的业务组件。

## 何时使用

当你需要「评分」相关能力时使用：星级评分组件，支持半星和只读模式。作为页面中的通用基础能力使用；优先复用而非在业务里再造一套。

## 代码示例

### 基础用法

<CodeBlock code={`import { Rating } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <Rating />
}`} lang="tsx" />

## API / Props

### Rating

| Prop            | Type                        | Default   | 说明              |
| --------------- | --------------------------- | --------- | ----------------- |
| `className`     | `string`                    | —         | 自定义样式类名    |
| `value`         | `number`                    | —         | 当前值(受控)      |
| `defaultValue`  | `number`                    | —         | 默认值(非受控)    |
| `onChange`      | `() => void`                | —         | 值变化回调        |
| `max`           | `number`                    | `5`       | 最大值            |
| `size`          | `"sm" \| "default" \| "lg"` | `default` | 组件尺寸          |
| `readonly`      | `boolean`                   | —         |                   |
| `allowHalf`     | `boolean`                   | —         |                   |
| `iconClassName` | `string`                    | —         |                   |
| ...rest         | `React.HTMLAttributes`      | —         | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](http://localhost:6006/?path=/docs/components-rating--docs)。
