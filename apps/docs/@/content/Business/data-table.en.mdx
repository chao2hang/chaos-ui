import { DataTable } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# DataTable

> Domain-specific business components for ERP scenarios.

DataTable — a business component for ERP / enterprise scenarios.

## When to Use

Business data table with sorting, filtering, pagination, and row actions.

## Examples

### Basic Usage

<CodeBlock code={`import { DataTable } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <DataTable />
}`} lang="tsx" />

## API / Props

### DataTable

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
