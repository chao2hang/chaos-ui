import { ChartTooltip } from "@/components/business/charts/shared/chart-tooltip";
import { CodeBlock } from "@/components/code-block";

# ChartTooltip

> Domain-specific business components for ERP scenarios.

ChartTooltip — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a chart tooltip.


## Examples

### Basic Usage

<CodeBlock code={`import { ChartTooltip } from "@/components/business/charts/shared/chart-tooltip"

export function Basic() {
  return <ChartTooltip />
}`} lang="tsx" />

## API / Props

### ChartTooltip

| Prop | Type | Default | Description |
| ----------- | ----------------------- | ------- | --------------------------------------------------------- |
| `className` | `string` | — | Custom CSS class |
| `active`    | `boolean`               | —       |                                                           |
| `payload`   | `TooltipPayloadEntry[]` | —       |                                                           |
| `label` | `unknown` | — | Label text |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


