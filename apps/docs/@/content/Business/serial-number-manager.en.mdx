import { SerialNumberManager } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# SerialNumberManager Serial Number Manager

> Domain-specific business components for ERP scenarios.

SerialNumberManager — a business component for ERP / enterprise scenarios.

## When to Use

Use SerialNumberManager when you need: SerialNumberManager: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { SerialNumberManager } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <SerialNumberManager />
}`} lang="tsx" />

## API / Props

### SerialNumberManager

| Prop        | Type                      | Default | Description                       |
| ----------- | ------------------------- | ------- | --------------------------------- |
| `className` | `string`                  | —       | Custom CSS class                  |
| `rules`     | `Array<SerialNumberRule>` | —       |                                   |
| `onChange`  | `() => void`              | —       | Callback when value changes       |
| ...rest     | `React.HTMLAttributes`    | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
