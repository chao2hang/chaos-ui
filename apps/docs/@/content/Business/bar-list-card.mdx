import { BarListCard } from "@/components/business/bar-list-card";
import { CodeBlock } from "@/components/code-block";

# BarListCard Bar List Card

> 业务组件 / Domain-specific business components for ERP scenarios.

`tsx <BarListCard title="销量TOP" data={[{label:"A",value:80},{label:"B",value:42}]} /> ` 排名列表卡
BarListCard — bar list card component.

## 何时使用 / When to Use

适用于Bar List Card相关场景。

Use when you need a bar list card.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { BarListCard } from "@/components/business/bar-list-card"

export function Basic() {
  return <BarListCard />
}`} lang="tsx" />

## API / Props

### BarListCard

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `title`     | `string`               | `排名`  | Card heading.                                             |
| `data`      | `Array<T>`             | —       | Ranked entries.                                           |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-bar-list-card--docs)。
