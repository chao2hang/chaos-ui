import { SegmentedControl } from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# SegmentedControl 分段控制器

> 业务组件 / Domain-specific business components for ERP scenarios.

分段控制器 — 面向 ERP/企业场景的业务组件。
SegmentedControl — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

当你需要「分段控制器」相关能力时使用：分段控制器 — Chaos UI 的数据展示。用于展示列表、状态、统计与内容详情；让用户先看懂数据再操作。

Use SegmentedControl when you need: SegmentedControl — a components for presenting data and content. from Chaos UI. Use to present lists, status, metrics, and detail content for quick scanning.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { SegmentedControl } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <SegmentedControl />
}`} lang="tsx" />

### 状态 / States

<CodeBlock
  code={`<SegmentedControl>Normal</SegmentedControl>
<SegmentedControl disabled>Disabled</SegmentedControl>`}
  lang="tsx"
/>

## API / Props

### SegmentedControl

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](http://localhost:6006/?path=/docs/components-segmented-control--docs)。
