import { ChatMessageInput } from "@chaos_team/chaos-ui/business"
import { CodeBlock } from "@/components/code-block"

# ChatMessageInput Chat Message Input

> Domain-specific business components for ERP scenarios.

ChatMessageInput — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a chat message input.


## Examples

### Basic Usage

<CodeBlock code={`import { ChatMessageInput } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <ChatMessageInput />
}`} lang="tsx" />


## API / Props

### ChatMessageInput

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `value` | `string` | — | Current value (controlled) |
| `onChange` | `() => void` | — | Callback when value changes |
| `onSend` | `() => void` | — |  |
| `placeholder` | `string` | `Type a message...` | Placeholder text |
| `disabled` | `boolean` | `false` | Whether disabled |
| `maxLength` | `number` | — | Maximum input length |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


