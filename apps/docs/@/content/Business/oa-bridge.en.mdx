import { CodeBlock } from "@/components/code-block";

# OaBridge Oa Bridge

> Domain-specific business components for ERP scenarios.

OaBridge — oa bridge component.

## When to Use

Use OaBridge when this capability is required. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { OaBridge } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <OaBridge />
}`} lang="tsx" />

## API / Props

### OaBridge

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| `billId`    | `string`               | —       |                                   |
| `billType`  | `string`               | —       |                                   |
| `onSubmit`  | `() => void`           | —       | Submit handler                    |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
