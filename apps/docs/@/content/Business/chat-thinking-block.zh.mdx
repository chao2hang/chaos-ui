import { ChatThinkingBlock } from "@chaos_team/chaos-ui/business"
import { CodeBlock } from "@/components/code-block"

# ChatThinkingBlock Chat Thinking Block

> 业务组件

Chat Thinking Block — 面向 ERP/企业场景的业务组件。

## 何时使用

适用于Chat Thinking Block相关场景。


## 代码示例

### 基础用法

<CodeBlock code={`import { ChatThinkingBlock } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <ChatThinkingBlock />
}`} lang="tsx" />


## API / Props

### ChatThinkingBlock

| Prop | Type | Default | 说明 |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 |
| `content` | `string` | — | 内容 |
| `duration` | `number` | — | 持续时间/展示时长(毫秒) |
| `collapsed` | `boolean` | `false` |  |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-chatthinkingblock--docs)。

