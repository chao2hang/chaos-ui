import { TopLoader } from "@/components/business/top-loader"
import { CodeBlock } from "@/components/code-block"

# TopLoader 顶部加载条

> 业务组件 / Domain-specific business components for ERP scenarios.

顶部加载条 — 面向 ERP/企业场景的业务组件。
TopLoader — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

需要顶部加载条功能时使用本组件。

Use when you need a top loader.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { TopLoader } from "@/components/business/top-loader"

export function Basic() {
  return <TopLoader />
}`} lang="tsx" />


## API / Props

### TopLoader

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-top-loader--docs)。
