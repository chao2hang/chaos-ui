import { MobilePicker } from "@/components/business/mobile-picker"
import { CodeBlock } from "@/components/code-block"

# MobilePicker Mobile Picker

> 业务组件 / Domain-specific business components for ERP scenarios.

Mobile Picker — 面向 ERP/企业场景的业务组件。
MobilePicker — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Mobile Picker相关场景。

Use when you need a mobile picker.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { MobilePicker } from "@/components/business/mobile-picker"

export function Basic() {
  return <MobilePicker />
}`} lang="tsx" />


## API / Props

### MobilePicker

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| `options` | `Array<T>` | — | 可选项列表 <br/> Available options |
| `value` | `string` | — | 当前值(受控) <br/> Current value (controlled) |
| `onChange` | `() => void` | — | 值变化回调 <br/> Callback when value changes |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-mobilepicker--docs)。
