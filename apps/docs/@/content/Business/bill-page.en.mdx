import { BillPage } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# BillPage

> Domain-specific business components for ERP scenarios.

BillPage — a business component for ERP / enterprise scenarios.

## When to Use

Use BillPage when you need: BillPage: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { BillPage } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <BillPage />
}`} lang="tsx" />

## API / Props

### BillPage

| Prop          | Type                                      | Default | Description                     |
| ------------- | ----------------------------------------- | ------- | ------------------------------- |
| `className`   | `string`                                  | —       | Custom CSS class                |
| `headerProps` | `React.ComponentProps<typeof BillHeader>` | —       |                                 |
| `footerProps` | `React.ComponentProps<typeof BillFooter>` | —       |                                 |
| `lineItems`   | `BillLineItem[]`                          | —       |                                 |
| ...rest       | `React.ComponentProps<"div">`             | —       | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
