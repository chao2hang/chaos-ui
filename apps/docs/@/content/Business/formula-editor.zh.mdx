import { FormulaEditor } from "@/components/business/formula-editor";
import { CodeBlock } from "@/components/code-block";

# FormulaEditor 公式编辑器

> 业务组件

公式编辑器 — 面向 ERP/企业场景的业务组件。

## 何时使用

需要公式编辑器功能时使用本组件。

## 代码示例

### 基础用法

<CodeBlock code={`import { FormulaEditor } from "@/components/business/formula-editor"

export function Basic() {
  return <FormulaEditor />
}`} lang="tsx" />

## API / Props

### FormulaEditor

| Prop          | Type                          | Default       | 说明                  |
| ------------- | ----------------------------- | ------------- | --------------------- |
| `className`   | `string`                      | —             | 自定义样式类名        |
| `value`       | `string`                      | ``            | 当前值(受控)          |
| `onChange`    | `() => void`                  | —             | 值变化回调            |
| `placeholder` | `string`                      | `输入公式...` | 占位文本              |
| ...rest       | `React.ComponentProps<"div">` | —             | 透传 `<div>` 原生属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-formulaeditor--docs)。
