import { ChatBranch } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# ChatBranch Chat Branch

> Domain-specific business components for ERP scenarios.

ChatBranch — a business component for ERP / enterprise scenarios.

## When to Use

Use ChatBranch when you need: ChatBranch: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { ChatBranch } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <ChatBranch />
}`} lang="tsx" />

## API / Props

### ChatBranch

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| `branches`  | `Array<T>`             | —       |                                   |
| `onSelect`  | `() => void`           | —       | Selection handler                 |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
