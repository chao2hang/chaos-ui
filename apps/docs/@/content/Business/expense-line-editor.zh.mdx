import { ExpenseLineEditor } from "@/components/business/expense-line-editor";
import { CodeBlock } from "@/components/code-block";

# ExpenseLineEditor 费用行编辑器

> 业务组件

费用行编辑器 — 面向 ERP/企业场景的业务组件。

## 何时使用

需要费用行编辑器功能时使用本组件。

## 代码示例

### 基础用法

<CodeBlock code={`import { ExpenseLineEditor } from "@/components/business/expense-line-editor"

export function Basic() {
  return <ExpenseLineEditor />
}`} lang="tsx" />

## API / Props

### ExpenseLineEditor

| Prop         | Type                          | Default | 说明                  |
| ------------ | ----------------------------- | ------- | --------------------- |
| `className`  | `string`                      | —       | 自定义样式类名        |
| `lines`      | `ExpenseLine[]`               | —       |                       |
| `onChange`   | `() => void`                  | —       | 值变化回调            |
| `categories` | `string[]`                    | —       |                       |
| ...rest      | `React.ComponentProps<"div">` | —       | 透传 `<div>` 原生属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-expenselineeditor--docs)。
