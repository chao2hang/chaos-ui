import { StatCardWithSparkline } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# StatCardWithSparkline Stat Card With Sparkline

> 业务组件 / Domain-specific business components for ERP scenarios.

<StatCardWithSparkline
  label="日活"
  value="12,480"
  trend={8}
  sparklineData={[5, 8, 6, 10]}
/>
StatCardWithSparkline — stat card with sparkline component.

## 何时使用 / When to Use

需要Stat Card With Sparkline时使用本组件。用于 ERP/业务域场景（单据、选择器、审批、对账等）；按业务语义接入而不是当纯 UI 积木。

Use StatCardWithSparkline when this capability is required. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { StatCardWithSparkline } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <StatCardWithSparkline />
}`} lang="tsx" />

## API / Props

### StatCardWithSparkline

| Prop            | Type                   | Default | 说明 / Description                                        |
| --------------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className`     | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `label`         | `string`               | —       | 标签文本 <br/> Label text                                 |
| `value`         | `string \| number`     | —       | 当前值(受控) <br/> Current value (controlled)             |
| `trend`         | `number`               | —       |                                                           |
| `sparklineData` | `number[]`             | —       |                                                           |
| ...rest         | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **无障碍**: 组件遵循 WAI-ARIA 最佳实践。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](http://localhost:6006/?path=/docs/business-statcardwithsparkline--docs)。
