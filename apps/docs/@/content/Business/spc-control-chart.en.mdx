import { SPCControlChart } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# SpcControlChart Spc Control Chart

> Domain-specific business components for ERP scenarios.

SpcControlChart — a business component for ERP / enterprise scenarios.

## When to Use

Use SpcControlChart when you need: SpcControlChart: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { SPCControlChart } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <SPCControlChart />
}`} lang="tsx" />

## API / Props

### SPCControlChart

| Prop                | Type                   | Default             | Description                                                        |
| ------------------- | ---------------------- | ------------------- | ------------------------------------------------------------------ |
| `className`         | `string`               | —                   | Custom CSS class                                                   |
| `samples`           | `SPCSample[]`          | —                   | Sample data.                                                       |
| `title`             | `string`               | `SPC Control Chart` | Chart title.                                                       |
| `centerLine`        | `number`               | —                   | Pre-calculated center line (mean). If omitted, computed from data. |
| `ucl`               | `number`               | —                   | Pre-calculated UCL. If omitted, computed as mean + 3σ.             |
| `lcl`               | `number`               | —                   | Pre-calculated LCL. If omitted, computed as mean - 3σ.             |
| `sigma`             | `number`               | —                   | Pre-calculated σ (sigma). If omitted, computed from data.          |
| `showWarningLimits` | `boolean`              | `true`              | Show warning limits at ±2σ.                                        |
| `yLabel`            | `string`               | —                   | Y-axis label.                                                      |
| `unit`              | `string`               | —                   | Unit suffix for values.                                            |
| `height`            | `number`               | `320`               | Chart height in px.                                                |
| ...rest             | `React.HTMLAttributes` | —                   | Inherits standard HTML attributes                                  |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
