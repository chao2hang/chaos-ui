import { CategoryBar } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# CategoryBar Category Bar

> Domain-specific business components for ERP scenarios.

CategoryBar — a business component for ERP / enterprise scenarios.

## When to Use

Use CategoryBar when you need: CategoryBar: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { CategoryBar } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <CategoryBar />
}`} lang="tsx" />

## API / Props

### CategoryBar

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| `data`      | `Array<T>`             | —       | Data source                       |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
