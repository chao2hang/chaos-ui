import { CategoryBar } from "@/components/business/category-bar";
import { CodeBlock } from "@/components/code-block";

# CategoryBar Category Bar

> Domain-specific business components for ERP scenarios.

CategoryBar — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a category bar.

## Examples

### Basic Usage

<CodeBlock code={`import { CategoryBar } from "@/components/business/category-bar"

export function Basic() {
  return <CategoryBar />
}`} lang="tsx" />

## API / Props

### CategoryBar

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| `data`      | `Array<T>`             | —       | Data source                       |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
