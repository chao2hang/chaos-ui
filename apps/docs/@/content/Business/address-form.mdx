import { CodeBlock } from "@/components/code-block";

# AddressForm 地址表单

> 业务组件 / Domain-specific business components for ERP scenarios.

地址表单 — 面向 ERP/企业场景的业务组件。
AddressForm — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

收件地址填写与编辑表单。

Address entry and editing form.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock
  code={`export function Basic() {
  return (
    <AddressForm
      onSubmit={(data) => console.log(data)}
    />
  )
}`}
  lang="tsx"
/>

## API / Props

### AddressForm

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `value`     | `AddressValue`         | —       | 当前值(受控) <br/> Current value (controlled)             |
| `onChange`  | `() => void`           | —       | 值变化回调 <br/> Callback when value changes              |
| `country`   | `string`               | —       |                                                           |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **表单校验**: 集成 `zod` + `react-hook-form`,支持类型安全的表单校验与提交。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](http://localhost:6006/?path=/docs/components-address-form--docs)。
