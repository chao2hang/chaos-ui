import { CodeBlock } from "@/components/code-block";

# MasterListTemplate Master List Template

> Domain-specific business components for ERP scenarios.

MasterListTemplate — master list template component.

## When to Use

Use MasterListTemplate when this capability is required. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { MasterListTemplate } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <MasterListTemplate />
}`} lang="tsx" />

## API / Props

### MasterListTemplate

| Prop                | Type                   | Default   | Description                       |
| ------------------- | ---------------------- | --------- | --------------------------------- |
| `className`         | `string`               | —         | Custom CSS class                  |
| `title`             | `string`               | `列表`    | Title                             |
| `onCreate`          | `() => void`           | —         |                                   |
| `onSearch`          | `() => void`           | —         |                                   |
| `searchPlaceholder` | `string`               | `搜索...` |                                   |
| ...rest             | `React.HTMLAttributes` | —         | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
