import { CodeBlock } from "@/components/code-block";

# AuthForms

> Domain-specific business components for ERP scenarios.

AuthForms — a business component for ERP / enterprise scenarios.

## When to Use

Authentication forms for sign-in, sign-up, and password recovery.

## Examples

### Basic Usage

<CodeBlock code={`import { Button, Input, Label } from "@chaos_team/chaos-ui/ui";

export function Basic() {
  return (
    <form className="grid w-full max-w-sm gap-3 rounded-lg border p-4">
      <div className="grid gap-1.5">
        <Label htmlFor="email">邮箱</Label>
        <Input id="email" type="email" placeholder="you@example.com" />
      </div>
      <div className="grid gap-1.5">
        <Label htmlFor="password">密码</Label>
        <Input id="password" type="password" placeholder="••••••••" />
      </div>
      <Button type="button">登录</Button>
    </form>
  );
}`} lang="tsx" />

## API / Props

### SignInForm

| Prop           | Type                   | Default | Description                       |
| -------------- | ---------------------- | ------- | --------------------------------- |
| `className`    | `string`               | —       | Custom CSS class                  |
| `onSubmit`     | `() => void`           | —       | Submit handler                    |
| `ssoProviders` | `Array<T>`             | —       |                                   |
| `signupHref`   | `string`               | —       |                                   |
| `forgotHref`   | `string`               | —       |                                   |
| ...rest        | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
