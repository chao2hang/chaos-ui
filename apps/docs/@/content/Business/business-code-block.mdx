import { CodeBlock } from "@/components/business/code-block";
import { CodeBlock } from "@/components/code-block";

# CodeBlock Business Code Block

> 业务组件 / Domain-specific business components for ERP scenarios.

Business Code Block — 面向 ERP/企业场景的业务组件。
CodeBlock — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Business Code Block相关场景。

Use when you need a code block.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { CodeBlock } from "@/components/business/code-block"

export function Basic() {
  return <CodeBlock />
}`} lang="tsx" />

## API / Props

### CodeBlock

| Prop              | Type                          | Default | 说明 / Description                                          |
| ----------------- | ----------------------------- | ------- | ----------------------------------------------------------- |
| `className`       | `string`                      | —       | 自定义样式类名 <br/> Custom CSS class                       |
| `code`            | `string`                      | —       |                                                             |
| `language`        | `string`                      | —       |                                                             |
| `filename`        | `string`                      | —       |                                                             |
| `showLineNumbers` | `boolean`                     | `true`  |                                                             |
| `showCopy`        | `boolean`                     | `true`  |                                                             |
| `highlightLines`  | `number[]`                    | —       |                                                             |
| `maxHeight`       | `number`                      | `480`   |                                                             |
| ...rest           | `React.ComponentProps<"div">` | —       | 透传 `<div>` 原生属性 <br/> Inherits all `<div>` attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-business-code-block--docs)。
