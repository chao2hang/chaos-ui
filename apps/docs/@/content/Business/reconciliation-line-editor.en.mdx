import { ReconciliationLineEditor } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# ReconciliationLineEditor Reconciliation Line Editor

> Domain-specific business components for ERP scenarios.

ReconciliationLineEditor — a business component for ERP / enterprise scenarios.

## When to Use

Use ReconciliationLineEditor when you need: ReconciliationLineEditor: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { ReconciliationLineEditor } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <ReconciliationLineEditor />
}`} lang="tsx" />

## API / Props

### ReconciliationLineEditor

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| `rows`      | `ReconciliationRow[]`  | —       | Row data                          |
| `onChange`  | `() => void`           | —       | Callback when value changes       |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
