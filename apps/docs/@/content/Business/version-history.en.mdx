import { VersionHistory } from "@/components/business/version-history";
import { CodeBlock } from "@/components/code-block";

# VersionHistory

> Domain-specific business components for ERP scenarios.

VersionHistory — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a version history.

## Examples

### Basic Usage

<CodeBlock code={`import { VersionHistory } from "@/components/business/version-history"

export function Basic() {
  return <VersionHistory />
}`} lang="tsx" />

## API / Props

### VersionHistory

| Prop        | Type                          | Default | Description                     |
| ----------- | ----------------------------- | ------- | ------------------------------- |
| `className` | `string`                      | —       | Custom CSS class                |
| `entries`   | `VersionEntry[]`              | —       |                                 |
| `onRestore` | `() => void`                  | —       |                                 |
| `onView`    | `() => void`                  | —       |                                 |
| ...rest     | `React.ComponentProps<"div">` | —       | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
