import { WorkflowViewer, OrgChart } from "@/components/business/workflow";
import { CodeBlock } from "@/components/code-block";

# Workflow 工作流

> 业务组件

工作流 — 面向 ERP/企业场景的业务组件。

## 何时使用

需要工作流功能时使用本组件。

## 代码示例

### 基础用法

<CodeBlock code={`import { WorkflowViewer, OrgChart } from "@/components/business/workflow"

export function Basic() {
  return (
    <div className="space-y-4">
  <WorkflowViewer />
  <OrgChart />
    </div>
  )
}`} lang="tsx" />

## API / Props

### WorkflowViewer

| Prop           | Type                          | Default | 说明                  |
| -------------- | ----------------------------- | ------- | --------------------- |
| `className`    | `string`                      | —       | 自定义样式类名        |
| `nodes`        | `Node<WorkflowNodeData>[]`    | —       |                       |
| `edges`        | `Edge[]`                      | —       |                       |
| `onNodeClick`  | `() => void`                  | —       |                       |
| `showControls` | `boolean`                     | `true`  |                       |
| `showMinimap`  | `boolean`                     | `false` |                       |
| `height`       | `number`                      | `480`   | 组件高度              |
| ...rest        | `React.ComponentProps<"div">` | —       | 透传 `<div>` 原生属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-workflow--docs)。
