import { FormDesigner } from "@/components/business/form-designer";
import { CodeBlock } from "@/components/code-block";

# FormDesigner Form Designer

> Domain-specific business components for ERP scenarios.

<FormDesigner
  fields={[{ id: "1", type: "text", label: "客户名称", required: true }]}
  onChange={(f) => console.log(f)}
/>
FormDesigner — form designer component.

## When to Use

Use when you need a form designer.

## Examples

### Basic Usage

<CodeBlock code={`import { FormDesigner } from "@/components/business/form-designer"

export function Basic() {
  return <FormDesigner />
}`} lang="tsx" />

## API / Props

### FormDesigner

| Prop        | Type                       | Default | Description                       |
| ----------- | -------------------------- | ------- | --------------------------------- |
| `className` | `string`                   | —       | Custom CSS class                  |
| `fields`    | `Array<FormDesignerField>` | —       |                                   |
| `onChange`  | `() => void`               | —       | Callback when value changes       |
| ...rest     | `React.HTMLAttributes`     | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
