import { CodeBlock } from "@/components/code-block";

# MobileListItem Mobile List Item

> Domain-specific business components for ERP scenarios.

MobileListItem — mobile list item component.

## When to Use

Use MobileListItem when this capability is required. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { MobileListItem } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <MobileListItem />
}`} lang="tsx" />

## API / Props

### MobileListItem

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| `title`     | `string`               | —       | Title                             |
| `subtitle`  | `string`               | —       |                                   |
| `trailing`  | `React.ReactNode`      | —       |                                   |
| `onClick`   | `() => void`           | —       | Click handler                     |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
