import { MobileTabs } from "@chaos_team/chaos-ui/mobile";
import { CodeBlock } from "@/components/code-block";

# MobileTabs

> Domain-specific business components for ERP scenarios.

MobileTabs — a business component for ERP / enterprise scenarios.

## When to Use

Use MobileTabs when you need: MobileTabs: enterprise business component — see Storybook for interactive demos. Use for touch and narrow screens; prioritize large hit targets and safe areas.

## Examples

### Basic Usage

<CodeBlock code={`import { MobileTabs } from "@chaos_team/chaos-ui/mobile"

export function Basic() {
  return <MobileTabs />
}`} lang="tsx" />

## API / Props

### MobileTabs

| Prop           | Type                   | Default | Description                       |
| -------------- | ---------------------- | ------- | --------------------------------- |
| `className`    | `string`               | —       | Custom CSS class                  |
| `tabs`         | `MobileTabItem[]`      | —       |                                   |
| `defaultValue` | `string`               | —       | Default value (uncontrolled)      |
| ...rest        | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
