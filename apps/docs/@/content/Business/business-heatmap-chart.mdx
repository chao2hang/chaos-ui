import { HeatmapChart } from "@/components/business/heatmap-chart";
import { CodeBlock } from "@/components/code-block";

# HeatmapChart Business Heatmap Chart

> 业务组件 / Domain-specific business components for ERP scenarios.

<HeatmapChart data={[{ x: "周一", y: "上午", value: 8 }]} />
HeatmapChart — business heatmap chart component.

## 何时使用 / When to Use

适用于Business Heatmap Chart相关场景。

Use when you need a heatmap chart.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { HeatmapChart } from "@/components/business/heatmap-chart"

export function Basic() {
  return <HeatmapChart />
}`} lang="tsx" />

## API / Props

### HeatmapChart

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `data`      | `Array<T>`             | —       | 数据源 <br/> Data source                                  |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-business-heatmap-chart--docs)。
