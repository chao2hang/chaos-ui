import { BOMTreeEditor } from "@/components/business/bom-tree-editor";
import { CodeBlock } from "@/components/code-block";

# BomTreeEditor Bom Tree Editor

> 业务组件 / Domain-specific business components for ERP scenarios.

Bom Tree Editor — 面向 ERP/企业场景的业务组件。
BomTreeEditor — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Bom Tree Editor相关场景。

Use when you need a bom tree editor.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { BOMTreeEditor } from "@/components/business/bom-tree-editor"

export function Basic() {
  return <BOMTreeEditor />
}`} lang="tsx" />

## API / Props

### BOMTreeEditor

| Prop             | Type                   | Default | 说明 / Description                                        |
| ---------------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className`      | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `items`          | `BOMItem[]`            | —       | BOM items (tree structure)                                |
| `onChange`       | `() => void`           | —       | Change callback                                           |
| `readOnly`       | `boolean`              | `false` | Read-only mode                                            |
| `showCostRollup` | `boolean`              | `true`  | Show cost rollup column                                   |
| `currencySymbol` | `string`               | `¥`     | Currency symbol (default: "¥")                            |
| ...rest          | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-bom-tree-editor--docs)。
