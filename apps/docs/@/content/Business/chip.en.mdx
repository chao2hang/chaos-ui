import { Chip } from "@chaos_team/chaos-ui/business"
import { CodeBlock } from "@/components/code-block"

# Chip

> Domain-specific business components for ERP scenarios.

Chip — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a chip.


## Examples

### Basic Usage

<CodeBlock code={`import { Chip } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <Chip />
}`} lang="tsx" />


## API / Props

### Chip

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `variant` | `ChipVariant` | `default` | Style variant |
| `size` | `ChipSize` | `default` | Component size |
| `removable` | `boolean` | — |  |
| `onRemove` | `() => void` | — |  |
| `icon` | `React.ReactNode` | — | Icon element |
| ...rest | `React.ComponentProps<"span">` | — | Inherits all `<span>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


