import { MobileSignature } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# MobileSignature Mobile Signature

> Domain-specific business components for ERP scenarios.

MobileSignature — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a mobile signature.

## Examples

### Basic Usage

<CodeBlock code={`import { MobileSignature } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <MobileSignature />
}`} lang="tsx" />

## API / Props

### MobileSignature

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| `onSave`    | `() => void`           | —       |                                   |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
