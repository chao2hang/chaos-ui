import { ChannelPicker, CHANNELS } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# ChannelPicker

> Domain-specific business components for ERP scenarios.

ChannelPicker — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a channel picker.

## Examples

### Basic Usage

<CodeBlock code={`import { ChannelPicker, CHANNELS } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return (
    <div className="space-y-4">
  <ChannelPicker />
  <CHANNELS />
    </div>
  )
}`} lang="tsx" />

## API / Props

### ChannelPicker

| Prop        | Type                          | Default | Description                     |
| ----------- | ----------------------------- | ------- | ------------------------------- |
| `className` | `string`                      | —       | Custom CSS class                |
| `value`     | `string[]`                    | —       | Current value (controlled)      |
| `onChange`  | `() => void`                  | —       | Callback when value changes     |
| ...rest     | `React.ComponentProps<"div">` | —       | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
