import { InvoiceManager } from "@/components/business/invoice-manager";
import { CodeBlock } from "@/components/code-block";

# InvoiceManager Invoice Manager

> 业务组件

Invoice Manager — 面向 ERP/企业场景的业务组件。

## 何时使用

适用于Invoice Manager相关场景。

## 代码示例

### 基础用法

<CodeBlock code={`import { InvoiceManager } from "@/components/business/invoice-manager"

export function Basic() {
  return <InvoiceManager />
}`} lang="tsx" />

## API / Props

### InvoiceManager

| Prop        | Type                   | Default | 说明              |
| ----------- | ---------------------- | ------- | ----------------- |
| `className` | `string`               | —       | 自定义样式类名    |
| `invoices`  | `Array<{`              | —       |                   |
| `id`        | `string`               | —       | DOM 元素 id       |
| `number`    | `string`               | —       |                   |
| `amount`    | `number`               | —       |                   |
| `status`    | `string`               | —       | 状态              |
| `onIssue`   | `() => void`           | —       |                   |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-invoicemanager--docs)。
