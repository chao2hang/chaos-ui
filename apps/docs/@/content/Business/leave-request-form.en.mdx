import { LeaveRequestForm } from "@/components/business/leave-request-form";
import { CodeBlock } from "@/components/code-block";

# LeaveRequestForm Leave Request Form

> Domain-specific business components for ERP scenarios.

LeaveRequestForm — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a leave request form.

## Examples

### Basic Usage

<CodeBlock code={`import { LeaveRequestForm } from "@/components/business/leave-request-form"

export function Basic() {
  return <LeaveRequestForm />
}`} lang="tsx" />

## API / Props

### LeaveRequestForm

| Prop                | Type                   | Default  | Description                       |
| ------------------- | ---------------------- | -------- | --------------------------------- |
| `className`         | `string`               | —        | Custom CSS class                  |
| `leaveType`         | `LeaveType`            | `annual` | Current leave type                |
| `onLeaveTypeChange` | `() => void`           | —        | Leave type change callback        |
| `startDate`         | `string`               | ``       | Start date                        |
| `endDate`           | `string`               | ``       | End date                          |
| `onDateChange`      | `() => void`           | —        | Date change callback              |
| `balances`          | `LeaveBalance[]`       | —        | Available leave balances          |
| `approvers`         | `Approver[]`           | —        | Approver chain                    |
| `reason`            | `string`               | ``       | Reason text                       |
| `onReasonChange`    | `() => void`           | —        | Reason change callback            |
| `onSubmit`          | `() => void`           | —        | Submit handler                    |
| `onCancel`          | `() => void`           | —        | Cancel handler                    |
| `readOnly`          | `boolean`              | `false`  | Read-only mode                    |
| ...rest             | `React.HTMLAttributes` | —        | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
