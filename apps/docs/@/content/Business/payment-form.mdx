import { PaymentForm } from "@/components/business/payment-form"
import { CodeBlock } from "@/components/code-block"

# PaymentForm 支付表单

> 业务组件 / Domain-specific business components for ERP scenarios.

支付表单 — 面向 ERP/企业场景的业务组件。
PaymentForm — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

订单支付信息填写表单。

Payment information form for orders.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { PaymentForm } from "@/components/business/payment-form"

export function Basic() {
  return (
    <PaymentForm
      onSubmit={(data) => console.log(data)}
    />
  )
}`} lang="tsx" />


## API / Props

### PaymentForm

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| `onSubmit` | `() => void` | — | 提交回调 <br/> Submit handler |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **表单校验**: 集成 `zod` + `react-hook-form`,支持类型安全的表单校验与提交。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-payment-form--docs)。
