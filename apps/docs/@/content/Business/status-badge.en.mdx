import { StatusBadge } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# StatusBadge

> Domain-specific business components for ERP scenarios.

StatusBadge — a business component for ERP / enterprise scenarios.

## When to Use

Use StatusBadge when you need: StatusBadge: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { StatusBadge } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <StatusBadge />
}`} lang="tsx" />

## Style Variants

**status**

| status       | Description | 默认 |
| ------------ | ----------- | ---- |
| `default`    |             | ✓    |
| `success`    |             |      |
| `warning`    |             |      |
| `error`      |             |      |
| `info`       |             |      |
| `processing` |             |      |

## API / Props

### StatusBadge

| Prop        | Type                           | Default | Description                      |
| ----------- | ------------------------------ | ------- | -------------------------------- |
| `className` | `string`                       | —       | Custom CSS class                 |
| `label`     | `string`                       | —       | Label text                       |
| `dot`       | `boolean`                      | `true`  |                                  |
| ...rest     | `React.ComponentProps<"span">` | —       | Inherits all `<span>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
