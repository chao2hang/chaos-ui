import { ChartEmpty } from "@/components/business/charts/shared/chart-empty";
import { CodeBlock } from "@/components/code-block";

# ChartEmpty 图表空状态

> 业务组件 / Domain-specific business components for ERP scenarios.

图表空状态 — 面向 ERP/企业场景的业务组件。
ChartEmpty — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

需要图表空状态功能时使用本组件。

Use when you need a chart empty.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { ChartEmpty } from "@/components/business/charts/shared/chart-empty"

export function Basic() {
  return <ChartEmpty />
}`} lang="tsx" />

## API / Props

### ChartEmpty

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `height`    | `number`               | `320`   | 组件高度 <br/> Component height                           |
| `message`   | `string`               | —       | 提示消息内容 <br/> Message content                        |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-charts-shared-chart-empty--docs)。
