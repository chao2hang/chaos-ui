import { TerritoryMap } from "@/components/business/territory-map"
import { CodeBlock } from "@/components/code-block"

# TerritoryMap Territory Map

> Domain-specific business components for ERP scenarios.

TerritoryMap — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a territory map.


## Examples

### Basic Usage

<CodeBlock code={`import { TerritoryMap } from "@/components/business/territory-map"

export function Basic() {
  return <TerritoryMap />
}`} lang="tsx" />


## API / Props

### TerritoryMap

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `regions` | `TerritoryRegion[]` | — | Territory regions. |
| `title` | `string` | `Sales Territory Map` | Map title. |
| `currencySymbol` | `string` | `¥` | Currency symbol. |
| `gridCols` | `number` | `4` | Grid columns for layout. |
| `onRegionClick` | `() => void` | — | Region click handler. |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


