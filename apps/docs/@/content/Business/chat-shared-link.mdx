import { ChatSharedLink } from "@/components/business/chat-shared-link"
import { CodeBlock } from "@/components/code-block"

# ChatSharedLink 共享链接卡片

> 业务组件 / Domain-specific business components for ERP scenarios.

共享链接卡片 — 面向 ERP/企业场景的业务组件。
ChatSharedLink — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

需要共享链接卡片功能时使用本组件。

Use when you need a chat shared link.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { ChatSharedLink } from "@/components/business/chat-shared-link"

export function Basic() {
  return <ChatSharedLink />
}`} lang="tsx" />


## API / Props

### ChatSharedLink

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| `url` | `string` | — |  |
| `title` | `string` | — | 标题 <br/> Title |
| `description` | `string` | — | 描述文本 <br/> Description text |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-chatsharedlink--docs)。
