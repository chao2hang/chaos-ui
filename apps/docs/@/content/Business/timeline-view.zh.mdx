import { TimelineView } from "@/components/business/timeline-view";
import { CodeBlock } from "@/components/code-block";

# TimelineView Timeline View

> 业务组件

<TimelineView events={[{ id: "e1", date: "2026-06-30", title: "创建账单" }]} />

## 何时使用

适用于Timeline View相关场景。

## 代码示例

### 基础用法

<CodeBlock code={`import { TimelineView } from "@/components/business/timeline-view"

export function Basic() {
  return <TimelineView />
}`} lang="tsx" />

## API / Props

### TimelineView

| Prop          | Type                   | Default | 说明              |
| ------------- | ---------------------- | ------- | ----------------- |
| `className`   | `string`               | —       | 自定义样式类名    |
| `events`      | `Array<{`              | —       |                   |
| `id`          | `string`               | —       | DOM 元素 id       |
| `date`        | `string`               | —       |                   |
| `title`       | `string`               | —       | 标题              |
| `description` | `string`               | —       | 描述文本          |
| ...rest       | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-timelineview--docs)。
