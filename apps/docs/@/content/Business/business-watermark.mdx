import { Watermark } from "@/components/ui/watermark";
import { CodeBlock } from "@/components/code-block";

# Watermark Business Watermark

> 业务组件 / Domain-specific business components for ERP scenarios.

Business Watermark — 面向 ERP/企业场景的业务组件。
Watermark — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Business Watermark相关场景。

Use when you need a watermark.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { Watermark } from "@/components/ui/watermark"

export function Basic() {
  return <Watermark />
}`} lang="tsx" />

## API / Props

### Watermark

| Prop        | Type                   | Default    | 说明 / Description                                        |
| ----------- | ---------------------- | ---------- | --------------------------------------------------------- |
| `className` | `string`               | —          | 自定义样式类名 <br/> Custom CSS class                     |
| `text`      | `string`               | `内部资料` |                                                           |
| `image`     | `string`               | —          |                                                           |
| `rotate`    | `number`               | `-22`      |                                                           |
| `gap`       | `[number, number]`     | —          | 间距 <br/> Gap between items                              |
| `fontSize`  | `number`               | `14`       |                                                           |
| `fullPage`  | `boolean`              | `true`     |                                                           |
| `zIndex`    | `number`               | `1000`     |                                                           |
| `opacity`   | `number`               | `0.08`     |                                                           |
| `color`     | `string`               | `#000`     | 颜色主题 <br/> Color theme                                |
| ...rest     | `React.HTMLAttributes` | —          | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-business-watermark--docs)。
