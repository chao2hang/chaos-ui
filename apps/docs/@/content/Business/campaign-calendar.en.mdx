import { CampaignCalendar } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# CampaignCalendar

> Domain-specific business components for ERP scenarios.

CampaignCalendar — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a campaign calendar.

## Examples

### Basic Usage

<CodeBlock code={`import { CampaignCalendar } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <CampaignCalendar />
}`} lang="tsx" />

## API / Props

### CampaignCalendar

| Prop            | Type                          | Default | Description                     |
| --------------- | ----------------------------- | ------- | ------------------------------- |
| `className`     | `string`                      | —       | Custom CSS class                |
| `events`        | `CampaignEvent[]`             | —       |                                 |
| `year`          | `number`                      | —       |                                 |
| `month`         | `number`                      | —       |                                 |
| `onMonthChange` | `() => void`                  | —       |                                 |
| ...rest         | `React.ComponentProps<"div">` | —       | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
