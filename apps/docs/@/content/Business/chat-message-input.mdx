import { ChatMessageInput } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# ChatMessageInput Chat Message Input

> 业务组件 / Domain-specific business components for ERP scenarios.

Chat Message Input — 面向 ERP/企业场景的业务组件。
ChatMessageInput — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

需要Chat Message Input时使用本组件。用于 ERP/业务域场景（单据、选择器、审批、对账等）；按业务语义接入而不是当纯 UI 积木。

Use ChatMessageInput when you need: ChatMessageInput: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { ChatMessageInput } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <ChatMessageInput />
}`} lang="tsx" />

## API / Props

### ChatMessageInput

| Prop          | Type                   | Default             | 说明 / Description                                        |
| ------------- | ---------------------- | ------------------- | --------------------------------------------------------- |
| `className`   | `string`               | —                   | 自定义样式类名 <br/> Custom CSS class                     |
| `value`       | `string`               | —                   | 当前值(受控) <br/> Current value (controlled)             |
| `onChange`    | `() => void`           | —                   | 值变化回调 <br/> Callback when value changes              |
| `onSend`      | `() => void`           | —                   |                                                           |
| `placeholder` | `string`               | `Type a message...` | 占位文本 <br/> Placeholder text                           |
| `disabled`    | `boolean`              | `false`             | 是否禁用 <br/> Whether disabled                           |
| `maxLength`   | `number`               | —                   | 最大输入长度 <br/> Maximum input length                   |
| ...rest       | `React.HTMLAttributes` | —                   | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](http://localhost:6006/?path=/docs/business-chatmessageinput--docs)。
