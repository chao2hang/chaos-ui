import { BulkActionsToolbar } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# BulkActionsToolbar

> Domain-specific business components for ERP scenarios.

BulkActionsToolbar — a business component for ERP / enterprise scenarios.

## When to Use

Use BulkActionsToolbar when you need: BulkActionsToolbar: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { BulkActionsToolbar } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <BulkActionsToolbar />
}`} lang="tsx" />

## API / Props

### BulkActionsToolbar

| Prop            | Type                          | Default  | Description                     |
| --------------- | ----------------------------- | -------- | ------------------------------- |
| `className`     | `string`                      | —        | Custom CSS class                |
| `count`         | `number`                      | —        |                                 |
| `selectedCount` | `number`                      | —        |                                 |
| `onClear`       | `() => void`                  | —        |                                 |
| `actions`       | `BulkAction[]`                | —        |                                 |
| `label`         | `string`                      | `已选择` | Label text                      |
| ...rest         | `React.ComponentProps<"div">` | —        | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
