import { InvoiceManager } from "@/components/business/invoice-manager"
import { CodeBlock } from "@/components/code-block"

# InvoiceManager Invoice Manager

> 业务组件 / Domain-specific business components for ERP scenarios.

Invoice Manager — 面向 ERP/企业场景的业务组件。
InvoiceManager — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Invoice Manager相关场景。

Use when you need an invoice manager.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { InvoiceManager } from "@/components/business/invoice-manager"

export function Basic() {
  return <InvoiceManager />
}`} lang="tsx" />


## API / Props

### InvoiceManager

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| `invoices` | `Array<{` | — |  |
| `id` | `string` | — | DOM 元素 id <br/> DOM element id |
| `number` | `string` | — |  |
| `amount` | `number` | — |  |
| `status` | `string` | — | 状态 <br/> Status |
| `onIssue` | `() => void` | — |  |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-invoicemanager--docs)。
