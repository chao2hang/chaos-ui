import { CostCenterPicker } from "@chaos_team/chaos-ui/business"
import { CodeBlock } from "@/components/code-block"

# CostCenterPicker Cost Center Picker

> Domain-specific business components for ERP scenarios.

Cost center picker — combobox trigger with a filterable popover.
CostCenterPicker — cost center picker component.

## When to Use

Use when you need a cost center picker.


## Examples

### Basic Usage

<CodeBlock code={`import { CostCenterPicker } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <CostCenterPicker />
}`} lang="tsx" />


## API / Props

### CostCenterPicker

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `value` | `string` | — | Current value (controlled) |
| `onChange` | `() => void` | — | Callback when value changes |
| `options` | `CostCenterPickerOption[]` | — | Available options |
| `placeholder` | `string` | — | Placeholder text |
| `searchPlaceholder` | `string` | — |  |
| `emptyText` | `string` | — |  |
| `disabled` | `boolean` | `false` | Whether disabled |
| `clearable` | `boolean` | `true` |  |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


