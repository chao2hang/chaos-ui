import { AddressForm } from "@/components/business/address-form";
import { CodeBlock } from "@/components/code-block";

# AddressForm 地址表单

> 业务组件

地址表单 — 面向 ERP/企业场景的业务组件。

## 何时使用

收件地址填写与编辑表单。


## 代码示例

### 基础用法

<CodeBlock code={`import { AddressForm } from "@/components/business/address-form"

export function Basic() {
  return (
    <AddressForm
      onSubmit={(data) => console.log(data)}
    />
  )
}`} lang="tsx" />

## API / Props

### AddressForm

| Prop | Type | Default | 说明 |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string` | — | 自定义样式类名 |
| `value` | `AddressValue` | — | 当前值(受控) |
| `onChange` | `() => void` | — | 值变化回调 |
| `country`   | `string`               | —       |                                                           |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **表单校验**: 集成 `zod` + `react-hook-form`,支持类型安全的表单校验与提交。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-address-form--docs)。

