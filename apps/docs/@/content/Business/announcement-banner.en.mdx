import { AnnouncementBanner } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# AnnouncementBanner

> Domain-specific business components for ERP scenarios.

AnnouncementBanner — a business component for ERP / enterprise scenarios.

## When to Use

Use AnnouncementBanner when you need: AnnouncementBanner: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { AnnouncementBanner } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <AnnouncementBanner />
}`} lang="tsx" />

### Variants

<CodeBlock
  code={`{/* 展示 variant 变体 */}
<div className="flex flex-wrap items-center gap-3">
  <AnnouncementBanner variant="info">Info</AnnouncementBanner>
  <AnnouncementBanner variant="warning">Warning</AnnouncementBanner>
  <AnnouncementBanner variant="success">Success</AnnouncementBanner>
  <AnnouncementBanner variant="error">Error</AnnouncementBanner>
</div>`}
  lang="tsx"
/>

## Style Variants

**Variant**

| Variant   | Description         | 默认 |
| --------- | ------------------- | ---- |
| `info`    | Informational style | ✓    |
| `warning` | Warning style       |      |
| `success` | Success style       |      |
| `error`   | Error style         |      |

## API / Props

### AnnouncementBanner

| Prop          | Type                          | Default | Description                     |
| ------------- | ----------------------------- | ------- | ------------------------------- |
| `className`   | `string`                      | —       | Custom CSS class                |
| `message`     | `string`                      | —       | Message content                 |
| `action`      | `React.ReactNode`             | —       |                                 |
| `dismissible` | `boolean`                     | `true`  |                                 |
| `onDismiss`   | `() => void`                  | —       |                                 |
| `icon`        | `React.ElementType`           | —       | Icon element                    |
| ...rest       | `React.ComponentProps<"div">` | —       | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
