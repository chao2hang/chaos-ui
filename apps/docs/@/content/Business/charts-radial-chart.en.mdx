import { RadialChartComp } from "@/components/business/charts/radial-chart";
import { CodeBlock } from "@/components/code-block";

# RadialChart

> Domain-specific business components for ERP scenarios.

RadialChart — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a radial chart.


## Examples

### Basic Usage

<CodeBlock code={`import { RadialChartComp } from "@/components/business/charts/radial-chart"

export function Basic() {
  return <RadialChartComp />
}`} lang="tsx" />

## API / Props

### RadialChartComp

| Prop | Type | Default | Description |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string` | — | Custom CSS class |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


