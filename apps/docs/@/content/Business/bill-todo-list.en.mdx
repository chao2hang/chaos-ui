import { BillTodoList } from "@/components/business/bill-todo-list"
import { CodeBlock } from "@/components/code-block"

# BillTodoList Bill Todo List

> Domain-specific business components for ERP scenarios.

BillTodoList — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a bill todo list.


## Examples

### Basic Usage

<CodeBlock code={`import { BillTodoList } from "@/components/business/bill-todo-list"

export function Basic() {
  return <BillTodoList />
}`} lang="tsx" />


## API / Props

### BillTodoList

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `items` | `Array<T>` | — | Items / data list |
| `onItemClick` | `() => void` | — |  |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


