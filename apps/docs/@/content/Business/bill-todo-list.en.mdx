import { BillTodoList } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# BillTodoList Bill Todo List

> Domain-specific business components for ERP scenarios.

BillTodoList — a business component for ERP / enterprise scenarios.

## When to Use

Use BillTodoList when you need: BillTodoList: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { BillTodoList } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <BillTodoList />
}`} lang="tsx" />

## API / Props

### BillTodoList

| Prop          | Type                   | Default | Description                       |
| ------------- | ---------------------- | ------- | --------------------------------- |
| `className`   | `string`               | —       | Custom CSS class                  |
| `items`       | `Array<T>`             | —       | Items / data list                 |
| `onItemClick` | `() => void`           | —       |                                   |
| ...rest       | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
