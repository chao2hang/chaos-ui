import { StatCardRow } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# StatCardRow

> Domain-specific business components for ERP scenarios.

StatCardRow — a business component for ERP / enterprise scenarios.

## When to Use

Use StatCardRow when you need: StatCardRow: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { StatCardRow } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <StatCardRow />
}`} lang="tsx" />

## API / Props

### StatCardRow

| Prop        | Type                          | Default | Description                     |
| ----------- | ----------------------------- | ------- | ------------------------------- |
| `className` | `string`                      | —       | Custom CSS class                |
| `cards`     | `StatCard[]`                  | —       |                                 |
| ...rest     | `React.ComponentProps<"div">` | —       | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
