import { DiffViewerTable } from "@chaos_team/chaos-ui/business"
import { CodeBlock } from "@/components/code-block"

# DiffViewerTable

> Domain-specific business components for ERP scenarios.

DiffViewerTable — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a diff viewer table.


## Examples

### Basic Usage

<CodeBlock code={`import { DiffViewerTable } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <DiffViewerTable />
}`} lang="tsx" />


## API / Props

### DiffViewerTable

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `rows` | `DiffRow[]` | — | Row data |
| `leftTitle` | `string` | `修改前` |  |
| `rightTitle` | `string` | `修改后` |  |
| ...rest | `React.ComponentProps<"div">` | — | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


