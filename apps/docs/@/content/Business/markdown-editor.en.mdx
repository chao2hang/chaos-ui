import { MarkdownEditor } from "@/components/business/markdown-editor"
import { CodeBlock } from "@/components/code-block"

# MarkdownEditor Markdown Editor

> Domain-specific business components for ERP scenarios.

MarkdownEditor — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a markdown editor.


## Examples

### Basic Usage

<CodeBlock code={`import { MarkdownEditor } from "@/components/business/markdown-editor"

export function Basic() {
  return <MarkdownEditor />
}`} lang="tsx" />


## API / Props

### MarkdownEditor

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `value` | `string` | `` | Current value (controlled) |
| `onChange` | `() => void` | — | Callback when value changes |
| `placeholder` | `string` | `请输入 Markdown...` | Placeholder text |
| `mode` | `MarkdownMode` | — |  |
| `toolbar` | `boolean` | `true` |  |
| `height` | `number \| string` | `300` | Component height |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


