import { NotificationCenter } from "@/components/business/notification-center";
import { CodeBlock } from "@/components/code-block";

# NotificationCenter Business Notification Center

> 业务组件 / Domain-specific business components for ERP scenarios.

Business Notification Center — 面向 ERP/企业场景的业务组件。
NotificationCenter — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Business Notification Center相关场景。

Use when you need a notification center.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { NotificationCenter } from "@/components/business/notification-center"

export function Basic() {
  return <NotificationCenter />
}`} lang="tsx" />

## API / Props

### NotificationCenter

| Prop            | Type                           | Default    | 说明 / Description                                        |
| --------------- | ------------------------------ | ---------- | --------------------------------------------------------- |
| `className`     | `string`                       | —          | 自定义样式类名 <br/> Custom CSS class                     |
| `notifications` | `NotificationItem[]`           | —          |                                                           |
| `onMarkRead`    | `() => void`                   | —          |                                                           |
| `onMarkAllRead` | `() => void`                   | —          |                                                           |
| `onClear`       | `() => void`                   | —          |                                                           |
| `onItemClick`   | `() => void`                   | —          |                                                           |
| `align`         | `"start" \| "center" \| "end"` | `end`      | 对齐方式 <br/> Alignment                                  |
| `emptyText`     | `string`                       | `暂无通知` |                                                           |
| ...rest         | `React.HTMLAttributes`         | —          | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-business-notification-center--docs)。
