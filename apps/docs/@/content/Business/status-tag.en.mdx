import { StatusTag, Config } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# StatusTag

> Domain-specific business components for ERP scenarios.

StatusTag — a business component for ERP / enterprise scenarios.

## When to Use

Use StatusTag when you need: StatusTag: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { StatusTag } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <StatusTag>Default</StatusTag>
}`} lang="tsx" />

## API / Props

### StatusTag

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **无障碍**: 组件遵循 WAI-ARIA 最佳实践。
