import { MobileForm } from "@/components/mobile/mobile-form";
import { CodeBlock } from "@/components/code-block";

# MobileForm 移动端表单

> 业务组件 / Domain-specific business components for ERP scenarios.

移动端表单 — 面向 ERP/企业场景的业务组件。
MobileForm — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

需要移动端表单功能时使用本组件。

Use when you need a mobile form.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { MobileForm } from "@/components/mobile/mobile-form"

export function Basic() {
  return <MobileForm />
}`} lang="tsx" />

## API / Props

### MobileForm

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `onSubmit`  | `() => void`           | —       | 提交回调 <br/> Submit handler                             |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-mobile-form--docs)。
