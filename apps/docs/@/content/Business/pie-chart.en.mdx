import { PieChart } from "@chaos_team/chaos-ui/business"
import { CodeBlock } from "@/components/code-block"

# PieChart Pie Chart

> Domain-specific business components for ERP scenarios.

PieChart — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a pie chart.


## Examples

### Basic Usage

<CodeBlock code={`import { PieChart } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <PieChart />
}`} lang="tsx" />


## API / Props

### PieChart

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `data` | `Array<T>` | — | Slices to render. |
| `size` | `number` | — | Pie pixel size. |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


