import { EquipmentCard } from "@/components/business/equipment-card";
import { CodeBlock } from "@/components/code-block";

# EquipmentCard Business Equipment Card

> 业务组件 / Domain-specific business components for ERP scenarios.

Business Equipment Card — 面向 ERP/企业场景的业务组件。
EquipmentCard — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Business Equipment Card相关场景。

Use when you need an equipment card.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { EquipmentCard } from "@/components/business/equipment-card"

export function Basic() {
  return <EquipmentCard />
}`} lang="tsx" />

## API / Props

### EquipmentCard

| Prop              | Type                                       | Default  | 说明 / Description                                        |
| ----------------- | ------------------------------------------ | -------- | --------------------------------------------------------- |
| `className`       | `string`                                   | —        | 自定义样式类名 <br/> Custom CSS class                     |
| `name`            | `string`                                   | —        | 表单字段名 <br/> Form field name                          |
| `model`           | `string`                                   | —        |                                                           |
| `serialNumber`    | `string`                                   | —        |                                                           |
| `status`          | `"normal" \| "maintenance" \| "fault" ...` | `normal` | 状态 <br/> Status                                         |
| `location`        | `string`                                   | —        |                                                           |
| `lastMaintenance` | `string`                                   | —        |                                                           |
| ...rest           | `React.HTMLAttributes`                     | —        | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-business-equipment-card--docs)。
