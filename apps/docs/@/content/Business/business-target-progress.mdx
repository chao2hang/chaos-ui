import { TargetProgress } from "@/components/business/target-progress";
import { CodeBlock } from "@/components/code-block";

# TargetProgress Business Target Progress

> 业务组件 / Domain-specific business components for ERP scenarios.

<TargetProgress target={100000} actual={78000} period="2026 年 6 月" />
TargetProgress — business target progress component.

## 何时使用 / When to Use

适用于Business Target Progress相关场景。

Use when you need a target progress.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { TargetProgress } from "@/components/business/target-progress"

export function Basic() {
  return <TargetProgress />
}`} lang="tsx" />

## API / Props

### TargetProgress

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `target`    | `number`               | —       | 链接打开目标 <br/> Link target                            |
| `actual`    | `number`               | —       |                                                           |
| `period`    | `string`               | —       |                                                           |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-business-target-progress--docs)。
