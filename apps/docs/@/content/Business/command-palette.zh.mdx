import { CommandPalette } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# CommandPalette 命令面板

> 业务组件

命令面板 — 面向 ERP/企业场景的业务组件。

## 何时使用

需要命令面板时使用本组件。用于 ERP/业务域场景（单据、选择器、审批、对账等）；按业务语义接入而不是当纯 UI 积木。

## 代码示例

### 基础用法

<CodeBlock code={`import { CommandPalette } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <CommandPalette />
}`} lang="tsx" />

## API / Props

### CommandPalette

| Prop           | Type                   | Default             | 说明              |
| -------------- | ---------------------- | ------------------- | ----------------- |
| `className`    | `string`               | —                   | 自定义样式类名    |
| `open`         | `boolean`              | —                   | 是否打开(受控)    |
| `onOpenChange` | `() => void`           | —                   | 打开状态变化回调  |
| `groups`       | `CommandGroup[]`       | —                   |                   |
| `placeholder`  | `string`               | `输入命令或搜索...` | 占位文本          |
| `emptyText`    | `string`               | `未找到结果`        |                   |
| `trigger`      | `React.ReactNode`      | —                   |                   |
| `shortcut`     | `string`               | `mod+K`             |                   |
| `showShortcut` | `boolean`              | `true`              |                   |
| ...rest        | `React.HTMLAttributes` | —                   | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](http://localhost:6006/?path=/docs/business-commandpalette--docs)。
