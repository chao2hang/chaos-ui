import { CompanyBrowse } from "@/components/business/company-browse";
import { CodeBlock } from "@/components/code-block";

# CompanyBrowse Company Browse

> Domain-specific business components for ERP scenarios.

CompanyBrowse — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a company browse.

## Examples

### Basic Usage

<CodeBlock code={`import { CompanyBrowse } from "@/components/business/company-browse"

export function Basic() {
  return <CompanyBrowse />
}`} lang="tsx" />

## API / Props

### CompanyBrowse

| Prop                | Type                   | Default | Description                       |
| ------------------- | ---------------------- | ------- | --------------------------------- |
| `className`         | `string`               | —       | Custom CSS class                  |
| `open`              | `boolean`              | —       | Open state (controlled)           |
| `onOpenChange`      | `() => void`           | —       | Callback when open state changes  |
| `onSelect`          | `() => void`           | —       | Selection handler                 |
| `items`             | `CompanyBrowseItem[]`  | —       | Items / data list                 |
| `title`             | `string`               | —       | Title                             |
| `searchPlaceholder` | `string`               | —       |                                   |
| `emptyText`         | `string`               | —       |                                   |
| ...rest             | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
