import { ChatCommandMenu } from "@/components/business/chat-command-menu";
import { CodeBlock } from "@/components/code-block";

# ChatCommandMenu Chat Command Menu

> Domain-specific business components for ERP scenarios.

ChatCommandMenu — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a chat command menu.

## Examples

### Basic Usage

<CodeBlock code={`import { ChatCommandMenu } from "@/components/business/chat-command-menu"

export function Basic() {
  return <ChatCommandMenu />
}`} lang="tsx" />

## API / Props

### ChatCommandMenu

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| `commands`  | `Array<T>`             | —       |                                   |
| `onSelect`  | `() => void`           | —       | Selection handler                 |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
