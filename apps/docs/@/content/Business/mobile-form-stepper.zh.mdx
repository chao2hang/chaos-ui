import { MobileFormStepper } from "@/components/business/mobile-form-stepper";
import { CodeBlock } from "@/components/code-block";

# MobileFormStepper 移动端表单步骤器

> 业务组件

移动端表单步骤器 — 面向 ERP/企业场景的业务组件。

## 何时使用

需要移动端表单步骤器功能时使用本组件。

## 代码示例

### 基础用法

<CodeBlock code={`import { MobileFormStepper } from "@/components/business/mobile-form-stepper"

export function Basic() {
  return <MobileFormStepper />
}`} lang="tsx" />

## API / Props

### MobileFormStepper

| Prop          | Type                          | Default | 说明                  |
| ------------- | ----------------------------- | ------- | --------------------- |
| `className`   | `string`                      | —       | 自定义样式类名        |
| `current`     | `number`                      | —       |                       |
| `total`       | `number`                      | —       |                       |
| `labels`      | `string[]`                    | —       |                       |
| `onStepClick` | `() => void`                  | —       |                       |
| ...rest       | `React.ComponentProps<"div">` | —       | 透传 `<div>` 原生属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-mobile-form-stepper--docs)。
