import { MobileCheckout } from "@chaos_team/chaos-ui/mobile";
import { CodeBlock } from "@/components/code-block";

# MobileCheckout Mobile Checkout

> Domain-specific business components for ERP scenarios.

MobileCheckout — a business component for ERP / enterprise scenarios.

## When to Use

Use MobileCheckout when you need: MobileCheckout: enterprise business component — see Storybook for interactive demos. Use for touch and narrow screens; prioritize large hit targets and safe areas.

## Examples

### Basic Usage

<CodeBlock code={`import { MobileCheckout } from "@chaos_team/chaos-ui/mobile"

export function Basic() {
  return <MobileCheckout />
}`} lang="tsx" />

## API / Props

### MobileCheckout

| Prop        | Type                   | Default   | Description                       |
| ----------- | ---------------------- | --------- | --------------------------------- |
| `className` | `string`               | —         | Custom CSS class                  |
| `total`     | `string`               | —         | Total amount string.              |
| `itemCount` | `number`               | —         | Item count.                       |
| `payLabel`  | `string`               | `Pay Now` | Pay button label.                 |
| `isLoading` | `boolean`              | —         | Whether payment is in progress.   |
| `onPay`     | `() => void`           | —         | Called when user taps pay.        |
| `onBack`    | `() => void`           | —         | Called when user taps back.       |
| ...rest     | `React.HTMLAttributes` | —         | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
