import { WorkflowDesigner } from "@chaos_team/chaos-ui/business"
import { CodeBlock } from "@/components/code-block"

# WorkflowDesigner Workflow Designer

> 业务组件 / Domain-specific business components for ERP scenarios.

<WorkflowDesigner nodes={[{ id: "n1", type: "start", name: "开始" }]} edges={[{ from: "n1", to: "n2" }]} />
WorkflowDesigner — workflow designer component.

## 何时使用 / When to Use

适用于Workflow Designer相关场景。

Use when you need a workflow designer.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { WorkflowDesigner } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <WorkflowDesigner />
}`} lang="tsx" />


## API / Props

### WorkflowDesigner

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| `nodes` | `Array<WorkflowNode>` | — |  |
| `edges` | `Array<WorkflowEdge>` | — |  |
| `onChange` | `() => void` | — | 值变化回调 <br/> Callback when value changes |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-workflowdesigner--docs)。
