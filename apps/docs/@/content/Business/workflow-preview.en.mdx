import { WorkflowPreview } from "@chaos_team/chaos-ui/business"
import { CodeBlock } from "@/components/code-block"

# WorkflowPreview Workflow Preview

> Domain-specific business components for ERP scenarios.

<WorkflowPreview nodes={[ { id: "n1", name: "提交申请", status: "done" }, { id: "n2", name: "主管审批", status: "active" }, ]} edges={[{ from: "n1", to: "n2" }]} />
WorkflowPreview — workflow preview component.

## When to Use

Use when you need a workflow preview.


## Examples

### Basic Usage

<CodeBlock code={`import { WorkflowPreview } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <WorkflowPreview />
}`} lang="tsx" />


## API / Props

### WorkflowPreview

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `nodes` | `Array<T>` | — |  |
| `edges` | `Array<T>` | — |  |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


