import { PaymentMethodSelector } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# PaymentMethodSelector Payment Method Selector

> Domain-specific business components for ERP scenarios.

PaymentMethodSelector — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a payment method selector.

## Examples

### Basic Usage

<CodeBlock code={`import { PaymentMethodSelector } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <PaymentMethodSelector />
}`} lang="tsx" />

## API / Props

### PaymentMethodSelector

| Prop        | Type                   | Default | Description                            |
| ----------- | ---------------------- | ------- | -------------------------------------- |
| `className` | `string`               | —       | Custom CSS class                       |
| `value`     | `PaymentMethod`        | —       | Currently selected payment method.     |
| `onChange`  | `() => void`           | —       | Called when the user selects a method. |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes      |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
