import { HeatmapCalendar } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# HeatmapCalendar 热力日历

> 业务组件 / Domain-specific business components for ERP scenarios.

热力日历 — 面向 ERP/企业场景的业务组件。
HeatmapCalendar — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

需要热力日历功能时使用本组件。

Use when you need a heatmap calendar.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { HeatmapCalendar } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <HeatmapCalendar />
}`} lang="tsx" />

## API / Props

### HeatmapCalendar

| Prop              | Type                                       | Default | 说明 / Description                                          |
| ----------------- | ------------------------------------------ | ------- | ----------------------------------------------------------- |
| `className`       | `string`                                   | —       | 自定义样式类名 <br/> Custom CSS class                       |
| `data`            | `Array<T>`                                 | —       | 数据源 <br/> Data source                                    |
| `startDate`       | `Date`                                     | —       |                                                             |
| `endDate`         | `Date`                                     | —       |                                                             |
| `weekStartsOn`    | `0 \| 1`                                   | `0`     |                                                             |
| `cellSize`        | `number`                                   | `12`    |                                                             |
| `gap`             | `number`                                   | `2`     | 间距 <br/> Gap between items                                |
| `levels`          | `number`                                   | `5`     |                                                             |
| `legendClassName` | `string`                                   | —       |                                                             |
| `showLegend`      | `boolean`                                  | `true`  |                                                             |
| `colorStops`      | `[string, string, string, string, string]` | —       |                                                             |
| ...rest           | `React.ComponentProps<"div">`              | —       | 透传 `<div>` 原生属性 <br/> Inherits all `<div>` attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-heatmapcalendar--docs)。
