import { AuditSidebar } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# AuditSidebar

> Domain-specific business components for ERP scenarios.

AuditSidebar — a business component for ERP / enterprise scenarios.

## When to Use

Use AuditSidebar when you need: AuditSidebar: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { AuditSidebar } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <AuditSidebar />
}`} lang="tsx" />

## API / Props

### AuditSidebar

| Prop           | Type                          | Default    | Description                      |
| -------------- | ----------------------------- | ---------- | -------------------------------- |
| `className`    | `string`                      | —          | Custom CSS class                 |
| `entries`      | `AuditSidebarEntry[]`         | —          |                                  |
| `open`         | `boolean`                     | —          | Open state (controlled)          |
| `onOpenChange` | `() => void`                  | —          | Callback when open state changes |
| `title`        | `string`                      | `操作记录` | Title                            |
| ...rest        | `React.ComponentProps<"div">` | —          | Inherits all `<div>` attributes  |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
