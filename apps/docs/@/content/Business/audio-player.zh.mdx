import { AudioPlayer } from "@/components/business/audio-player";
import { CodeBlock } from "@/components/code-block";

# AudioPlayer 音频播放器

> 业务组件

音频播放器 — 面向 ERP/企业场景的业务组件。

## 何时使用

需要音频播放器功能时使用本组件。


## 代码示例

### 基础用法

<CodeBlock code={`import { AudioPlayer } from "@/components/business/audio-player"

export function Basic() {
  return <AudioPlayer />
}`} lang="tsx" />

## API / Props

### AudioPlayer

| Prop | Type | Default | 说明 |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string` | — | 自定义样式类名 |
| `src` | `string` | — | 资源地址 |
| `title` | `string` | — | 标题 |
| `artist`    | `string`               | —       |                                                           |
| `cover`     | `string`               | —       |                                                           |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-audio-player--docs)。

