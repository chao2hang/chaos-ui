import { CodeBlock } from "@/components/code-block";

# CalendarMonth

> Domain-specific business components for ERP scenarios.

CalendarMonth — a business component for ERP / enterprise scenarios.

## When to Use

Use CalendarCalendarMonth when this capability is required. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock
  code={`export function Basic() {
  return <CalendarMonth />
}`}
  lang="tsx"
/>

### States

<CodeBlock
  code={`<CalendarMonth>Normal</CalendarMonth>
<CalendarMonth disabled>Disabled</CalendarMonth>`}
  lang="tsx"
/>

## API / Props

### CalendarMonth

| Prop           | Type                   | Default | Description                       |
| -------------- | ---------------------- | ------- | --------------------------------- |
| `className`    | `string`               | —       | Custom CSS class                  |
| `date`         | `Date`                 | —       |                                   |
| `events`       | `CalendarEvent[]`      | —       |                                   |
| `onDateChange` | `() => void`           | —       |                                   |
| `onEventClick` | `() => void`           | —       |                                   |
| `onDayClick`   | `() => void`           | —       |                                   |
| `locale`       | `string`               | `zh-CN` |                                   |
| ...rest        | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
