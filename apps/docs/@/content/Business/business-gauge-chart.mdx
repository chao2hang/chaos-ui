import { GaugeChart } from "@/components/business/gauge-chart";
import { CodeBlock } from "@/components/code-block";

# GaugeChart Business Gauge Chart

> 业务组件 / Domain-specific business components for ERP scenarios.

`tsx <GaugeChart value={72} max={100} label="完成率" /> ` 仪表盘图
GaugeChart — business gauge chart component.

## 何时使用 / When to Use

适用于Business Gauge Chart相关场景。

Use when you need a gauge chart.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { GaugeChart } from "@/components/business/gauge-chart"

export function Basic() {
  return <GaugeChart />
}`} lang="tsx" />

## API / Props

### GaugeChart

| Prop        | Type                   | Default   | 说明 / Description                                        |
| ----------- | ---------------------- | --------- | --------------------------------------------------------- |
| `className` | `string`               | —         | 自定义样式类名 <br/> Custom CSS class                     |
| `value`     | `number`               | `64`      | Current value.                                            |
| `min`       | `number`               | `0`       | Range floor.                                              |
| `max`       | `number`               | `100`     | Range ceiling.                                            |
| `label`     | `string`               | —         | Caption below the gauge.                                  |
| `color`     | `string`               | `#3b82f6` | Arc color.                                                |
| ...rest     | `React.HTMLAttributes` | —         | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-business-gauge-chart--docs)。
