import { MessageList } from "@/components/business/message-list";
import { CodeBlock } from "@/components/code-block";

# MessageList Business Message List

> 业务组件 / Domain-specific business components for ERP scenarios.

`tsx <MessageList messages={[{ id: "m1", title: "审批通过", type: "success", time: "2026-06-30T10:00:00Z" }]} /> `
MessageList — business message list component.

## 何时使用 / When to Use

适用于Business Message List相关场景。

Use when you need a message list.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { MessageList } from "@/components/business/message-list"

export function Basic() {
  return <MessageList />
}`} lang="tsx" />

## API / Props

### MessageList

| Prop            | Type                                       | Default | 说明 / Description                                        |
| --------------- | ------------------------------------------ | ------- | --------------------------------------------------------- |
| `className`     | `string`                                   | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `messages`      | `Array<{`                                  | —       | 消息数组                                                  |
| `id`            | `string`                                   | —       | DOM 元素 id <br/> DOM element id                          |
| `title`         | `string`                                   | —       | 标题 <br/> Title                                          |
| `content`       | `string`                                   | —       | 内容 <br/> Content                                        |
| `type`          | `"info" \| "warning" \| "success" \| "...` | —       | 类型 <br/> Type                                           |
| `time`          | `string \| number \| Date`                 | —       |                                                           |
| `read`          | `boolean`                                  | —       |                                                           |
| `onSelect`      | `() => void`                               | —       | 选中消息回调                                              |
| `onMarkAllRead` | `() => void`                               | —       | 全部标记已读回调                                          |
| ...rest         | `React.HTMLAttributes`                     | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-business-message-list--docs)。
