import { FormulaEditor } from "@/components/business/formula-editor";
import { CodeBlock } from "@/components/code-block";

# FormulaEditor 公式编辑器

> 业务组件 / Domain-specific business components for ERP scenarios.

公式编辑器 — 面向 ERP/企业场景的业务组件。
FormulaEditor — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

需要公式编辑器功能时使用本组件。

Use when you need a formula editor.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { FormulaEditor } from "@/components/business/formula-editor"

export function Basic() {
  return <FormulaEditor />
}`} lang="tsx" />

## API / Props

### FormulaEditor

| Prop          | Type                          | Default       | 说明 / Description                                          |
| ------------- | ----------------------------- | ------------- | ----------------------------------------------------------- |
| `className`   | `string`                      | —             | 自定义样式类名 <br/> Custom CSS class                       |
| `value`       | `string`                      | ``            | 当前值(受控) <br/> Current value (controlled)               |
| `onChange`    | `() => void`                  | —             | 值变化回调 <br/> Callback when value changes                |
| `placeholder` | `string`                      | `输入公式...` | 占位文本 <br/> Placeholder text                             |
| ...rest       | `React.ComponentProps<"div">` | —             | 透传 `<div>` 原生属性 <br/> Inherits all `<div>` attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-formula-editor--docs)。
