import { CodeBlock } from "@/components/code-block";

# RebutNodeSelect Rebut Node Select

> Domain-specific business components for ERP scenarios.

RebutNodeSelect — rebut node select component.

## When to Use

Use RebutNodeSelect when this capability is required. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { RebutNodeSelect } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <RebutNodeSelect />
}`} lang="tsx" />

## API / Props

### RebutNodeSelect

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| `nodes`     | `Array<T>`             | —       |                                   |
| `onSelect`  | `() => void`           | —       | Selection handler                 |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
