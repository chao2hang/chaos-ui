import { CreativePreview } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# CreativePreview 创意预览

> 业务组件 / Domain-specific business components for ERP scenarios.

创意预览 — 面向 ERP/企业场景的业务组件。
CreativePreview — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

需要创意预览功能时使用本组件。

Use when you need a creative preview.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { CreativePreview } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <CreativePreview />
}`} lang="tsx" />

## API / Props

### CreativePreview

| Prop          | Type                           | Default    | 说明 / Description                                        |
| ------------- | ------------------------------ | ---------- | --------------------------------------------------------- |
| `className`   | `string`                       | —          | 自定义样式类名 <br/> Custom CSS class                     |
| `title`       | `string`                       | `创意素材` | 标题 <br/> Title                                          |
| `type`        | `"image" \| "video" \| "text"` | `image`    | 类型 <br/> Type                                           |
| `src`         | `string`                       | —          | 资源地址 <br/> Source URL                                 |
| `description` | `string`                       | —          | 描述文本 <br/> Description text                           |
| `size`        | `string`                       | —          | 组件尺寸 <br/> Component size                             |
| `status`      | `string`                       | —          | 状态 <br/> Status                                         |
| ...rest       | `React.HTMLAttributes`         | —          | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-creativepreview--docs)。
