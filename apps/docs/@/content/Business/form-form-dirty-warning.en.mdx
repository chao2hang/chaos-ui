import { CodeBlock } from "@/components/code-block";

# FormDirtyWarning

> Domain-specific business components for ERP scenarios.

FormDirtyWarning — a business component for ERP / enterprise scenarios.

## When to Use

Use FormFormDirtyWarning when this capability is required. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock
  code={`export function Basic() {
  return <FormDirtyWarning />
}`}
  lang="tsx"
/>

## API / Props

### FormDirtyWarning

| Prop           | Type                   | Default                                | Description                       |
| -------------- | ---------------------- | -------------------------------------- | --------------------------------- |
| `className`    | `string`               | —                                      | Custom CSS class                  |
| `open`         | `boolean`              | —                                      | Open state (controlled)           |
| `onOpenChange` | `() => void`           | —                                      | Callback when open state changes  |
| `onDiscard`    | `() => void`           | —                                      |                                   |
| `onSave`       | `() => void`           | —                                      |                                   |
| `message`      | `string`               | `您有未保存的更改。是否保存后再离开？` | Message content                   |
| `saveText`     | `string`               | `保存并继续`                           |                                   |
| `discardText`  | `string`               | `放弃修改`                             |                                   |
| `cancelText`   | `string`               | `取消`                                 |                                   |
| `saving`       | `boolean`              | —                                      |                                   |
| ...rest        | `React.HTMLAttributes` | —                                      | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
