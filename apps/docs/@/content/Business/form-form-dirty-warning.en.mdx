import { FormDirtyWarning } from "@/components/business/form/form-dirty-warning";
import { CodeBlock } from "@/components/code-block";

# FormDirtyWarning

> Domain-specific business components for ERP scenarios.

FormDirtyWarning — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a form dirty warning.


## Examples

### Basic Usage

<CodeBlock code={`import { FormDirtyWarning } from "@/components/business/form/form-dirty-warning"

export function Basic() {
  return <FormDirtyWarning />
}`} lang="tsx" />

## API / Props

### FormDirtyWarning

| Prop | Type | Default | Description |
| -------------- | ---------------------- | -------------------------------------- | --------------------------------------------------------- |
| `className` | `string` | — | Custom CSS class |
| `open` | `boolean` | — | Open state (controlled) |
| `onOpenChange` | `() => void` | — | Callback when open state changes |
| `onDiscard`    | `() => void`           | —                                      |                                                           |
| `onSave`       | `() => void`           | —                                      |                                                           |
| `message` | `string` | `您有未保存的更改。是否保存后再离开？` | Message content |
| `saveText`     | `string`               | `保存并继续`                           |                                                           |
| `discardText`  | `string`               | `放弃修改`                             |                                                           |
| `cancelText`   | `string`               | `取消`                                 |                                                           |
| `saving`       | `boolean`              | —                                      |                                                           |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


