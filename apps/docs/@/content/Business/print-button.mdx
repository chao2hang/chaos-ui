import { PrintButton } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# PrintButton 打印按钮

> 业务组件 / Domain-specific business components for ERP scenarios.

打印按钮 — 面向 ERP/企业场景的业务组件。
PrintButton — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

需要打印按钮功能时使用本组件。

Use when you need a print button.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { PrintButton } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <PrintButton />
}`} lang="tsx" />

## API / Props

### PrintButton

| Prop        | Type                             | Default | 说明 / Description                                                |
| ----------- | -------------------------------- | ------- | ----------------------------------------------------------------- |
| `className` | `string`                         | —       | 自定义样式类名 <br/> Custom CSS class                             |
| `onClick`   | `() => void`                     | —       | 点击回调 <br/> Click handler                                      |
| `label`     | `string`                         | `打印`  | 标签文本 <br/> Label text                                         |
| ...rest     | `React.ComponentProps<"button">` | —       | 透传 `<button>` 原生属性 <br/> Inherits all `<button>` attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **原生属性**: 继承所有 `<button>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-printbutton--docs)。
