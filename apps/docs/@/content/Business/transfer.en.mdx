import { Transfer } from "@/components/business/transfer";
import { CodeBlock } from "@/components/code-block";

# Transfer

> Domain-specific business components for ERP scenarios.

Transfer — a business component for ERP / enterprise scenarios.

## When to Use

Transfer items between two columns to make a selection.

## Examples

### Basic Usage

<CodeBlock code={`import { Transfer } from "@/components/business/transfer"

export function Basic() {
  return <Transfer />
}`} lang="tsx" />

## API / Props

### Transfer

| Prop           | Type                   | Default | Description                       |
| -------------- | ---------------------- | ------- | --------------------------------- |
| `className`    | `string`               | —       | Custom CSS class                  |
| `dataSource`   | `TransferItem[]`       | —       |                                   |
| `targetKeys`   | `string[]`             | —       |                                   |
| `onChange`     | `() => void`           | —       | Callback when value changes       |
| `titles`       | `[string, string]`     | —       |                                   |
| `searchable`   | `boolean`              | `true`  |                                   |
| `filterOption` | `(...args) => unknown` | —       |                                   |
| `disabled`     | `boolean`              | —       | Whether disabled                  |
| `oneWay`       | `boolean`              | `false` |                                   |
| ...rest        | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
