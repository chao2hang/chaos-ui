import { ChatConversation } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# ChatConversation Chat Conversation

> Domain-specific business components for ERP scenarios.

ChatConversation — a business component for ERP / enterprise scenarios.

## When to Use

Use ChatConversation when you need: ChatConversation: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { ChatConversation } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <ChatConversation />
}`} lang="tsx" />

## API / Props

### ChatConversation

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| `messages`  | `Array<T>`             | —       |                                   |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
