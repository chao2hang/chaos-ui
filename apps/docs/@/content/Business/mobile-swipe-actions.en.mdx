import { SwipeActions } from "@/components/business/mobile-swipe-actions";
import { CodeBlock } from "@/components/code-block";

# MobileSwipeActions

> Domain-specific business components for ERP scenarios.

MobileSwipeActions — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a mobile swipe actions.

## Examples

### Basic Usage

<CodeBlock code={`import { SwipeActions } from "@/components/business/mobile-swipe-actions"

export function Basic() {
  return <SwipeActions />
}`} lang="tsx" />

## API / Props

### SwipeActions

| Prop           | Type                          | Default | Description                     |
| -------------- | ----------------------------- | ------- | ------------------------------- |
| `className`    | `string`                      | —       | Custom CSS class                |
| `onSwipeLeft`  | `() => void`                  | —       |                                 |
| `onSwipeRight` | `() => void`                  | —       |                                 |
| `onSwipeUp`    | `() => void`                  | —       |                                 |
| `onSwipeDown`  | `() => void`                  | —       |                                 |
| `threshold`    | `number`                      | `80`    |                                 |
| `disabled`     | `boolean`                     | —       | Whether disabled                |
| `leftAction`   | `() => void`                  | —       |                                 |
| `rightAction`  | `() => void`                  | —       |                                 |
| ...rest        | `React.ComponentProps<"div">` | —       | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
