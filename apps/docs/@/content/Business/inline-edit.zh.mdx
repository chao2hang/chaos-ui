import { InlineEdit } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# InlineEdit 内联编辑

> 业务组件

内联编辑 — 面向 ERP/企业场景的业务组件。

## 何时使用

需要内联编辑时使用本组件。用于 ERP/业务域场景（单据、选择器、审批、对账等）；按业务语义接入而不是当纯 UI 积木。

## 代码示例

### 基础用法

<CodeBlock code={`import { InlineEdit } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <InlineEdit />
}`} lang="tsx" />

## API / Props

### InlineEdit

| Prop          | Type                           | Default    | 说明                   |
| ------------- | ------------------------------ | ---------- | ---------------------- |
| `className`   | `string`                       | —          | 自定义样式类名         |
| `value`       | `string`                       | —          | 当前值(受控)           |
| `onChange`    | `() => void`                   | —          | 值变化回调             |
| `onSave`      | `() => void`                   | —          |                        |
| `placeholder` | `string`                       | `点击编辑` | 占位文本               |
| ...rest       | `React.ComponentProps<"span">` | —          | 透传 `<span>` 原生属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **原生属性**: 继承所有 `<span>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](http://localhost:6006/?path=/docs/business-inlineedit--docs)。
