import { ApprovalActionBar } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# ApprovalActionBar Approval Action Bar

> Domain-specific business components for ERP scenarios.

ApprovalActionBar — a business component for ERP / enterprise scenarios.

## When to Use

Use ApprovalActionBar when you need: ApprovalActionBar: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { ApprovalActionBar } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <ApprovalActionBar />
}`} lang="tsx" />

## API / Props

### ApprovalActionBar

| Prop         | Type                   | Default | Description                       |
| ------------ | ---------------------- | ------- | --------------------------------- |
| `className`  | `string`               | —       | Custom CSS class                  |
| `onApprove`  | `() => void`           | —       |                                   |
| `onReject`   | `() => void`           | —       |                                   |
| `onTransfer` | `() => void`           | —       |                                   |
| `status`     | `string`               | —       | Status                            |
| `loading`    | `boolean`              | —       | Loading state                     |
| ...rest      | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
