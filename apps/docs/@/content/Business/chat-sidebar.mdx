import { ChatSidebar } from "@chaos_team/chaos-ui/business"
import { CodeBlock } from "@/components/code-block"

# ChatSidebar Chat Sidebar

> 业务组件 / Domain-specific business components for ERP scenarios.

Chat Sidebar — 面向 ERP/企业场景的业务组件。
ChatSidebar — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Chat Sidebar相关场景。

Use when you need a chat sidebar.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { ChatSidebar } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <ChatSidebar />
}`} lang="tsx" />


## API / Props

### ChatSidebar

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| `conversations` | `Array<{` | — |  |
| `id` | `string` | — | DOM 元素 id <br/> DOM element id |
| `title` | `string` | — | 标题 <br/> Title |
| `unread` | `number` | — |  |
| `lastMessage` | `string` | — |  |
| `time` | `string` | — |  |
| `activeId` | `string` | — |  |
| `onSelect` | `() => void` | — | 选中回调 <br/> Selection handler |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-chatsidebar--docs)。
