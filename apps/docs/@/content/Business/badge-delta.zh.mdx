import { BadgeDelta } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# BadgeDelta Badge Delta

> 业务组件

Badge Delta — 面向 ERP/企业场景的业务组件。

## 何时使用

适用于Badge Delta相关场景。

## 代码示例

### 基础用法

<CodeBlock code={`import { BadgeDelta } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <BadgeDelta />
}`} lang="tsx" />

## API / Props

### BadgeDelta

| Prop        | Type                   | Default | 说明              |
| ----------- | ---------------------- | ------- | ----------------- |
| `className` | `string`               | —       | 自定义样式类名    |
| `value`     | `number`               | —       | 当前值(受控)      |
| `suffix`    | `string`               | `%`     | 后缀内容          |
| `prefix`    | `string`               | —       | 前缀内容          |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-badgedelta--docs)。
