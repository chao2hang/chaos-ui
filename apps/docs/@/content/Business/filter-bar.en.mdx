import { FilterBar } from "@/components/business/filter-bar"
import { CodeBlock } from "@/components/code-block"

# FilterBar

> Domain-specific business components for ERP scenarios.

FilterBar — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a filter bar.


## Examples

### Basic Usage

<CodeBlock code={`import { FilterBar } from "@/components/business/filter-bar"

export function Basic() {
  return <FilterBar />
}`} lang="tsx" />


## API / Props

### FilterBar

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `fields` | `FilterField[]` | — |  |
| `onFilter` | `() => void` | — |  |
| `onReset` | `() => void` | — |  |
| ...rest | `React.ComponentProps<"div">` | — | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


