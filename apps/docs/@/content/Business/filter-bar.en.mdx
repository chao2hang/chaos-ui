import { FilterBar } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# FilterBar

> Domain-specific business components for ERP scenarios.

FilterBar — a business component for ERP / enterprise scenarios.

## When to Use

Use FilterBar when you need: FilterBar: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { FilterBar } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <FilterBar />
}`} lang="tsx" />

## API / Props

### FilterBar

| Prop        | Type                          | Default | Description                     |
| ----------- | ----------------------------- | ------- | ------------------------------- |
| `className` | `string`                      | —       | Custom CSS class                |
| `fields`    | `FilterField[]`               | —       |                                 |
| `onFilter`  | `() => void`                  | —       |                                 |
| `onReset`   | `() => void`                  | —       |                                 |
| ...rest     | `React.ComponentProps<"div">` | —       | Inherits all `<div>` attributes |

`FilterField.type` accepts `browser`. Pass BrowserField configuration through `browser`; filter values contain the selected entity IDs.

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
