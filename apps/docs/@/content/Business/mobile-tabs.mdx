import { MobileTabs } from "@/components/business/mobile-tabs";
import { CodeBlock } from "@/components/code-block";

# MobileTabs 移动端标签页

> 业务组件 / Domain-specific business components for ERP scenarios.

移动端标签页 — 面向 ERP/企业场景的业务组件。
MobileTabs — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

需要移动端标签页功能时使用本组件。

Use when you need a mobile tabs.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { MobileTabs } from "@/components/business/mobile-tabs"

export function Basic() {
  return <MobileTabs />
}`} lang="tsx" />

## API / Props

### MobileTabs

| Prop           | Type                   | Default | 说明 / Description                                        |
| -------------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className`    | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `tabs`         | `MobileTabItem[]`      | —       |                                                           |
| `defaultValue` | `string`               | —       | 默认值(非受控) <br/> Default value (uncontrolled)         |
| ...rest        | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-mobile-tabs--docs)。
