import { AdvancedDataTable } from "@/components/business/advanced-data-table";
import { CodeBlock } from "@/components/code-block";

# AdvancedDataTable Business Advanced Data Table

> 业务组件 / Domain-specific business components for ERP scenarios.

Business Advanced Data Table — 面向 ERP/企业场景的业务组件。
AdvancedDataTable — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Business Advanced Data Table相关场景。

Use when you need an advanced data table.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { AdvancedDataTable } from "@/components/business/advanced-data-table"

export function Basic() {
  return <AdvancedDataTable />
}`} lang="tsx" />

## API / Props

### AdvancedDataTable

| Prop                | Type                                       | Default     | 说明 / Description                                        |
| ------------------- | ------------------------------------------ | ----------- | --------------------------------------------------------- |
| `className`         | `string`                                   | —           | 自定义样式类名 <br/> Custom CSS class                     |
| `columns`           | `ColumnDef[]`                              | —           | 列配置 <br/> Column configuration                         |
| `data`              | `Record<string, unknown>[]`                | —           | 数据源 <br/> Data source                                  |
| `searchable`        | `boolean`                                  | `true`      |                                                           |
| `searchPlaceholder` | `string`                                   | `Search...` |                                                           |
| `pageSize`          | `number`                                   | —           |                                                           |
| `onRowClick`        | `() => void`                               | —           |                                                           |
| `emptyVariant`      | `"default" \| "search" \| "error" \| "...` | `default`   |                                                           |
| ...rest             | `React.HTMLAttributes`                     | —           | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-business-advanced-data-table--docs)。
