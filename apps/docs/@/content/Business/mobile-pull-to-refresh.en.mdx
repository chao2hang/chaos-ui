import { PullToRefresh } from "@chaos_team/chaos-ui/mobile";
import { CodeBlock } from "@/components/code-block";

# MobilePullToRefresh

> Domain-specific business components for ERP scenarios.

MobilePullToRefresh — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a mobile pull to refresh.

## Examples

### Basic Usage

<CodeBlock code={`import { PullToRefresh } from "@chaos_team/chaos-ui/mobile"

export function Basic() {
  return <PullToRefresh />
}`} lang="tsx" />

## API / Props

### PullToRefresh

| Prop             | Type                          | Default | Description                     |
| ---------------- | ----------------------------- | ------- | ------------------------------- |
| `className`      | `string`                      | —       | Custom CSS class                |
| `onRefresh`      | `() => void`                  | —       |                                 |
| `threshold`      | `number`                      | `60`    |                                 |
| `disabled`       | `boolean`                     | —       | Whether disabled                |
| `refreshingText` | `string`                      | —       |                                 |
| `pullText`       | `string`                      | —       |                                 |
| `releaseText`    | `string`                      | —       |                                 |
| ...rest          | `React.ComponentProps<"div">` | —       | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
