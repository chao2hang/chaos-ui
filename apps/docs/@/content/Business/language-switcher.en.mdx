import { LanguageSwitcher } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# LanguageSwitcher

> Domain-specific business components for ERP scenarios.

LanguageSwitcher — a business component for ERP / enterprise scenarios.

## When to Use

Use LanguageSwitcher when you need: LanguageSwitcher: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { LanguageSwitcher } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <LanguageSwitcher />
}`} lang="tsx" />

## API / Props

### LanguageSwitcher

| Prop        | Type                           | Default | Description                       |
| ----------- | ------------------------------ | ------- | --------------------------------- |
| `className` | `string`                       | —       | Custom CSS class                  |
| `value`     | `string`                       | —       | Current value (controlled)        |
| `onChange`  | `() => void`                   | —       | Callback when value changes       |
| `options`   | `LanguageOption[]`             | —       | Available options                 |
| `align`     | `"start" \| "center" \| "end"` | `end`   | Alignment                         |
| ...rest     | `React.HTMLAttributes`         | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
