import { LanguageSwitcher } from "@/components/business/language-switcher";
import { CodeBlock } from "@/components/code-block";

# LanguageSwitcher

> Domain-specific business components for ERP scenarios.

LanguageSwitcher — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a language switcher.

## Examples

### Basic Usage

<CodeBlock code={`import { LanguageSwitcher } from "@/components/business/language-switcher"

export function Basic() {
  return <LanguageSwitcher />
}`} lang="tsx" />

## API / Props

### LanguageSwitcher

| Prop        | Type                           | Default | Description                       |
| ----------- | ------------------------------ | ------- | --------------------------------- |
| `className` | `string`                       | —       | Custom CSS class                  |
| `value`     | `string`                       | —       | Current value (controlled)        |
| `onChange`  | `() => void`                   | —       | Callback when value changes       |
| `options`   | `LanguageOption[]`             | —       | Available options                 |
| `align`     | `"start" \| "center" \| "end"` | `end`   | Alignment                         |
| ...rest     | `React.HTMLAttributes`         | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
