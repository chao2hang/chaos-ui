import { ChartFrame } from "@/components/business/charts/chart-frame";
import { CodeBlock } from "@/components/code-block";

# ChartFrame

> Domain-specific business components for ERP scenarios.

ChartFrame — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a chart frame.


## Examples

### Basic Usage

<CodeBlock code={`import { ChartFrame } from "@/components/business/charts/chart-frame"

export function Basic() {
  return <ChartFrame />
}`} lang="tsx" />

## API / Props

### ChartFrame

| Prop | Type | Default | Description |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string` | — | Custom CSS class |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


