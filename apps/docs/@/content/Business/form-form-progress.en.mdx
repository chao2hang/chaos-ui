import { FormProgress } from "@/components/business/form/form-progress";
import { CodeBlock } from "@/components/code-block";

# FormProgress

> Domain-specific business components for ERP scenarios.

FormProgress — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a form progress.

## Examples

### Basic Usage

<CodeBlock code={`import { FormProgress } from "@/components/business/form/form-progress"

export function Basic() {
  return <FormProgress />
}`} lang="tsx" />

## API / Props

### FormProgress

| Prop         | Type                         | Default | Description                       |
| ------------ | ---------------------------- | ------- | --------------------------------- |
| `className`  | `string`                     | —       | Custom CSS class                  |
| `current`    | `number`                     | —       |                                   |
| `total`      | `number`                     | —       |                                   |
| `labels`     | `string[]`                   | —       |                                   |
| `showLabels` | `boolean`                    | `true`  |                                   |
| `variant`    | `"bar" \| "steps" \| "dots"` | `bar`   | Style variant                     |
| ...rest      | `React.HTMLAttributes`       | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
