import { ApprovalActionBar } from "@/components/business/approval-action-bar"
import { CodeBlock } from "@/components/code-block"

# ApprovalActionBar Approval Action Bar

> 业务组件 / Domain-specific business components for ERP scenarios.

Approval Action Bar — 面向 ERP/企业场景的业务组件。
ApprovalActionBar — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Approval Action Bar相关场景。

Use when you need an approval action bar.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { ApprovalActionBar } from "@/components/business/approval-action-bar"

export function Basic() {
  return <ApprovalActionBar />
}`} lang="tsx" />


## API / Props

### ApprovalActionBar

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| `onApprove` | `() => void` | — |  |
| `onReject` | `() => void` | — |  |
| `onTransfer` | `() => void` | — |  |
| `status` | `string` | — | 状态 <br/> Status |
| `loading` | `boolean` | — | 是否加载中 <br/> Loading state |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-approvalactionbar--docs)。
