import { WarehousePicker } from "@/components/business/warehouse-picker";
import { CodeBlock } from "@/components/code-block";

# WarehousePicker Warehouse Picker

> Domain-specific business components for ERP scenarios.

Warehouse picker — combobox trigger with a filterable popover.
WarehousePicker — warehouse picker component.

## When to Use

Use when you need a warehouse picker.

## Examples

### Basic Usage

<CodeBlock code={`import { WarehousePicker } from "@/components/business/warehouse-picker"

export function Basic() {
  return <WarehousePicker />
}`} lang="tsx" />

## API / Props

### WarehousePicker

| Prop                | Type                      | Default | Description                       |
| ------------------- | ------------------------- | ------- | --------------------------------- |
| `className`         | `string`                  | —       | Custom CSS class                  |
| `value`             | `string`                  | —       | Current value (controlled)        |
| `onChange`          | `() => void`              | —       | Callback when value changes       |
| `options`           | `WarehousePickerOption[]` | —       | Available options                 |
| `placeholder`       | `string`                  | —       | Placeholder text                  |
| `searchPlaceholder` | `string`                  | —       |                                   |
| `emptyText`         | `string`                  | —       |                                   |
| `disabled`          | `boolean`                 | `false` | Whether disabled                  |
| `clearable`         | `boolean`                 | `true`  |                                   |
| ...rest             | `React.HTMLAttributes`    | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
