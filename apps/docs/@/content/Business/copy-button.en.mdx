import { CopyButton } from "@/components/business/copy-button";
import { CodeBlock } from "@/components/code-block";

# CopyButton

> Domain-specific business components for ERP scenarios.

CopyButton — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a copy button.


## Examples

### Basic Usage

<CodeBlock code={`import { CopyButton } from "@/components/business/copy-button"

export function Basic() {
  return <CopyButton />
}`} lang="tsx" />

## API / Props

### CopyButton

| Prop | Type | Default | Description |
| ------------- | ----------------------------------- | -------- | --------------------------------------------------------- |
| `className` | `string` | — | Custom CSS class |
| `text`        | `string`                            | —        |                                                           |
| `label` | `string` | `复制` | Label text |
| `copiedLabel` | `string`                            | `已复制` |                                                           |
| `variant` | `"default" \| "ghost" \| "outline"` | `ghost` | Style variant |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


