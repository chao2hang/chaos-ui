import { ChatSharedLink } from "@/components/business/chat-shared-link";
import { CodeBlock } from "@/components/code-block";

# ChatSharedLink 共享链接卡片

> 业务组件

共享链接卡片 — 面向 ERP/企业场景的业务组件。

## 何时使用

需要共享链接卡片功能时使用本组件。

## 代码示例

### 基础用法

<CodeBlock code={`import { ChatSharedLink } from "@/components/business/chat-shared-link"

export function Basic() {
  return <ChatSharedLink />
}`} lang="tsx" />

## API / Props

### ChatSharedLink

| Prop          | Type                   | Default | 说明              |
| ------------- | ---------------------- | ------- | ----------------- |
| `className`   | `string`               | —       | 自定义样式类名    |
| `url`         | `string`               | —       |                   |
| `title`       | `string`               | —       | 标题              |
| `description` | `string`               | —       | 描述文本          |
| ...rest       | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-chatsharedlink--docs)。
