import { ChatMessageInput } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# ChatMessageInput Chat Message Input

> 业务组件

Chat Message Input — 面向 ERP/企业场景的业务组件。

## 何时使用

需要采集或编辑业务字段，并进入校验/提交链路时使用。（组件：Chat Message Input）

## 代码示例

### 基础用法

<CodeBlock code={`import { ChatMessageInput } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <ChatMessageInput />
}`} lang="tsx" />

## API / Props

### ChatMessageInput

| Prop          | Type                   | Default             | 说明              |
| ------------- | ---------------------- | ------------------- | ----------------- |
| `className`   | `string`               | —                   | 自定义样式类名    |
| `value`       | `string`               | —                   | 当前值(受控)      |
| `onChange`    | `() => void`           | —                   | 值变化回调        |
| `onSend`      | `() => void`           | —                   |                   |
| `placeholder` | `string`               | `Type a message...` | 占位文本          |
| `disabled`    | `boolean`              | `false`             | 是否禁用          |
| `maxLength`   | `number`               | —                   | 最大输入长度      |
| ...rest       | `React.HTMLAttributes` | —                   | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](http://localhost:6006/?path=/docs/business-chatmessageinput--docs)。
