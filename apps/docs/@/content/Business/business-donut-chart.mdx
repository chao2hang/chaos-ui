import { DonutChart } from "@/components/business/donut-chart";
import { CodeBlock } from "@/components/code-block";

# DonutChart Business Donut Chart

> 业务组件 / Domain-specific business components for ERP scenarios.

`tsx <DonutChart data={[{label:"A",value:60,color:"#3b82f6"}]} /> ` 环形图(占比)
DonutChart — business donut chart component.

## 何时使用 / When to Use

适用于Business Donut Chart相关场景。

Use when you need a donut chart.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { DonutChart } from "@/components/business/donut-chart"

export function Basic() {
  return <DonutChart />
}`} lang="tsx" />

## API / Props

### DonutChart

| Prop          | Type                   | Default | 说明 / Description                                        |
| ------------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className`   | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `data`        | `Array<T>`             | —       | Segments to render.                                       |
| `size`        | `number`               | `128`   | Donut pixel size.                                         |
| `thickness`   | `number`               | `12`    | Stroke thickness.                                         |
| `centerLabel` | `string`               | —       | Center hole label.                                        |
| ...rest       | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-business-donut-chart--docs)。
