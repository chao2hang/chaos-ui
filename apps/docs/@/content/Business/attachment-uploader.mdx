import { AttachmentUploader } from "@/components/business/attachment-uploader";
import { CodeBlock } from "@/components/code-block";

# AttachmentUploader Attachment Uploader

> 业务组件 / Domain-specific business components for ERP scenarios.

Attachment Uploader — 面向 ERP/企业场景的业务组件。
AttachmentUploader — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Attachment Uploader相关场景。

Use when you need an attachment uploader.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { AttachmentUploader } from "@/components/business/attachment-uploader"

export function Basic() {
  return <AttachmentUploader />
}`} lang="tsx" />

## API / Props

### AttachmentUploader

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `accept`    | `string`               | —       |                                                           |
| `maxSize`   | `number`               | —       |                                                           |
| `onUpload`  | `() => void`           | —       |                                                           |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-attachment-uploader--docs)。
