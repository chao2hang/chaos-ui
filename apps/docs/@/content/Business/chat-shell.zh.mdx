import { ChatShell } from "@chaos_team/chaos-ui/business"
import { CodeBlock } from "@/components/code-block"

# ChatShell Chat Shell

> 业务组件

Chat Shell — 面向 ERP/企业场景的业务组件。

## 何时使用

适用于Chat Shell相关场景。


## 代码示例

### 基础用法

<CodeBlock code={`import { ChatShell } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <ChatShell />
}`} lang="tsx" />


## API / Props

### ChatShell

| Prop | Type | Default | 说明 |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 |
| `sidebar` | `React.ReactNode` | — |  |
| `header` | `React.ReactNode` | — |  |
| `detailPanel` | `React.ReactNode` | — |  |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-chatshell--docs)。

