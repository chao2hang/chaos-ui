import { CodeEditor } from "@/components/business/code-editor"
import { CodeBlock } from "@/components/code-block"

# CodeEditor

> Domain-specific business components for ERP scenarios.

CodeEditor — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a code editor.


## Examples

### Basic Usage

<CodeBlock code={`import { CodeEditor } from "@/components/business/code-editor"

export function Basic() {
  return <CodeEditor />
}`} lang="tsx" />


## API / Props

### CodeEditor

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `value` | `string` | `` | Current value (controlled) |
| `onChange` | `() => void` | — | Callback when value changes |
| `language` | `SupportedLang` | `typescript` |  |
| `readonly` | `boolean` | `false` |  |
| `showLineNumbers` | `boolean` | `true` |  |
| `showCopy` | `boolean` | `true` |  |
| `height` | `string \| number` | `320` | Component height |
| `theme` | `"light" \| "dark"` | `light` | Theme |
| `filename` | `string` | — |  |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


