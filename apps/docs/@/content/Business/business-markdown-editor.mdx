import { MarkdownEditor } from "@/components/business/markdown-editor";
import { CodeBlock } from "@/components/code-block";

# MarkdownEditor Business Markdown Editor

> 业务组件 / Domain-specific business components for ERP scenarios.

Business Markdown Editor — 面向 ERP/企业场景的业务组件。
MarkdownEditor — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Business Markdown Editor相关场景。

Use when you need a markdown editor.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { MarkdownEditor } from "@/components/business/markdown-editor"

export function Basic() {
  return <MarkdownEditor />
}`} lang="tsx" />

## API / Props

### MarkdownEditor

| Prop          | Type                   | Default              | 说明 / Description                                        |
| ------------- | ---------------------- | -------------------- | --------------------------------------------------------- |
| `className`   | `string`               | —                    | 自定义样式类名 <br/> Custom CSS class                     |
| `value`       | `string`               | ``                   | 当前值(受控) <br/> Current value (controlled)             |
| `onChange`    | `() => void`           | —                    | 值变化回调 <br/> Callback when value changes              |
| `placeholder` | `string`               | `请输入 Markdown...` | 占位文本 <br/> Placeholder text                           |
| `mode`        | `MarkdownMode`         | —                    |                                                           |
| `toolbar`     | `boolean`              | `true`               |                                                           |
| `height`      | `number \| string`     | `300`                | 组件高度 <br/> Component height                           |
| ...rest       | `React.HTMLAttributes` | —                    | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-business-markdown-editor--docs)。
