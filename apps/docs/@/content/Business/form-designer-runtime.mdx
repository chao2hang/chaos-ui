import { FormDesignerRuntime } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# FormDesignerRuntime Form Designer Runtime

> 业务组件 / Domain-specific business components for ERP scenarios.

<FormDesignerRuntime
  schema={[{ id: "name", type: "text", label: "姓名", required: true }]}
  value={{ name: "张三" }}
/>
FormDesignerRuntime — form designer runtime component.

## 何时使用 / When to Use

适用于Form Designer Runtime相关场景。

Use when you need a form designer runtime.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { FormDesignerRuntime } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <FormDesignerRuntime />
}`} lang="tsx" />

## API / Props

### FormDesignerRuntime

| Prop        | Type                      | Default | 说明 / Description                                        |
| ----------- | ------------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`                  | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `schema`    | `unknown`                 | —       |                                                           |
| `value`     | `Record<string, unknown>` | —       | 当前值(受控) <br/> Current value (controlled)             |
| `onChange`  | `() => void`              | —       | 值变化回调 <br/> Callback when value changes              |
| ...rest     | `React.HTMLAttributes`    | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-formdesignerruntime--docs)。
