import { ReportBuilder } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# ReportBuilder

> Domain-specific business components for ERP scenarios.

ReportBuilder — a business component for ERP / enterprise scenarios.

## When to Use

Use ReportBuilder when you need: ReportBuilder: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { ReportBuilder } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <ReportBuilder />
}`} lang="tsx" />

## API / Props

### ReportBuilder

| Prop              | Type                          | Default | Description                     |
| ----------------- | ----------------------------- | ------- | ------------------------------- |
| `className`       | `string`                      | —       | Custom CSS class                |
| `fields`          | `ReportField[]`               | —       |                                 |
| `filters`         | `ReportFilter[]`              | —       |                                 |
| `onFieldsChange`  | `() => void`                  | —       |                                 |
| `onFiltersChange` | `() => void`                  | —       |                                 |
| `onPreview`       | `() => void`                  | —       |                                 |
| `onExport`        | `() => void`                  | —       |                                 |
| `availableFields` | `string[]`                    | —       |                                 |
| ...rest           | `React.ComponentProps<"div">` | —       | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
