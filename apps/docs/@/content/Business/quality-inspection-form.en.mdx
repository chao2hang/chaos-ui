import { QualityInspectionForm } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# QualityInspectionForm

> Domain-specific business components for ERP scenarios.

QualityInspectionForm — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a quality inspection form.

## Examples

### Basic Usage

<CodeBlock code={`import { QualityInspectionForm } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <QualityInspectionForm />
}`} lang="tsx" />

## API / Props

### QualityInspectionForm

| Prop        | Type                          | Default | Description                     |
| ----------- | ----------------------------- | ------- | ------------------------------- |
| `className` | `string`                      | —       | Custom CSS class                |
| `items`     | `InspectionItem[]`            | —       | Items / data list               |
| `onChange`  | `() => void`                  | —       | Callback when value changes     |
| `onSubmit`  | `() => void`                  | —       | Submit handler                  |
| ...rest     | `React.ComponentProps<"div">` | —       | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
