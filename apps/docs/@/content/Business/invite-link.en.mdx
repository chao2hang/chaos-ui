import { InviteLink } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# InviteLink Invite Link

> Domain-specific business components for ERP scenarios.

InviteLink — a business component for ERP / enterprise scenarios.

## When to Use

Use InviteLink when you need: InviteLink: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { InviteLink } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <InviteLink />
}`} lang="tsx" />

## API / Props

### InviteLink

| Prop          | Type                   | Default | Description                       |
| ------------- | ---------------------- | ------- | --------------------------------- |
| `className`   | `string`               | —       | Custom CSS class                  |
| `url`         | `string`               | —       | The invite URL.                   |
| `description` | `string`               | —       | Invite description text.          |
| ...rest       | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
