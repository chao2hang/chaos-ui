import { CodeBlock } from "@/components/code-block";

# EventDetail

> Domain-specific business components for ERP scenarios.

EventDetail — a business component for ERP / enterprise scenarios.

## When to Use

Use CalendarEventDetail when this capability is required. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock
  code={`export function Basic() {
  return <EventDetail />
}`}
  lang="tsx"
/>

### States

<CodeBlock
  code={`<EventDetail>Normal</EventDetail>
<EventDetail disabled>Disabled</EventDetail>`}
  lang="tsx"
/>

## API / Props

### EventDetail

| Prop        | Type                          | Default | Description                     |
| ----------- | ----------------------------- | ------- | ------------------------------- |
| `className` | `string`                      | —       | Custom CSS class                |
| `event`     | `EventDetailData`             | —       |                                 |
| `onRsvp`    | `() => void`                  | —       |                                 |
| ...rest     | `React.ComponentProps<"div">` | —       | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
