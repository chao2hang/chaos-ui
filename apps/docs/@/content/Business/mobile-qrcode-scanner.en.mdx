import { MobileQrCodeScanner } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# MobileQrCodeScanner Mobile Qrcode Scanner

> Domain-specific business components for ERP scenarios.

MobileQrCodeScanner — a business component for ERP / enterprise scenarios.

## When to Use

Use MobileQrCodeScanner when you need: MobileQrCodeScanner: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { MobileQrCodeScanner } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <MobileQrCodeScanner />
}`} lang="tsx" />

## API / Props

### MobileQrCodeScanner

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| `onScan`    | `() => void`           | —       |                                   |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
