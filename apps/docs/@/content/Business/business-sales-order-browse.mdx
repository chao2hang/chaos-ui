import { SalesOrderBrowse } from "@/components/business/sales-order-browse";
import { CodeBlock } from "@/components/code-block";

# SalesOrderBrowse Business Sales Order Browse

> 业务组件 / Domain-specific business components for ERP scenarios.

Business Sales Order Browse — 面向 ERP/企业场景的业务组件。
SalesOrderBrowse — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Business Sales Order Browse相关场景。

Use when you need a sales order browse.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { SalesOrderBrowse } from "@/components/business/sales-order-browse"

export function Basic() {
  return <SalesOrderBrowse />
}`} lang="tsx" />

## API / Props

### SalesOrderBrowse

| Prop                | Type                     | Default | 说明 / Description                                        |
| ------------------- | ------------------------ | ------- | --------------------------------------------------------- |
| `className`         | `string`                 | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `open`              | `boolean`                | —       | 是否打开(受控) <br/> Open state (controlled)              |
| `onOpenChange`      | `() => void`             | —       | 打开状态变化回调 <br/> Callback when open state changes   |
| `onSelect`          | `() => void`             | —       | 选中回调 <br/> Selection handler                          |
| `items`             | `SalesOrderBrowseItem[]` | —       | 选项/数据项列表 <br/> Items / data list                   |
| `title`             | `string`                 | —       | 标题 <br/> Title                                          |
| `searchPlaceholder` | `string`                 | —       |                                                           |
| `emptyText`         | `string`                 | —       |                                                           |
| ...rest             | `React.HTMLAttributes`   | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-business-sales-order-browse--docs)。
