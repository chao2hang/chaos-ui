import { Rating } from "@/components/ui/rating";
import { CodeBlock } from "@/components/code-block";

# Rating

> Domain-specific business components for ERP scenarios.

Rating — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a rating.

## Examples

### Basic Usage

<CodeBlock code={`import { Rating } from "@/components/ui/rating"

export function Basic() {
  return <Rating />
}`} lang="tsx" />

## API / Props

### Rating

| Prop            | Type                        | Default   | Description                       |
| --------------- | --------------------------- | --------- | --------------------------------- |
| `className`     | `string`                    | —         | Custom CSS class                  |
| `value`         | `number`                    | —         | Current value (controlled)        |
| `defaultValue`  | `number`                    | —         | Default value (uncontrolled)      |
| `onChange`      | `() => void`                | —         | Callback when value changes       |
| `max`           | `number`                    | `5`       | Maximum value                     |
| `size`          | `"sm" \| "default" \| "lg"` | `default` | Component size                    |
| `readonly`      | `boolean`                   | —         |                                   |
| `allowHalf`     | `boolean`                   | —         |                                   |
| `iconClassName` | `string`                    | —         |                                   |
| ...rest         | `React.HTMLAttributes`      | —         | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
