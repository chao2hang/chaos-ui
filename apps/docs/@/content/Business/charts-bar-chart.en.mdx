import { BarChart } from "@/components/business/charts/bar-chart";
import { CodeBlock } from "@/components/code-block";

# BarChart

> Domain-specific business components for ERP scenarios.

BarChart — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a bar chart.

## Examples

### Basic Usage

<CodeBlock code={`import { BarChart } from "@/components/business/charts/bar-chart"

export function Basic() {
  return <BarChart />
}`} lang="tsx" />

## API / Props

### BarChart

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
