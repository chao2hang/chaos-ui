import { CodeBlock } from "@/components/code-block";

# BarChart

> Domain-specific business components for ERP scenarios.

BarChart — a business component for ERP / enterprise scenarios.

## When to Use

Use ChartsBarChart when this capability is required. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock
  code={`export function Basic() {
  return <BarChart />
}`}
  lang="tsx"
/>

## API / Props

### BarChart

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
