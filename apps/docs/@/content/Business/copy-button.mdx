import { CopyButton } from "@/components/business/copy-button";
import { CodeBlock } from "@/components/code-block";

# CopyButton 复制按钮

> 业务组件 / Domain-specific business components for ERP scenarios.

复制按钮 — 面向 ERP/企业场景的业务组件。
CopyButton — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

需要复制按钮功能时使用本组件。

Use when you need a copy button.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { CopyButton } from "@/components/business/copy-button"

export function Basic() {
  return <CopyButton />
}`} lang="tsx" />

## API / Props

### CopyButton

| Prop          | Type                                | Default  | 说明 / Description                                        |
| ------------- | ----------------------------------- | -------- | --------------------------------------------------------- |
| `className`   | `string`                            | —        | 自定义样式类名 <br/> Custom CSS class                     |
| `text`        | `string`                            | —        |                                                           |
| `label`       | `string`                            | `复制`   | 标签文本 <br/> Label text                                 |
| `copiedLabel` | `string`                            | `已复制` |                                                           |
| `variant`     | `"default" \| "ghost" \| "outline"` | `ghost`  | 样式变体 <br/> Style variant                              |
| ...rest       | `React.HTMLAttributes`              | —        | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-copy-button--docs)。
