import { SparkChart } from "@/components/business/spark-chart";
import { CodeBlock } from "@/components/code-block";

# SparkChart Spark Chart

> 业务组件 / Domain-specific business components for ERP scenarios.

`tsx <SparkChart data={[3,6,4,8,7,10]} /> ` 迷你趋势图
SparkChart — spark chart component.

## 何时使用 / When to Use

适用于Spark Chart相关场景。

Use when you need a spark chart.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { SparkChart } from "@/components/business/spark-chart"

export function Basic() {
  return <SparkChart />
}`} lang="tsx" />

## API / Props

### SparkChart

| Prop        | Type                   | Default        | 说明 / Description                                        |
| ----------- | ---------------------- | -------------- | --------------------------------------------------------- |
| `className` | `string`               | —              | 自定义样式类名 <br/> Custom CSS class                     |
| `data`      | `number[]`             | —              | Numeric series.                                           |
| `width`     | `number`               | `120`          | SVG width in px.                                          |
| `height`    | `number`               | `32`           | SVG height in px.                                         |
| `color`     | `string`               | `currentColor` | Line color.                                               |
| ...rest     | `React.HTMLAttributes` | —              | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-spark-chart--docs)。
