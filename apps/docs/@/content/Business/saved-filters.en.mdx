import { SavedFilters } from "@/components/business/saved-filters"
import { CodeBlock } from "@/components/code-block"

# SavedFilters

> Domain-specific business components for ERP scenarios.

SavedFilters — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a saved filters.


## Examples

### Basic Usage

<CodeBlock code={`import { SavedFilters } from "@/components/business/saved-filters"

export function Basic() {
  return <SavedFilters />
}`} lang="tsx" />


## API / Props

### SavedFilters

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `filters` | `SavedFilter[]` | — |  |
| `activeId` | `string` | — |  |
| `onApply` | `() => void` | — |  |
| `onSave` | `() => void` | — |  |
| `onDelete` | `() => void` | — |  |
| `onPin` | `() => void` | — |  |
| `label` | `string` | `已保存的筛选` | Label text |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


