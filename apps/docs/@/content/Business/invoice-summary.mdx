import { InvoiceSummary } from "@/components/business/invoice-summary"
import { CodeBlock } from "@/components/code-block"

# InvoiceSummary Invoice Summary

> 业务组件 / Domain-specific business components for ERP scenarios.

Invoice Summary — 面向 ERP/企业场景的业务组件。
InvoiceSummary — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Invoice Summary相关场景。

Use when you need an invoice summary.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { InvoiceSummary } from "@/components/business/invoice-summary"

export function Basic() {
  return <InvoiceSummary />
}`} lang="tsx" />


## API / Props

### InvoiceSummary

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| `total` | `number` | — |  |
| `issued` | `number` | — |  |
| `pending` | `number` | — |  |
| `amount` | `number` | — |  |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-invoicesummary--docs)。
