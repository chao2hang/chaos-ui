import { CityBrowse } from "@/components/business/city-browse";
import { CodeBlock } from "@/components/code-block";

# CityBrowse City Browse

> Domain-specific business components for ERP scenarios.

CityBrowse — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a city browse.

## Examples

### Basic Usage

<CodeBlock code={`import { CityBrowse } from "@/components/business/city-browse"

export function Basic() {
  return <CityBrowse />
}`} lang="tsx" />

## API / Props

### CityBrowse

| Prop                | Type                   | Default | Description                                                 |
| ------------------- | ---------------------- | ------- | ----------------------------------------------------------- |
| `className`         | `string`               | —       | Custom CSS class                                            |
| `open`              | `boolean`              | —       | Whether the dialog is open. / 是否打开                      |
| `onOpenChange`      | `() => void`           | —       | Open state change handler. / 开关回调                       |
| `onSelect`          | `() => void`           | —       | Called with the selected city item. / 选中回调              |
| `items`             | `CityBrowseItem[]`     | —       | City list to browse (defaults to an empty list). / 候选城市 |
| `title`             | `string`               | —       | 弹窗标题                                                    |
| `searchPlaceholder` | `string`               | —       | Search placeholder. / 搜索占位                              |
| `emptyText`         | `string`               | —       | Empty-state text. / 空状态文案                              |
| ...rest             | `React.HTMLAttributes` | —       | Inherits standard HTML attributes                           |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
