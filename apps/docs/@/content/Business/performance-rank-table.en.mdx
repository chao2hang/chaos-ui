import { PerformanceRankTable } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# PerformanceRankTable Performance Rank Table

> Domain-specific business components for ERP scenarios.

Performance rank table — displays ranked performance data with growth indicators.
PerformanceRankTable — performance rank table component.

## When to Use

Use PerformanceRankTable when this capability is required. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { PerformanceRankTable } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <PerformanceRankTable />
}`} lang="tsx" />

## API / Props

### PerformanceRankTable

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| `rows`      | `Array<{`              | —       | Row data                          |
| `id`        | `string`               | —       | DOM element id                    |
| `rank`      | `number`               | —       |                                   |
| `name`      | `string`               | —       | Form field name                   |
| `amount`    | `number`               | —       |                                   |
| `growth`    | `number`               | —       |                                   |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
