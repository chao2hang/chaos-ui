import { InlineEdit } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# InlineEdit

> Domain-specific business components for ERP scenarios.

InlineEdit — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need an inline edit.

## Examples

### Basic Usage

<CodeBlock code={`import { InlineEdit } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <InlineEdit />
}`} lang="tsx" />

## API / Props

### InlineEdit

| Prop          | Type                           | Default    | Description                      |
| ------------- | ------------------------------ | ---------- | -------------------------------- |
| `className`   | `string`                       | —          | Custom CSS class                 |
| `value`       | `string`                       | —          | Current value (controlled)       |
| `onChange`    | `() => void`                   | —          | Callback when value changes      |
| `onSave`      | `() => void`                   | —          |                                  |
| `placeholder` | `string`                       | `点击编辑` | Placeholder text                 |
| ...rest       | `React.ComponentProps<"span">` | —          | Inherits all `<span>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
