import { FormDesigner } from "@/components/business/form-designer";
import { CodeBlock } from "@/components/code-block";

# FormDesigner Business Form Designer

> 业务组件 / Domain-specific business components for ERP scenarios.

<FormDesigner
  fields={[{ id: "1", type: "text", label: "客户名称", required: true }]}
  onChange={(f) => console.log(f)}
/>
FormDesigner — business form designer component.

## 何时使用 / When to Use

适用于Business Form Designer相关场景。

Use when you need a form designer.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { FormDesigner } from "@/components/business/form-designer"

export function Basic() {
  return <FormDesigner />
}`} lang="tsx" />

## API / Props

### FormDesigner

| Prop        | Type                       | Default | 说明 / Description                                        |
| ----------- | -------------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`                   | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `fields`    | `Array<FormDesignerField>` | —       |                                                           |
| `onChange`  | `() => void`               | —       | 值变化回调 <br/> Callback when value changes              |
| ...rest     | `React.HTMLAttributes`     | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-business-form-designer--docs)。
