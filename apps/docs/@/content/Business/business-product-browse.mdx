import { ProductBrowse } from "@/components/business/product-browse";
import { CodeBlock } from "@/components/code-block";

# ProductBrowse Business Product Browse

> 业务组件 / Domain-specific business components for ERP scenarios.

Business Product Browse — 面向 ERP/企业场景的业务组件。
ProductBrowse — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Business Product Browse相关场景。

Use when you need a product browse.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { ProductBrowse } from "@/components/business/product-browse"

export function Basic() {
  return <ProductBrowse />
}`} lang="tsx" />

## API / Props

### ProductBrowse

| Prop                | Type                   | Default | 说明 / Description                                                             |
| ------------------- | ---------------------- | ------- | ------------------------------------------------------------------------------ |
| `className`         | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                                          |
| `open`              | `boolean`              | —       | 是否打开(受控) <br/> Open state (controlled)                                   |
| `onOpenChange`      | `() => void`           | —       | 打开状态变化回调 <br/> Callback when open state changes                        |
| `onSelect`          | `() => void`           | —       | Single-select emits the chosen item; multi-select emits the last toggled item. |
| `multiple`          | `boolean`              | `false` | Enable multi-select (default false). / 多选                                    |
| `items`             | `ProductBrowseItem[]`  | —       | 选项/数据项列表 <br/> Items / data list                                        |
| `title`             | `string`               | —       | 标题 <br/> Title                                                               |
| `searchPlaceholder` | `string`               | —       |                                                                                |
| `emptyText`         | `string`               | —       |                                                                                |
| ...rest             | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes                      |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-business-product-browse--docs)。
