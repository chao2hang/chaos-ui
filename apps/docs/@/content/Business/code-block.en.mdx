import { CodeBlock } from "@chaos_team/chaos-ui/business";

# CodeBlock

> Domain-specific business components for ERP scenarios.

CodeBlock — a business component for ERP / enterprise scenarios.

## When to Use

Use CodeBlock when you need: CodeBlock: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { CodeBlock } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <CodeBlock />
}`} lang="tsx" />

## API / Props

### CodeBlock

| Prop              | Type                          | Default | Description                     |
| ----------------- | ----------------------------- | ------- | ------------------------------- |
| `className`       | `string`                      | —       | Custom CSS class                |
| `code`            | `string`                      | —       |                                 |
| `language`        | `string`                      | —       |                                 |
| `filename`        | `string`                      | —       |                                 |
| `showLineNumbers` | `boolean`                     | `true`  |                                 |
| `showCopy`        | `boolean`                     | `true`  |                                 |
| `highlightLines`  | `number[]`                    | —       |                                 |
| `maxHeight`       | `number`                      | `480`   |                                 |
| ...rest           | `React.ComponentProps<"div">` | —       | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
