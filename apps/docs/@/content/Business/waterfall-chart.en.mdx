import { WaterfallChart } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# WaterfallChart Waterfall Chart

> Domain-specific business components for ERP scenarios.

WaterfallChart — waterfall chart component.

## When to Use

Use WaterfallChart when this capability is required. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { WaterfallChart } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <WaterfallChart />
}`} lang="tsx" />

## API / Props

### WaterfallChart

| Prop        | Type                   | Default | Description                                                |
| ----------- | ---------------------- | ------- | ---------------------------------------------------------- |
| `className` | `string`               | —       | Custom CSS class                                           |
| `data`      | `Array<T>`             | —       | Steps: label + value (delta, or absolute when `absolute`). |
| `height`    | `number`               | —       | Component height                                           |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes                          |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
