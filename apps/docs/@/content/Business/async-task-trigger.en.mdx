import { AsyncTaskTrigger } from "@/components/business/async-task-trigger";
import { CodeBlock } from "@/components/code-block";

# AsyncTaskTrigger Async Task Trigger

> Domain-specific business components for ERP scenarios.

AsyncTaskTrigger — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need an async task trigger.

## Examples

### Basic Usage

<CodeBlock code={`import { AsyncTaskTrigger } from "@/components/business/async-task-trigger"

export function Basic() {
  return <AsyncTaskTrigger />
}`} lang="tsx" />

## API / Props

### AsyncTaskTrigger

| Prop         | Type                      | Default | Description                       |
| ------------ | ------------------------- | ------- | --------------------------------- |
| `className`  | `string`                  | —       | Custom CSS class                  |
| `taskType`   | `string`                  | —       |                                   |
| `params`     | `Record<string, unknown>` | —       |                                   |
| `onComplete` | `() => void`              | —       |                                   |
| ...rest      | `React.HTMLAttributes`    | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
