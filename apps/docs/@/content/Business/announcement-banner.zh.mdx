import { AnnouncementBanner } from "@/components/business/announcement-banner";
import { CodeBlock } from "@/components/code-block";

# AnnouncementBanner 公告横幅

> 业务组件

公告横幅 — 面向 ERP/企业场景的业务组件。

## 何时使用

需要公告横幅功能时使用本组件。

## 代码示例

### 基础用法

<CodeBlock code={`import { AnnouncementBanner } from "@/components/business/announcement-banner"

export function Basic() {
  return <AnnouncementBanner />
}`} lang="tsx" />

### 变体

<CodeBlock
  code={`{/* 展示 variant 变体 */}
<div className="flex flex-wrap items-center gap-3">
  <AnnouncementBanner variant="info">Info</AnnouncementBanner>
  <AnnouncementBanner variant="warning">Warning</AnnouncementBanner>
  <AnnouncementBanner variant="success">Success</AnnouncementBanner>
  <AnnouncementBanner variant="error">Error</AnnouncementBanner>
</div>`}
  lang="tsx"
/>

## 样式变体

**变体**

| Variant   | 说明     | 默认 |
| --------- | -------- | ---- |
| `info`    | 信息提示 | ✓    |
| `warning` | 警告状态 |      |
| `success` | 成功状态 |      |
| `error`   | 错误状态 |      |

## API / Props

### AnnouncementBanner

| Prop          | Type                          | Default | 说明                  |
| ------------- | ----------------------------- | ------- | --------------------- |
| `className`   | `string`                      | —       | 自定义样式类名        |
| `message`     | `string`                      | —       | 提示消息内容          |
| `action`      | `React.ReactNode`             | —       |                       |
| `dismissible` | `boolean`                     | `true`  |                       |
| `onDismiss`   | `() => void`                  | —       |                       |
| `icon`        | `React.ElementType`           | —       | 图标元素              |
| ...rest       | `React.ComponentProps<"div">` | —       | 透传 `<div>` 原生属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **CVA 变体**: 通过 `class-variance-authority` 管理样式变体,类型安全。变体对象可导出复用。
- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-announcementbanner--docs)。
