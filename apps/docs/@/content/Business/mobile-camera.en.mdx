import { MobileCamera } from "@/components/business/mobile-camera"
import { CodeBlock } from "@/components/code-block"

# MobileCamera Mobile Camera

> Domain-specific business components for ERP scenarios.

MobileCamera — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a mobile camera.


## Examples

### Basic Usage

<CodeBlock code={`import { MobileCamera } from "@/components/business/mobile-camera"

export function Basic() {
  return <MobileCamera />
}`} lang="tsx" />


## API / Props

### MobileCamera

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `onCapture` | `() => void` | — |  |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


