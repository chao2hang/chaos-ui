import { MobileCamera } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# MobileCamera Mobile Camera

> Domain-specific business components for ERP scenarios.

MobileCamera — a business component for ERP / enterprise scenarios.

## When to Use

Use MobileCamera when you need: MobileCamera: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { MobileCamera } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <MobileCamera />
}`} lang="tsx" />

## API / Props

### MobileCamera

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| `onCapture` | `() => void`           | —       |                                   |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
