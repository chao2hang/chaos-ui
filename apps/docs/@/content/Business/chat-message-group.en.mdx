import { ChatMessageGroup } from "@/components/business/chat-message-group"
import { CodeBlock } from "@/components/code-block"

# ChatMessageGroup Chat Message Group

> Domain-specific business components for ERP scenarios.

ChatMessageGroup — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a chat message group.


## Examples

### Basic Usage

<CodeBlock code={`import { ChatMessageGroup } from "@/components/business/chat-message-group"

export function Basic() {
  return <ChatMessageGroup />
}`} lang="tsx" />


## API / Props

### ChatMessageGroup

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `sender` | `string` | — |  |
| `avatar` | `React.ReactNode` | — |  |
| `messages` | `Array<T>` | — |  |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


