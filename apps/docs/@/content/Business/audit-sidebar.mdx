import { AuditSidebar } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# AuditSidebar 审计侧栏

> 业务组件 / Domain-specific business components for ERP scenarios.

审计侧栏 — 面向 ERP/企业场景的业务组件。
AuditSidebar — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

需要审计侧栏功能时使用本组件。

Use when you need an audit sidebar.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { AuditSidebar } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <AuditSidebar />
}`} lang="tsx" />

## API / Props

### AuditSidebar

| Prop           | Type                          | Default    | 说明 / Description                                          |
| -------------- | ----------------------------- | ---------- | ----------------------------------------------------------- |
| `className`    | `string`                      | —          | 自定义样式类名 <br/> Custom CSS class                       |
| `entries`      | `AuditSidebarEntry[]`         | —          |                                                             |
| `open`         | `boolean`                     | —          | 是否打开(受控) <br/> Open state (controlled)                |
| `onOpenChange` | `() => void`                  | —          | 打开状态变化回调 <br/> Callback when open state changes     |
| `title`        | `string`                      | `操作记录` | 标题 <br/> Title                                            |
| ...rest        | `React.ComponentProps<"div">` | —          | 透传 `<div>` 原生属性 <br/> Inherits all `<div>` attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-auditsidebar--docs)。
