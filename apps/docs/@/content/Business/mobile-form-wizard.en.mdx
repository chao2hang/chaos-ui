import { MobileFormWizard } from "@chaos_team/chaos-ui/mobile";
import { CodeBlock } from "@/components/code-block";

# MobileFormWizard

> Domain-specific business components for ERP scenarios.

MobileFormWizard — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a mobile form wizard.

## Examples

### Basic Usage

<CodeBlock code={`import { MobileFormWizard } from "@chaos_team/chaos-ui/mobile"

export function Basic() {
  return <MobileFormWizard />
}`} lang="tsx" />

## API / Props

### MobileFormWizard

| Prop          | Type                   | Default | Description                       |
| ------------- | ---------------------- | ------- | --------------------------------- |
| `className`   | `string`               | —       | Custom CSS class                  |
| `steps`       | `{`                    | —       |                                   |
| `title`       | `string`               | —       | Title                             |
| `description` | `string`               | —       | Description text                  |
| `validate`    | `(...args) => unknown` | —       |                                   |
| `onComplete`  | `() => void`           | —       |                                   |
| ...rest       | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
