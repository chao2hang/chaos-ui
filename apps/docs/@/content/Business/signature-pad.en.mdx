import { SignaturePad } from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# SignaturePad

> Domain-specific business components for ERP scenarios.

SignaturePad — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a signature pad.


## Examples

### Basic Usage

<CodeBlock code={`import { SignaturePad } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <SignaturePad />
}`} lang="tsx" />

## API / Props

### SignaturePad

| Prop | Type | Default | Description |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string` | — | Custom CSS class |
| `width` | `number` | — | Component width |
| `height` | `number` | — | Component height |
| `onChange` | `() => void` | — | Callback when value changes |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


