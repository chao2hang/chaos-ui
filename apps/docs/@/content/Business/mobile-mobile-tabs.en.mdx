import { MobileTabs } from "@/components/mobile/mobile-tabs";
import { CodeBlock } from "@/components/code-block";

# MobileTabs Mobile Mobile Tabs

> Domain-specific business components for ERP scenarios.

MobileTabs — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a mobile tabs.

## Examples

### Basic Usage

<CodeBlock code={`import { MobileTabs } from "@/components/mobile/mobile-tabs"

export function Basic() {
  return <MobileTabs />
}`} lang="tsx" />

## API / Props

### MobileTabs

| Prop           | Type                   | Default | Description                       |
| -------------- | ---------------------- | ------- | --------------------------------- |
| `className`    | `string`               | —       | Custom CSS class                  |
| `tabs`         | `MobileTabItem[]`      | —       |                                   |
| `defaultValue` | `string`               | —       | Default value (uncontrolled)      |
| ...rest        | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
