import { FileUploadManager } from "@/components/business/file-upload-manager";
import { CodeBlock } from "@/components/code-block";

# FileUploadManager

> Domain-specific business components for ERP scenarios.

FileUploadManager — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a file upload manager.

## Examples

### Basic Usage

<CodeBlock code={`import { FileUploadManager } from "@/components/business/file-upload-manager"

export function Basic() {
  return <FileUploadManager />
}`} lang="tsx" />

## API / Props

### FileUploadManager

| Prop            | Type                       | Default | Description                       |
| --------------- | -------------------------- | ------- | --------------------------------- |
| `className`     | `string`                   | —       | Custom CSS class                  |
| `files`         | `FileInfo[]`               | —       |                                   |
| `onFilesChange` | `() => void`               | —       |                                   |
| `accept`        | `Record<string, string[]>` | —       |                                   |
| `maxFiles`      | `number`                   | `10`    |                                   |
| `maxSize`       | `number`                   | —       |                                   |
| ...rest         | `React.HTMLAttributes`     | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
