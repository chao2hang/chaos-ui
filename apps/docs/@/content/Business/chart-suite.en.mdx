import { ChartSuite } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# ChartSuite Chart Suite

> Domain-specific business components for ERP scenarios.

ChartSuite — a business component for ERP / enterprise scenarios.

## When to Use

Use ChartSuite when you need: ChartSuite: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { ChartSuite } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <ChartSuite />
}`} lang="tsx" />

## API / Props

### ChartSuite

| Prop        | Type                                       | Default | Description                       |
| ----------- | ------------------------------------------ | ------- | --------------------------------- |
| `className` | `string`                                   | —       | Custom CSS class                  |
| `type`      | `"line" \| "bar" \| "pie" \| "radar" \...` | —       | Type                              |
| `data`      | `unknown[]`                                | —       | Data source                       |
| `xField`    | `string`                                   | `x`     |                                   |
| `yField`    | `string`                                   | `y`     |                                   |
| `series`    | `string`                                   | —       |                                   |
| `height`    | `number`                                   | `200`   | Component height                  |
| ...rest     | `React.HTMLAttributes`                     | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
