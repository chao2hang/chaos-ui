import { TreeCrudPage } from "@/components/business/tree-crud-page";
import { CodeBlock } from "@/components/code-block";

# TreeCrudPage Tree Crud Page

> Domain-specific business components for ERP scenarios.

TreeCrudPage — tree crud page component.

## When to Use

Use when you need a tree crud page.

## Examples

### Basic Usage

<CodeBlock code={`import { TreeCrudPage } from "@/components/business/tree-crud-page"

export function Basic() {
  return <TreeCrudPage />
}`} lang="tsx" />

## API / Props

### TreeCrudPage

| Prop              | Type                   | Default | Description                        |
| ----------------- | ---------------------- | ------- | ---------------------------------- |
| `className`       | `string`               | —       | Custom CSS class                   |
| `tree`            | `TreeNodeData[]`       | —       | 左侧分类树                         |
| `selected`        | `string`               | —       | 当前选中节点 id（受控）            |
| `defaultSelected` | `string`               | —       | 默认选中节点 id（非受控）          |
| `onSelect`        | `() => void`           | —       | 选中节点回调                       |
| `onCreate`        | `() => void`           | —       | 点击“新增分类”回调；不传则隐藏按钮 |
| `onRefresh`       | `() => void`           | —       | 点击“刷新”回调；不传则隐藏按钮     |
| `query`           | `string`               | —       | 搜索关键字（受控）                 |
| `onQueryChange`   | `() => void`           | —       | 搜索关键字变化回调                 |
| ...rest           | `React.HTMLAttributes` | —       | Inherits standard HTML attributes  |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
