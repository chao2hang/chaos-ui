import { FileCard } from "@/components/business/file-card";
import { CodeBlock } from "@/components/code-block";

# FileCard File Card

> Domain-specific business components for ERP scenarios.

FileCard — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a file card.

## Examples

### Basic Usage

<CodeBlock code={`import { FileCard } from "@/components/business/file-card"

export function Basic() {
  return <FileCard />
}`} lang="tsx" />

## API / Props

### FileCard

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| `name`      | `string`               | —       | Form field name                   |
| `size`      | `number`               | —       | Component size                    |
| `type`      | `string`               | —       | Type                              |
| `url`       | `string`               | —       |                                   |
| `onRemove`  | `() => void`           | —       |                                   |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
