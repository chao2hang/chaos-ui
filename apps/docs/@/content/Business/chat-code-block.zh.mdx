import { ChatCodeBlock } from "@chaos_team/chaos-ui/business"
import { CodeBlock } from "@/components/code-block"

# ChatCodeBlock Chat Code Block

> 业务组件

Chat Code Block — 面向 ERP/企业场景的业务组件。

## 何时使用

适用于Chat Code Block相关场景。


## 代码示例

### 基础用法

<CodeBlock code={`import { ChatCodeBlock } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <ChatCodeBlock />
}`} lang="tsx" />


## API / Props

### ChatCodeBlock

| Prop | Type | Default | 说明 |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 |
| `code` | `string` | — |  |
| `language` | `string` | — |  |
| `filename` | `string` | — |  |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-chatcodeblock--docs)。

