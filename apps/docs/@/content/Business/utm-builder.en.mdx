import { UtmBuilder } from "@/components/business/utm-builder";
import { CodeBlock } from "@/components/code-block";

# UtmBuilder UTM

> Domain-specific business components for ERP scenarios.

UtmBuilder — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need an utm builder.

## Examples

### Basic Usage

<CodeBlock code={`import { UtmBuilder } from "@/components/business/utm-builder"

export function Basic() {
  return <UtmBuilder />
}`} lang="tsx" />

## API / Props

### UtmBuilder

| Prop        | Type                          | Default | Description                     |
| ----------- | ----------------------------- | ------- | ------------------------------- |
| `className` | `string`                      | —       | Custom CSS class                |
| `baseUrl`   | `string`                      | ``      |                                 |
| `onChange`  | `() => void`                  | —       | Callback when value changes     |
| ...rest     | `React.ComponentProps<"div">` | —       | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
