import { TargetProgress } from "@chaos_team/chaos-ui/business"
import { CodeBlock } from "@/components/code-block"

# TargetProgress Target Progress

> 业务组件

<TargetProgress target={100000} actual={78000} period="2026 年 6 月" />

## 何时使用

适用于Target Progress相关场景。


## 代码示例

### 基础用法

<CodeBlock code={`import { TargetProgress } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <TargetProgress />
}`} lang="tsx" />


## API / Props

### TargetProgress

| Prop | Type | Default | 说明 |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 |
| `target` | `number` | — | 链接打开目标 |
| `actual` | `number` | — |  |
| `period` | `string` | — |  |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-targetprogress--docs)。

