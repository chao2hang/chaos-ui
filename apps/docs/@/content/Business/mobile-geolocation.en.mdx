import { MobileGeolocation } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# MobileGeolocation Mobile Geolocation

> Domain-specific business components for ERP scenarios.

MobileGeolocation — a business component for ERP / enterprise scenarios.

## When to Use

Use MobileGeolocation when you need: MobileGeolocation: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { MobileGeolocation } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <MobileGeolocation />
}`} lang="tsx" />

## API / Props

### MobileGeolocation

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| `onLocate`  | `() => void`           | —       |                                   |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
