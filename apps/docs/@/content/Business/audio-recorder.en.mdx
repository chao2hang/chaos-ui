import { AudioRecorder } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# AudioRecorder

> Domain-specific business components for ERP scenarios.

AudioRecorder — Audio recording component with waveform animation and timer.

## When to Use

Use AudioRecorder when you need: Audio recording component with waveform animation and timer. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { AudioRecorder } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <AudioRecorder />
}`} lang="tsx" />

## API / Props

### AudioRecorder

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
