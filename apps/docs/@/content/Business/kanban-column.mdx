import { KanbanColumn } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# KanbanColumn Kanban Column

> 业务组件 / Domain-specific business components for ERP scenarios.

Kanban Column — 面向 ERP/企业场景的业务组件。
KanbanColumn — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Kanban Column相关场景。

Use when you need a kanban column.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { KanbanColumn } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <KanbanColumn />
}`} lang="tsx" />

## API / Props

### KanbanColumn

| Prop          | Type                   | Default    | 说明 / Description                                        |
| ------------- | ---------------------- | ---------- | --------------------------------------------------------- |
| `className`   | `string`               | —          | 自定义样式类名 <br/> Custom CSS class                     |
| `title`       | `string`               | `未命名列` | 标题 <br/> Title                                          |
| `accent`      | `string`               | `primary`  | 列强调色                                                  |
| `cards`       | `Array<{`              | —          | 卡片列表                                                  |
| `id`          | `string`               | —          | DOM 元素 id <br/> DOM element id                          |
| `label`       | `string`               | —          | 标签文本 <br/> Label text                                 |
| `assignee`    | `string`               | —          |                                                           |
| `onAddCard`   | `() => void`           | —          | 新增卡片回调                                              |
| `onCardClick` | `() => void`           | —          | 卡片点击回调                                              |
| ...rest       | `React.HTMLAttributes` | —          | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-kanbancolumn--docs)。
