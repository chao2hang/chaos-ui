import { CrudToolbar } from "@/components/business/crud-toolbar";
import { CodeBlock } from "@/components/code-block";

# CrudToolbar Business Crud Toolbar

> 业务组件 / Domain-specific business components for ERP scenarios.

Business Crud Toolbar — 面向 ERP/企业场景的业务组件。
CrudToolbar — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Business Crud Toolbar相关场景。

Use when you need a crud toolbar.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { CrudToolbar } from "@/components/business/crud-toolbar"

export function Basic() {
  return <CrudToolbar />
}`} lang="tsx" />

## API / Props

### CrudToolbar

| Prop                | Type                          | Default   | 说明 / Description                                          |
| ------------------- | ----------------------------- | --------- | ----------------------------------------------------------- |
| `className`         | `string`                      | —         | 自定义样式类名 <br/> Custom CSS class                       |
| `onAdd`             | `() => void`                  | —         |                                                             |
| `onDelete`          | `() => void`                  | —         |                                                             |
| `onExport`          | `() => void`                  | —         |                                                             |
| `onImport`          | `() => void`                  | —         |                                                             |
| `onRefresh`         | `() => void`                  | —         |                                                             |
| `onSearch`          | `() => void`                  | —         |                                                             |
| `searchPlaceholder` | `string`                      | `搜索...` |                                                             |
| `selectedCount`     | `number`                      | `0`       |                                                             |
| ...rest             | `React.ComponentProps<"div">` | —         | 透传 `<div>` 原生属性 <br/> Inherits all `<div>` attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-business-crud-toolbar--docs)。
