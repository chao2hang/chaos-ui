import { BulkActionsToolbar } from "@/components/business/bulk-actions-toolbar";
import { CodeBlock } from "@/components/code-block";

# BulkActionsToolbar 批量操作工具栏

> 业务组件

批量操作工具栏 — 面向 ERP/企业场景的业务组件。

## 何时使用

需要批量操作工具栏功能时使用本组件。

## 代码示例

### 基础用法

<CodeBlock code={`import { BulkActionsToolbar } from "@/components/business/bulk-actions-toolbar"

export function Basic() {
  return <BulkActionsToolbar />
}`} lang="tsx" />

## API / Props

### BulkActionsToolbar

| Prop            | Type                          | Default  | 说明                  |
| --------------- | ----------------------------- | -------- | --------------------- |
| `className`     | `string`                      | —        | 自定义样式类名        |
| `count`         | `number`                      | —        |                       |
| `selectedCount` | `number`                      | —        |                       |
| `onClear`       | `() => void`                  | —        |                       |
| `actions`       | `BulkAction[]`                | —        |                       |
| `label`         | `string`                      | `已选择` | 标签文本              |
| ...rest         | `React.ComponentProps<"div">` | —        | 透传 `<div>` 原生属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-bulk-actions-toolbar--docs)。
