import { KanbanBoard } from "@/components/business/kanban-board";
import { CodeBlock } from "@/components/code-block";

# KanbanBoard

> Domain-specific business components for ERP scenarios.

KanbanBoard — a business component for ERP / enterprise scenarios.

## When to Use

Kanban view showing tasks/tickets as cards across columns.

## Examples

### Basic Usage

<CodeBlock code={`import { KanbanBoard } from "@/components/business/kanban-board"

export function Basic() {
  return <KanbanBoard />
}`} lang="tsx" />

## API / Props

### KanbanBoard

| Prop              | Type                   | Default | Description                       |
| ----------------- | ---------------------- | ------- | --------------------------------- |
| `className`       | `string`               | —       | Custom CSS class                  |
| `columns`         | `KanbanColumnData[]`   | —       | Column configuration              |
| `onColumnsChange` | `() => void`           | —       |                                   |
| `renderCard`      | `(...args) => unknown` | —       |                                   |
| ...rest           | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
