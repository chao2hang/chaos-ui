import { InventorySnapshot } from "@/components/business/inventory-snapshot";
import { CodeBlock } from "@/components/code-block";

# InventorySnapshot Business Inventory Snapshot

> 业务组件 / Domain-specific business components for ERP scenarios.

<InventorySnapshot
  items={[
    { id: "s1", name: "原料A", qty: 120, status: "normal" },
    { id: "s2", name: "原料B", qty: 5, status: "low" },
  ]}
/>
InventorySnapshot — business inventory snapshot component.

## 何时使用 / When to Use

适用于Business Inventory Snapshot相关场景。

Use when you need an inventory snapshot.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { InventorySnapshot } from "@/components/business/inventory-snapshot"

export function Basic() {
  return <InventorySnapshot />
}`} lang="tsx" />

## API / Props

### InventorySnapshot

| Prop        | Type                         | Default | 说明 / Description                                        |
| ----------- | ---------------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`                     | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `items`     | `Array<{`                    | —       | 选项/数据项列表 <br/> Items / data list                   |
| `id`        | `string`                     | —       | DOM 元素 id <br/> DOM element id                          |
| `name`      | `string`                     | —       | 表单字段名 <br/> Form field name                          |
| `qty`       | `number`                     | —       |                                                           |
| `status`    | `"normal" \| "low" \| "out"` | —       | 状态 <br/> Status                                         |
| ...rest     | `React.HTMLAttributes`       | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-business-inventory-snapshot--docs)。
