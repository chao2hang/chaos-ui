import { CronEditor } from "@/components/business/cron-editor"
import { CodeBlock } from "@/components/code-block"

# CronEditor Cron Editor

> Domain-specific business components for ERP scenarios.

CronEditor — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a cron editor.


## Examples

### Basic Usage

<CodeBlock code={`import { CronEditor } from "@/components/business/cron-editor"

export function Basic() {
  return <CronEditor />
}`} lang="tsx" />


## API / Props

### CronEditor

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `value` | `string` | `* * * * *` | 5 段 Cron 表达式 |
| `onChange` | `() => void` | — | 表达式变化回调 |
| `showDescription` | `boolean` | `true` | 是否显示人类可读描述 |
| `readOnly` | `boolean` | `false` | 只读模式 |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


