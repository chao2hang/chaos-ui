import { ShiftCalendar } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# ShiftCalendar

> Domain-specific business components for ERP scenarios.

ShiftCalendar — a business component for ERP / enterprise scenarios.

## When to Use

Use ShiftCalendar when you need: ShiftCalendar: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { ShiftCalendar } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <ShiftCalendar />
}`} lang="tsx" />

## API / Props

### ShiftCalendar

| Prop            | Type                          | Default | Description                     |
| --------------- | ----------------------------- | ------- | ------------------------------- |
| `className`     | `string`                      | —       | Custom CSS class                |
| `shifts`        | `Shift[]`                     | —       |                                 |
| `year`          | `number`                      | —       |                                 |
| `month`         | `number`                      | —       |                                 |
| `onMonthChange` | `() => void`                  | —       |                                 |
| ...rest         | `React.ComponentProps<"div">` | —       | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
