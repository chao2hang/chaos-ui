import { DonutChart } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# DonutChart Donut Chart

> Domain-specific business components for ERP scenarios.

DonutChart — donut chart component.

## When to Use

Use DonutChart when this capability is required. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { DonutChart } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <DonutChart />
}`} lang="tsx" />

## API / Props

### DonutChart

| Prop          | Type                   | Default | Description                       |
| ------------- | ---------------------- | ------- | --------------------------------- |
| `className`   | `string`               | —       | Custom CSS class                  |
| `data`        | `Array<T>`             | —       | Segments to render.               |
| `size`        | `number`               | `128`   | Donut pixel size.                 |
| `thickness`   | `number`               | `12`    | Stroke thickness.                 |
| `centerLabel` | `string`               | —       | Center hole label.                |
| ...rest       | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
