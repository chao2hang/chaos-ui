import { SegmentedControl } from "@/components/business/segmented-control";
import { CodeBlock } from "@/components/code-block";

# SegmentedControl

> Domain-specific business components for ERP scenarios.

SegmentedControl — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a segmented control.

## Examples

### Basic Usage

<CodeBlock code={`import { SegmentedControl } from "@/components/business/segmented-control"

export function Basic() {
  return <SegmentedControl />
}`} lang="tsx" />

### States

<CodeBlock
  code={`<SegmentedControl>Normal</SegmentedControl>
<SegmentedControl disabled>Disabled</SegmentedControl>`}
  lang="tsx"
/>

## API / Props

### SegmentedControl

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
