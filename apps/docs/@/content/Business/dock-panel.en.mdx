import { CodeBlock } from "@/components/code-block";

# DockPanel Dock Panel

> Domain-specific business components for ERP scenarios.

DockPanel — dock panel component.

## When to Use

Use DockPanel when this capability is required. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { DockPanel } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <DockPanel />
}`} lang="tsx" />

## API / Props

### DockPanel

| Prop        | Type                            | Default | Description                       |
| ----------- | ------------------------------- | ------- | --------------------------------- |
| `className` | `string`                        | —       | Custom CSS class                  |
| `side`      | `"left" \| "right" \| "bottom"` | —       |                                   |
| `collapsed` | `boolean`                       | `false` |                                   |
| `onToggle`  | `() => void`                    | —       |                                   |
| ...rest     | `React.HTMLAttributes`          | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
