import { TimelineView } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# TimelineView Timeline View

> 业务组件 / Domain-specific business components for ERP scenarios.

<TimelineView events={[{ id: "e1", date: "2026-06-30", title: "创建账单" }]} />
TimelineView — timeline view component.

## 何时使用 / When to Use

适用于Timeline View相关场景。

Use when you need a timeline view.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { TimelineView } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <TimelineView />
}`} lang="tsx" />

## API / Props

### TimelineView

| Prop          | Type                   | Default | 说明 / Description                                        |
| ------------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className`   | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `events`      | `Array<{`              | —       |                                                           |
| `id`          | `string`               | —       | DOM 元素 id <br/> DOM element id                          |
| `date`        | `string`               | —       |                                                           |
| `title`       | `string`               | —       | 标题 <br/> Title                                          |
| `description` | `string`               | —       | 描述文本 <br/> Description text                           |
| ...rest       | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-timelineview--docs)。
