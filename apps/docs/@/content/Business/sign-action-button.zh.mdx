import { SignActionButton } from "@/components/business/sign-action-button";
import { CodeBlock } from "@/components/code-block";

# SignActionButton Sign Action Button

> 业务组件

Sign Action Button — 面向 ERP/企业场景的业务组件。

## 何时使用

适用于Sign Action Button相关场景。

## 代码示例

### 基础用法

<CodeBlock code={`import { SignActionButton } from "@/components/business/sign-action-button"

export function Basic() {
  return <SignActionButton />
}`} lang="tsx" />

## API / Props

### SignActionButton

| Prop        | Type                   | Default | 说明              |
| ----------- | ---------------------- | ------- | ----------------- |
| `className` | `string`               | —       | 自定义样式类名    |
| `onSign`    | `() => void`           | —       |                   |
| `signed`    | `boolean`              | `false` |                   |
| `label`     | `string`               | —       | 标签文本          |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-signactionbutton--docs)。
