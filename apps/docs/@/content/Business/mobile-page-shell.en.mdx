import { CodeBlock } from "@/components/code-block";

# MobilePageShell Mobile Page Shell

> Domain-specific business components for ERP scenarios.

MobilePageShell — mobile page shell component.

## When to Use

Use MobilePageShell when this capability is required. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { MobilePageShell } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <MobilePageShell />
}`} lang="tsx" />

## API / Props

### MobilePageShell

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| `title`     | `string`               | —       | Title                             |
| `onBack`    | `() => void`           | —       |                                   |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
