import { FeatureTour } from "@/components/business/feature-tour";
import { CodeBlock } from "@/components/code-block";

# FeatureTour Business Feature Tour

> 业务组件 / Domain-specific business components for ERP scenarios.

<FeatureTour
  open
  steps={[{ target: "#btn", title: "开始", content: "点击此处开始" }]}
  onClose={() => {}}
/>
FeatureTour — business feature tour component.

## 何时使用 / When to Use

适用于Business Feature Tour相关场景。

Use when you need a feature tour.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { FeatureTour } from "@/components/business/feature-tour"

export function Basic() {
  return <FeatureTour />
}`} lang="tsx" />

## API / Props

### FeatureTour

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `steps`     | `Array<T>`             | —       |                                                           |
| `open`      | `boolean`              | —       | 是否打开(受控) <br/> Open state (controlled)              |
| `onClose`   | `() => void`           | —       | 关闭回调 <br/> Close handler                              |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-business-feature-tour--docs)。
