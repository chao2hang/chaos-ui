import { HeatmapChart } from "@/components/business/heatmap-chart";
import { CodeBlock } from "@/components/code-block";

# HeatmapChart Heatmap Chart

> 业务组件

<HeatmapChart data={[{ x: "周一", y: "上午", value: 8 }]} />

## 何时使用

适用于Heatmap Chart相关场景。

## 代码示例

### 基础用法

<CodeBlock code={`import { HeatmapChart } from "@/components/business/heatmap-chart"

export function Basic() {
  return <HeatmapChart />
}`} lang="tsx" />

## API / Props

### HeatmapChart

| Prop        | Type                   | Default | 说明              |
| ----------- | ---------------------- | ------- | ----------------- |
| `className` | `string`               | —       | 自定义样式类名    |
| `data`      | `Array<T>`             | —       | 数据源            |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-heatmapchart--docs)。
