import { ChatMessageInput } from "@/components/business/chat-message-input";
import { CodeBlock } from "@/components/code-block";

# ChatMessageInput Business Chat Message Input

> 业务组件 / Domain-specific business components for ERP scenarios.

Business Chat Message Input — 面向 ERP/企业场景的业务组件。
ChatMessageInput — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Business Chat Message Input相关场景。

Use when you need a chat message input.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { ChatMessageInput } from "@/components/business/chat-message-input"

export function Basic() {
  return <ChatMessageInput />
}`} lang="tsx" />

## API / Props

### ChatMessageInput

| Prop          | Type                   | Default             | 说明 / Description                                        |
| ------------- | ---------------------- | ------------------- | --------------------------------------------------------- |
| `className`   | `string`               | —                   | 自定义样式类名 <br/> Custom CSS class                     |
| `value`       | `string`               | —                   | 当前值(受控) <br/> Current value (controlled)             |
| `onChange`    | `() => void`           | —                   | 值变化回调 <br/> Callback when value changes              |
| `onSend`      | `() => void`           | —                   |                                                           |
| `placeholder` | `string`               | `Type a message...` | 占位文本 <br/> Placeholder text                           |
| `disabled`    | `boolean`              | `false`             | 是否禁用 <br/> Whether disabled                           |
| `maxLength`   | `number`               | —                   | 最大输入长度 <br/> Maximum input length                   |
| ...rest       | `React.HTMLAttributes` | —                   | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-business-chat-message-input--docs)。
