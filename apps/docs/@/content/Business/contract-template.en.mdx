import { ContractTemplate } from "@/components/business/contract-template"
import { CodeBlock } from "@/components/code-block"

# ContractTemplate

> Domain-specific business components for ERP scenarios.

ContractTemplate — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a contract template.


## Examples

### Basic Usage

<CodeBlock code={`import { ContractTemplate } from "@/components/business/contract-template"

export function Basic() {
  return <ContractTemplate />
}`} lang="tsx" />


## API / Props

### ContractTemplate

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `title` | `string` | `合同模板` | Title |
| `fields` | `ContractField[]` | — |  |
| `onChange` | `() => void` | — | Callback when value changes |
| `onPreview` | `() => void` | — |  |
| `onDownload` | `() => void` | — |  |
| ...rest | `React.ComponentProps<"div">` | — | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


