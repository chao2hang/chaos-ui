import { SparkChart } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# SparkChart Spark Chart

> Domain-specific business components for ERP scenarios.

SparkChart — spark chart component.

## When to Use

Use SparkChart when this capability is required. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { SparkChart } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <SparkChart />
}`} lang="tsx" />

## API / Props

### SparkChart

| Prop        | Type                   | Default        | Description                       |
| ----------- | ---------------------- | -------------- | --------------------------------- |
| `className` | `string`               | —              | Custom CSS class                  |
| `data`      | `number[]`             | —              | Numeric series.                   |
| `width`     | `number`               | `120`          | SVG width in px.                  |
| `height`    | `number`               | `32`           | SVG height in px.                 |
| `color`     | `string`               | `currentColor` | Line color.                       |
| ...rest     | `React.HTMLAttributes` | —              | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
