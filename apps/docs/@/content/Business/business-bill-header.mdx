import { BillHeader } from "@/components/business/bill-header";
import { CodeBlock } from "@/components/code-block";

# BillHeader Business Bill Header

> 业务组件 / Domain-specific business components for ERP scenarios.

Business Bill Header — 面向 ERP/企业场景的业务组件。
BillHeader — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Business Bill Header相关场景。

Use when you need a bill header.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { BillHeader } from "@/components/business/bill-header"

export function Basic() {
  return <BillHeader />
}`} lang="tsx" />

## API / Props

### BillHeader

| Prop              | Type                          | Default    | 说明 / Description                                          |
| ----------------- | ----------------------------- | ---------- | ----------------------------------------------------------- |
| `className`       | `string`                      | —          | 自定义样式类名 <br/> Custom CSS class                       |
| `companyName`     | `string`                      | `公司名称` |                                                             |
| `companyAddress`  | `string`                      | —          |                                                             |
| `companyPhone`    | `string`                      | —          |                                                             |
| `invoiceNumber`   | `string`                      | —          |                                                             |
| `invoiceDate`     | `string`                      | —          |                                                             |
| `dueDate`         | `string`                      | —          |                                                             |
| `customerName`    | `string`                      | —          |                                                             |
| `customerAddress` | `string`                      | —          |                                                             |
| ...rest           | `React.ComponentProps<"div">` | —          | 透传 `<div>` 原生属性 <br/> Inherits all `<div>` attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-business-bill-header--docs)。
