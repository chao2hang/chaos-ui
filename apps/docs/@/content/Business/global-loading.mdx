import { GlobalLoading } from "@/components/business/global-loading";
import { CodeBlock } from "@/components/code-block";

# GlobalLoading 全局加载

> 业务组件 / Domain-specific business components for ERP scenarios.

全局加载 — 面向 ERP/企业场景的业务组件。
GlobalLoading — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

需要全局加载功能时使用本组件。

Use when you need a global loading.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { GlobalLoading } from "@/components/business/global-loading"

export function Basic() {
  return <GlobalLoading />
}`} lang="tsx" />

## API / Props

### GlobalLoading

| Prop         | Type                   | Default     | 说明 / Description                                        |
| ------------ | ---------------------- | ----------- | --------------------------------------------------------- |
| `className`  | `string`               | —           | 自定义样式类名 <br/> Custom CSS class                     |
| `loading`    | `boolean`              | `true`      | 是否加载中 <br/> Loading state                            |
| `text`       | `string`               | `加载中...` |                                                           |
| `fullscreen` | `boolean`              | `true`      |                                                           |
| ...rest      | `React.HTMLAttributes` | —           | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-global-loading--docs)。
