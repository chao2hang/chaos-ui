import { InvoicePreview } from "@/components/business/invoice-preview";
import { CodeBlock } from "@/components/code-block";

# InvoicePreview

> Domain-specific business components for ERP scenarios.

InvoicePreview — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need an invoice preview.


## Examples

### Basic Usage

<CodeBlock code={`import { InvoicePreview } from "@/components/business/invoice-preview"

export function Basic() {
  return <InvoicePreview />
}`} lang="tsx" />

## API / Props

### InvoicePreview

| Prop | Type | Default | Description |
| ----------- | ----------------------------- | ------- | ----------------------------------------------------------- |
| `className` | `string` | — | Custom CSS class |
| `invoice`   | `InvoiceData`                 | —       |                                                             |
| ...rest | `React.ComponentProps<"div">` | — | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


