import { SalesOrderBrowse } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# SalesOrderBrowse Sales Order Browse

> Domain-specific business components for ERP scenarios.

SalesOrderBrowse — a business component for ERP / enterprise scenarios.

## When to Use

Use SalesOrderBrowse when you need: SalesOrderBrowse: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { SalesOrderBrowse } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <SalesOrderBrowse />
}`} lang="tsx" />

## API / Props

### SalesOrderBrowse

| Prop                | Type                     | Default | Description                       |
| ------------------- | ------------------------ | ------- | --------------------------------- |
| `className`         | `string`                 | —       | Custom CSS class                  |
| `open`              | `boolean`                | —       | Open state (controlled)           |
| `onOpenChange`      | `() => void`             | —       | Callback when open state changes  |
| `onSelect`          | `() => void`             | —       | Selection handler                 |
| `items`             | `SalesOrderBrowseItem[]` | —       | Items / data list                 |
| `title`             | `string`                 | —       | Title                             |
| `searchPlaceholder` | `string`                 | —       |                                   |
| `emptyText`         | `string`                 | —       |                                   |
| ...rest             | `React.HTMLAttributes`   | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
