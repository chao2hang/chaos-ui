import { DonutCard } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# DonutCard Donut Card

> 业务组件 / Domain-specific business components for ERP scenarios.

<DonutCard
  data={[{ label: "A", value: 60, color: "#3b82f6" }]}
  centerLabel="总计"
/>
DonutCard — donut card component.

## 何时使用 / When to Use

需要Donut Card时使用本组件。用于 ERP/业务域场景（单据、选择器、审批、对账等）；按业务语义接入而不是当纯 UI 积木。

Use DonutCard when this capability is required. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { DonutCard } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <DonutCard />
}`} lang="tsx" />

## API / Props

### DonutCard

| Prop          | Type                   | Default | 说明 / Description                                        |
| ------------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className`   | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `data`        | `Array<T>`             | —       | 数据源 <br/> Data source                                  |
| `centerLabel` | `string`               | —       |                                                           |
| ...rest       | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **无障碍**: 组件遵循 WAI-ARIA 最佳实践。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](http://localhost:6006/?path=/docs/business-donutcard--docs)。
