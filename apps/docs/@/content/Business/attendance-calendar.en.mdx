import { AttendanceCalendar } from "@/components/business/attendance-calendar"
import { CodeBlock } from "@/components/code-block"

# AttendanceCalendar Attendance Calendar

> Domain-specific business components for ERP scenarios.

AttendanceCalendar — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need an attendance calendar.


## Examples

### Basic Usage

<CodeBlock code={`import { AttendanceCalendar } from "@/components/business/attendance-calendar"

export function Basic() {
  return <AttendanceCalendar />
}`} lang="tsx" />


## API / Props

### AttendanceCalendar

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `year` | `number` | — |  |
| `month` | `number` | — |  |
| `records` | `AttendanceRecord[]` | — |  |
| `onMonthChange` | `() => void` | — |  |
| `onDayClick` | `() => void` | — |  |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


