import { ChatVoiceMessage } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# ChatVoiceMessage Chat Voice Message

> Domain-specific business components for ERP scenarios.

ChatVoiceMessage — a business component for ERP / enterprise scenarios.

## When to Use

Use ChatVoiceMessage when you need: ChatVoiceMessage: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { ChatVoiceMessage } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <ChatVoiceMessage />
}`} lang="tsx" />

## API / Props

### ChatVoiceMessage

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| `duration`  | `number`               | —       | Duration in milliseconds          |
| `src`       | `string`               | —       | Source URL                        |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
