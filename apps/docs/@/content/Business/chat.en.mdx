import { ChatMessageList, ChatComposer } from "@/components/business/chat";
import { CodeBlock } from "@/components/code-block";

# Chat

> Domain-specific business components for ERP scenarios.

Chat — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a chat.


## Examples

### Basic Usage

<CodeBlock code={`import { ChatMessageList, ChatComposer } from "@/components/business/chat"

export function Basic() {
  return (
    <div className="space-y-4">
  <ChatMessageList />
  <ChatComposer />
    </div>
  )
}`} lang="tsx" />

## API / Props

### ChatMessageList

| Prop | Type | Default | Description |
| --------------- | ----------------------------- | ------- | ----------------------------------------------------------- |
| `className` | `string` | — | Custom CSS class |
| `messages`      | `ChatMessage[]`               | —       |                                                             |
| `currentUserId` | `string`                      | —       |                                                             |
| ...rest | `React.ComponentProps<"div">` | — | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


