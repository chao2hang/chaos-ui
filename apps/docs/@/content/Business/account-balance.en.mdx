import { AccountBalance } from "@chaos_team/chaos-ui/business"
import { CodeBlock } from "@/components/code-block"

# AccountBalance Account Balance

> Domain-specific business components for ERP scenarios.

AccountBalance — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need an account balance.


## Examples

### Basic Usage

<CodeBlock code={`import { AccountBalance } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <AccountBalance />
}`} lang="tsx" />


## API / Props

### AccountBalance

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `balance` | `number` | — |  |
| `currency` | `string` | `CNY` |  |
| `label` | `string` | `账户余额` | Label text |
| `trend` | `number` | — |  |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


