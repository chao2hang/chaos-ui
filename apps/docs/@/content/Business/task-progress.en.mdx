import { TaskProgress } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# TaskProgress Task Progress

> Domain-specific business components for ERP scenarios.

<TaskProgress percent={75} status="running" message="正在生成报表…" />
TaskProgress — task progress component.

## When to Use

Use TaskProgress when this capability is required. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { TaskProgress } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <TaskProgress />
}`} lang="tsx" />

## API / Props

### TaskProgress

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| `percent`   | `number`               | —       |                                   |
| `status`    | `string`               | —       | Status                            |
| `message`   | `string`               | —       | Message content                   |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
