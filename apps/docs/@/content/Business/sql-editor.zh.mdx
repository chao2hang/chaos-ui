import { SqlEditor } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# SqlEditor Sql Editor

> 业务组件

Sql Editor — 面向 ERP/企业场景的业务组件。

## 何时使用

需要采集或编辑业务字段，并进入校验/提交链路时使用。（组件：Sql Editor）

## 代码示例

### 基础用法

<CodeBlock code={`import { SqlEditor } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <SqlEditor />
}`} lang="tsx" />

## API / Props

### SqlEditor

| Prop          | Type                   | Default    | 说明                                   |
| ------------- | ---------------------- | ---------- | -------------------------------------- |
| `className`   | `string`               | —          | 自定义样式类名                         |
| `value`       | `string`               | —          | SQL content (controlled).              |
| `onChange`    | `() => void`           | —          | Called on every change.                |
| `dialect`     | `SQLDialect`           | `standard` | SQL dialect. Defaults to `"standard"`. |
| `placeholder` | `string`               | —          | Placeholder text.                      |
| `readOnly`    | `boolean`              | —          | Whether the editor is read-only.       |
| `minHeight`   | `number`               | `200`      | Minimum height.                        |
| `maxHeight`   | `number`               | —          | Maximum height.                        |
| `onFocus`     | `() => void`           | —          | Called when the editor receives focus. |
| `onBlur`      | `() => void`           | —          | Called when the editor loses focus.    |
| ...rest       | `React.HTMLAttributes` | —          | 透传原生 DOM 属性                      |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](http://localhost:6006/?path=/docs/business-sqleditor--docs)。
