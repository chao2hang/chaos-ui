import { ProductCategoryPicker } from "@/components/business/product-category-picker";
import { CodeBlock } from "@/components/code-block";

# ProductCategoryPicker Product Category Picker

> 业务组件

Product Category Picker — 面向 ERP/企业场景的业务组件。

## 何时使用

适用于Product Category Picker相关场景。

## 代码示例

### 基础用法

<CodeBlock code={`import { ProductCategoryPicker } from "@/components/business/product-category-picker"

export function Basic() {
  return <ProductCategoryPicker />
}`} lang="tsx" />

## API / Props

### ProductCategoryPicker

| Prop                | Type                            | Default | 说明              |
| ------------------- | ------------------------------- | ------- | ----------------- |
| `className`         | `string`                        | —       | 自定义样式类名    |
| `value`             | `string`                        | —       | 当前值(受控)      |
| `onChange`          | `() => void`                    | —       | 值变化回调        |
| `options`           | `ProductCategoryPickerOption[]` | —       | 可选项列表        |
| `placeholder`       | `string`                        | —       | 占位文本          |
| `searchPlaceholder` | `string`                        | —       |                   |
| `emptyText`         | `string`                        | —       |                   |
| `disabled`          | `boolean`                       | `false` | 是否禁用          |
| `clearable`         | `boolean`                       | `true`  |                   |
| ...rest             | `React.HTMLAttributes`          | —       | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-productcategorypicker--docs)。
