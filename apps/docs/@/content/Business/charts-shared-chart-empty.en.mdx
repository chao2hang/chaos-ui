import { CodeBlock } from "@/components/code-block";

# ChartEmpty

> Domain-specific business components for ERP scenarios.

ChartEmpty — a business component for ERP / enterprise scenarios.

## When to Use

Use ChartsSharedChartEmpty when this capability is required. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock
  code={`export function Basic() {
  return <ChartEmpty />
}`}
  lang="tsx"
/>

## API / Props

### ChartEmpty

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| `height`    | `number`               | `320`   | Component height                  |
| `message`   | `string`               | —       | Message content                   |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
