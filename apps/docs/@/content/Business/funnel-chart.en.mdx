import { FunnelChart } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# FunnelChart Funnel Chart

> Domain-specific business components for ERP scenarios.

FunnelChart — funnel chart component.

## When to Use

Use FunnelChart when this capability is required. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { FunnelChart } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <FunnelChart />
}`} lang="tsx" />

## API / Props

### FunnelChart

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| `data`      | `Array<T>`             | —       | Data source                       |
| `height`    | `number`               | `44`    | Component height                  |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
