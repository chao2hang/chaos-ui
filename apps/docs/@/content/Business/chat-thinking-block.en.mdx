import { ChatThinkingBlock } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# ChatThinkingBlock Chat Thinking Block

> Domain-specific business components for ERP scenarios.

ChatThinkingBlock — a business component for ERP / enterprise scenarios.

## When to Use

Use ChatThinkingBlock when you need: ChatThinkingBlock: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { ChatThinkingBlock } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <ChatThinkingBlock />
}`} lang="tsx" />

## API / Props

### ChatThinkingBlock

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| `content`   | `string`               | —       | Content                           |
| `duration`  | `number`               | —       | Duration in milliseconds          |
| `collapsed` | `boolean`              | `false` |                                   |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
