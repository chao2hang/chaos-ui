import { UserMenu } from "@chaos_team/chaos-ui/business"
import { CodeBlock } from "@/components/code-block"

# UserMenu 用户菜单

> 业务组件

用户菜单 — 面向 ERP/企业场景的业务组件。

## 何时使用

需要用户菜单功能时使用本组件。


## 代码示例

### 基础用法

<CodeBlock code={`import { UserMenu } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <UserMenu />
}`} lang="tsx" />


## API / Props

### UserMenu

| Prop | Type | Default | 说明 |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 |
| `user` | `UserMenuUser` | — |  |
| `actions` | `UserMenuAction[]` | — |  |
| `showProfile` | `boolean` | `true` |  |
| `showSettings` | `boolean` | `true` |  |
| `onProfile` | `() => void` | — |  |
| `onSettings` | `() => void` | — |  |
| `onSignOut` | `() => void` | — |  |
| `align` | `"start" \| "center" \| "end"` | `end` | 对齐方式 |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-usermenu--docs)。

