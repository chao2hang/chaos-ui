import { RoleAssignment } from "@/components/business/role-assignment";
import { CodeBlock } from "@/components/code-block";

# RoleAssignment Business Role Assignment

> 业务组件 / Domain-specific business components for ERP scenarios.

Business Role Assignment — 面向 ERP/企业场景的业务组件。
RoleAssignment — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Business Role Assignment相关场景。

Use when you need a role assignment.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { RoleAssignment } from "@/components/business/role-assignment"

export function Basic() {
  return <RoleAssignment />
}`} lang="tsx" />

## API / Props

### RoleAssignment

| Prop        | Type                          | Default | 说明 / Description                                          |
| ----------- | ----------------------------- | ------- | ----------------------------------------------------------- |
| `className` | `string`                      | —       | 自定义样式类名 <br/> Custom CSS class                       |
| `roles`     | `Role[]`                      | —       |                                                             |
| `assigned`  | `string[]`                    | —       |                                                             |
| `onChange`  | `() => void`                  | —       | 值变化回调 <br/> Callback when value changes                |
| ...rest     | `React.ComponentProps<"div">` | —       | 透传 `<div>` 原生属性 <br/> Inherits all `<div>` attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-business-role-assignment--docs)。
