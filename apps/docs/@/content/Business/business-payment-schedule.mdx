import { PaymentSchedule } from "@/components/business/payment-schedule";
import { CodeBlock } from "@/components/code-block";

# PaymentSchedule Business Payment Schedule

> 业务组件 / Domain-specific business components for ERP scenarios.

Business Payment Schedule — 面向 ERP/企业场景的业务组件。
PaymentSchedule — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Business Payment Schedule相关场景。

Use when you need a payment schedule.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { PaymentSchedule } from "@/components/business/payment-schedule"

export function Basic() {
  return <PaymentSchedule />
}`} lang="tsx" />

## API / Props

### PaymentSchedule

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `schedule`  | `Array<T>`             | —       |                                                           |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-business-payment-schedule--docs)。
