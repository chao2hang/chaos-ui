import { TreemapChart } from "@/components/business/charts/treemap-chart";
import { CodeBlock } from "@/components/code-block";

# TreemapChart

> Domain-specific business components for ERP scenarios.

TreemapChart — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a treemap chart.

## Examples

### Basic Usage

<CodeBlock code={`import { TreemapChart } from "@/components/business/charts/treemap-chart"

export function Basic() {
  return <TreemapChart />
}`} lang="tsx" />

## API / Props

### TreemapChart

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
