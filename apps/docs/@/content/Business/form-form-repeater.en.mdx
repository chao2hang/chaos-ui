import { FormRepeater } from "@/components/business/form/form-repeater";
import { CodeBlock } from "@/components/code-block";

# FormRepeater

> Domain-specific business components for ERP scenarios.

FormRepeater — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a form repeater.

## Examples

### Basic Usage

<CodeBlock code={`import { FormRepeater } from "@/components/business/form/form-repeater"

export function Basic() {
  return <FormRepeater />
}`} lang="tsx" />

## API / Props

### FormRepeater

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
