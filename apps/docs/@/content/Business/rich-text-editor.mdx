import { RichTextEditor } from "@/components/business/rich-text-editor"
import { CodeBlock } from "@/components/code-block"

# RichTextEditor 富文本编辑器

> 业务组件 / Domain-specific business components for ERP scenarios.

富文本编辑器 — 面向 ERP/企业场景的业务组件。
RichTextEditor — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

提供带格式的富文本编辑能力(加粗、列表、链接等)。

Provide formatted rich-text editing (bold, lists, links, etc.).

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { RichTextEditor } from "@/components/business/rich-text-editor"

export function Basic() {
  return <RichTextEditor />
}`} lang="tsx" />


## API / Props

### RichTextEditor

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| `value` | `string` | `` | 当前值(受控) <br/> Current value (controlled) |
| `onChange` | `() => void` | — | 值变化回调 <br/> Callback when value changes |
| `placeholder` | `string` | `开始输入...` | 占位文本 <br/> Placeholder text |
| `editable` | `boolean` | `true` |  |
| `showToolbar` | `boolean` | `true` |  |
| `minHeight` | `number` | `200` |  |
| `onUpload` | `(...args) => unknown` | — |  |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-rich-text-editor--docs)。
