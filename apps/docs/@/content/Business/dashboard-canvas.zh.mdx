import { DashboardCanvas } from "@/components/business/dashboard-canvas"
import { CodeBlock } from "@/components/code-block"

# DashboardCanvas Dashboard Canvas

> 业务组件

<DashboardCanvas widgets={[{ id: "w1", title: "销售额", x: 0, y: 0, w: 6, h: 2 }]} />

## 何时使用

适用于Dashboard Canvas相关场景。


## 代码示例

### 基础用法

<CodeBlock code={`import { DashboardCanvas } from "@/components/business/dashboard-canvas"

export function Basic() {
  return <DashboardCanvas />
}`} lang="tsx" />


## API / Props

### DashboardCanvas

| Prop | Type | Default | 说明 |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 |
| `widgets` | `Array<DashboardWidget>` | — |  |
| `onChange` | `() => void` | — | 值变化回调 |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-dashboardcanvas--docs)。

