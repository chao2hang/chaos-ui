import { MobileListItem } from "@/components/business/mobile-list-item"
import { CodeBlock } from "@/components/code-block"

# MobileListItem Mobile List Item

> 业务组件

<MobileListItem title="张三" subtitle="销售部" onClick={() => open(id)} />

## 何时使用

适用于Mobile List Item相关场景。


## 代码示例

### 基础用法

<CodeBlock code={`import { MobileListItem } from "@/components/business/mobile-list-item"

export function Basic() {
  return <MobileListItem />
}`} lang="tsx" />


## API / Props

### MobileListItem

| Prop | Type | Default | 说明 |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 |
| `title` | `string` | — | 标题 |
| `subtitle` | `string` | — |  |
| `trailing` | `React.ReactNode` | — |  |
| `onClick` | `() => void` | — | 点击回调 |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-mobilelistitem--docs)。

