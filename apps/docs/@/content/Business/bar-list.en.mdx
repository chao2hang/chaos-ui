import { BarList } from "@/components/business/bar-list";
import { CodeBlock } from "@/components/code-block";

# BarList Bar List

> Domain-specific business components for ERP scenarios.

BarList — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a bar list.

## Examples

### Basic Usage

<CodeBlock code={`import { BarList } from "@/components/business/bar-list"

export function Basic() {
  return <BarList />
}`} lang="tsx" />

## API / Props

### BarList

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| `data`      | `Array<T>`             | —       | Data source                       |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
