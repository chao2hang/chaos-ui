import { NotificationCenter } from "@chaos_team/chaos-ui/business"
import { CodeBlock } from "@/components/code-block"

# NotificationCenter

> Domain-specific business components for ERP scenarios.

NotificationCenter — a business component for ERP / enterprise scenarios.

## When to Use

In-app notification center listing unread/read messages.


## Examples

### Basic Usage

<CodeBlock code={`import { NotificationCenter } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <NotificationCenter />
}`} lang="tsx" />


## API / Props

### NotificationCenter

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `notifications` | `NotificationItem[]` | — |  |
| `onMarkRead` | `() => void` | — |  |
| `onMarkAllRead` | `() => void` | — |  |
| `onClear` | `() => void` | — |  |
| `onItemClick` | `() => void` | — |  |
| `align` | `"start" \| "center" \| "end"` | `end` | Alignment |
| `emptyText` | `string` | `暂无通知` |  |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


