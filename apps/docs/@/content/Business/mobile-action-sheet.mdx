import { MobileActionSheet } from "@/components/business/mobile-action-sheet"
import { CodeBlock } from "@/components/code-block"

# MobileActionSheet Mobile Action Sheet

> 业务组件 / Domain-specific business components for ERP scenarios.

<MobileActionSheet open={open} onOpenChange={setOpen} actions={[{label:"编辑",onClick:edit},{label:"删除",onClick:del,danger:true}]} />
MobileActionSheet — mobile action sheet component.

## 何时使用 / When to Use

适用于Mobile Action Sheet相关场景。

Use when you need a mobile action sheet.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { MobileActionSheet } from "@/components/business/mobile-action-sheet"

export function Basic() {
  return <MobileActionSheet />
}`} lang="tsx" />


## API / Props

### MobileActionSheet

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| `open` | `boolean` | — | 是否打开(受控) <br/> Open state (controlled) |
| `onOpenChange` | `() => void` | — | 打开状态变化回调 <br/> Callback when open state changes |
| `actions` | `() => void` | — |  |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-mobileactionsheet--docs)。
