import { MobileTabBar } from "@/components/business/mobile-tab-bar";
import { CodeBlock } from "@/components/code-block";

# MobileTabBar Mobile Tab Bar

> Domain-specific business components for ERP scenarios.

<MobileTabBar
  tabs={[
    { id: "home", label: "首页" },
    { id: "me", label: "我的" },
  ]}
  activeId="home"
  onSelect={setTab}
/>
MobileTabBar — mobile tab bar component.

## When to Use

Use when you need a mobile tab bar.

## Examples

### Basic Usage

<CodeBlock code={`import { MobileTabBar } from "@/components/business/mobile-tab-bar"

export function Basic() {
  return <MobileTabBar />
}`} lang="tsx" />

## API / Props

### MobileTabBar

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| `tabs`      | `Array<T>`             | —       |                                   |
| `activeId`  | `string`               | —       |                                   |
| `onSelect`  | `() => void`           | —       | Selection handler                 |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
