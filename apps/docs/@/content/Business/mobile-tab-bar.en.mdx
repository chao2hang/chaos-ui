import { CodeBlock } from "@/components/code-block";

# MobileTabBar Mobile Tab Bar

> Domain-specific business components for ERP scenarios.

MobileTabBar — mobile tab bar component.

## When to Use

Use MobileTabBar when this capability is required. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { MobileTabBar } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <MobileTabBar />
}`} lang="tsx" />

## API / Props

### MobileTabBar

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| `tabs`      | `Array<T>`             | —       |                                   |
| `activeId`  | `string`               | —       |                                   |
| `onSelect`  | `() => void`           | —       | Selection handler                 |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
