import { IotSensorGrid } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# IotSensorGrid Iot Sensor Grid

> Domain-specific business components for ERP scenarios.

IotSensorGrid — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need an iot sensor grid.

## Examples

### Basic Usage

<CodeBlock code={`import { IotSensorGrid } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <IotSensorGrid />
}`} lang="tsx" />

## API / Props

### IotSensorGrid

| Prop              | Type                   | Default           | Description                                 |
| ----------------- | ---------------------- | ----------------- | ------------------------------------------- |
| `className`       | `string`               | —                 | Custom CSS class                            |
| `sensors`         | `Sensor[]`             | —                 | Sensors to display.                         |
| `title`           | `string`               | `IoT Sensor Grid` | Dashboard title.                            |
| `refreshInterval` | `number`               | `0`               | Auto-refresh interval in ms (0 = disabled). |
| `onSensorClick`   | `() => void`           | —                 | Sensor click handler.                       |
| ...rest           | `React.HTMLAttributes` | —                 | Inherits standard HTML attributes           |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
