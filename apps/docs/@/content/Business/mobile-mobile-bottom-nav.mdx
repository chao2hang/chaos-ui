import { MobileBottomNav } from "@/components/mobile/mobile-bottom-nav";
import { CodeBlock } from "@/components/code-block";

# MobileBottomNav Mobile Mobile Bottom Nav

> 业务组件 / Domain-specific business components for ERP scenarios.

Mobile Mobile Bottom Nav — 面向 ERP/企业场景的业务组件。
MobileBottomNav — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Mobile Mobile Bottom Nav相关场景。

Use when you need a mobile bottom nav.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { MobileBottomNav } from "@/components/mobile/mobile-bottom-nav"

export function Basic() {
  return <MobileBottomNav />
}`} lang="tsx" />

## API / Props

### MobileBottomNav

| Prop        | Type                      | Default   | 说明 / Description                                        |
| ----------- | ------------------------- | --------- | --------------------------------------------------------- |
| `className` | `string`                  | —         | 自定义样式类名 <br/> Custom CSS class                     |
| `items`     | `MobileNavItem[]`         | —         | 选项/数据项列表 <br/> Items / data list                   |
| `onChange`  | `() => void`              | —         | 值变化回调 <br/> Callback when value changes              |
| `variant`   | `"default" \| "floating"` | `default` | 样式变体 <br/> Style variant                              |
| ...rest     | `React.HTMLAttributes`    | —         | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-mobile-mobile-bottom-nav--docs)。
