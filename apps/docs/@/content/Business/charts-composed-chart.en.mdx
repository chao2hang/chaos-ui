import { CodeBlock } from "@/components/code-block";

# ComposedChart

> Domain-specific business components for ERP scenarios.

ComposedChart — a business component for ERP / enterprise scenarios.

## When to Use

Use ChartsComposedChart when this capability is required. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock
  code={`export function Basic() {
  return <ComposedChartComp />
}`}
  lang="tsx"
/>

## API / Props

### ComposedChartComp

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
