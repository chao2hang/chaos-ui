import { ComposedChartComp } from "@/components/business/charts/composed-chart";
import { CodeBlock } from "@/components/code-block";

# ComposedChart

> Domain-specific business components for ERP scenarios.

ComposedChart — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a composed chart.


## Examples

### Basic Usage

<CodeBlock code={`import { ComposedChartComp } from "@/components/business/charts/composed-chart"

export function Basic() {
  return <ComposedChartComp />
}`} lang="tsx" />

## API / Props

### ComposedChartComp

| Prop | Type | Default | Description |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string` | — | Custom CSS class |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


