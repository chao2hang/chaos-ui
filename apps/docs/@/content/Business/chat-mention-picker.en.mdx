import { ChatMentionPicker } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# ChatMentionPicker Chat Mention Picker

> Domain-specific business components for ERP scenarios.

ChatMentionPicker — a business component for ERP / enterprise scenarios.

## When to Use

Use ChatMentionPicker when you need: ChatMentionPicker: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { ChatMentionPicker } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <ChatMentionPicker />
}`} lang="tsx" />

## API / Props

### ChatMentionPicker

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| `users`     | `Array<T>`             | —       |                                   |
| `onSelect`  | `() => void`           | —       | Selection handler                 |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
