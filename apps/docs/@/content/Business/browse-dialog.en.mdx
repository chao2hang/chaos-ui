import { BrowseDialog } from "@/components/business/browse-dialog"
import { CodeBlock } from "@/components/code-block"

# BrowseDialog Browse Dialog

> Domain-specific business components for ERP scenarios.

BrowseDialog — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a browse dialog.


## Examples

### Basic Usage

<CodeBlock code={`import { BrowseDialog } from "@/components/business/browse-dialog"

export function Basic() {
  return <BrowseDialog />
}`} lang="tsx" />


## API / Props

### BrowseDialog

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `node` | `BrowseTreeNode` | — |  |
| `depth` | `number` | — |  |
| `expandedKeys` | `Set<string \| number>` | — |  |
| `activeCategoryId` | `string \| number \| undefined` | — |  |
| `onToggleExpand` | `() => void` | — |  |
| `onSelect` | `() => void` | — | Selection handler |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


