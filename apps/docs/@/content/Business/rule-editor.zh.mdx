import { RuleEditor } from "@/components/business/rule-editor"
import { CodeBlock } from "@/components/code-block"

# RuleEditor Rule Editor

> 业务组件

Rule Editor — 面向 ERP/企业场景的业务组件。

## 何时使用

适用于Rule Editor相关场景。


## 代码示例

### 基础用法

<CodeBlock code={`import { RuleEditor } from "@/components/business/rule-editor"

export function Basic() {
  return <RuleEditor />
}`} lang="tsx" />


## API / Props

### RuleEditor

| Prop | Type | Default | 说明 |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 |
| `rules` | `Array<RuleItem>` | — |  |
| `onChange` | `() => void` | — | 值变化回调 |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-ruleeditor--docs)。

