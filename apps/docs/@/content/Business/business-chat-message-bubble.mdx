import { ChatMessageBubble } from "@/components/business/chat-message-bubble";
import { CodeBlock } from "@/components/code-block";

# ChatMessageBubble Business Chat Message Bubble

> 业务组件 / Domain-specific business components for ERP scenarios.

Business Chat Message Bubble — 面向 ERP/企业场景的业务组件。
ChatMessageBubble — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Business Chat Message Bubble相关场景。

Use when you need a chat message bubble.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { ChatMessageBubble } from "@/components/business/chat-message-bubble"

export function Basic() {
  return <ChatMessageBubble />
}`} lang="tsx" />

## API / Props

### ChatMessageBubble

| Prop        | Type                                | Default | 说明 / Description                                        |
| ----------- | ----------------------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`                            | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `role`      | `"user" \| "assistant" \| "system"` | —       |                                                           |
| `content`   | `string`                            | —       | 内容 <br/> Content                                        |
| `avatar`    | `React.ReactNode`                   | —       |                                                           |
| `name`      | `string`                            | —       | 表单字段名 <br/> Form field name                          |
| `time`      | `string`                            | —       |                                                           |
| `status`    | `string`                            | —       | 状态 <br/> Status                                         |
| ...rest     | `React.HTMLAttributes`              | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-business-chat-message-bubble--docs)。
