import { MeetingRoomBooking } from "@chaos_team/chaos-ui/business"
import { CodeBlock } from "@/components/code-block"

# MeetingRoomBooking Meeting Room Booking

> 业务组件 / Domain-specific business components for ERP scenarios.

Meeting Room Booking — 面向 ERP/企业场景的业务组件。
MeetingRoomBooking — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Meeting Room Booking相关场景。

Use when you need a meeting room booking.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { MeetingRoomBooking } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <MeetingRoomBooking />
}`} lang="tsx" />


## API / Props

### MeetingRoomBooking

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| `rooms` | `MeetingRoom[]` | — |  |
| `existingBookings` | `ExistingBooking[]` | — |  |
| `selectedRoom` | `string` | `` |  |
| `onRoomChange` | `() => void` | — |  |
| `date` | `string` | `` |  |
| `onDateChange` | `() => void` | — |  |
| `startTime` | `string` | `09:00` |  |
| `endTime` | `string` | `10:00` |  |
| `onTimeChange` | `() => void` | — |  |
| `title` | `string` | `` | 标题 <br/> Title |
| `onTitleChange` | `() => void` | — |  |
| `attendeeCount` | `number` | `1` |  |
| `onAttendeeCountChange` | `() => void` | — |  |
| `onBook` | `() => void` | — |  |
| `readOnly` | `boolean` | `false` | 是否只读 <br/> Whether read-only |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-meetingroombooking--docs)。
