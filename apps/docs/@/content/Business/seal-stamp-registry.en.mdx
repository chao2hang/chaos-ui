import { SealStampRegistry } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# SealStampRegistry Seal Stamp Registry

> Domain-specific business components for ERP scenarios.

SealStampRegistry — a business component for ERP / enterprise scenarios.

## When to Use

Use SealStampRegistry when you need: SealStampRegistry: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { SealStampRegistry } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <SealStampRegistry />
}`} lang="tsx" />

## API / Props

### SealStampRegistry

| Prop           | Type                   | Default | Description                       |
| -------------- | ---------------------- | ------- | --------------------------------- |
| `className`    | `string`               | —       | Custom CSS class                  |
| `seals`        | `SealRecord[]`         | —       | Registered seals.                 |
| `usageLogs`    | `SealUsageLog[]`       | —       | Usage log entries.                |
| `onSealClick`  | `() => void`           | —       | Seal click handler.               |
| `onApplyUsage` | `() => void`           | —       | Apply for usage handler.          |
| `readOnly`     | `boolean`              | `false` | Read-only mode.                   |
| ...rest        | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
