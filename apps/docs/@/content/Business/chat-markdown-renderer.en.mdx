import { ChatMarkdownRenderer } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# ChatMarkdownRenderer Chat Markdown Renderer

> Domain-specific business components for ERP scenarios.

ChatMarkdownRenderer — a business component for ERP / enterprise scenarios.

## When to Use

Use ChatMarkdownRenderer when you need: ChatMarkdownRenderer: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { ChatMarkdownRenderer } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <ChatMarkdownRenderer />
}`} lang="tsx" />

## API / Props

### ChatMarkdownRenderer

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| `content`   | `string`               | —       | Content                           |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
