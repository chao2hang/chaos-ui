import { DashboardDesigner } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# DashboardDesigner

> Domain-specific business components for ERP scenarios.

DashboardDesigner — a business component for ERP / enterprise scenarios.

## When to Use

Use DashboardDesigner when you need: DashboardDesigner: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { DashboardDesigner } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <DashboardDesigner />
}`} lang="tsx" />

## API / Props

### DashboardDesigner

| Prop        | Type                          | Default | Description                     |
| ----------- | ----------------------------- | ------- | ------------------------------- |
| `className` | `string`                      | —       | Custom CSS class                |
| `widgets`   | `DashboardWidget[]`           | —       |                                 |
| `onChange`  | `() => void`                  | —       | Callback when value changes     |
| `cols`      | `number`                      | `12`    |                                 |
| ...rest     | `React.ComponentProps<"div">` | —       | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
