import { OrderLineEditor } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# OrderLineEditor

> Domain-specific business components for ERP scenarios.

OrderLineEditor — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need an order line editor.

## Examples

### Basic Usage

<CodeBlock code={`import { OrderLineEditor } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <OrderLineEditor />
}`} lang="tsx" />

## API / Props

### OrderLineEditor

| Prop        | Type                          | Default | Description                     |
| ----------- | ----------------------------- | ------- | ------------------------------- |
| `className` | `string`                      | —       | Custom CSS class                |
| `lines`     | `OrderLine[]`                 | —       |                                 |
| `onChange`  | `() => void`                  | —       | Callback when value changes     |
| ...rest     | `React.ComponentProps<"div">` | —       | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
