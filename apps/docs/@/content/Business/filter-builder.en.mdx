import { FilterBuilder, FieldDef, Filter } from "@chaos_team/chaos-ui/business"
import { CodeBlock } from "@/components/code-block"

# FilterBuilder

> Domain-specific business components for ERP scenarios.

FilterBuilder — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a filter builder.


## Examples

### Basic Usage

<CodeBlock code={`import { FilterBuilder, FieldDef, Filter } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return (
    <div className="space-y-4">
  <FilterBuilder />
  <FieldDef />
  <Filter />
    </div>
  )
}`} lang="tsx" />


## API / Props

### FilterBuilder

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `fields` | `FieldDef[]` | — |  |
| `initialFilters` | `Filter[]` | — |  |
| `onChange` | `() => void` | — | Callback when value changes |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


