import { MobileKanban } from "@chaos_team/chaos-ui/mobile";
import { CodeBlock } from "@/components/code-block";

# MobileKanban

> Domain-specific business components for ERP scenarios.

MobileKanban — a business component for ERP / enterprise scenarios.

## When to Use

Use MobileKanban when you need: MobileKanban: enterprise business component — see Storybook for interactive demos. Use for touch and narrow screens; prioritize large hit targets and safe areas.

## Examples

### Basic Usage

<CodeBlock code={`import { MobileKanban } from "@chaos_team/chaos-ui/mobile"

export function Basic() {
  return <MobileKanban />
}`} lang="tsx" />

## API / Props

### MobileKanban

| Prop              | Type                   | Default | Description                       |
| ----------------- | ---------------------- | ------- | --------------------------------- |
| `className`       | `string`               | —       | Custom CSS class                  |
| `columns`         | `KanbanColumnData[]`   | —       | Column configuration              |
| `onColumnsChange` | `() => void`           | —       |                                   |
| `renderCard`      | `(...args) => unknown` | —       |                                   |
| ...rest           | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
