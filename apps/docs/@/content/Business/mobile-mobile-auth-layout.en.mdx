import { MobileAuthLayout } from "@chaos_team/chaos-ui/mobile";
import { CodeBlock } from "@/components/code-block";

# MobileAuthLayout Mobile Mobile Auth Layout

> Domain-specific business components for ERP scenarios.

MobileAuthLayout — a business component for ERP / enterprise scenarios.

## When to Use

Use MobileMobileAuthLayout when this capability is required. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { MobileAuthLayout } from "@chaos_team/chaos-ui/mobile"

export function Basic() {
  return <MobileAuthLayout />
}`} lang="tsx" />

## API / Props

### MobileAuthLayout

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
