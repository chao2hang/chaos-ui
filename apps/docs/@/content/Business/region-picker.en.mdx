import { RegionPicker } from "@/components/business/region-picker";
import { CodeBlock } from "@/components/code-block";

# RegionPicker Region Picker

> Domain-specific business components for ERP scenarios.

RegionPicker — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a region picker.

## Examples

### Basic Usage

<CodeBlock code={`import { RegionPicker } from "@/components/business/region-picker"

export function Basic() {
  return <RegionPicker />
}`} lang="tsx" />

## API / Props

### RegionPicker

| Prop                | Type                   | Default | Description                       |
| ------------------- | ---------------------- | ------- | --------------------------------- |
| `className`         | `string`               | —       | Custom CSS class                  |
| `value`             | `string`               | —       | Current value (controlled)        |
| `onChange`          | `() => void`           | —       | Callback when value changes       |
| `options`           | `RegionPickerOption[]` | —       | Available options                 |
| `placeholder`       | `string`               | —       | Placeholder text                  |
| `searchPlaceholder` | `string`               | —       |                                   |
| `emptyText`         | `string`               | —       |                                   |
| `disabled`          | `boolean`              | `false` | Whether disabled                  |
| `clearable`         | `boolean`              | `true`  |                                   |
| ...rest             | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
