import { SavedFilters } from "@/components/business/saved-filters";
import { CodeBlock } from "@/components/code-block";

# SavedFilters 已保存筛选

> 业务组件

已保存筛选 — 面向 ERP/企业场景的业务组件。

## 何时使用

需要已保存筛选功能时使用本组件。

## 代码示例

### 基础用法

<CodeBlock code={`import { SavedFilters } from "@/components/business/saved-filters"

export function Basic() {
  return <SavedFilters />
}`} lang="tsx" />

## API / Props

### SavedFilters

| Prop        | Type                   | Default        | 说明              |
| ----------- | ---------------------- | -------------- | ----------------- |
| `className` | `string`               | —              | 自定义样式类名    |
| `filters`   | `SavedFilter[]`        | —              |                   |
| `activeId`  | `string`               | —              |                   |
| `onApply`   | `() => void`           | —              |                   |
| `onSave`    | `() => void`           | —              |                   |
| `onDelete`  | `() => void`           | —              |                   |
| `onPin`     | `() => void`           | —              |                   |
| `label`     | `string`               | `已保存的筛选` | 标签文本          |
| ...rest     | `React.HTMLAttributes` | —              | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-savedfilters--docs)。
