import { ChartSuite } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# ChartSuite Chart Suite

> 业务组件

Chart Suite — 面向 ERP/企业场景的业务组件。

## 何时使用

需要把经营指标或趋势做成图表供扫读时使用。（组件：Chart Suite）

## 代码示例

### 基础用法

<CodeBlock code={`import { ChartSuite } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <ChartSuite type={undefined} data={[]} xField={"example"} />
}`} lang="tsx" />
}`} lang="tsx" />

## API / Props

### ChartSuite

| Prop        | Type                                       | Default | 说明              |
| ----------- | ------------------------------------------ | ------- | ----------------- |
| `className` | `string`                                   | —       | 自定义样式类名    |
| `type`      | `"line" \| "bar" \| "pie" \| "radar" \...` | —       | 类型              |
| `data`      | `unknown[]`                                | —       | 数据源            |
| `xField`    | `string`                                   | `x`     |                   |
| `yField`    | `string`                                   | `y`     |                   |
| `series`    | `string`                                   | —       |                   |
| `height`    | `number`                                   | `200`   | 组件高度          |
| ...rest     | `React.HTMLAttributes`                     | —       | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](http://localhost:6006/?path=/docs/business-chartsuite--docs)。
