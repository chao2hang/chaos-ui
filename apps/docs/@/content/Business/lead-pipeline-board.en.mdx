import { LeadPipelineBoard } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# LeadPipelineBoard Lead Pipeline Board

> Domain-specific business components for ERP scenarios.

LeadPipelineBoard — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a lead pipeline board.

## Examples

### Basic Usage

<CodeBlock code={`import { LeadPipelineBoard } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <LeadPipelineBoard />
}`} lang="tsx" />

## API / Props

### LeadPipelineBoard

| Prop             | Type                   | Default | Description                                      |
| ---------------- | ---------------------- | ------- | ------------------------------------------------ |
| `className`      | `string`               | —       | Custom CSS class                                 |
| `stages`         | `PipelineStage[]`      | —       | Stage definitions in funnel order.               |
| `deals`          | `Deal[]`               | —       | All deals.                                       |
| `onDealMove`     | `() => void`           | —       | Callback when a deal moves to a different stage. |
| `onDealClick`    | `() => void`           | —       | Deal click handler.                              |
| `currencySymbol` | `string`               | `¥`     | Currency symbol for display.                     |
| `readOnly`       | `boolean`              | `false` | Read-only mode disables drag.                    |
| ...rest          | `React.HTMLAttributes` | —       | Inherits standard HTML attributes                |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
