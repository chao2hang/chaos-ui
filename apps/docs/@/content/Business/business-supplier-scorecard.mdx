import { SupplierScorecard } from "@/components/business/supplier-scorecard";
import { CodeBlock } from "@/components/code-block";

# SupplierScorecard Business Supplier Scorecard

> 业务组件 / Domain-specific business components for ERP scenarios.

Business Supplier Scorecard — 面向 ERP/企业场景的业务组件。
SupplierScorecard — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Business Supplier Scorecard相关场景。

Use when you need a supplier scorecard.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { SupplierScorecard } from "@/components/business/supplier-scorecard"

export function Basic() {
  return <SupplierScorecard />
}`} lang="tsx" />

## API / Props

### SupplierScorecard

| Prop                | Type                   | Default | 说明 / Description                                        |
| ------------------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className`         | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `supplier`          | `Supplier`             | —       | Supplier data                                             |
| `showWeightedTotal` | `boolean`              | `true`  | Show weighted total (default: true)                       |
| `showTrends`        | `boolean`              | `true`  | Show trend indicators (default: true)                     |
| `showBars`          | `boolean`              | `true`  | Show bar visualization (default: true)                    |
| ...rest             | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-business-supplier-scorecard--docs)。
