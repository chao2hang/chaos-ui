import { RedPacket } from "@/components/business/red-packet";
import { CodeBlock } from "@/components/code-block";

# RedPacket Business Red Packet

> 业务组件 / Domain-specific business components for ERP scenarios.

Business Red Packet — 面向 ERP/企业场景的业务组件。
RedPacket — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Business Red Packet相关场景。

Use when you need a red packet.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { RedPacket } from "@/components/business/red-packet"

export function Basic() {
  return <RedPacket />
}`} lang="tsx" />

## API / Props

### RedPacket

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `label`     | `string`               | —       | Amount or greeting text.                                  |
| `subtitle`  | `string`               | —       | Subtitle.                                                 |
| `opened`    | `boolean`              | —       | Whether the red packet has been opened.                   |
| `onOpen`    | `() => void`           | —       | Called when user clicks to open.                          |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-business-red-packet--docs)。
