import { MobileSwipeCard } from "@chaos_team/chaos-ui/mobile";
import { CodeBlock } from "@/components/code-block";

# MobileSwipeCard Mobile Mobile Swipe Card

> 业务组件

Mobile Mobile Swipe Card — 面向 ERP/企业场景的业务组件。

## 何时使用

适用于Mobile Mobile Swipe Card相关场景。


## 代码示例

### 基础用法

<CodeBlock code={`import { MobileSwipeCard } from "@chaos_team/chaos-ui/mobile"

export function Basic() {
  return <MobileSwipeCard />
}`} lang="tsx" />

## API / Props

### MobileSwipeCard

| Prop | Type | Default | 说明 |
| ------------ | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string` | — | 自定义样式类名 |
| `height`     | `number`               | `400`   | Height of the card stack.                                 |
| `onComplete` | `() => void`           | —       | Called when the last card is swiped away.                 |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-mobile-mobile-swipe-card--docs)。

