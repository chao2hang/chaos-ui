import { BarList } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# BarList Bar List

> 业务组件 / Domain-specific business components for ERP scenarios.

Bar List — 面向 ERP/企业场景的业务组件。
BarList — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Bar List相关场景。

Use when you need a bar list.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { BarList } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <BarList />
}`} lang="tsx" />

## API / Props

### BarList

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `data`      | `Array<T>`             | —       | 数据源 <br/> Data source                                  |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-barlist--docs)。
