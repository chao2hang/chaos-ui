import {
  Avatar,
  AvatarFallback,
  Button,
  Input,
  Label,
} from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# Profile

> Domain-specific business components for ERP scenarios.

Profile — a business component for ERP / enterprise scenarios.

## When to Use

Use Profile when this capability is required. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { Avatar, AvatarFallback, Button } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <ProfileHeader>Default</ProfileHeader>
}`} lang="tsx" />

## API / Props

### ProfileHeader

| Prop             | Type                          | Default            | Description                     |
| ---------------- | ----------------------------- | ------------------ | ------------------------------- |
| `className`      | `string`                      | —                  | Custom CSS class                |
| `user`           | `ProfileHeaderUser`           | —                  |                                 |
| `stats`          | `Array<T>`                    | —                  |                                 |
| `actions`        | `React.ReactNode`             | —                  |                                 |
| `onAvatarUpload` | `() => void`                  | —                  |                                 |
| `coverColor`     | `string`                      | `var(--brand-200)` | @default "var(--brand-200)"     |
| ...rest          | `React.ComponentProps<"div">` | —                  | Inherits all `<div>` attributes |

## Notes
