import { ProfileHeader, ProfileForm } from "@/components/business/profile";
import { CodeBlock } from "@/components/code-block";

# Profile

> Domain-specific business components for ERP scenarios.

Profile — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a profile.


## Examples

### Basic Usage

<CodeBlock code={`import { ProfileHeader } from "@/components/business/profile"

export function Basic() {
  return <ProfileHeader>Default</ProfileHeader>
}`} lang="tsx" />

## API / Props

### ProfileHeader

| Prop | Type | Default | Description |
| ---------------- | ----------------------------- | ------------------ | ----------------------------------------------------------- |
| `className` | `string` | — | Custom CSS class |
| `user`           | `ProfileHeaderUser`           | —                  |                                                             |
| `stats`          | `Array<T>`                    | —                  |                                                             |
| `actions`        | `React.ReactNode`             | —                  |                                                             |
| `onAvatarUpload` | `() => void`                  | —                  |                                                             |
| `coverColor`     | `string`                      | `var(--brand-200)` | @default "var(--brand-200)"                                 |
| ...rest | `React.ComponentProps<"div">` | — | Inherits all `<div>` attributes |

## Notes



