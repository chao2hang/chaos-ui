import { UnitConverter } from "@/components/business/unit-converter"
import { CodeBlock } from "@/components/code-block"

# UnitConverter

> Domain-specific business components for ERP scenarios.

UnitConverter — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need an unit converter.


## Examples

### Basic Usage

<CodeBlock code={`import { UnitConverter } from "@/components/business/unit-converter"

export function Basic() {
  return <UnitConverter />
}`} lang="tsx" />


## API / Props

### UnitConverter

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `units` | `string[]` | — |  |
| ...rest | `React.ComponentProps<"div">` | — | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


