import { RebutNodeSelect } from "@/components/business/rebut-node-select"
import { CodeBlock } from "@/components/code-block"

# RebutNodeSelect Rebut Node Select

> 业务组件 / Domain-specific business components for ERP scenarios.

<RebutNodeSelect nodes={[{ id: "n1", name: "部门审批" }]} onSelect={console.log} />
RebutNodeSelect — rebut node select component.

## 何时使用 / When to Use

适用于Rebut Node Select相关场景。

Use when you need a rebut node select.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { RebutNodeSelect } from "@/components/business/rebut-node-select"

export function Basic() {
  return <RebutNodeSelect />
}`} lang="tsx" />


## API / Props

### RebutNodeSelect

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| `nodes` | `Array<T>` | — |  |
| `onSelect` | `() => void` | — | 选中回调 <br/> Selection handler |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-rebutnodeselect--docs)。
