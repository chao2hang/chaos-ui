import { BillTimeline } from "@/components/business/bill-timeline";
import { CodeBlock } from "@/components/code-block";

# BillTimeline Bill Timeline

> 业务组件

Bill Timeline — 面向 ERP/企业场景的业务组件。

## 何时使用

适用于Bill Timeline相关场景。

## 代码示例

### 基础用法

<CodeBlock code={`import { BillTimeline } from "@/components/business/bill-timeline"

export function Basic() {
  return <BillTimeline />
}`} lang="tsx" />

## API / Props

### BillTimeline

| Prop          | Type                   | Default | 说明              |
| ------------- | ---------------------- | ------- | ----------------- |
| `className`   | `string`               | —       | 自定义样式类名    |
| `events`      | `Array<{`              | —       |                   |
| `id`          | `string`               | —       | DOM 元素 id       |
| `title`       | `string`               | —       | 标题              |
| `description` | `string`               | —       | 描述文本          |
| `timestamp`   | `string`               | —       |                   |
| `status`      | `string`               | —       | 状态              |
| ...rest       | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-billtimeline--docs)。
