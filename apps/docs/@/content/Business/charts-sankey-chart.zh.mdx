import { SankeyChart } from "@/components/business/charts/sankey-chart";
import { CodeBlock } from "@/components/code-block";

# SankeyChart 桑基图

> 业务组件

桑基图 — 面向 ERP/企业场景的业务组件。

## 何时使用

需要桑基图功能时使用本组件。

## 代码示例

### 基础用法

<CodeBlock code={`import { SankeyChart } from "@/components/business/charts/sankey-chart"

export function Basic() {
  return <SankeyChart />
}`} lang="tsx" />

## API / Props

### SankeyChart

| Prop        | Type                   | Default | 说明              |
| ----------- | ---------------------- | ------- | ----------------- |
| `className` | `string`               | —       | 自定义样式类名    |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-charts-sankey-chart--docs)。
