import { MobileCard } from "@chaos_team/chaos-ui/mobile"
import { CodeBlock } from "@/components/code-block"

# MobileCard

> Domain-specific business components for ERP scenarios.

MobileCard — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a mobile card.


## Examples

### Basic Usage

<CodeBlock code={`import { MobileCard } from "@chaos_team/chaos-ui/mobile"

export function Basic() {
  return <MobileCard />
}`} lang="tsx" />


## API / Props

### MobileCard

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `title` | `string` | — | Title |
| `description` | `string` | — | Description text |
| `actions` | `React.ReactNode` | — |  |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


