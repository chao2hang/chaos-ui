import { MobileKanban } from "@/components/mobile/mobile-kanban";
import { CodeBlock } from "@/components/code-block";

# MobileKanban Mobile Mobile Kanban

> Domain-specific business components for ERP scenarios.

MobileKanban — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a mobile kanban.


## Examples

### Basic Usage

<CodeBlock code={`import { MobileKanban } from "@/components/mobile/mobile-kanban"

export function Basic() {
  return <MobileKanban />
}`} lang="tsx" />

## API / Props

### MobileKanban

| Prop | Type | Default | Description |
| ----------------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string` | — | Custom CSS class |
| `columns` | `KanbanColumnData[]` | — | Column configuration |
| `onColumnsChange` | `() => void`           | —       |                                                           |
| `renderCard`      | `(...args) => unknown` | —       |                                                           |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


