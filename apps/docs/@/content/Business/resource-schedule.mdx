import { ResourceSchedule } from "@/components/business/resource-schedule";
import { CodeBlock } from "@/components/code-block";

# ResourceSchedule Resource Schedule

> 业务组件 / Domain-specific business components for ERP scenarios.

<ResourceSchedule resources={[{ id: "r1", name: "会议室 A" }]} bookings={[{ resourceId: "r1", start: "2026-06-30T09:00", end: "2026-06-30T10:00", title: "周会" }]} /
ResourceSchedule — resource schedule component.

## 何时使用 / When to Use

适用于Resource Schedule相关场景。

Use when you need a resource schedule.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { ResourceSchedule } from "@/components/business/resource-schedule"

export function Basic() {
  return <ResourceSchedule />
}`} lang="tsx" />

## API / Props

### ResourceSchedule

| Prop         | Type                   | Default | 说明 / Description                                        |
| ------------ | ---------------------- | ------- | --------------------------------------------------------- |
| `className`  | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `resources`  | `Array<T>`             | —       |                                                           |
| `bookings`   | `Array<{`              | —       |                                                           |
| `resourceId` | `string`               | —       |                                                           |
| `start`      | `string`               | —       |                                                           |
| `end`        | `string`               | —       |                                                           |
| `title`      | `string`               | —       | 标题 <br/> Title                                          |
| ...rest      | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-resource-schedule--docs)。
