import { ScreenCapture } from "@/components/business/screen-capture"
import { CodeBlock } from "@/components/code-block"

# ScreenCapture 屏幕截图

> 业务组件 / Domain-specific business components for ERP scenarios.

屏幕截图 — 屏幕截图与标注组件，基于 getDisplayMedia API。
ScreenCapture — Screen capture and annotation component with getDisplayMedia API.

## 何时使用 / When to Use

需要屏幕截图功能时使用本组件。

Use when you need a screencapture.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { ScreenCapture } from "@/components/business/screen-capture"

export function Basic() {
  return <ScreenCapture />
}`} lang="tsx" />


## API / Props

### ScreenCapture

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-screencapture--docs)。
