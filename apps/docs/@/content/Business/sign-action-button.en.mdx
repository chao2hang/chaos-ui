import { SignActionButton } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# SignActionButton Sign Action Button

> Domain-specific business components for ERP scenarios.

SignActionButton — a business component for ERP / enterprise scenarios.

## When to Use

Use SignActionButton when you need: SignActionButton: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { SignActionButton } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <SignActionButton />
}`} lang="tsx" />

## API / Props

### SignActionButton

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| `onSign`    | `() => void`           | —       |                                   |
| `signed`    | `boolean`              | `false` |                                   |
| `label`     | `string`               | —       | Label text                        |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
