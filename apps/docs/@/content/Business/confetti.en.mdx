import { CodeBlock } from "@/components/code-block";

# Confetti

> Domain-specific business components for ERP scenarios.

Confetti — a business component for ERP / enterprise scenarios.

## When to Use

Use Confetti when this capability is required. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock
  code={`export function Basic() {
  return <Confetti />
}`}
  lang="tsx"
/>

## API / Props

### Confetti

| Prop         | Type                   | Default | Description                       |
| ------------ | ---------------------- | ------- | --------------------------------- |
| `className`  | `string`               | —       | Custom CSS class                  |
| `trigger`    | `number`               | —       |                                   |
| `count`      | `number`               | `120`   |                                   |
| `duration`   | `number`               | `3000`  | Duration in milliseconds          |
| `onComplete` | `() => void`           | —       |                                   |
| ...rest      | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
