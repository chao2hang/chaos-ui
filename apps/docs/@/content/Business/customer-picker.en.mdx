import { CustomerPicker } from "@chaos_team/chaos-ui/business"
import { CodeBlock } from "@/components/code-block"

# CustomerPicker Customer Picker

> Domain-specific business components for ERP scenarios.

CustomerPicker — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a customer picker.


## Examples

### Basic Usage

<CodeBlock code={`import { CustomerPicker } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <CustomerPicker />
}`} lang="tsx" />


## API / Props

### CustomerPicker

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `value` | `string` | — | Current value (controlled) |
| `onChange` | `() => void` | — | Callback when value changes |
| `options` | `CustomerPickerOption[]` | — | Available options |
| `placeholder` | `string` | — | Placeholder text |
| `searchPlaceholder` | `string` | — |  |
| `emptyText` | `string` | — |  |
| `disabled` | `boolean` | `false` | Whether disabled |
| `clearable` | `boolean` | `true` |  |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


