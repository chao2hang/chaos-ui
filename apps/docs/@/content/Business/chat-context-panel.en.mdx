import { ChatContextPanel } from "@/components/business/chat-context-panel";
import { CodeBlock } from "@/components/code-block";

# ChatContextPanel Chat Context Panel

> Domain-specific business components for ERP scenarios.

ChatContextPanel — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a chat context panel.

## Examples

### Basic Usage

<CodeBlock code={`import { ChatContextPanel } from "@/components/business/chat-context-panel"

export function Basic() {
  return <ChatContextPanel />
}`} lang="tsx" />

## API / Props

### ChatContextPanel

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| `context`   | `Array<T>`             | —       |                                   |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
