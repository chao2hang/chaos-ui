import { MasterListTemplate } from "@/components/business/master-list-template";
import { CodeBlock } from "@/components/code-block";

# MasterListTemplate Master List Template

> 业务组件 / Domain-specific business components for ERP scenarios.

<MasterListTemplate title="供应商列表" onCreate={() => {}}>
  {" "}
  <table />{" "}
</MasterListTemplate>
MasterListTemplate — master list template component.

## 何时使用 / When to Use

适用于Master List Template相关场景。

Use when you need a master list template.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { MasterListTemplate } from "@/components/business/master-list-template"

export function Basic() {
  return <MasterListTemplate />
}`} lang="tsx" />

## API / Props

### MasterListTemplate

| Prop                | Type                   | Default   | 说明 / Description                                        |
| ------------------- | ---------------------- | --------- | --------------------------------------------------------- |
| `className`         | `string`               | —         | 自定义样式类名 <br/> Custom CSS class                     |
| `title`             | `string`               | `列表`    | 标题 <br/> Title                                          |
| `onCreate`          | `() => void`           | —         |                                                           |
| `onSearch`          | `() => void`           | —         |                                                           |
| `searchPlaceholder` | `string`               | `搜索...` |                                                           |
| ...rest             | `React.HTMLAttributes` | —         | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-master-list-template--docs)。
