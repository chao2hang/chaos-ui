import { DeltaBar } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# DeltaBar Delta Bar

> 业务组件

colored by positive/negative delta, with an accessible label. <DeltaBar value={42} maxValue={100} label="增长率" />

## 何时使用

当你需要「Delta Bar（DeltaBar）」解决具体交互/展示问题时使用；优先看下方示例与 API，按业务字段接入。

## 代码示例

### 基础用法

<CodeBlock code={`import { DeltaBar } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <DeltaBar />
}`} lang="tsx" />

## API / Props

### DeltaBar

| Prop        | Type                   | Default | 说明              |
| ----------- | ---------------------- | ------- | ----------------- |
| `className` | `string`               | —       | 自定义样式类名    |
| `value`     | `number`               | —       | 当前值(受控)      |
| `maxValue`  | `number`               | —       |                   |
| `label`     | `string`               | —       | 标签文本          |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 |

## 注意事项

- **无障碍**: 组件遵循 WAI-ARIA 最佳实践。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](http://localhost:6006/?path=/docs/business-deltabar--docs)。
