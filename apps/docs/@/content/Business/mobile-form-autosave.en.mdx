import { CodeBlock } from "@/components/code-block";

# MobileFormAutosave

> Domain-specific business components for ERP scenarios.

MobileFormAutosave — a business component for ERP / enterprise scenarios.

## When to Use

Use MobileFormAutosave when this capability is required. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock
  code={`export function Basic() {
  return <MobileFormAutosave />
}`}
  lang="tsx"
/>

## API / Props

### MobileFormAutosave

| Prop        | Type                          | Default | Description                     |
| ----------- | ----------------------------- | ------- | ------------------------------- |
| `className` | `string`                      | —       | Custom CSS class                |
| `status`    | `AutosaveStatus`              | —       | Status                          |
| `lastSaved` | `number \| null`              | —       |                                 |
| `error`     | `Error \| null`               | —       |                                 |
| ...rest     | `React.ComponentProps<"div">` | —       | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
