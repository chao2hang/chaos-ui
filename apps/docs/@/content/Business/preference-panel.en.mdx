import { PreferencePanel } from "@chaos_team/chaos-ui/business"
import { CodeBlock } from "@/components/code-block"

# PreferencePanel Preference Panel

> Domain-specific business components for ERP scenarios.

PreferencePanel — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a preference panel.


## Examples

### Basic Usage

<CodeBlock code={`import { PreferencePanel } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <PreferencePanel />
}`} lang="tsx" />


## API / Props

### PreferencePanel

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `open` | `boolean` | — | Open state (controlled) |
| `onOpenChange` | `() => void` | — | Callback when open state changes |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


