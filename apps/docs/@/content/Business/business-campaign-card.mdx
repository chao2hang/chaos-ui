import { CampaignCard, Labels } from "@/components/business/campaign-card";
import { CodeBlock } from "@/components/code-block";

# CampaignCard Business Campaign Card

> 业务组件 / Domain-specific business components for ERP scenarios.

Business Campaign Card — 面向 ERP/企业场景的业务组件。
CampaignCard — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Business Campaign Card相关场景。

Use when you need a campaign card.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { CampaignCard, Labels } from "@/components/business/campaign-card"

export function Basic() {
  return (
    <div className="space-y-4">
  <CampaignCard />
  <Labels />
    </div>
  )
}`} lang="tsx" />

## 样式变体 / Style Variants

**状态 / status**

| status    | 说明 / Description | 默认 |
| --------- | ------------------ | ---- |
| `planned` |                    | ✓    |
| `active`  |                    |      |
| `paused`  |                    |      |
| `ended`   |                    |      |

## API / Props

### CampaignCard

| Prop         | Type                                       | Default   | 说明 / Description                                        |
| ------------ | ------------------------------------------ | --------- | --------------------------------------------------------- |
| `className`  | `string`                                   | —         | 自定义样式类名 <br/> Custom CSS class                     |
| `name`       | `string`                                   | —         | 表单字段名 <br/> Form field name                          |
| `status`     | `"planned" \| "active" \| "paused" \| ...` | `planned` | 状态 <br/> Status                                         |
| `startDate`  | `string`                                   | —         |                                                           |
| `endDate`    | `string`                                   | —         |                                                           |
| `budget`     | `number`                                   | —         |                                                           |
| `location`   | `string`                                   | —         |                                                           |
| `reachCount` | `number`                                   | —         |                                                           |
| ...rest      | `React.HTMLAttributes`                     | —         | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **CVA 变体**: 通过 `class-variance-authority` 管理样式变体,类型安全。变体对象可导出复用。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-business-campaign-card--docs)。
