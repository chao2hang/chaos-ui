import { MobileInput } from "@chaos_team/chaos-ui/mobile";
import { CodeBlock } from "@/components/code-block";

# MobileInput Mobile Mobile Input

> Domain-specific business components for ERP scenarios.

MobileInput — a business component for ERP / enterprise scenarios.

## When to Use

Use MobileMobileInput when this capability is required. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { MobileInput } from "@chaos_team/chaos-ui/mobile"

export function Basic() {
  return <MobileInput />
}`} lang="tsx" />

## API / Props

### MobileInput

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
