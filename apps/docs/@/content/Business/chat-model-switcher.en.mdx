import { ChatModelSwitcher } from "@/components/business/chat-model-switcher"
import { CodeBlock } from "@/components/code-block"

# ChatModelSwitcher Chat Model Switcher

> Domain-specific business components for ERP scenarios.

ChatModelSwitcher — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a chat model switcher.


## Examples

### Basic Usage

<CodeBlock code={`import { ChatModelSwitcher } from "@/components/business/chat-model-switcher"

export function Basic() {
  return <ChatModelSwitcher />
}`} lang="tsx" />


## API / Props

### ChatModelSwitcher

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `models` | `Array<T>` | — |  |
| `activeId` | `string` | — |  |
| `onSwitch` | `() => void` | — |  |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


