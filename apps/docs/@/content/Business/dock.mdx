import { Dock } from "@/components/business/dock";
import { CodeBlock } from "@/components/code-block";

# Dock 停靠栏

> 业务组件 / Domain-specific business components for ERP scenarios.

停靠栏 — 面向 ERP/企业场景的业务组件。
Dock — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

需要停靠栏功能时使用本组件。

Use when you need a dock.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { Dock } from "@/components/business/dock"

export function Basic() {
  return <Dock />
}`} lang="tsx" />

## API / Props

### Dock

| Prop            | Type                          | Default      | 说明 / Description                                          |
| --------------- | ----------------------------- | ------------ | ----------------------------------------------------------- |
| `className`     | `string`                      | —            | 自定义样式类名 <br/> Custom CSS class                       |
| `items`         | `DockItem[]`                  | —            | 选项/数据项列表 <br/> Items / data list                     |
| `orientation`   | `"horizontal" \| "vertical"`  | `horizontal` | 排列方向 <br/> Orientation                                  |
| `magnification` | `boolean`                     | `true`       |                                                             |
| ...rest         | `React.ComponentProps<"div">` | —            | 透传 `<div>` 原生属性 <br/> Inherits all `<div>` attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-dock--docs)。
