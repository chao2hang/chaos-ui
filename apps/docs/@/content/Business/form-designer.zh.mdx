import { FormDesigner } from "@/components/business/form-designer"
import { CodeBlock } from "@/components/code-block"

# FormDesigner Form Designer

> 业务组件

<FormDesigner fields={[{ id: "1", type: "text", label: "客户名称", required: true }]} onChange={(f) => console.log(f)} />

## 何时使用

适用于Form Designer相关场景。


## 代码示例

### 基础用法

<CodeBlock code={`import { FormDesigner } from "@/components/business/form-designer"

export function Basic() {
  return <FormDesigner />
}`} lang="tsx" />


## API / Props

### FormDesigner

| Prop | Type | Default | 说明 |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 |
| `fields` | `Array<FormDesignerField>` | — |  |
| `onChange` | `() => void` | — | 值变化回调 |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-formdesigner--docs)。

