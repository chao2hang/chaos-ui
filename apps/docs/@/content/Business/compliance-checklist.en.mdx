import { ComplianceChecklist } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# ComplianceChecklist Compliance Checklist

> Domain-specific business components for ERP scenarios.

ComplianceChecklist — a business component for ERP / enterprise scenarios.

## When to Use

Use ComplianceChecklist when you need: ComplianceChecklist: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { ComplianceChecklist } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <ComplianceChecklist />
}`} lang="tsx" />

## API / Props

### ComplianceChecklist

| Prop             | Type                   | Default                | Description                       |
| ---------------- | ---------------------- | ---------------------- | --------------------------------- |
| `className`      | `string`               | —                      | Custom CSS class                  |
| `items`          | `ComplianceItem[]`     | —                      | Compliance items.                 |
| `title`          | `string`               | `Compliance Checklist` | Checklist title.                  |
| `onItemClick`    | `() => void`           | —                      | Item click handler.               |
| `onStatusChange` | `() => void`           | —                      | Status change handler.            |
| `readOnly`       | `boolean`              | `false`                | Read-only mode.                   |
| ...rest          | `React.HTMLAttributes` | —                      | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
