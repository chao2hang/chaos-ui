import { SettingsLayout } from "@/components/business/settings-layout";
import { CodeBlock } from "@/components/code-block";

# SettingsLayout

> Domain-specific business components for ERP scenarios.

SettingsLayout — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a settings layout.

## Examples

### Basic Usage

<CodeBlock code={`import { SettingsLayout } from "@/components/business/settings-layout"

export function Basic() {
  return <SettingsLayout />
}`} lang="tsx" />

## API / Props

### SettingsLayout

| Prop          | Type                          | Default | Description                     |
| ------------- | ----------------------------- | ------- | ------------------------------- |
| `className`   | `string`                      | —       | Custom CSS class                |
| `nav`         | `SettingsNavEntry[]`          | —       |                                 |
| `active`      | `string`                      | —       |                                 |
| `onNavChange` | `() => void`                  | —       |                                 |
| `sections`    | `SettingsSection[]`           | —       |                                 |
| ...rest       | `React.ComponentProps<"div">` | —       | Inherits all `<div>` attributes |

## Notes
