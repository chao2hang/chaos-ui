import { ChatSharedLink } from "@chaos_team/chaos-ui/business"
import { CodeBlock } from "@/components/code-block"

# ChatSharedLink

> Domain-specific business components for ERP scenarios.

ChatSharedLink — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a chat shared link.


## Examples

### Basic Usage

<CodeBlock code={`import { ChatSharedLink } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <ChatSharedLink />
}`} lang="tsx" />


## API / Props

### ChatSharedLink

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `url` | `string` | — |  |
| `title` | `string` | — | Title |
| `description` | `string` | — | Description text |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


