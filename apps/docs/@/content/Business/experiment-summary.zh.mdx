import { ExperimentSummary } from "@chaos_team/chaos-ui/business"
import { CodeBlock } from "@/components/code-block"

# ExperimentSummary 实验摘要

> 业务组件

实验摘要 — 面向 ERP/企业场景的业务组件。

## 何时使用

需要实验摘要功能时使用本组件。


## 代码示例

### 基础用法

<CodeBlock code={`import { ExperimentSummary } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <ExperimentSummary />
}`} lang="tsx" />


## API / Props

### ExperimentSummary

| Prop | Type | Default | 说明 |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 |
| `name` | `string` | — | 表单字段名 |
| `controlRate` | `number` | — |  |
| `variantRate` | `number` | — |  |
| `metric` | `string` | `转化率` |  |
| `confidence` | `number` | `95` |  |
| `status` | `"running" \| "completed" \| "stopped"` | `running` | 状态 |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-experimentsummary--docs)。

