import {
  FilterBuilder,
  FieldDef,
  Filter,
} from "@/components/business/filter-builder";
import { CodeBlock } from "@/components/code-block";

# FilterBuilder 筛选构建器

> 业务组件

筛选构建器 — 面向 ERP/企业场景的业务组件。

## 何时使用

需要筛选构建器功能时使用本组件。

## 代码示例

### 基础用法

<CodeBlock code={`import { FilterBuilder, FieldDef, Filter } from "@/components/business/filter-builder"

export function Basic() {
  return (
    <div className="space-y-4">
  <FilterBuilder />
  <FieldDef />
  <Filter />
    </div>
  )
}`} lang="tsx" />

## API / Props

### FilterBuilder

| Prop             | Type                   | Default | 说明              |
| ---------------- | ---------------------- | ------- | ----------------- |
| `className`      | `string`               | —       | 自定义样式类名    |
| `fields`         | `FieldDef[]`           | —       |                   |
| `initialFilters` | `Filter[]`             | —       |                   |
| `onChange`       | `() => void`           | —       | 值变化回调        |
| ...rest          | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-filter-builder--docs)。
