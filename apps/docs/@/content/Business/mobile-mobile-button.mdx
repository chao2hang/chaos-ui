import { MobileButton } from "@/components/mobile/mobile-button";
import { CodeBlock } from "@/components/code-block";

# MobileButton Mobile Mobile Button

> 业务组件 / Domain-specific business components for ERP scenarios.

Mobile Mobile Button — 面向 ERP/企业场景的业务组件。
MobileButton — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Mobile Mobile Button相关场景。

Use when you need a mobile button.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { MobileButton } from "@/components/mobile/mobile-button"

export function Basic() {
  return <MobileButton />
}`} lang="tsx" />

## API / Props

### MobileButton

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `fullWidth` | `boolean`              | `true`  | 是否占满父容器宽度 <br/> Take full parent width           |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-mobile-mobile-button--docs)。
