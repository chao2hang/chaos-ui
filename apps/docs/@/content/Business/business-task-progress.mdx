import { TaskProgress } from "@/components/business/task-progress";
import { CodeBlock } from "@/components/code-block";

# TaskProgress Business Task Progress

> 业务组件 / Domain-specific business components for ERP scenarios.

<TaskProgress percent={75} status="running" message="正在生成报表…" />
TaskProgress — business task progress component.

## 何时使用 / When to Use

适用于Business Task Progress相关场景。

Use when you need a task progress.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { TaskProgress } from "@/components/business/task-progress"

export function Basic() {
  return <TaskProgress />
}`} lang="tsx" />

## API / Props

### TaskProgress

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `percent`   | `number`               | —       |                                                           |
| `status`    | `string`               | —       | 状态 <br/> Status                                         |
| `message`   | `string`               | —       | 提示消息内容 <br/> Message content                        |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-business-task-progress--docs)。
