import {
  ResponsivePreview,
  Sizes,
} from "@/components/business/responsive-preview";
import { CodeBlock } from "@/components/code-block";

# ResponsivePreview Business Responsive Preview

> 业务组件 / Domain-specific business components for ERP scenarios.

Business Responsive Preview — 面向 ERP/企业场景的业务组件。
ResponsivePreview — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Business Responsive Preview相关场景。

Use when you need a responsive preview.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { ResponsivePreview, Sizes } from "@/components/business/responsive-preview"

export function Basic() {
  return (
    <div className="space-y-4">
  <ResponsivePreview />
  <Sizes />
    </div>
  )
}`} lang="tsx" />

## API / Props

### ResponsivePreview

| Prop        | Type                                | Default   | 说明 / Description                                        |
| ----------- | ----------------------------------- | --------- | --------------------------------------------------------- |
| `className` | `string`                            | —         | 自定义样式类名 <br/> Custom CSS class                     |
| `device`    | `"mobile" \| "tablet" \| "desktop"` | `desktop` |                                                           |
| `showFrame` | `boolean`                           | `true`    |                                                           |
| `showLabel` | `boolean`                           | `true`    |                                                           |
| ...rest     | `React.HTMLAttributes`              | —         | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-business-responsive-preview--docs)。
