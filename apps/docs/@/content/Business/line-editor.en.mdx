import { LineEditor } from "@chaos_team/chaos-ui/business"
import { CodeBlock } from "@/components/code-block"

# LineEditor

> Domain-specific business components for ERP scenarios.

LineEditor — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a line editor.


## Examples

### Basic Usage

<CodeBlock code={`import { LineEditor } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <LineEditor />
}`} lang="tsx" />


## API / Props

### LineEditor

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `lines` | `LineItem[]` | — |  |
| `columns` | `Array<T>` | — | Column configuration |
| `onChange` | `() => void` | — | Callback when value changes |
| ...rest | `React.ComponentProps<"div">` | — | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


