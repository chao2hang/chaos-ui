import { PaymentSchedule } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# PaymentSchedule Payment Schedule

> Domain-specific business components for ERP scenarios.

PaymentSchedule — a business component for ERP / enterprise scenarios.

## When to Use

Use PaymentSchedule when you need: PaymentSchedule: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { PaymentSchedule } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <PaymentSchedule />
}`} lang="tsx" />

## API / Props

### PaymentSchedule

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| `schedule`  | `Array<T>`             | —       |                                   |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
