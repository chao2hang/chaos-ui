import { QRCode } from "@/components/business/qr-code";
import { CodeBlock } from "@/components/code-block";

# QrCode 二维码

> 业务组件 / Domain-specific business components for ERP scenarios.

二维码 — 面向 ERP/企业场景的业务组件。
QrCode — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

生成并展示二维码。

Generate and display a QR code.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { QRCode } from "@/components/business/qr-code"

export function Basic() {
  return <QRCode />
}`} lang="tsx" />

## API / Props

### QRCode

| Prop        | Type                       | Default   | 说明 / Description                                        |
| ----------- | -------------------------- | --------- | --------------------------------------------------------- |
| `className` | `string`                   | —         | 自定义样式类名 <br/> Custom CSS class                     |
| `value`     | `string`                   | —         | 当前值(受控) <br/> Current value (controlled)             |
| `size`      | `number`                   | `200`     | 组件尺寸 <br/> Component size                             |
| `level`     | `"L" \| "M" \| "Q" \| "H"` | `M`       | 级别 <br/> Level / severity                               |
| `fgColor`   | `string`                   | `#000000` |                                                           |
| `bgColor`   | `string`                   | `#ffffff` |                                                           |
| ...rest     | `React.HTMLAttributes`     | —         | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-qr-code--docs)。
