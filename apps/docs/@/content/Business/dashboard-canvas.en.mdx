import { DashboardCanvas } from "@/components/business/dashboard-canvas";
import { CodeBlock } from "@/components/code-block";

# DashboardCanvas Dashboard Canvas

> Domain-specific business components for ERP scenarios.

<DashboardCanvas
  widgets={[{ id: "w1", title: "销售额", x: 0, y: 0, w: 6, h: 2 }]}
/>
DashboardCanvas — dashboard canvas component.

## When to Use

Use when you need a dashboard canvas.

## Examples

### Basic Usage

<CodeBlock code={`import { DashboardCanvas } from "@/components/business/dashboard-canvas"

export function Basic() {
  return <DashboardCanvas />
}`} lang="tsx" />

## API / Props

### DashboardCanvas

| Prop        | Type                     | Default | Description                       |
| ----------- | ------------------------ | ------- | --------------------------------- |
| `className` | `string`                 | —       | Custom CSS class                  |
| `widgets`   | `Array<DashboardWidget>` | —       |                                   |
| `onChange`  | `() => void`             | —       | Callback when value changes       |
| ...rest     | `React.HTMLAttributes`   | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
