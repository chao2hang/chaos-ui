import { AttachmentUploader } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# AttachmentUploader Attachment Uploader

> Domain-specific business components for ERP scenarios.

AttachmentUploader — a business component for ERP / enterprise scenarios.

## When to Use

Use AttachmentUploader when you need: AttachmentUploader: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { AttachmentUploader } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <AttachmentUploader />
}`} lang="tsx" />

## API / Props

### AttachmentUploader

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| `accept`    | `string`               | —       |                                   |
| `maxSize`   | `number`               | —       |                                   |
| `onUpload`  | `() => void`           | —       |                                   |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
