import { FormProgress } from "@/components/business/form/form-progress";
import { CodeBlock } from "@/components/code-block";

# FormProgress 表单进度

> 业务组件

表单进度 — 面向 ERP/企业场景的业务组件。

## 何时使用

需要表单进度功能时使用本组件。


## 代码示例

### 基础用法

<CodeBlock code={`import { FormProgress } from "@/components/business/form/form-progress"

export function Basic() {
  return <FormProgress />
}`} lang="tsx" />

## API / Props

### FormProgress

| Prop | Type | Default | 说明 |
| ------------ | ---------------------------- | ------- | --------------------------------------------------------- |
| `className` | `string` | — | 自定义样式类名 |
| `current`    | `number`                     | —       |                                                           |
| `total`      | `number`                     | —       |                                                           |
| `labels`     | `string[]`                   | —       |                                                           |
| `showLabels` | `boolean`                    | `true`  |                                                           |
| `variant` | `"bar" \| "steps" \| "dots"` | `bar` | 样式变体 |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-form-form-progress--docs)。

