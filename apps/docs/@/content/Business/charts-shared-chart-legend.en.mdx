import { CodeBlock } from "@/components/code-block";

# ChartLegend

> Domain-specific business components for ERP scenarios.

ChartLegend — a business component for ERP / enterprise scenarios.

## When to Use

Use ChartsSharedChartLegend when this capability is required. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock
  code={`export function Basic() {
  return <ChartLegend />
}`}
  lang="tsx"
/>

## API / Props

### ChartLegend

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| `payload`   | `LegendPayloadEntry[]` | —       |                                   |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
