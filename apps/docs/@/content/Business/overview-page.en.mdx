import { OverviewPage } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# OverviewPage Overview Page

> Domain-specific business components for ERP scenarios.

OverviewPage — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need an overview page.

## Examples

### Basic Usage

<CodeBlock code={`import { OverviewPage } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <OverviewPage />
}`} lang="tsx" />

## API / Props

### OverviewPage

| Prop         | Type                   | Default | Description                       |
| ------------ | ---------------------- | ------- | --------------------------------- |
| `className`  | `string`               | —       | Custom CSS class                  |
| `title`      | `string`               | `总览`  | 页面标题                          |
| `subtitle`   | `string`               | ``      | 副标题                            |
| `kpis`       | `Array<{`              | —       | KPI 指标数组                      |
| `label`      | `string`               | —       | Label text                        |
| `value`      | `number`               | —       | Current value (controlled)        |
| `delta`      | `number`               | —       |                                   |
| `unit`       | `string`               | —       |                                   |
| `activities` | `Array<T>`             | —       | 最近活动条目                      |
| ...rest      | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
