import { AccountBalance } from "@chaos_team/chaos-ui/business"
import { CodeBlock } from "@/components/code-block"

# AccountBalance Account Balance

> 业务组件 / Domain-specific business components for ERP scenarios.

Account Balance — 面向 ERP/企业场景的业务组件。
AccountBalance — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Account Balance相关场景。

Use when you need an account balance.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { AccountBalance } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <AccountBalance />
}`} lang="tsx" />


## API / Props

### AccountBalance

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| `balance` | `number` | — |  |
| `currency` | `string` | `CNY` |  |
| `label` | `string` | `账户余额` | 标签文本 <br/> Label text |
| `trend` | `number` | — |  |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-accountbalance--docs)。
