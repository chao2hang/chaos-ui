import { ApprovalActionBar } from "@chaos_team/chaos-ui/business"
import { CodeBlock } from "@/components/code-block"

# ApprovalActionBar Approval Action Bar

> 业务组件

Approval Action Bar — 面向 ERP/企业场景的业务组件。

## 何时使用

适用于Approval Action Bar相关场景。


## 代码示例

### 基础用法

<CodeBlock code={`import { ApprovalActionBar } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <ApprovalActionBar />
}`} lang="tsx" />


## API / Props

### ApprovalActionBar

| Prop | Type | Default | 说明 |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 |
| `onApprove` | `() => void` | — |  |
| `onReject` | `() => void` | — |  |
| `onTransfer` | `() => void` | — |  |
| `status` | `string` | — | 状态 |
| `loading` | `boolean` | — | 是否加载中 |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-approvalactionbar--docs)。

