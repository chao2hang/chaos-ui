import { PermissionMatrix } from "@chaos_team/chaos-ui/business"
import { CodeBlock } from "@/components/code-block"

# PermissionMatrix

> Domain-specific business components for ERP scenarios.

PermissionMatrix — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a permission matrix.


## Examples

### Basic Usage

<CodeBlock code={`import { PermissionMatrix } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <PermissionMatrix />
}`} lang="tsx" />


## API / Props

### PermissionMatrix

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `permissions` | `PermissionItem[]` | — |  |
| `roles` | `string[]` | — |  |
| `value` | `Record<string, string[]>` | — | Current value (controlled) |
| `onChange` | `() => void` | — | Callback when value changes |
| ...rest | `React.ComponentProps<"div">` | — | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


