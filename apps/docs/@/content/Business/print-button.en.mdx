import { PrintButton } from "@chaos_team/chaos-ui/business"
import { CodeBlock } from "@/components/code-block"

# PrintButton

> Domain-specific business components for ERP scenarios.

PrintButton — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a print button.


## Examples

### Basic Usage

<CodeBlock code={`import { PrintButton } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <PrintButton />
}`} lang="tsx" />


## API / Props

### PrintButton

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `onClick` | `() => void` | — | Click handler |
| `label` | `string` | `打印` | Label text |
| ...rest | `React.ComponentProps<"button">` | — | Inherits all `<button>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


