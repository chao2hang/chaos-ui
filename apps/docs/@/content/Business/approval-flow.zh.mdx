import { ApprovalFlow } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# ApprovalFlow 审批流程

> 业务组件

审批流程 — 面向 ERP/企业场景的业务组件。

## 何时使用

审批流程展示与操作,查看节点、同意/拒绝。

## 代码示例

### 基础用法

<CodeBlock code={`import { ApprovalFlow } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <ApprovalFlow />
}`} lang="tsx" />

## API / Props

### ApprovalFlow

| Prop          | Type                          | Default | 说明                  |
| ------------- | ----------------------------- | ------- | --------------------- |
| `className`   | `string`                      | —       | 自定义样式类名        |
| `steps`       | `ApprovalStep[]`              | —       |                       |
| `currentStep` | `number`                      | —       |                       |
| `onApprove`   | `() => void`                  | —       |                       |
| `onReject`    | `() => void`                  | —       |                       |
| ...rest       | `React.ComponentProps<"div">` | —       | 透传 `<div>` 原生属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-approvalflow--docs)。
