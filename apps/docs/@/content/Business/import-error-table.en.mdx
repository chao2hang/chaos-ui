import { ImportErrorTable } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# ImportErrorTable Import Error Table

> Domain-specific business components for ERP scenarios.

ImportErrorTable — a business component for ERP / enterprise scenarios.

## When to Use

Use ImportErrorTable when you need: ImportErrorTable: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { ImportErrorTable } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <ImportErrorTable />
}`} lang="tsx" />

## API / Props

### ImportErrorTable

| Prop        | Type                   | Default | Description                                           |
| ----------- | ---------------------- | ------- | ----------------------------------------------------- |
| `className` | `string`               | —       | Custom CSS class                                      |
| `errors`    | `Array<{`              | —       | Validation errors, one per offending import row.      |
| `row`       | `number`               | —       | 1-based source spreadsheet row number.                |
| `field`     | `string`               | —       | Field/column that failed validation, if known.        |
| `value`     | `string`               | —       | The raw value that failed, if known.                  |
| `message`   | `string`               | —       | Human-readable reason the row was rejected.           |
| `onExport`  | `() => void`           | —       | Called when the user clicks the export-errors button. |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes                     |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
