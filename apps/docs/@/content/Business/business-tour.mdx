import { Tour } from "@/components/ui/tour";
import { CodeBlock } from "@/components/code-block";

# Tour Business Tour

> 业务组件 / Domain-specific business components for ERP scenarios.

Business Tour — 面向 ERP/企业场景的业务组件。
Tour — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Business Tour相关场景。

Use when you need a tour.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { Tour } from "@/components/ui/tour"

export function Basic() {
  return <Tour />
}`} lang="tsx" />

## API / Props

### Tour

| Prop           | Type                   | Default | 说明 / Description                                        |
| -------------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className`    | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `steps`        | `TourStep[]`           | —       |                                                           |
| `open`         | `boolean`              | —       | 是否打开(受控) <br/> Open state (controlled)              |
| `onOpenChange` | `() => void`           | —       | 打开状态变化回调 <br/> Callback when open state changes   |
| `onComplete`   | `() => void`           | —       |                                                           |
| `onSkip`       | `() => void`           | —       |                                                           |
| `storageKey`   | `string`               | —       |                                                           |
| ...rest        | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-business-tour--docs)。
