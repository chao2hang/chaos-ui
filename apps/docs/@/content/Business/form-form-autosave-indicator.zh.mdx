import { FormAutosaveIndicator } from "@/components/business/form/form-autosave-indicator";
import { CodeBlock } from "@/components/code-block";

# FormAutosaveIndicator 表单自动保存指示器

> 业务组件

表单自动保存指示器 — 面向 ERP/企业场景的业务组件。

## 何时使用

需要表单自动保存指示器功能时使用本组件。


## 代码示例

### 基础用法

<CodeBlock code={`import { FormAutosaveIndicator } from "@/components/business/form/form-autosave-indicator"

export function Basic() {
  return <FormAutosaveIndicator />
}`} lang="tsx" />

## API / Props

### FormAutosaveIndicator

| Prop | Type | Default | 说明 |
| ----------- | ----------------------------- | ------- | ----------------------------------------------------------- |
| `className` | `string` | — | 自定义样式类名 |
| `status` | `AutosaveStatus` | — | 状态 |
| `lastSaved` | `number \| null`              | —       |                                                             |
| `error`     | `Error \| null`               | —       |                                                             |
| ...rest | `React.ComponentProps<"div">` | — | 透传 `<div>` 原生属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-form-form-autosave-indicator--docs)。

