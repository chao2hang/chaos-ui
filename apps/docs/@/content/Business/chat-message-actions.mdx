import { ChatMessageActions } from "@/components/business/chat-message-actions"
import { CodeBlock } from "@/components/code-block"

# ChatMessageActions Chat Message Actions

> 业务组件 / Domain-specific business components for ERP scenarios.

Chat Message Actions — 面向 ERP/企业场景的业务组件。
ChatMessageActions — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Chat Message Actions相关场景。

Use when you need a chat message actions.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { ChatMessageActions } from "@/components/business/chat-message-actions"

export function Basic() {
  return <ChatMessageActions />
}`} lang="tsx" />


## API / Props

### ChatMessageActions

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| `onReply` | `() => void` | — |  |
| `onCopy` | `() => void` | — |  |
| `onDelete` | `() => void` | — |  |
| `onRegenerate` | `() => void` | — |  |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-chatmessageactions--docs)。
