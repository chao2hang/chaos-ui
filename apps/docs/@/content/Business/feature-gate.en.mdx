import { CodeBlock } from "@/components/code-block";

# FeatureGate

> Domain-specific business components for ERP scenarios.

FeatureGate — a business component for ERP / enterprise scenarios.

## When to Use

Use FeatureGate when this capability is required. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { PermissionButton } from "@chaos_team/chaos-ui/business";

export function Basic() {
  // docs-only FeatureGate was removed; PermissionButton illustrates gated UI
  return <PermissionButton permission="order:create">创建订单</PermissionButton>;
}`} lang="tsx" />

## API / Props

### FeatureGate

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
