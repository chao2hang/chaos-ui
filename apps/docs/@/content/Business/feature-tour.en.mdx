import { CodeBlock } from "@/components/code-block";

# FeatureTour Feature Tour

> Domain-specific business components for ERP scenarios.

FeatureTour — feature tour component.

## When to Use

Use FeatureTour when this capability is required. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { FeatureTour } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <FeatureTour />
}`} lang="tsx" />

## API / Props

### FeatureTour

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| `steps`     | `Array<T>`             | —       |                                   |
| `open`      | `boolean`              | —       | Open state (controlled)           |
| `onClose`   | `() => void`           | —       | Close handler                     |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
