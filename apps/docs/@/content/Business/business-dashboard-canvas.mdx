import { DashboardCanvas } from "@/components/business/dashboard-canvas";
import { CodeBlock } from "@/components/code-block";

# DashboardCanvas Business Dashboard Canvas

> 业务组件 / Domain-specific business components for ERP scenarios.

<DashboardCanvas
  widgets={[{ id: "w1", title: "销售额", x: 0, y: 0, w: 6, h: 2 }]}
/>
DashboardCanvas — business dashboard canvas component.

## 何时使用 / When to Use

适用于Business Dashboard Canvas相关场景。

Use when you need a dashboard canvas.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { DashboardCanvas } from "@/components/business/dashboard-canvas"

export function Basic() {
  return <DashboardCanvas />
}`} lang="tsx" />

## API / Props

### DashboardCanvas

| Prop        | Type                     | Default | 说明 / Description                                        |
| ----------- | ------------------------ | ------- | --------------------------------------------------------- |
| `className` | `string`                 | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `widgets`   | `Array<DashboardWidget>` | —       |                                                           |
| `onChange`  | `() => void`             | —       | 值变化回调 <br/> Callback when value changes              |
| ...rest     | `React.HTMLAttributes`   | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-business-dashboard-canvas--docs)。
