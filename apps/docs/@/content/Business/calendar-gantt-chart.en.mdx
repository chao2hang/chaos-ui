import { GanttChart } from "@/components/business/calendar/gantt-chart";
import { CodeBlock } from "@/components/code-block";

# GanttChart

> Domain-specific business components for ERP scenarios.

GanttChart — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a gantt chart.

## Examples

### Basic Usage

<CodeBlock code={`import { GanttChart } from "@/components/business/calendar/gantt-chart"

export function Basic() {
  return <GanttChart />
}`} lang="tsx" />

### States

<CodeBlock
  code={`<GanttChart>Normal</GanttChart>
<GanttChart disabled>Disabled</GanttChart>`}
  lang="tsx"
/>

## API / Props

### GanttChart

| Prop          | Type                          | Default | Description                     |
| ------------- | ----------------------------- | ------- | ------------------------------- |
| `className`   | `string`                      | —       | Custom CSS class                |
| `tasks`       | `GanttTask[]`                 | —       |                                 |
| `onTaskClick` | `() => void`                  | —       |                                 |
| `locale`      | `string`                      | —       |                                 |
| ...rest       | `React.ComponentProps<"div">` | —       | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
