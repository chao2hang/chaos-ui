import { SkuPicker } from "@/components/business/sku-picker"
import { CodeBlock } from "@/components/code-block"

# SkuPicker Sku Picker

> Domain-specific business components for ERP scenarios.

SKU picker — combobox trigger with a filterable popover.
SkuPicker — sku picker component.

## When to Use

Use when you need a sku picker.


## Examples

### Basic Usage

<CodeBlock code={`import { SkuPicker } from "@/components/business/sku-picker"

export function Basic() {
  return <SkuPicker />
}`} lang="tsx" />


## API / Props

### SkuPicker

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `value` | `string` | — | Current value (controlled) |
| `onChange` | `() => void` | — | Callback when value changes |
| `options` | `SkuPickerOption[]` | — | Available options |
| `placeholder` | `string` | — | Placeholder text |
| `searchPlaceholder` | `string` | — |  |
| `emptyText` | `string` | — |  |
| `disabled` | `boolean` | `false` | Whether disabled |
| `clearable` | `boolean` | `true` |  |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


