import { ShippingWayBrowse } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# ShippingWayBrowse Shipping Way Browse

> Domain-specific business components for ERP scenarios.

ShippingWayBrowse — a business component for ERP / enterprise scenarios.

## When to Use

Use ShippingWayBrowse when you need: ShippingWayBrowse: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { ShippingWayBrowse } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <ShippingWayBrowse />
}`} lang="tsx" />

## API / Props

### ShippingWayBrowse

| Prop                | Type                      | Default | Description                       |
| ------------------- | ------------------------- | ------- | --------------------------------- |
| `className`         | `string`                  | —       | Custom CSS class                  |
| `open`              | `boolean`                 | —       | Open state (controlled)           |
| `onOpenChange`      | `() => void`              | —       | Callback when open state changes  |
| `onSelect`          | `() => void`              | —       | Selection handler                 |
| `items`             | `ShippingWayBrowseItem[]` | —       | Items / data list                 |
| `title`             | `string`                  | —       | Title                             |
| `searchPlaceholder` | `string`                  | —       |                                   |
| `emptyText`         | `string`                  | —       |                                   |
| ...rest             | `React.HTMLAttributes`    | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
