import { ScreenCapture } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# ScreenCapture

> Domain-specific business components for ERP scenarios.

ScreenCapture — Screen capture and annotation component with getDisplayMedia API.

## When to Use

Use ScreenCapture when you need: Screen capture and annotation component with getDisplayMedia API. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { ScreenCapture } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <ScreenCapture />
}`} lang="tsx" />

## API / Props

### ScreenCapture

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
