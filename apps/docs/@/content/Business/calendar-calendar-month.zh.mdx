import { CalendarMonth } from "@/components/business/calendar/calendar-month";
import { CodeBlock } from "@/components/code-block";

# CalendarMonth 日历月视图

> 业务组件

日历月视图 — 面向 ERP/企业场景的业务组件。

## 何时使用

需要日历月视图功能时使用本组件。


## 代码示例

### 基础用法

<CodeBlock code={`import { CalendarMonth } from "@/components/business/calendar/calendar-month"

export function Basic() {
  return <CalendarMonth />
}`} lang="tsx" />

### 状态

<CodeBlock
  code={`<CalendarMonth>Normal</CalendarMonth>
<CalendarMonth disabled>Disabled</CalendarMonth>`}
  lang="tsx"
/>

## API / Props

### CalendarMonth

| Prop | Type | Default | 说明 |
| -------------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string` | — | 自定义样式类名 |
| `date`         | `Date`                 | —       |                                                           |
| `events`       | `CalendarEvent[]`      | —       |                                                           |
| `onDateChange` | `() => void`           | —       |                                                           |
| `onEventClick` | `() => void`           | —       |                                                           |
| `onDayClick`   | `() => void`           | —       |                                                           |
| `locale`       | `string`               | `zh-CN` |                                                           |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-calendar-calendar-month--docs)。

