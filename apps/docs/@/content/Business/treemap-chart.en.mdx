import { TreemapChart } from "@chaos_team/chaos-ui/business"
import { CodeBlock } from "@/components/code-block"

# TreemapChart Treemap Chart

> Domain-specific business components for ERP scenarios.

TreemapChart — treemap chart component.

## When to Use

Use when you need a treemap chart.


## Examples

### Basic Usage

<CodeBlock code={`import { TreemapChart } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <TreemapChart />
}`} lang="tsx" />


## API / Props

### TreemapChart

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `data` | `Array<T>` | — | Tiles to render. |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


