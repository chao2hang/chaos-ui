import { ChatStreamingText } from "@/components/business/chat-streaming-text"
import { CodeBlock } from "@/components/code-block"

# ChatStreamingText Chat Streaming Text

> 业务组件

Chat Streaming Text — 面向 ERP/企业场景的业务组件。

## 何时使用

适用于Chat Streaming Text相关场景。


## 代码示例

### 基础用法

<CodeBlock code={`import { ChatStreamingText } from "@/components/business/chat-streaming-text"

export function Basic() {
  return <ChatStreamingText />
}`} lang="tsx" />


## API / Props

### ChatStreamingText

| Prop | Type | Default | 说明 |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 |
| `chunks` | `string[]` | — |  |
| `isStreaming` | `boolean` | `false` |  |
| `speed` | `number` | `30` |  |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-chatstreamingtext--docs)。

