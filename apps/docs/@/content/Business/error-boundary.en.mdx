import {} from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# ErrorBoundary

> Domain-specific business components for ERP scenarios.

ErrorBoundary — a business component for ERP / enterprise scenarios.

## When to Use

Use ErrorBoundary when you need: ErrorBoundary: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { ErrorBoundary } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <ErrorBoundary />
}`} lang="tsx" />

## API / Props

### ErrorBoundary

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| `fallback`  | `(...args) => unknown` | —       |                                   |
| `onError`   | `() => void`           | —       |                                   |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
