import { EquipmentCard } from "@/components/business/equipment-card";
import { CodeBlock } from "@/components/code-block";

# EquipmentCard

> Domain-specific business components for ERP scenarios.

EquipmentCard — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need an equipment card.

## Examples

### Basic Usage

<CodeBlock code={`import { EquipmentCard } from "@/components/business/equipment-card"

export function Basic() {
  return <EquipmentCard />
}`} lang="tsx" />

## API / Props

### EquipmentCard

| Prop              | Type                                       | Default  | Description                       |
| ----------------- | ------------------------------------------ | -------- | --------------------------------- |
| `className`       | `string`                                   | —        | Custom CSS class                  |
| `name`            | `string`                                   | —        | Form field name                   |
| `model`           | `string`                                   | —        |                                   |
| `serialNumber`    | `string`                                   | —        |                                   |
| `status`          | `"normal" \| "maintenance" \| "fault" ...` | `normal` | Status                            |
| `location`        | `string`                                   | —        |                                   |
| `lastMaintenance` | `string`                                   | —        |                                   |
| ...rest           | `React.HTMLAttributes`                     | —        | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
