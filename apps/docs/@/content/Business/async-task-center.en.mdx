import { AsyncTaskCenter } from "@chaos_team/chaos-ui/business"
import { CodeBlock } from "@/components/code-block"

# AsyncTaskCenter

> Domain-specific business components for ERP scenarios.

AsyncTaskCenter — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need an async task center.


## Examples

### Basic Usage

<CodeBlock code={`import { AsyncTaskCenter } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <AsyncTaskCenter />
}`} lang="tsx" />


## API / Props

### AsyncTaskCenter

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `tasks` | `AsyncTask[]` | — |  |
| `onRetry` | `() => void` | — |  |
| `onDelete` | `() => void` | — |  |
| ...rest | `React.ComponentProps<"div">` | — | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


