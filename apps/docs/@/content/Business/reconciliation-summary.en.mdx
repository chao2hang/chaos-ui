import { ReconciliationSummary } from "@/components/business/reconciliation-summary"
import { CodeBlock } from "@/components/code-block"

# ReconciliationSummary Reconciliation Summary

> Domain-specific business components for ERP scenarios.

ReconciliationSummary — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a reconciliation summary.


## Examples

### Basic Usage

<CodeBlock code={`import { ReconciliationSummary } from "@/components/business/reconciliation-summary"

export function Basic() {
  return <ReconciliationSummary />
}`} lang="tsx" />


## API / Props

### ReconciliationSummary

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `totalAmount` | `number` | — |  |
| `matchedAmount` | `number` | — |  |
| `unmatchedAmount` | `number` | — |  |
| `matchedCount` | `number` | — |  |
| `unmatchedCount` | `number` | — |  |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


