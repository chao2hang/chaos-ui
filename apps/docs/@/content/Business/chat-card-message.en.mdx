import { ChatCardMessage } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# ChatCardMessage Chat Card Message

> Domain-specific business components for ERP scenarios.

ChatCardMessage — a business component for ERP / enterprise scenarios.

## When to Use

Use ChatCardMessage when you need: ChatCardMessage: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { ChatCardMessage } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <ChatCardMessage />
}`} lang="tsx" />

## API / Props

### ChatCardMessage

| Prop          | Type                   | Default | Description                       |
| ------------- | ---------------------- | ------- | --------------------------------- |
| `className`   | `string`               | —       | Custom CSS class                  |
| `title`       | `string`               | —       | Title                             |
| `description` | `string`               | —       | Description text                  |
| `thumbnail`   | `string`               | —       |                                   |
| `metadata`    | `Array<T>`             | —       |                                   |
| `actions`     | `React.ReactNode`      | —       |                                   |
| ...rest       | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
