import { ChatImageGallery } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# ChatImageGallery Chat Image Gallery

> 业务组件

Chat Image Gallery — 面向 ERP/企业场景的业务组件。

## 何时使用

需要上传、预览或管理附件与媒体时使用。（组件：Chat Image Gallery）

## 代码示例

### 基础用法

<CodeBlock code={`import { ChatImageGallery } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <ChatImageGallery />
}`} lang="tsx" />

## API / Props

### ChatImageGallery

| Prop        | Type                   | Default | 说明              |
| ----------- | ---------------------- | ------- | ----------------- |
| `className` | `string`               | —       | 自定义样式类名    |
| `images`    | `Array<T>`             | —       |                   |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](http://localhost:6006/?path=/docs/business-chatimagegallery--docs)。
