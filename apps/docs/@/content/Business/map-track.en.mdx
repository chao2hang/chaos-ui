import { MapTrack } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# MapTrack

> Domain-specific business components for ERP scenarios.

MapTrack — a business component for ERP / enterprise scenarios.

## When to Use

Use MapTrack when you need: MapTrack: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { MapTrack } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <MapTrack />
}`} lang="tsx" />

## API / Props

### MapTrack

| Prop        | Type                   | Default   | Description                                                  |
| ----------- | ---------------------- | --------- | ------------------------------------------------------------ |
| `className` | `string`               | —         | Custom CSS class                                             |
| `map`       | `AMap.Map \| null`     | —         | AMap instance from MapView's `onReady`.                      |
| `points`    | `MapTrackPoint[]`      | —         | Track point array in chronological order.                    |
| `playing`   | `boolean`              | `false`   | Whether the track animation is playing.                      |
| `speed`     | `number`               | `10`      | Playback speed multiplier. Default: 10 (10 points per tick). |
| `color`     | `string`               | `#1677FF` | Track line color. Default: "#1677FF".                        |
| `onStart`   | `() => void`           | —         | Called when playback starts.                                 |
| `onEnd`     | `() => void`           | —         | Called when playback ends (reaches last point).              |
| ...rest     | `React.HTMLAttributes` | —         | Inherits standard HTML attributes                            |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
