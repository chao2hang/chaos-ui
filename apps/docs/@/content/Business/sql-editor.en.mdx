import { SqlEditor } from "@/components/business/sql-editor"
import { CodeBlock } from "@/components/code-block"

# SqlEditor Sql Editor

> Domain-specific business components for ERP scenarios.

SqlEditor — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a sql editor.


## Examples

### Basic Usage

<CodeBlock code={`import { SqlEditor } from "@/components/business/sql-editor"

export function Basic() {
  return <SqlEditor />
}`} lang="tsx" />


## API / Props

### SqlEditor

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `value` | `string` | — | SQL content (controlled). |
| `onChange` | `() => void` | — | Called on every change. |
| `dialect` | `SQLDialect` | `standard` | SQL dialect. Defaults to `"standard"`. |
| `placeholder` | `string` | — | Placeholder text. |
| `readOnly` | `boolean` | — | Whether the editor is read-only. |
| `minHeight` | `number` | `200` | Minimum height. |
| `maxHeight` | `number` | — | Maximum height. |
| `onFocus` | `() => void` | — | Called when the editor receives focus. |
| `onBlur` | `() => void` | — | Called when the editor loses focus. |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


