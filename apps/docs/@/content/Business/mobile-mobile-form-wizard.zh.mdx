import { MobileFormWizard } from "@chaos_team/chaos-ui/mobile";
import { CodeBlock } from "@/components/code-block";

# MobileFormWizard Mobile Mobile Form Wizard

> 业务组件

Mobile Mobile Form Wizard — 面向 ERP/企业场景的业务组件。

## 何时使用

适用于Mobile Mobile Form Wizard相关场景。

## 代码示例

### 基础用法

<CodeBlock code={`import { MobileFormWizard } from "@chaos_team/chaos-ui/mobile"

export function Basic() {
  return <MobileFormWizard />
}`} lang="tsx" />

## API / Props

### MobileFormWizard

| Prop          | Type                   | Default | 说明              |
| ------------- | ---------------------- | ------- | ----------------- |
| `className`   | `string`               | —       | 自定义样式类名    |
| `steps`       | `{`                    | —       |                   |
| `title`       | `string`               | —       | 标题              |
| `description` | `string`               | —       | 描述文本          |
| `validate`    | `(...args) => unknown` | —       |                   |
| `onComplete`  | `() => void`           | —       |                   |
| ...rest       | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-mobile-mobile-form-wizard--docs)。
