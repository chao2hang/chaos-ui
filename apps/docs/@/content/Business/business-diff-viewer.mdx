import { DiffViewer } from "@/components/business/diff-viewer";
import { CodeBlock } from "@/components/code-block";

# DiffViewer Business Diff Viewer

> 业务组件 / Domain-specific business components for ERP scenarios.

Business Diff Viewer — 面向 ERP/企业场景的业务组件。
DiffViewer — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Business Diff Viewer相关场景。

Use when you need a diff viewer.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { DiffViewer } from "@/components/business/diff-viewer"

export function Basic() {
  return <DiffViewer />
}`} lang="tsx" />

## API / Props

### DiffViewer

| Prop         | Type                          | Default  | 说明 / Description                                          |
| ------------ | ----------------------------- | -------- | ----------------------------------------------------------- |
| `className`  | `string`                      | —        | 自定义样式类名 <br/> Custom CSS class                       |
| `left`       | `string`                      | —        |                                                             |
| `right`      | `string`                      | —        |                                                             |
| `leftTitle`  | `string`                      | `修改前` |                                                             |
| `rightTitle` | `string`                      | `修改后` |                                                             |
| ...rest      | `React.ComponentProps<"div">` | —        | 透传 `<div>` 原生属性 <br/> Inherits all `<div>` attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-business-diff-viewer--docs)。
