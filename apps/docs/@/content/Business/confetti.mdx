import { Confetti } from "@/components/business/confetti";
import { CodeBlock } from "@/components/code-block";

# Confetti 彩带特效

> 业务组件 / Domain-specific business components for ERP scenarios.

彩带特效 — 面向 ERP/企业场景的业务组件。
Confetti — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

需要彩带特效功能时使用本组件。

Use when you need a confetti.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { Confetti } from "@/components/business/confetti"

export function Basic() {
  return <Confetti />
}`} lang="tsx" />

## API / Props

### Confetti

| Prop         | Type                   | Default | 说明 / Description                                        |
| ------------ | ---------------------- | ------- | --------------------------------------------------------- |
| `className`  | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `trigger`    | `number`               | —       |                                                           |
| `count`      | `number`               | `120`   |                                                           |
| `duration`   | `number`               | `3000`  | 持续时间/展示时长(毫秒) <br/> Duration in milliseconds    |
| `onComplete` | `() => void`           | —       |                                                           |
| ...rest      | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-confetti--docs)。
