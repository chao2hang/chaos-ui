import { ProductBrowse } from "@chaos_team/chaos-ui/business"
import { CodeBlock } from "@/components/code-block"

# ProductBrowse Product Browse

> Domain-specific business components for ERP scenarios.

ProductBrowse — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a product browse.


## Examples

### Basic Usage

<CodeBlock code={`import { ProductBrowse } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <ProductBrowse />
}`} lang="tsx" />


## API / Props

### ProductBrowse

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `open` | `boolean` | — | Open state (controlled) |
| `onOpenChange` | `() => void` | — | Callback when open state changes |
| `onSelect` | `() => void` | — | Single-select emits the chosen item; multi-select emits the last toggled item. |
| `multiple` | `boolean` | `false` | Enable multi-select (default false). / 多选 |
| `items` | `ProductBrowseItem[]` | — | Items / data list |
| `title` | `string` | — | Title |
| `searchPlaceholder` | `string` | — |  |
| `emptyText` | `string` | — |  |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


