import { FormField } from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# FormField

> Domain-specific business components for ERP scenarios.

FormField — a business component for ERP / enterprise scenarios.

## When to Use

Use FormField when you need: FormField: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { FormField } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <FormField />
}`} lang="tsx" />

## API / Props

### FormField

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **无障碍**: 组件遵循 WAI-ARIA 最佳实践。
