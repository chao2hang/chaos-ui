import { LabelDesigner } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# LabelDesigner Label Designer

> Domain-specific business components for ERP scenarios.

LabelDesigner — a business component for ERP / enterprise scenarios.

## When to Use

Use LabelDesigner when you need: LabelDesigner: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { LabelDesigner } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <LabelDesigner />
}`} lang="tsx" />

## API / Props

### LabelDesigner

| Prop             | Type                   | Default | Description                       |
| ---------------- | ---------------------- | ------- | --------------------------------- |
| `className`      | `string`               | —       | Custom CSS class                  |
| `fields`         | `LabelField[]`         | —       | Label fields.                     |
| `onFieldsChange` | `() => void`           | —       | On fields change.                 |
| `labelSize`      | `LabelSize`            | —       | Label size preset.                |
| `sizePresets`    | `LabelSize[]`          | —       | Available size presets.           |
| `onSizeChange`   | `() => void`           | —       | On size change.                   |
| `onPrint`        | `() => void`           | —       | Print handler.                    |
| `readOnly`       | `boolean`              | `false` | Read-only mode.                   |
| ...rest          | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
