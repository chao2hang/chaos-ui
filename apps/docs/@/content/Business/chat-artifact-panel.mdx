import { ChatArtifactPanel } from "@/components/business/chat-artifact-panel"
import { CodeBlock } from "@/components/code-block"

# ChatArtifactPanel Chat Artifact Panel

> 业务组件 / Domain-specific business components for ERP scenarios.

Chat Artifact Panel — 面向 ERP/企业场景的业务组件。
ChatArtifactPanel — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Chat Artifact Panel相关场景。

Use when you need a chat artifact panel.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { ChatArtifactPanel } from "@/components/business/chat-artifact-panel"

export function Basic() {
  return <ChatArtifactPanel />
}`} lang="tsx" />


## API / Props

### ChatArtifactPanel

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| `title` | `string` | — | 标题 <br/> Title |
| `type` | `string` | — | 类型 <br/> Type |
| `content` | `string` | — | 内容 <br/> Content |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-chatartifactpanel--docs)。
