import { SegmentedControl } from "@/components/ui/segmented-control";
import { CodeBlock } from "@/components/code-block";

# SegmentedControl 分段控制器

> 业务组件

分段控制器 — 面向 ERP/企业场景的业务组件。

## 何时使用

需要分段控制器功能时使用本组件。


## 代码示例

### 基础用法

<CodeBlock code={`import { SegmentedControl } from "@/components/ui/segmented-control"

export function Basic() {
  return <SegmentedControl />
}`} lang="tsx" />

### 状态

<CodeBlock
  code={`<SegmentedControl>Normal</SegmentedControl>
<SegmentedControl disabled>Disabled</SegmentedControl>`}
  lang="tsx"
/>

## API / Props

### SegmentedControl

| Prop | Type | Default | 说明 |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string` | — | 自定义样式类名 |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-segmented-control--docs)。

