import { ChatConversationSearch } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# ChatConversationSearch Chat Conversation Search

> Domain-specific business components for ERP scenarios.

ChatConversationSearch — a business component for ERP / enterprise scenarios.

## When to Use

Use ChatConversationSearch when you need: ChatConversationSearch: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { ChatConversationSearch } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <ChatConversationSearch />
}`} lang="tsx" />

## API / Props

### ChatConversationSearch

| Prop            | Type                   | Default | Description                       |
| --------------- | ---------------------- | ------- | --------------------------------- |
| `className`     | `string`               | —       | Custom CSS class                  |
| `query`         | `string`               | —       |                                   |
| `onQueryChange` | `() => void`           | —       |                                   |
| `results`       | `Array<T>`             | —       |                                   |
| ...rest         | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
