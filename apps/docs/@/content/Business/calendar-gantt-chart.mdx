import { GanttChart } from "@/components/business/calendar/gantt-chart"
import { CodeBlock } from "@/components/code-block"

# GanttChart 甘特图

> 业务组件 / Domain-specific business components for ERP scenarios.

甘特图 — 面向 ERP/企业场景的业务组件。
GanttChart — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

需要甘特图功能时使用本组件。

Use when you need a gantt chart.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { GanttChart } from "@/components/business/calendar/gantt-chart"

export function Basic() {
  return <GanttChart />
}`} lang="tsx" />

### 状态 / States

<CodeBlock code={`<GanttChart>Normal</GanttChart>
<GanttChart disabled>Disabled</GanttChart>`} lang="tsx" />


## API / Props

### GanttChart

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| `tasks` | `GanttTask[]` | — |  |
| `onTaskClick` | `() => void` | — |  |
| `locale` | `string` | — |  |
| ...rest | `React.ComponentProps<"div">` | — | 透传 `<div>` 原生属性 <br/> Inherits all `<div>` attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-calendar-gantt-chart--docs)。
