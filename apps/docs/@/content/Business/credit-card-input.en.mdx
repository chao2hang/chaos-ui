import {
  CreditCardInput,
  CreditCard,
} from "@/components/business/credit-card-input";
import { CodeBlock } from "@/components/code-block";

# CreditCardInput

> Domain-specific business components for ERP scenarios.

CreditCardInput — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a credit card input.

## Examples

### Basic Usage

<CodeBlock code={`import { CreditCardInput, CreditCard } from "@/components/business/credit-card-input"

export function Basic() {
  return (
    <div className="space-y-4">
  <CreditCardInput />
  <CreditCard />
    </div>
  )
}`} lang="tsx" />

## API / Props

### CreditCardInput

| Prop          | Type                   | Default               | Description                       |
| ------------- | ---------------------- | --------------------- | --------------------------------- |
| `className`   | `string`               | —                     | Custom CSS class                  |
| `value`       | `string`               | —                     | Current value (controlled)        |
| `onChange`    | `() => void`           | —                     | Callback when value changes       |
| `placeholder` | `string`               | `0000 0000 0000 0000` | Placeholder text                  |
| `disabled`    | `boolean`              | —                     | Whether disabled                  |
| ...rest       | `React.HTMLAttributes` | —                     | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
