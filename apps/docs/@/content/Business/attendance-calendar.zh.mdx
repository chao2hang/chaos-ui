import { AttendanceCalendar } from "@chaos_team/chaos-ui/business"
import { CodeBlock } from "@/components/code-block"

# AttendanceCalendar Attendance Calendar

> 业务组件

Attendance Calendar — 面向 ERP/企业场景的业务组件。

## 何时使用

适用于Attendance Calendar相关场景。


## 代码示例

### 基础用法

<CodeBlock code={`import { AttendanceCalendar } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <AttendanceCalendar />
}`} lang="tsx" />


## API / Props

### AttendanceCalendar

| Prop | Type | Default | 说明 |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 |
| `year` | `number` | — |  |
| `month` | `number` | — |  |
| `records` | `AttendanceRecord[]` | — |  |
| `onMonthChange` | `() => void` | — |  |
| `onDayClick` | `() => void` | — |  |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-attendancecalendar--docs)。

