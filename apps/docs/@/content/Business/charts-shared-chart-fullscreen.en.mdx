import { CodeBlock } from "@/components/code-block";

# ChartFullscreen

> Domain-specific business components for ERP scenarios.

ChartFullscreen — a business component for ERP / enterprise scenarios.

## When to Use

Use ChartsSharedChartFullscreen when this capability is required. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock
  code={`export function Basic() {
  return <ChartFullscreenButton />
}`}
  lang="tsx"
/>

## API / Props

### ChartFullscreenButton

| Prop        | Type                                   | Default | Description                       |
| ----------- | -------------------------------------- | ------- | --------------------------------- |
| `className` | `string`                               | —       | Custom CSS class                  |
| `target`    | `React.RefObject<HTMLElement \| null>` | —       | Link target                       |
| ...rest     | `React.HTMLAttributes`                 | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
