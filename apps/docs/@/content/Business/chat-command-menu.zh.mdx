import { ChatCommandMenu } from "@/components/business/chat-command-menu"
import { CodeBlock } from "@/components/code-block"

# ChatCommandMenu Chat Command Menu

> 业务组件

Chat Command Menu — 面向 ERP/企业场景的业务组件。

## 何时使用

适用于Chat Command Menu相关场景。


## 代码示例

### 基础用法

<CodeBlock code={`import { ChatCommandMenu } from "@/components/business/chat-command-menu"

export function Basic() {
  return <ChatCommandMenu />
}`} lang="tsx" />


## API / Props

### ChatCommandMenu

| Prop | Type | Default | 说明 |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 |
| `commands` | `Array<T>` | — |  |
| `onSelect` | `() => void` | — | 选中回调 |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-chatcommandmenu--docs)。

