import { ChatConversationSearch } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# ChatConversationSearch Chat Conversation Search

> 业务组件 / Domain-specific business components for ERP scenarios.

Chat Conversation Search — 面向 ERP/企业场景的业务组件。
ChatConversationSearch — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Chat Conversation Search相关场景。

Use when you need a chat conversation search.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { ChatConversationSearch } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <ChatConversationSearch />
}`} lang="tsx" />

## API / Props

### ChatConversationSearch

| Prop            | Type                   | Default | 说明 / Description                                        |
| --------------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className`     | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `query`         | `string`               | —       |                                                           |
| `onQueryChange` | `() => void`           | —       |                                                           |
| `results`       | `Array<T>`             | —       |                                                           |
| ...rest         | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-chatconversationsearch--docs)。
