import { MobileForm } from "@chaos_team/chaos-ui/mobile"
import { CodeBlock } from "@/components/code-block"

# MobileForm

> Domain-specific business components for ERP scenarios.

MobileForm — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a mobile form.


## Examples

### Basic Usage

<CodeBlock code={`import { MobileForm } from "@chaos_team/chaos-ui/mobile"

export function Basic() {
  return <MobileForm />
}`} lang="tsx" />


## API / Props

### MobileForm

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `onSubmit` | `() => void` | — | Submit handler |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


