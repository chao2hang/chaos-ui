import { ColorTag } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# ColorTag

> Domain-specific business components for ERP scenarios.

ColorTag — a business component for ERP / enterprise scenarios.

## When to Use

Use ColorTag when you need: ColorTag: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { ColorTag } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <ColorTag />
}`} lang="tsx" />

## Style Variants

**color**

| color    | Description | 默认 |
| -------- | ----------- | ---- |
| `red`    |             |      |
| `orange` |             |      |
| `yellow` |             |      |
| `green`  |             |      |
| `blue`   |             |      |
| `purple` |             |      |
| `pink`   |             |      |
| `gray`   |             | ✓    |

## API / Props

### ColorTag

| Prop        | Type                           | Default | Description                      |
| ----------- | ------------------------------ | ------- | -------------------------------- |
| `className` | `string`                       | —       | Custom CSS class                 |
| `label`     | `string`                       | —       | Label text                       |
| `dot`       | `boolean`                      | `true`  |                                  |
| ...rest     | `React.ComponentProps<"span">` | —       | Inherits all `<span>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
