import { ApplicationForm } from "@chaos_team/chaos-ui/business"
import { CodeBlock } from "@/components/code-block"

# ApplicationForm Application Form

> Domain-specific business components for ERP scenarios.

ApplicationForm — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need an application form.


## Examples

### Basic Usage

<CodeBlock code={`import { ApplicationForm } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <ApplicationForm />
}`} lang="tsx" />


## API / Props

### ApplicationForm

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `type` | `"open" \| "close" \| "change"` | — | Type |
| `onSubmit` | `() => void` | — | Submit handler |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


