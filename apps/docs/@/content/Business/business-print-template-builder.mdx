import { PrintTemplateBuilder } from "@/components/business/print-template-builder";
import { CodeBlock } from "@/components/code-block";

# PrintTemplateBuilder Business Print Template Builder

> 业务组件 / Domain-specific business components for ERP scenarios.

Business Print Template Builder — 面向 ERP/企业场景的业务组件。
PrintTemplateBuilder — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Business Print Template Builder相关场景。

Use when you need a print template builder.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { PrintTemplateBuilder } from "@/components/business/print-template-builder"

export function Basic() {
  return <PrintTemplateBuilder />
}`} lang="tsx" />

## API / Props

### PrintTemplateBuilder

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `template`  | `PrintTemplateModel`   | —       | Template to edit. Defaults to an empty template.          |
| `onChange`  | `() => void`           | —       | Receives the updated template on every edit.              |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-business-print-template-builder--docs)。
