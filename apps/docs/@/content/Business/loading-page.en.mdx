import { LoadingPage, FullPageLoader } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# LoadingPage

> Domain-specific business components for ERP scenarios.

LoadingPage — a business component for ERP / enterprise scenarios.

## When to Use

Use LoadingPage when you need: LoadingPage: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { LoadingPage, FullPageLoader } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return (
    <div className="space-y-4">
  <LoadingPage />
  <FullPageLoader />
    </div>
  )
}`} lang="tsx" />

## API / Props

### LoadingPage

| Prop          | Type                             | Default     | Description                     |
| ------------- | -------------------------------- | ----------- | ------------------------------- |
| `className`   | `string`                         | —           | Custom CSS class                |
| `title`       | `string`                         | `加载中...` | Title                           |
| `description` | `string`                         | —           | Description text                |
| `icon`        | `React.ReactNode`                | —           | Icon element                    |
| `variant`     | `"spinner" \| "dots" \| "pulse"` | `spinner`   | Style variant                   |
| ...rest       | `React.ComponentProps<"div">`    | —           | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
