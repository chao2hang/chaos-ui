import { OaBridge } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# OaBridge Oa Bridge

> 业务组件 / Domain-specific business components for ERP scenarios.

<OaBridge billId="EXP-2026-001" billType="报销单" onSubmit={async (id) => {}} />
OaBridge — oa bridge component.

## 何时使用 / When to Use

适用于Oa Bridge相关场景。

Use when you need an oa bridge.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { OaBridge } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <OaBridge />
}`} lang="tsx" />

## API / Props

### OaBridge

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `billId`    | `string`               | —       |                                                           |
| `billType`  | `string`               | —       |                                                           |
| `onSubmit`  | `() => void`           | —       | 提交回调 <br/> Submit handler                             |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-oabridge--docs)。
