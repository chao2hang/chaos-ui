import { CodeBlock } from "@/components/code-block";

# ScatterChart

> Domain-specific business components for ERP scenarios.

ScatterChart — a business component for ERP / enterprise scenarios.

## When to Use

Use ChartsScatterChart when this capability is required. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock
  code={`export function Basic() {
  return <ScatterChartComp />
}`}
  lang="tsx"
/>

## API / Props

### ScatterChartComp

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
