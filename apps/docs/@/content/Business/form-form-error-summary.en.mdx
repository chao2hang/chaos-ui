import { FormErrorSummary } from "@/components/business/form/form-error-summary";
import { CodeBlock } from "@/components/code-block";

# FormErrorSummary

> Domain-specific business components for ERP scenarios.

FormErrorSummary — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a form error summary.

## Examples

### Basic Usage

<CodeBlock code={`import { FormErrorSummary } from "@/components/business/form/form-error-summary"

export function Basic() {
  return <FormErrorSummary />
}`} lang="tsx" />

## API / Props

### FormErrorSummary

| Prop        | Type                          | Default | Description                     |
| ----------- | ----------------------------- | ------- | ------------------------------- |
| `className` | `string`                      | —       | Custom CSS class                |
| `errors`    | `FormError[]`                 | —       |                                 |
| `onJumpTo`  | `() => void`                  | —       |                                 |
| `title`     | `string`                      | —       | Title                           |
| ...rest     | `React.ComponentProps<"div">` | —       | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
