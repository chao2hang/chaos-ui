import { LineChart } from "@/components/business/chart";
import { CodeBlock } from "@/components/code-block";

# Chart

> Domain-specific business components for ERP scenarios.

Chart — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a chart.

## Examples

### Basic Usage

<CodeBlock code={`import { LineChart, BarChart, AreaChart } from "@/components/business/chart"

export function Basic() {
  return (
    <div className="space-y-4">
  <LineChart />
  <BarChart />
  <AreaChart />
    </div>
  )
}`} lang="tsx" />

## API / Props

### LineChart

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
