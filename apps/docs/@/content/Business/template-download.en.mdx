import { TemplateDownload } from "@chaos_team/chaos-ui/business"
import { CodeBlock } from "@/components/code-block"

# TemplateDownload Template Download

> Domain-specific business components for ERP scenarios.

TemplateDownload — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a template download.


## Examples

### Basic Usage

<CodeBlock code={`import { TemplateDownload } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <TemplateDownload />
}`} lang="tsx" />


## API / Props

### TemplateDownload

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `templates` | `TemplateDownloadItem[]` | — | Templates available for download. |
| `onDownload` | `() => void` | — | Called with the selected template when a link is clicked. |
| `emptyLabel` | `string` | `暂无可用模板` | Text shown when the list is empty. Defaults to "暂无可用模板". |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **无障碍**: 组件遵循 WAI-ARIA 最佳实践。


