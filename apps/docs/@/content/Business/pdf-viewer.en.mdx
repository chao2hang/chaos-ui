import { PDFViewer } from "@/components/business/pdf-viewer";
import { CodeBlock } from "@/components/code-block";

# PdfViewer PDF

> Domain-specific business components for ERP scenarios.

PdfViewer — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a pdf viewer.

## Examples

### Basic Usage

<CodeBlock code={`import { PDFViewer } from "@/components/business/pdf-viewer"

export function Basic() {
  return <PDFViewer />
}`} lang="tsx" />

## API / Props

### PDFViewer

| Prop        | Type                          | Default | Description                     |
| ----------- | ----------------------------- | ------- | ------------------------------- |
| `className` | `string`                      | —       | Custom CSS class                |
| `src`       | `string`                      | —       | Source URL                      |
| `title`     | `string`                      | —       | Title                           |
| ...rest     | `React.ComponentProps<"div">` | —       | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
