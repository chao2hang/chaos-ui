import { ChatAgentStatus } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# ChatAgentStatus Chat Agent Status

> Domain-specific business components for ERP scenarios.

ChatAgentStatus — a business component for ERP / enterprise scenarios.

## When to Use

Use ChatAgentStatus when you need: ChatAgentStatus: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { ChatAgentStatus } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <ChatAgentStatus />
}`} lang="tsx" />

## API / Props

### ChatAgentStatus

| Prop        | Type                                       | Default | Description                       |
| ----------- | ------------------------------------------ | ------- | --------------------------------- |
| `className` | `string`                                   | —       | Custom CSS class                  |
| `status`    | `"idle" \| "thinking" \| "acting" \| "...` | —       | Status                            |
| `label`     | `string`                                   | —       | Label text                        |
| ...rest     | `React.HTMLAttributes`                     | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
