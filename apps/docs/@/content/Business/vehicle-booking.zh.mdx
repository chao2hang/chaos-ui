import { VehicleBooking } from "@/components/business/vehicle-booking";
import { CodeBlock } from "@/components/code-block";

# VehicleBooking Vehicle Booking

> 业务组件

Vehicle Booking — 面向 ERP/企业场景的业务组件。

## 何时使用

适用于Vehicle Booking相关场景。

## 代码示例

### 基础用法

<CodeBlock code={`import { VehicleBooking } from "@/components/business/vehicle-booking"

export function Basic() {
  return <VehicleBooking />
}`} lang="tsx" />

## API / Props

### VehicleBooking

| Prop        | Type                    | Default | 说明                                      |
| ----------- | ----------------------- | ------- | ----------------------------------------- |
| `className` | `string`                | —       | 自定义样式类名                            |
| `vehicles`  | `Vehicle[]`             | —       | Available vehicles.                       |
| `bookings`  | `VehicleBookingEntry[]` | —       | Existing bookings for conflict detection. |
| `onSubmit`  | `() => void`            | —       | Submit handler.                           |
| `onCancel`  | `() => void`            | —       | Cancel handler.                           |
| `readOnly`  | `boolean`               | `false` | Read-only mode.                           |
| ...rest     | `React.HTMLAttributes`  | —       | 透传原生 DOM 属性                         |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-vehiclebooking--docs)。
