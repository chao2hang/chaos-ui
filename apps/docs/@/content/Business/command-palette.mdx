import { CommandPalette } from "@/components/business/command-palette"
import { CodeBlock } from "@/components/code-block"

# CommandPalette 命令面板

> 业务组件 / Domain-specific business components for ERP scenarios.

命令面板 — 面向 ERP/企业场景的业务组件。
CommandPalette — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

需要命令面板功能时使用本组件。

Use when you need a command palette.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { CommandPalette } from "@/components/business/command-palette"

export function Basic() {
  return <CommandPalette />
}`} lang="tsx" />


## API / Props

### CommandPalette

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| `open` | `boolean` | — | 是否打开(受控) <br/> Open state (controlled) |
| `onOpenChange` | `() => void` | — | 打开状态变化回调 <br/> Callback when open state changes |
| `groups` | `CommandGroup[]` | — |  |
| `placeholder` | `string` | `输入命令或搜索...` | 占位文本 <br/> Placeholder text |
| `emptyText` | `string` | `未找到结果` |  |
| `trigger` | `React.ReactNode` | — |  |
| `shortcut` | `string` | `mod+K` |  |
| `showShortcut` | `boolean` | `true` |  |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-command-palette--docs)。
