import { AttachmentList } from "@/components/business/attachment-list";
import { CodeBlock } from "@/components/code-block";

# AttachmentList Attachment List

> 业务组件

Attachment List — 面向 ERP/企业场景的业务组件。

## 何时使用

适用于Attachment List相关场景。

## 代码示例

### 基础用法

<CodeBlock code={`import { AttachmentList } from "@/components/business/attachment-list"

export function Basic() {
  return <AttachmentList />
}`} lang="tsx" />

## API / Props

### AttachmentList

| Prop          | Type                   | Default | 说明              |
| ------------- | ---------------------- | ------- | ----------------- |
| `className`   | `string`               | —       | 自定义样式类名    |
| `attachments` | `Array<{`              | —       |                   |
| `id`          | `string`               | —       | DOM 元素 id       |
| `name`        | `string`               | —       | 表单字段名        |
| `size`        | `number`               | —       | 组件尺寸          |
| `type`        | `string`               | —       | 类型              |
| `url`         | `string`               | —       |                   |
| `onRemove`    | `() => void`           | —       |                   |
| ...rest       | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-attachmentlist--docs)。
