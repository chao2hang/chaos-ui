import { PasteUpload } from "@/components/business/paste-upload";
import { CodeBlock } from "@/components/code-block";

# PasteUpload Paste Upload

> Domain-specific business components for ERP scenarios.

PasteUpload — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a paste upload.

## Examples

### Basic Usage

<CodeBlock code={`import { PasteUpload } from "@/components/business/paste-upload"

export function Basic() {
  return <PasteUpload />
}`} lang="tsx" />

## API / Props

### PasteUpload

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| `onPaste`   | `() => void`           | —       |                                   |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
