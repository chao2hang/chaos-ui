import { CrudPage } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# CrudPage

> Domain-specific business components for ERP scenarios.

CrudPage — a business component for ERP / enterprise scenarios.

## When to Use

Use CrudPage when you need: CrudPage: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { CrudPage } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <CrudPage />
}`} lang="tsx" />

## API / Props

### CrudPage

| Prop           | Type                          | Default    | Description                     |
| -------------- | ----------------------------- | ---------- | ------------------------------- |
| `className`    | `string`                      | —          | Custom CSS class                |
| `title`        | `string`                      | `数据列表` | Title                           |
| `toolbar`      | `React.ReactNode`             | —          |                                 |
| `columns`      | `Array<{`                     | —          | Column configuration            |
| `key`          | `string`                      | —          |                                 |
| `header`       | `string`                      | —          |                                 |
| `data`         | `Record<string, unknown>[]`   | —          | Data source                     |
| `total`        | `number`                      | `0`        |                                 |
| `page`         | `number`                      | `1`        |                                 |
| `onPageChange` | `() => void`                  | —          |                                 |
| `onCreate`     | `() => void`                  | —          |                                 |
| ...rest        | `React.ComponentProps<"div">` | —          | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
