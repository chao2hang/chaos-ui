import { AuditLog } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# AuditLog

> Domain-specific business components for ERP scenarios.

AuditLog — a business component for ERP / enterprise scenarios.

## When to Use

Use AuditLog when you need: AuditLog: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { AuditLog } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <AuditLog />
}`} lang="tsx" />

## API / Props

### AuditLog

| Prop        | Type                         | Default | Description                    |
| ----------- | ---------------------------- | ------- | ------------------------------ |
| `className` | `string`                     | —       | Custom CSS class               |
| `entries`   | `AuditEntry[]`               | —       |                                |
| ...rest     | `React.ComponentProps<"ol">` | —       | Inherits all `<ol>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
