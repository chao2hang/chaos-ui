import { ChartFullscreenButton } from "@/components/business/charts/shared/chart-fullscreen"
import { CodeBlock } from "@/components/code-block"

# ChartFullscreen 图表全屏

> 业务组件 / Domain-specific business components for ERP scenarios.

图表全屏 — 面向 ERP/企业场景的业务组件。
ChartFullscreen — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

需要图表全屏功能时使用本组件。

Use when you need a chart fullscreen.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { ChartFullscreenButton } from "@/components/business/charts/shared/chart-fullscreen"

export function Basic() {
  return <ChartFullscreenButton />
}`} lang="tsx" />


## API / Props

### ChartFullscreenButton

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| `target` | `React.RefObject<HTMLElement \| null>` | — | 链接打开目标 <br/> Link target |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-charts-shared-chart-fullscreen--docs)。
