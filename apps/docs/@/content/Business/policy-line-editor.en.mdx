import { PolicyLineEditor } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# PolicyLineEditor Policy Line Editor

> Domain-specific business components for ERP scenarios.

<PolicyLineEditor
  rows={[
    {
      id: "1",
      name: "满100减20",
      type: "discount",
      condition: "≥100",
      reward: "-20",
      quota: 1000,
      used: 120,
    },
  ]}
/>
PolicyLineEditor — policy line editor component.

## When to Use

Use PolicyLineEditor when this capability is required. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { PolicyLineEditor } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <PolicyLineEditor />
}`} lang="tsx" />

## API / Props

### PolicyLineEditor

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| `rows`      | `PolicyLine[]`         | —       | Row data                          |
| `onChange`  | `() => void`           | —       | Callback when value changes       |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
