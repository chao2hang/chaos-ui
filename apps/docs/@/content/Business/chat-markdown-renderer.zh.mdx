import { ChatMarkdownRenderer } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# ChatMarkdownRenderer Chat Markdown Renderer

> 业务组件

Chat Markdown Renderer — 面向 ERP/企业场景的业务组件。

## 何时使用

在需要Chat Markdown Renderer时使用：Chat Markdown Renderer — 组件库业务组件，详见 Storybook 与源码。面向 ERP/业务域（单据、选择器、审批、结算等），按业务语义接入。

## 代码示例

### 基础用法

<CodeBlock code={`import { ChatMarkdownRenderer } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <ChatMarkdownRenderer />
}`} lang="tsx" />

## API / Props

### ChatMarkdownRenderer

| Prop        | Type                   | Default | 说明              |
| ----------- | ---------------------- | ------- | ----------------- |
| `className` | `string`               | —       | 自定义样式类名    |
| `content`   | `string`               | —       | 内容              |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](http://localhost:6006/?path=/docs/business-chatmarkdownrenderer--docs)。
