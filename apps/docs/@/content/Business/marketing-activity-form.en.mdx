import { MarketingActivityForm } from "@chaos_team/chaos-ui/business"
import { CodeBlock } from "@/components/code-block"

# MarketingActivityForm Marketing Activity Form

> Domain-specific business components for ERP scenarios.

MarketingActivityForm — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a marketing activity form.


## Examples

### Basic Usage

<CodeBlock code={`import { MarketingActivityForm } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <MarketingActivityForm />
}`} lang="tsx" />


## API / Props

### MarketingActivityForm

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `initial` | `Record<string, unknown>` | — |  |
| `onSubmit` | `() => void` | — | Submit handler |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


