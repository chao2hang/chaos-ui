import { MobileEmptyState } from "@/components/business/mobile-empty-state"
import { CodeBlock } from "@/components/code-block"

# MobileEmptyState 移动端空状态

> 业务组件 / Domain-specific business components for ERP scenarios.

移动端空状态 — 面向 ERP/企业场景的业务组件。
MobileEmptyState — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

需要移动端空状态功能时使用本组件。

Use when you need a mobile empty state.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { MobileEmptyState } from "@/components/business/mobile-empty-state"

export function Basic() {
  return <MobileEmptyState />
}`} lang="tsx" />


## API / Props

### MobileEmptyState

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| `variant` | `string` | — | 样式变体 <br/> Style variant |
| `icon` | `React.ElementType` | — | 图标元素 <br/> Icon element |
| `title` | `string` | — | 标题 <br/> Title |
| `description` | `string` | — | 描述文本 <br/> Description text |
| `action` | `React.ReactNode` | — |  |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-mobile-empty-state--docs)。
