import { BarcodeDisplay } from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# BarcodeDisplay

> Domain-specific business components for ERP scenarios.

BarcodeDisplay — a business component for ERP / enterprise scenarios.

## When to Use

Use BarcodeDisplay when you need: BarcodeDisplay — a components for presenting data and content. from Chaos UI. Use to present lists, status, metrics, and detail content for quick scanning.

## Examples

### Basic Usage

<CodeBlock code={`import { BarcodeDisplay } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <BarcodeDisplay />
}`} lang="tsx" />

## API / Props

### BarcodeDisplay

| Prop        | Type                            | Default   | Description                     |
| ----------- | ------------------------------- | --------- | ------------------------------- |
| `className` | `string`                        | —         | Custom CSS class                |
| `value`     | `string`                        | —         | Current value (controlled)      |
| `format`    | `"CODE128" \| "EAN13" \| "UPC"` | `CODE128` |                                 |
| `width`     | `number`                        | `2`       | Component width                 |
| `height`    | `number`                        | `100`     | Component height                |
| ...rest     | `React.ComponentProps<"div">`   | —         | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
