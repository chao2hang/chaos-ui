import { PivotTable } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# PivotTable

> Domain-specific business components for ERP scenarios.

PivotTable — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a pivot table.

## Examples

### Basic Usage

<CodeBlock code={`import { PivotTable } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <PivotTable />
}`} lang="tsx" />

## API / Props

### PivotTable

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
