import { SwipeActions } from "@/components/mobile/mobile-swipe-actions";
import { CodeBlock } from "@/components/code-block";

# MobileSwipeActions Mobile Mobile Swipe Actions

> 业务组件 / Domain-specific business components for ERP scenarios.

Mobile Mobile Swipe Actions — 面向 ERP/企业场景的业务组件。
MobileSwipeActions — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Mobile Mobile Swipe Actions相关场景。

Use when you need a mobile swipe actions.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { SwipeActions } from "@/components/mobile/mobile-swipe-actions"

export function Basic() {
  return <SwipeActions />
}`} lang="tsx" />

## API / Props

### SwipeActions

| Prop           | Type                          | Default | 说明 / Description                                          |
| -------------- | ----------------------------- | ------- | ----------------------------------------------------------- |
| `className`    | `string`                      | —       | 自定义样式类名 <br/> Custom CSS class                       |
| `onSwipeLeft`  | `() => void`                  | —       |                                                             |
| `onSwipeRight` | `() => void`                  | —       |                                                             |
| `onSwipeUp`    | `() => void`                  | —       |                                                             |
| `onSwipeDown`  | `() => void`                  | —       |                                                             |
| `threshold`    | `number`                      | `80`    |                                                             |
| `disabled`     | `boolean`                     | —       | 是否禁用 <br/> Whether disabled                             |
| `leftAction`   | `{`                           | —       |                                                             |
| `label`        | `string`                      | —       | 标签文本 <br/> Label text                                   |
| `color`        | `string`                      | —       | 颜色主题 <br/> Color theme                                  |
| `onClick`      | `() => void`                  | —       | 点击回调 <br/> Click handler                                |
| `icon`         | `React.ReactNode`             | —       | 图标元素 <br/> Icon element                                 |
| `rightAction`  | `{`                           | —       |                                                             |
| ...rest        | `React.ComponentProps<"div">` | —       | 透传 `<div>` 原生属性 <br/> Inherits all `<div>` attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-mobile-mobile-swipe-actions--docs)。
