import { UserMenu } from "@/components/business/user-menu";
import { CodeBlock } from "@/components/code-block";

# UserMenu Business User Menu

> 业务组件 / Domain-specific business components for ERP scenarios.

Business User Menu — 面向 ERP/企业场景的业务组件。
UserMenu — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Business User Menu相关场景。

Use when you need an user menu.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { UserMenu } from "@/components/business/user-menu"

export function Basic() {
  return <UserMenu />
}`} lang="tsx" />

## API / Props

### UserMenu

| Prop           | Type                           | Default | 说明 / Description                                        |
| -------------- | ------------------------------ | ------- | --------------------------------------------------------- |
| `className`    | `string`                       | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `user`         | `UserMenuUser`                 | —       |                                                           |
| `actions`      | `UserMenuAction[]`             | —       |                                                           |
| `showProfile`  | `boolean`                      | `true`  |                                                           |
| `showSettings` | `boolean`                      | `true`  |                                                           |
| `onProfile`    | `() => void`                   | —       |                                                           |
| `onSettings`   | `() => void`                   | —       |                                                           |
| `onSignOut`    | `() => void`                   | —       |                                                           |
| `align`        | `"start" \| "center" \| "end"` | `end`   | 对齐方式 <br/> Alignment                                  |
| ...rest        | `React.HTMLAttributes`         | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-business-user-menu--docs)。
