import { OrderLineEditor } from "@/components/business/order-line-editor"
import { CodeBlock } from "@/components/code-block"

# OrderLineEditor 订单行编辑器

> 业务组件 / Domain-specific business components for ERP scenarios.

订单行编辑器 — 面向 ERP/企业场景的业务组件。
OrderLineEditor — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

需要订单行编辑器功能时使用本组件。

Use when you need an order line editor.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { OrderLineEditor } from "@/components/business/order-line-editor"

export function Basic() {
  return <OrderLineEditor />
}`} lang="tsx" />


## API / Props

### OrderLineEditor

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| `lines` | `OrderLine[]` | — |  |
| `onChange` | `() => void` | — | 值变化回调 <br/> Callback when value changes |
| ...rest | `React.ComponentProps<"div">` | — | 透传 `<div>` 原生属性 <br/> Inherits all `<div>` attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-orderlineeditor--docs)。
