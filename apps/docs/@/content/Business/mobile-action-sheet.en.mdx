import { CodeBlock } from "@/components/code-block";

# MobileActionSheet Mobile Action Sheet

> Domain-specific business components for ERP scenarios.

MobileActionSheet — mobile action sheet component.

## When to Use

Use MobileActionSheet when this capability is required. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { MobileActionSheet } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <MobileActionSheet />
}`} lang="tsx" />

## API / Props

### MobileActionSheet

| Prop           | Type                   | Default | Description                       |
| -------------- | ---------------------- | ------- | --------------------------------- |
| `className`    | `string`               | —       | Custom CSS class                  |
| `open`         | `boolean`              | —       | Open state (controlled)           |
| `onOpenChange` | `() => void`           | —       | Callback when open state changes  |
| `actions`      | `() => void`           | —       |                                   |
| ...rest        | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
