import { FormErrorSummary } from "@/components/business/form/form-error-summary";
import { CodeBlock } from "@/components/code-block";

# FormErrorSummary 表单错误汇总

> 业务组件

表单错误汇总 — 面向 ERP/企业场景的业务组件。

## 何时使用

需要表单错误汇总功能时使用本组件。

## 代码示例

### 基础用法

<CodeBlock code={`import { FormErrorSummary } from "@/components/business/form/form-error-summary"

export function Basic() {
  return <FormErrorSummary />
}`} lang="tsx" />

## API / Props

### FormErrorSummary

| Prop        | Type                          | Default | 说明                  |
| ----------- | ----------------------------- | ------- | --------------------- |
| `className` | `string`                      | —       | 自定义样式类名        |
| `errors`    | `FormError[]`                 | —       |                       |
| `onJumpTo`  | `() => void`                  | —       |                       |
| `title`     | `string`                      | —       | 标题                  |
| ...rest     | `React.ComponentProps<"div">` | —       | 透传 `<div>` 原生属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-form-form-error-summary--docs)。
