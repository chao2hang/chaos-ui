import { PieChartComp } from "@/components/business/charts/pie-chart";
import { CodeBlock } from "@/components/code-block";

# PieChart

> Domain-specific business components for ERP scenarios.

PieChart — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a pie chart.

## Examples

### Basic Usage

<CodeBlock code={`import { PieChartComp } from "@/components/business/charts/pie-chart"

export function Basic() {
  return <PieChartComp />
}`} lang="tsx" />

## API / Props

### PieChartComp

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
