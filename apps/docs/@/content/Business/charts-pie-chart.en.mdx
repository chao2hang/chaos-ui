import { CodeBlock } from "@/components/code-block";

# PieChart

> Domain-specific business components for ERP scenarios.

PieChart — a business component for ERP / enterprise scenarios.

## When to Use

Use ChartsPieChart when this capability is required. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock
  code={`export function Basic() {
  return <PieChartComp />
}`}
  lang="tsx"
/>

## API / Props

### PieChartComp

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
