import { DateRangePicker } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# DateRangePicker 日期范围选择器

> 业务组件 / Domain-specific business components for ERP scenarios.

日期范围选择器 — 面向 ERP/企业场景的业务组件。
DateRangePicker — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

选择日期范围。

Select a date range.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { DateRangePicker } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <DateRangePicker />
}`} lang="tsx" />

## API / Props

### DateRangePicker

| Prop             | Type                           | Default        | 说明 / Description                                        |
| ---------------- | ------------------------------ | -------------- | --------------------------------------------------------- |
| `className`      | `string`                       | —              | 自定义样式类名 <br/> Custom CSS class                     |
| `value`          | `DateRange`                    | —              | 当前值(受控) <br/> Current value (controlled)             |
| `onChange`       | `() => void`                   | —              | 值变化回调 <br/> Callback when value changes              |
| `placeholder`    | `string`                       | `选择日期范围` | 占位文本 <br/> Placeholder text                           |
| `numberOfMonths` | `number`                       | `2`            |                                                           |
| `disabled`       | `boolean`                      | —              | 是否禁用 <br/> Whether disabled                           |
| `align`          | `"start" \| "center" \| "end"` | `start`        | 对齐方式 <br/> Alignment                                  |
| `presets`        | `boolean`                      | `true`         |                                                           |
| ...rest          | `React.HTMLAttributes`         | —              | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-daterangepicker--docs)。
