import { FormField } from "@/components/business/form-field"
import { CodeBlock } from "@/components/code-block"

# FormField 表单字段

> 业务组件 / Domain-specific business components for ERP scenarios.

表单字段 — 面向 ERP/企业场景的业务组件。
FormField — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

需要表单字段功能时使用本组件。

Use when you need a form field.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { FormField } from "@/components/business/form-field"

export function Basic() {
  return <FormField />
}`} lang="tsx" />


## API / Props

### FormField

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **无障碍**: 组件遵循 WAI-ARIA 最佳实践。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-form-field--docs)。
