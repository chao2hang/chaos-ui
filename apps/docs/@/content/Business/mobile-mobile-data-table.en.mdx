import { MobileDataTable } from "@chaos_team/chaos-ui/mobile";
import { CodeBlock } from "@/components/code-block";

# MobileDataTable Mobile Mobile Data Table

> Domain-specific business components for ERP scenarios.

MobileDataTable — a business component for ERP / enterprise scenarios.

## When to Use

Use MobileMobileDataTable when this capability is required. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { MobileDataTable } from "@chaos_team/chaos-ui/mobile"

export function Basic() {
  return <MobileDataTable />
}`} lang="tsx" />

## API / Props

### MobileDataTable

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
