import { MobileDataTable } from "@/components/mobile/mobile-data-table";
import { CodeBlock } from "@/components/code-block";

# MobileDataTable Mobile Mobile Data Table

> Domain-specific business components for ERP scenarios.

MobileDataTable — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a mobile data table.


## Examples

### Basic Usage

<CodeBlock code={`import { MobileDataTable } from "@/components/mobile/mobile-data-table"

export function Basic() {
  return <MobileDataTable />
}`} lang="tsx" />

## API / Props

### MobileDataTable

| Prop | Type | Default | Description |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string` | — | Custom CSS class |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


