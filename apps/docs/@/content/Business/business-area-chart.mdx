import { AreaChart } from "@/components/business/area-chart";
import { CodeBlock } from "@/components/code-block";

# AreaChart Business Area Chart

> 业务组件 / Domain-specific business components for ERP scenarios.

`tsx <AreaChart data={[10, 40, 25, 60, 45]} labels={["一","二","三","四","五"]} /> ` 面积图(趋势+填充)
AreaChart — business area chart component.

## 何时使用 / When to Use

适用于Business Area Chart相关场景。

Use when you need an area chart.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { AreaChart } from "@/components/business/area-chart"

export function Basic() {
  return <AreaChart />
}`} lang="tsx" />

## API / Props

### AreaChart

| Prop        | Type                   | Default        | 说明 / Description                                        |
| ----------- | ---------------------- | -------------- | --------------------------------------------------------- |
| `className` | `string`               | —              | 自定义样式类名 <br/> Custom CSS class                     |
| `data`      | `number[]`             | —              | Numeric series values, left-to-right.                     |
| `labels`    | `string[]`             | —              | X-axis labels aligned 1:1 with `data`.                    |
| `height`    | `number`               | `180`          | SVG height in px.                                         |
| `color`     | `string`               | `currentColor` | Stroke + fill color.                                      |
| ...rest     | `React.HTMLAttributes` | —              | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-business-area-chart--docs)。
