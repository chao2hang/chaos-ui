import { TreemapChart } from "@/components/business/treemap-chart";
import { CodeBlock } from "@/components/code-block";

# TreemapChart Treemap Chart

> 业务组件

树图(层级占比)

## 何时使用

适用于Treemap Chart相关场景。

## 代码示例

### 基础用法

<CodeBlock code={`import { TreemapChart } from "@/components/business/treemap-chart"

export function Basic() {
  return <TreemapChart />
}`} lang="tsx" />

## API / Props

### TreemapChart

| Prop        | Type                   | Default | 说明              |
| ----------- | ---------------------- | ------- | ----------------- |
| `className` | `string`               | —       | 自定义样式类名    |
| `data`      | `Array<T>`             | —       | Tiles to render.  |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-treemapchart--docs)。
