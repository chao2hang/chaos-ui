import { MasterListTemplate } from "@chaos_team/chaos-ui/business"
import { CodeBlock } from "@/components/code-block"

# MasterListTemplate Master List Template

> 业务组件

<MasterListTemplate title="供应商列表" onCreate={() => {}}> <table /> </MasterListTemplate>

## 何时使用

适用于Master List Template相关场景。


## 代码示例

### 基础用法

<CodeBlock code={`import { MasterListTemplate } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <MasterListTemplate />
}`} lang="tsx" />


## API / Props

### MasterListTemplate

| Prop | Type | Default | 说明 |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 |
| `title` | `string` | `列表` | 标题 |
| `onCreate` | `() => void` | — |  |
| `onSearch` | `() => void` | — |  |
| `searchPlaceholder` | `string` | `搜索...` |  |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-masterlisttemplate--docs)。

