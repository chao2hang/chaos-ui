import { TaskListTable } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# TaskListTable Task List Table

> 业务组件 / Domain-specific business components for ERP scenarios.

<TaskListTable
  tasks={[{ id: "1", title: "对账", status: "进行中", priority: "高" }]}
/>
TaskListTable — task list table component.

## 何时使用 / When to Use

适用于Task List Table相关场景。

Use when you need a task list table.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { TaskListTable } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <TaskListTable />
}`} lang="tsx" />

## API / Props

### TaskListTable

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `tasks`     | `Array<{`              | —       |                                                           |
| `id`        | `string`               | —       | DOM 元素 id <br/> DOM element id                          |
| `title`     | `string`               | —       | 标题 <br/> Title                                          |
| `status`    | `string`               | —       | 状态 <br/> Status                                         |
| `priority`  | `string`               | —       |                                                           |
| `assignee`  | `string`               | —       |                                                           |
| `deadline`  | `string`               | —       |                                                           |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-tasklisttable--docs)。
