import { MobileQrCodeScanner } from "@/components/business/mobile-qrcode-scanner"
import { CodeBlock } from "@/components/code-block"

# MobileQrcodeScanner Mobile Qrcode Scanner

> 业务组件

Mobile Qrcode Scanner — 面向 ERP/企业场景的业务组件。

## 何时使用

适用于Mobile Qrcode Scanner相关场景。


## 代码示例

### 基础用法

<CodeBlock code={`import { MobileQrCodeScanner } from "@/components/business/mobile-qrcode-scanner"

export function Basic() {
  return <MobileQrCodeScanner />
}`} lang="tsx" />


## API / Props

### MobileQrCodeScanner

| Prop | Type | Default | 说明 |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 |
| `onScan` | `() => void` | — |  |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-mobileqrcodescanner--docs)。

