import { ExperimentSummary } from "@/components/business/experiment-summary";
import { CodeBlock } from "@/components/code-block";

# ExperimentSummary 实验摘要

> 业务组件 / Domain-specific business components for ERP scenarios.

实验摘要 — 面向 ERP/企业场景的业务组件。
ExperimentSummary — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

需要实验摘要功能时使用本组件。

Use when you need an experiment summary.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { ExperimentSummary } from "@/components/business/experiment-summary"

export function Basic() {
  return <ExperimentSummary />
}`} lang="tsx" />

## API / Props

### ExperimentSummary

| Prop          | Type                                    | Default   | 说明 / Description                                        |
| ------------- | --------------------------------------- | --------- | --------------------------------------------------------- |
| `className`   | `string`                                | —         | 自定义样式类名 <br/> Custom CSS class                     |
| `name`        | `string`                                | —         | 表单字段名 <br/> Form field name                          |
| `controlRate` | `number`                                | —         |                                                           |
| `variantRate` | `number`                                | —         |                                                           |
| `metric`      | `string`                                | `转化率`  |                                                           |
| `confidence`  | `number`                                | `95`      |                                                           |
| `status`      | `"running" \| "completed" \| "stopped"` | `running` | 状态 <br/> Status                                         |
| ...rest       | `React.HTMLAttributes`                  | —         | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-experiment-summary--docs)。
