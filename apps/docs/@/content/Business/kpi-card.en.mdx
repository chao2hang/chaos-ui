import { KPICard } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# KpiCard KPI

> Domain-specific business components for ERP scenarios.

KpiCard — a business component for ERP / enterprise scenarios.

## When to Use

Use KpiCard when you need: KpiCard: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { KPICard } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <KPICard />
}`} lang="tsx" />

## API / Props

### KPICard

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
