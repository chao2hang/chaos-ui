import { VersionHistory } from "@/components/business/version-history";
import { CodeBlock } from "@/components/code-block";

# VersionHistory 版本历史

> 业务组件

版本历史 — 面向 ERP/企业场景的业务组件。

## 何时使用

需要版本历史功能时使用本组件。

## 代码示例

### 基础用法

<CodeBlock code={`import { VersionHistory } from "@/components/business/version-history"

export function Basic() {
  return <VersionHistory />
}`} lang="tsx" />

## API / Props

### VersionHistory

| Prop        | Type                          | Default | 说明                  |
| ----------- | ----------------------------- | ------- | --------------------- |
| `className` | `string`                      | —       | 自定义样式类名        |
| `entries`   | `VersionEntry[]`              | —       |                       |
| `onRestore` | `() => void`                  | —       |                       |
| `onView`    | `() => void`                  | —       |                       |
| ...rest     | `React.ComponentProps<"div">` | —       | 透传 `<div>` 原生属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-versionhistory--docs)。
