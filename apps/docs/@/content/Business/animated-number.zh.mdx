import { AnimatedNumber } from "@/components/business/animated-number";
import { CodeBlock } from "@/components/code-block";

# AnimatedNumber 动画数字

> 业务组件

动画数字 — 面向 ERP/企业场景的业务组件。

## 何时使用

需要动画数字功能时使用本组件。


## 代码示例

### 基础用法

<CodeBlock code={`import { AnimatedNumber } from "@/components/business/animated-number"

export function Basic() {
  return <AnimatedNumber />
}`} lang="tsx" />

## API / Props

### AnimatedNumber

| Prop | Type | Default | 说明 |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string` | — | 自定义样式类名 |
| `value` | `number` | — | 当前值(受控) |
| `duration` | `number` | `1000` | 持续时间/展示时长(毫秒) |
| `decimals`  | `number`               | `0`     |                                                           |
| `prefix` | `string` | `` | 前缀内容 |
| `suffix` | `string` | `` | 后缀内容 |
| `format`    | `(...args) => unknown` | —       |                                                           |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-animated-number--docs)。

