import { MobileChartFallback } from "@/components/business/mobile-chart-fallback";
import { CodeBlock } from "@/components/code-block";

# MobileChartFallback

> Domain-specific business components for ERP scenarios.

MobileChartFallback — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a mobile chart fallback.

## Examples

### Basic Usage

<CodeBlock code={`import { MobileChartFallback } from "@/components/business/mobile-chart-fallback"

export function Basic() {
  return <MobileChartFallback />
}`} lang="tsx" />

## API / Props

### MobileChartFallback

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
