import { MobilePicker } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# MobilePicker Mobile Picker

> 业务组件

Mobile Picker — 面向 ERP/企业场景的业务组件。

## 何时使用

适用于Mobile Picker相关场景。

## 代码示例

### 基础用法

<CodeBlock code={`import { MobilePicker } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <MobilePicker />
}`} lang="tsx" />

## API / Props

### MobilePicker

| Prop        | Type                   | Default | 说明              |
| ----------- | ---------------------- | ------- | ----------------- |
| `className` | `string`               | —       | 自定义样式类名    |
| `options`   | `Array<T>`             | —       | 可选项列表        |
| `value`     | `string`               | —       | 当前值(受控)      |
| `onChange`  | `() => void`           | —       | 值变化回调        |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-mobilepicker--docs)。
