import { ArApAgingTable } from "@chaos_team/chaos-ui/business"
import { CodeBlock } from "@/components/code-block"

# ArApAgingTable Ar Ap Aging Table

> Domain-specific business components for ERP scenarios.

ArApAgingTable — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need an ar ap aging table.


## Examples

### Basic Usage

<CodeBlock code={`import { ArApAgingTable } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <ArApAgingTable />
}`} lang="tsx" />


## API / Props

### ArApAgingTable

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `entries` | `AgingEntry[]` | — | Entries to display. |
| `asOfDate` | `string` | — | Reference date for aging calculation. Defaults to today. |
| `type` | `"ar" \| "ap"` | `ar` | "ar" for receivable, "ap" for payable. |
| `buckets` | `AgingBucket[]` | — | Custom bucket definitions. |
| `currencySymbol` | `string` | `¥` | Currency symbol for display. |
| `onRowClick` | `() => void` | — | Row click handler. |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


