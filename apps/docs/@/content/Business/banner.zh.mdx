import {
  Alert as Banner,
  AlertTitle,
  AlertDescription,
} from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# Banner 横幅

> 业务组件

横幅 — 面向 ERP/企业场景的业务组件。

## 何时使用

需要banner时使用本组件。用于 ERP/业务域场景（单据、选择器、审批、对账等）；按业务语义接入而不是当纯 UI 积木。

## 代码示例

### 基础用法

<CodeBlock code={`import { Alert as Banner } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <Banner />
}`} lang="tsx" />

### 变体

<CodeBlock
  code={`{/* 展示 variant 变体 */}
<div className="flex flex-wrap items-center gap-3">
  <Banner variant="info">Info</Banner>
  <Banner variant="success">Success</Banner>
  <Banner variant="warning">Warning</Banner>
  <Banner variant="error">Error</Banner>
</div>`}
  lang="tsx"
/>

## 样式变体

**变体**

| Variant   | 说明     | 默认 |
| --------- | -------- | ---- |
| `info`    | 信息提示 | ✓    |
| `success` | 成功状态 |      |
| `warning` | 警告状态 |      |
| `error`   | 错误状态 |      |

## API / Props

### Banner

| Prop          | Type                   | Default | 说明              |
| ------------- | ---------------------- | ------- | ----------------- |
| `className`   | `string`               | —       | 自定义样式类名    |
| `variant`     | `BannerVariant`        | `info`  | 样式变体          |
| `title`       | `string`               | —       | 标题              |
| `description` | `string`               | —       | 描述文本          |
| `action`      | `React.ReactNode`      | —       |                   |
| `closable`    | `boolean`              | `true`  |                   |
| `onClose`     | `() => void`           | —       | 关闭回调          |
| ...rest       | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **CVA 变体**: 通过 `class-variance-authority` 管理样式变体,类型安全。变体对象可导出复用。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](http://localhost:6006/?path=/docs/components-banner--docs)。
