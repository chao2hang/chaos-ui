import { MarkdownViewerBiz } from "@/components/business/markdown-viewer-biz"
import { CodeBlock } from "@/components/code-block"

# MarkdownViewerBiz 业务Markdown

> 业务组件 / Domain-specific business components for ERP scenarios.

业务Markdown — 业务 Markdown 查看器，支持附件列表与下载链接。
MarkdownViewerBiz — Business markdown viewer with attachment support and download links.

## 何时使用 / When to Use

需要业务Markdown功能时使用本组件。

Use when you need a markdownviewerbiz.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { MarkdownViewerBiz } from "@/components/business/markdown-viewer-biz"

export function Basic() {
  return <MarkdownViewerBiz />
}`} lang="tsx" />


## API / Props

### MarkdownViewerBiz

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-markdownviewerbiz--docs)。
