import { SignaturePad } from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# SignaturePad 签名板

> 业务组件

签名板 — 面向 ERP/企业场景的业务组件。

## 何时使用

需要签名板功能时使用本组件。


## 代码示例

### 基础用法

<CodeBlock code={`import { SignaturePad } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <SignaturePad />
}`} lang="tsx" />

## API / Props

### SignaturePad

| Prop | Type | Default | 说明 |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string` | — | 自定义样式类名 |
| `width` | `number` | — | 组件宽度 |
| `height` | `number` | — | 组件高度 |
| `onChange` | `() => void` | — | 值变化回调 |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-signature-pad--docs)。

