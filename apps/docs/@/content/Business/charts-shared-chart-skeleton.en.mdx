import { ChartSkeleton } from "@/components/business/charts/shared/chart-skeleton";
import { CodeBlock } from "@/components/code-block";

# ChartSkeleton

> Domain-specific business components for ERP scenarios.

ChartSkeleton — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a chart skeleton.


## Examples

### Basic Usage

<CodeBlock code={`import { ChartSkeleton } from "@/components/business/charts/shared/chart-skeleton"

export function Basic() {
  return <ChartSkeleton />
}`} lang="tsx" />

## API / Props

### ChartSkeleton

| Prop | Type | Default | Description |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string` | — | Custom CSS class |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


