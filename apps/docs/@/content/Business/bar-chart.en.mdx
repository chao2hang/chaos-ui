import { BarChart } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# BarChart Bar Chart

> Domain-specific business components for ERP scenarios.

BarChart — bar chart component.

## When to Use

Use when you need a bar chart.

## Examples

### Basic Usage

<CodeBlock code={`import { BarChart } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <BarChart />
}`} lang="tsx" />

## API / Props

### BarChart

| Prop          | Type                         | Default    | Description                       |
| ------------- | ---------------------------- | ---------- | --------------------------------- |
| `className`   | `string`                     | —          | Custom CSS class                  |
| `data`        | `Array<T>`                   | —          | Bars to render.                   |
| `orientation` | `"vertical" \| "horizontal"` | `vertical` | Bar orientation.                  |
| `height`      | `number`                     | `200`      | Chart height in px.               |
| ...rest       | `React.HTMLAttributes`       | —          | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
