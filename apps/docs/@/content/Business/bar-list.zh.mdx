import { BarList } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# BarList Bar List

> 业务组件

Bar List — 面向 ERP/企业场景的业务组件。

## 何时使用

需要表格/列表呈现多行业务数据，便于浏览、对比与操作时使用。（组件：Bar List）

## 代码示例

### 基础用法

<CodeBlock code={`import { BarList } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <BarList />
}`} lang="tsx" />

## API / Props

### BarList

| Prop        | Type                   | Default | 说明              |
| ----------- | ---------------------- | ------- | ----------------- |
| `className` | `string`               | —       | 自定义样式类名    |
| `data`      | `Array<T>`             | —       | 数据源            |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](http://localhost:6006/?path=/docs/business-barlist--docs)。
