import { Banner } from "@/components/business/banner";
import { CodeBlock } from "@/components/code-block";

# Banner

> Domain-specific business components for ERP scenarios.

Banner — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a banner.


## Examples

### Basic Usage

<CodeBlock code={`import { Banner } from "@/components/business/banner"

export function Basic() {
  return <Banner />
}`} lang="tsx" />

### Variants

<CodeBlock
  code={`{/* 展示 variant 变体 */}
<div className="flex flex-wrap items-center gap-3">
  <Banner variant="info">Info</Banner>
  <Banner variant="success">Success</Banner>
  <Banner variant="warning">Warning</Banner>
  <Banner variant="error">Error</Banner>
</div>`}
  lang="tsx"
/>

## Style Variants

**Variant**

| Variant | Description | 默认 |
| --------- | ------------------------------ | ---- |
| `info` | Informational style | ✓ |
| `success` | Success style |  |
| `warning` | Warning style |  |
| `error` | Error style |  |

## API / Props

### Banner

| Prop | Type | Default | Description |
| ------------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string` | — | Custom CSS class |
| `variant` | `BannerVariant` | `info` | Style variant |
| `title` | `string` | — | Title |
| `description` | `string` | — | Description text |
| `action`      | `React.ReactNode`      | —       |                                                           |
| `closable`    | `boolean`              | `true`  |                                                           |
| `onClose` | `() => void` | — | Close handler |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


