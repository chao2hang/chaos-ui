import { MobileSheet } from "@/components/business/mobile-sheet"
import { CodeBlock } from "@/components/code-block"

# MobileSheet 移动端面板

> 业务组件 / Domain-specific business components for ERP scenarios.

移动端面板 — 面向 ERP/企业场景的业务组件。
MobileSheet — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

需要移动端面板功能时使用本组件。

Use when you need a mobile sheet.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { MobileSheet } from "@/components/business/mobile-sheet"

export function Basic() {
  return <MobileSheet />
}`} lang="tsx" />


## API / Props

### MobileSheet

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| `title` | `string` | — | 标题 <br/> Title |
| `description` | `string` | — | 描述文本 <br/> Description text |
| `trigger` | `React.ReactNode` | — |  |
| `actions` | `React.ReactNode` | — |  |
| `side` | `"top" \| "right" \| "bottom" \| "left"` | `bottom` |  |
| `open` | `boolean` | — | 是否打开(受控) <br/> Open state (controlled) |
| `onOpenChange` | `() => void` | — | 打开状态变化回调 <br/> Callback when open state changes |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-mobile-sheet--docs)。
