import { DictSelect } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# DictSelect 字典选择

> 业务组件

字典选择 — 面向 ERP/企业场景的业务组件。

## 何时使用

需要字典选择功能时使用本组件。

## 代码示例

### 基础用法

<CodeBlock code={`import { DictSelect } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <DictSelect />
}`} lang="tsx" />

## API / Props

### DictSelect

| Prop            | Type                   | Default  | 说明              |
| --------------- | ---------------------- | -------- | ----------------- |
| `className`     | `string`               | —        | 自定义样式类名    |
| `dictCode`      | `string`               | —        |                   |
| `options`       | `DictOption[]`         | —        | 可选项列表        |
| `value`         | `string`               | —        | 当前值(受控)      |
| `onValueChange` | `() => void`           | —        |                   |
| `placeholder`   | `string`               | `请选择` | 占位文本          |
| ...rest         | `React.HTMLAttributes` | —        | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-dictselect--docs)。
