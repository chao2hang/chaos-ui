import { ApprovalTimeline } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# ApprovalTimeline

> Domain-specific business components for ERP scenarios.

ApprovalTimeline — a business component for ERP / enterprise scenarios.

## When to Use

Use ApprovalTimeline when you need: ApprovalTimeline: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { ApprovalTimeline } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <ApprovalTimeline />
}`} lang="tsx" />

## API / Props

### ApprovalTimeline

| Prop        | Type                         | Default | Description                    |
| ----------- | ---------------------------- | ------- | ------------------------------ |
| `className` | `string`                     | —       | Custom CSS class               |
| `steps`     | `ApprovalStep[]`             | —       |                                |
| ...rest     | `React.ComponentProps<"ol">` | —       | Inherits all `<ol>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
