import { ApprovalTimeline } from "@/components/business/approval-timeline"
import { CodeBlock } from "@/components/code-block"

# ApprovalTimeline

> Domain-specific business components for ERP scenarios.

ApprovalTimeline — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need an approval timeline.


## Examples

### Basic Usage

<CodeBlock code={`import { ApprovalTimeline } from "@/components/business/approval-timeline"

export function Basic() {
  return <ApprovalTimeline />
}`} lang="tsx" />


## API / Props

### ApprovalTimeline

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `steps` | `ApprovalStep[]` | — |  |
| ...rest | `React.ComponentProps<"ol">` | — | Inherits all `<ol>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


