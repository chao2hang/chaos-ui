import { Watermark } from "@chaos_team/chaos-ui/ui";
import { CodeBlock } from "@/components/code-block";

# Watermark 水印

> 业务组件 / Domain-specific business components for ERP scenarios.

在内容上叠加指定文字/图片水印,用于版权标识。
Overlays specified text/image watermark on content for copyright.

## 何时使用 / When to Use

当你需要「水印」相关能力时使用：在内容上叠加指定文字/图片水印,用于版权标识。用于展示列表、状态、统计与内容详情；让用户先看懂数据再操作。

Use Watermark when you need: Overlays specified text/image watermark on content for copyright. Use to present lists, status, metrics, and detail content for quick scanning.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { Watermark } from "@chaos_team/chaos-ui/ui"

export function Basic() {
  return <Watermark />
}`} lang="tsx" />

## API / Props

### Watermark

| Prop        | Type                   | Default    | 说明 / Description                                        |
| ----------- | ---------------------- | ---------- | --------------------------------------------------------- |
| `className` | `string`               | —          | 自定义样式类名 <br/> Custom CSS class                     |
| `text`      | `string`               | `内部资料` |                                                           |
| `image`     | `string`               | —          |                                                           |
| `rotate`    | `number`               | `-22`      |                                                           |
| `gap`       | `[number, number]`     | —          | 间距 <br/> Gap between items                              |
| `fontSize`  | `number`               | `14`       |                                                           |
| `fullPage`  | `boolean`              | `true`     |                                                           |
| `zIndex`    | `number`               | `1000`     |                                                           |
| `opacity`   | `number`               | `0.08`     |                                                           |
| `color`     | `string`               | `#000`     | 颜色主题 <br/> Color theme                                |
| ...rest     | `React.HTMLAttributes` | —          | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](http://localhost:6006/?path=/docs/components-watermark--docs)。
