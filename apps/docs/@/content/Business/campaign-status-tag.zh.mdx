import { CampaignStatusTag, StatusMap } from "@chaos_team/chaos-ui/business"
import { CodeBlock } from "@/components/code-block"

# CampaignStatusTag 活动状态标签

> 业务组件

活动状态标签 — 面向 ERP/企业场景的业务组件。

## 何时使用

需要活动状态标签功能时使用本组件。


## 代码示例

### 基础用法

<CodeBlock code={`import { CampaignStatusTag, StatusMap } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return (
    <div className="space-y-4">
  <CampaignStatusTag />
  <StatusMap />
    </div>
  )
}`} lang="tsx" />


## API / Props

### CampaignStatusTag

| Prop | Type | Default | 说明 |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 |
| `status` | `string` | — | 状态 |
| `label` | `string` | — | 标签文本 |
| ...rest | `React.ComponentProps<"span">` | — | 透传 `<span>` 原生属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **原生属性**: 继承所有 `<span>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-campaignstatustag--docs)。

