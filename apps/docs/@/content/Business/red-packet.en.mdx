import { RedPacket } from "@chaos_team/chaos-ui/business"
import { CodeBlock } from "@/components/code-block"

# RedPacket Red Packet

> Domain-specific business components for ERP scenarios.

RedPacket — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a red packet.


## Examples

### Basic Usage

<CodeBlock code={`import { RedPacket } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <RedPacket />
}`} lang="tsx" />


## API / Props

### RedPacket

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `label` | `string` | — | Amount or greeting text. |
| `subtitle` | `string` | — | Subtitle. |
| `opened` | `boolean` | — | Whether the red packet has been opened. |
| `onOpen` | `() => void` | — | Called when user clicks to open. |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


