import { PieChart } from "@/components/business/pie-chart"
import { CodeBlock } from "@/components/code-block"

# PieChart Pie Chart

> 业务组件

Pie Chart — 面向 ERP/企业场景的业务组件。

## 何时使用

适用于Pie Chart相关场景。


## 代码示例

### 基础用法

<CodeBlock code={`import { PieChart } from "@/components/business/pie-chart"

export function Basic() {
  return <PieChart />
}`} lang="tsx" />


## API / Props

### PieChart

| Prop | Type | Default | 说明 |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 |
| `data` | `Array<T>` | — | Slices to render. |
| `size` | `number` | — | Pie pixel size. |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-piechart--docs)。

