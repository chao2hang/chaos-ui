import { ConnectionStatus } from "@/components/business/connection-status";
import { CodeBlock } from "@/components/code-block";

# ConnectionStatus

> Domain-specific business components for ERP scenarios.

ConnectionStatus — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a connection status.


## Examples

### Basic Usage

<CodeBlock code={`import { ConnectionStatus } from "@/components/business/connection-status"

export function Basic() {
  return <ConnectionStatus />
}`} lang="tsx" />

## API / Props

### ConnectionStatus

| Prop | Type | Default | Description |
| ---------------- | --------------------------------- | -------- | ----------------------------------------------------------- |
| `className` | `string` | — | Custom CSS class |
| `onSync`         | `() => void`                      | —        |                                                             |
| `showWhenOnline` | `boolean`                         | `false`  |                                                             |
| `variant` | `"banner" \| "inline" \| "toast"` | `banner` | Style variant |
| ...rest | `React.ComponentProps<"div">` | — | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


