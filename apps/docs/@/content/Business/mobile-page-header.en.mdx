import { MobilePageHeader } from "@chaos_team/chaos-ui/mobile";
import { CodeBlock } from "@/components/code-block";

# MobilePageHeader

> Domain-specific business components for ERP scenarios.

MobilePageHeader — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a mobile page header.

## Examples

### Basic Usage

<CodeBlock code={`import { MobilePageHeader } from "@chaos_team/chaos-ui/mobile"

export function Basic() {
  return <MobilePageHeader />
}`} lang="tsx" />

## API / Props

### MobilePageHeader

| Prop              | Type                                 | Default | Description                       |
| ----------------- | ------------------------------------ | ------- | --------------------------------- |
| `className`       | `string`                             | —       | Custom CSS class                  |
| `title`           | `string`                             | —       | Title                             |
| `description`     | `string`                             | —       | Description text                  |
| `breadcrumbItems` | `{ label: string; href?: string }[]` | —       |                                   |
| `actions`         | `React.ReactNode`                    | —       |                                   |
| `onBack`          | `() => void`                         | —       |                                   |
| `onMenu`          | `() => void`                         | —       |                                   |
| ...rest           | `React.HTMLAttributes`               | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
