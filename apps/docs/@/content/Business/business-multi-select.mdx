import { MultiSelect } from "@/components/business/multi-select";
import { CodeBlock } from "@/components/code-block";

# MultiSelect Business Multi Select

> 业务组件 / Domain-specific business components for ERP scenarios.

Business Multi Select — 面向 ERP/企业场景的业务组件。
MultiSelect — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Business Multi Select相关场景。

Use when you need a multi select.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { MultiSelect } from "@/components/business/multi-select"

export function Basic() {
  return <MultiSelect />
}`} lang="tsx" />

## API / Props

### MultiSelect

| Prop                | Type                   | Default            | 说明 / Description                                        |
| ------------------- | ---------------------- | ------------------ | --------------------------------------------------------- |
| `className`         | `string`               | —                  | 自定义样式类名 <br/> Custom CSS class                     |
| `options`           | `MultiSelectOption[]`  | —                  | 可选项列表 <br/> Available options                        |
| `value`             | `string[]`             | —                  | 当前值(受控) <br/> Current value (controlled)             |
| `onChange`          | `() => void`           | —                  | 值变化回调 <br/> Callback when value changes              |
| `placeholder`       | `string`               | `Select...`        | 占位文本 <br/> Placeholder text                           |
| `searchPlaceholder` | `string`               | `Search...`        |                                                           |
| `emptyText`         | `string`               | `No results found` |                                                           |
| `disabled`          | `boolean`              | —                  | 是否禁用 <br/> Whether disabled                           |
| `maxCount`          | `number`               | `3`                |                                                           |
| `maxSelected`       | `number`               | —                  |                                                           |
| `clearable`         | `boolean`              | `true`             |                                                           |
| ...rest             | `React.HTMLAttributes` | —                  | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-business-multi-select--docs)。
