import { CodeBlock } from "@/components/code-block";

# CurrencyInput

> Domain-specific business components for ERP scenarios.

CurrencyInput — a business component for ERP / enterprise scenarios.

## When to Use

Use CurrencyInput when this capability is required. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock
  code={`export function Basic() {
  return <CurrencyInput />
}`}
  lang="tsx"
/>

## API / Props

### CurrencyInput

| Prop          | Type                   | Default | Description                       |
| ------------- | ---------------------- | ------- | --------------------------------- |
| `className`   | `string`               | —       | Custom CSS class                  |
| `value`       | `number \| undefined`  | —       | Current value (controlled)        |
| `onChange`    | `() => void`           | —       | Callback when value changes       |
| `currency`    | `string`               | `USD`   |                                   |
| `locale`      | `string`               | `en-US` |                                   |
| `placeholder` | `string`               | —       | Placeholder text                  |
| `disabled`    | `boolean`              | —       | Whether disabled                  |
| ...rest       | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
