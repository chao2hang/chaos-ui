import { FeeTypeBrowse } from "@/components/business/fee-type-browse"
import { CodeBlock } from "@/components/code-block"

# FeeTypeBrowse Fee Type Browse

> Domain-specific business components for ERP scenarios.

FeeTypeBrowse — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a fee type browse.


## Examples

### Basic Usage

<CodeBlock code={`import { FeeTypeBrowse } from "@/components/business/fee-type-browse"

export function Basic() {
  return <FeeTypeBrowse />
}`} lang="tsx" />


## API / Props

### FeeTypeBrowse

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `open` | `boolean` | — | Open state (controlled) |
| `onOpenChange` | `() => void` | — | Callback when open state changes |
| `onSelect` | `() => void` | — | Selection handler |
| `items` | `FeeTypeBrowseItem[]` | — | Items / data list |
| `title` | `string` | — | Title |
| `searchPlaceholder` | `string` | — |  |
| `emptyText` | `string` | — |  |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


