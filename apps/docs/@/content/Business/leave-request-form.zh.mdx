import { LeaveRequestForm } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# LeaveRequestForm Leave Request Form

> 业务组件

Leave Request Form — 面向 ERP/企业场景的业务组件。

## 何时使用

需要采集或编辑业务字段，并进入校验/提交链路时使用。（组件：Leave Request Form）

## 代码示例

### 基础用法

<CodeBlock code={`import { LeaveRequestForm } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <LeaveRequestForm />
}`} lang="tsx" />

## API / Props

### LeaveRequestForm

| Prop                | Type                   | Default  | 说明                       |
| ------------------- | ---------------------- | -------- | -------------------------- |
| `className`         | `string`               | —        | 自定义样式类名             |
| `leaveType`         | `LeaveType`            | `annual` | Current leave type         |
| `onLeaveTypeChange` | `() => void`           | —        | Leave type change callback |
| `startDate`         | `string`               | ``       | Start date                 |
| `endDate`           | `string`               | ``       | End date                   |
| `onDateChange`      | `() => void`           | —        | Date change callback       |
| `balances`          | `LeaveBalance[]`       | —        | Available leave balances   |
| `approvers`         | `Approver[]`           | —        | Approver chain             |
| `reason`            | `string`               | ``       | Reason text                |
| `onReasonChange`    | `() => void`           | —        | Reason change callback     |
| `onSubmit`          | `() => void`           | —        | Submit handler             |
| `onCancel`          | `() => void`           | —        | Cancel handler             |
| `readOnly`          | `boolean`              | `false`  | Read-only mode             |
| ...rest             | `React.HTMLAttributes` | —        | 透传原生 DOM 属性          |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](http://localhost:6006/?path=/docs/business-leaverequestform--docs)。
