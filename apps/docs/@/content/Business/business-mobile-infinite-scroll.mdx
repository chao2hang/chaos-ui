import { MobileInfiniteScroll } from "@/components/business/mobile-infinite-scroll";
import { CodeBlock } from "@/components/code-block";

# MobileInfiniteScroll Business Mobile Infinite Scroll

> 业务组件 / Domain-specific business components for ERP scenarios.

Business Mobile Infinite Scroll — 面向 ERP/企业场景的业务组件。
MobileInfiniteScroll — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Business Mobile Infinite Scroll相关场景。

Use when you need a mobile infinite scroll.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { MobileInfiniteScroll } from "@/components/business/mobile-infinite-scroll"

export function Basic() {
  return <MobileInfiniteScroll />
}`} lang="tsx" />

## API / Props

### MobileInfiniteScroll

| Prop         | Type                   | Default | 说明 / Description                                        |
| ------------ | ---------------------- | ------- | --------------------------------------------------------- |
| `className`  | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `onLoadMore` | `() => void`           | —       |                                                           |
| `hasMore`    | `boolean`              | `true`  |                                                           |
| `loading`    | `boolean`              | `false` | 是否加载中 <br/> Loading state                            |
| ...rest      | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-business-mobile-infinite-scroll--docs)。
