import { BudgetOverview } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# BudgetOverview Budget Overview

> Domain-specific business components for ERP scenarios.

BudgetOverview — a business component for ERP / enterprise scenarios.

## When to Use

Use BudgetOverview when you need: BudgetOverview: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { BudgetOverview } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <BudgetOverview />
}`} lang="tsx" />

## API / Props

### BudgetOverview

| Prop         | Type                   | Default | Description                       |
| ------------ | ---------------------- | ------- | --------------------------------- |
| `className`  | `string`               | —       | Custom CSS class                  |
| `total`      | `number`               | —       |                                   |
| `used`       | `number`               | —       |                                   |
| `remaining`  | `number`               | —       |                                   |
| `categories` | `Array<T>`             | —       |                                   |
| ...rest      | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
