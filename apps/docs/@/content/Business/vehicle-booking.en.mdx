import { VehicleBooking } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# VehicleBooking Vehicle Booking

> Domain-specific business components for ERP scenarios.

VehicleBooking — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a vehicle booking.

## Examples

### Basic Usage

<CodeBlock code={`import { VehicleBooking } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <VehicleBooking />
}`} lang="tsx" />

## API / Props

### VehicleBooking

| Prop        | Type                    | Default | Description                               |
| ----------- | ----------------------- | ------- | ----------------------------------------- |
| `className` | `string`                | —       | Custom CSS class                          |
| `vehicles`  | `Vehicle[]`             | —       | Available vehicles.                       |
| `bookings`  | `VehicleBookingEntry[]` | —       | Existing bookings for conflict detection. |
| `onSubmit`  | `() => void`            | —       | Submit handler.                           |
| `onCancel`  | `() => void`            | —       | Cancel handler.                           |
| `readOnly`  | `boolean`               | `false` | Read-only mode.                           |
| ...rest     | `React.HTMLAttributes`  | —       | Inherits standard HTML attributes         |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
