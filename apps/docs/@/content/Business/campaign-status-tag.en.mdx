import {
  CampaignStatusTag,
  StatusMap,
} from "@/components/business/campaign-status-tag";
import { CodeBlock } from "@/components/code-block";

# CampaignStatusTag

> Domain-specific business components for ERP scenarios.

CampaignStatusTag — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a campaign status tag.

## Examples

### Basic Usage

<CodeBlock code={`import { CampaignStatusTag, StatusMap } from "@/components/business/campaign-status-tag"

export function Basic() {
  return (
    <div className="space-y-4">
  <CampaignStatusTag />
  <StatusMap />
    </div>
  )
}`} lang="tsx" />

## API / Props

### CampaignStatusTag

| Prop        | Type                           | Default | Description                      |
| ----------- | ------------------------------ | ------- | -------------------------------- |
| `className` | `string`                       | —       | Custom CSS class                 |
| `status`    | `string`                       | —       | Status                           |
| `label`     | `string`                       | —       | Label text                       |
| ...rest     | `React.ComponentProps<"span">` | —       | Inherits all `<span>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
