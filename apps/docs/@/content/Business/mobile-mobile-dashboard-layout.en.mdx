import { MobileDashboardLayout } from "@chaos_team/chaos-ui/mobile";
import { CodeBlock } from "@/components/code-block";

# MobileDashboardLayout Mobile Mobile Dashboard Layout

> Domain-specific business components for ERP scenarios.

MobileDashboardLayout — a business component for ERP / enterprise scenarios.

## When to Use

Use MobileMobileDashboardLayout when this capability is required. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { MobileDashboardLayout } from "@chaos_team/chaos-ui/mobile"

export function Basic() {
  return <MobileDashboardLayout />
}`} lang="tsx" />

## API / Props

### MobileDashboardLayout

| Prop          | Type                   | Default | Description                       |
| ------------- | ---------------------- | ------- | --------------------------------- |
| `className`   | `string`               | —       | Custom CSS class                  |
| `title`       | `string`               | —       | Title                             |
| `description` | `string`               | —       | Description text                  |
| `actions`     | `React.ReactNode`      | —       |                                   |
| `onBack`      | `() => void`           | —       |                                   |
| `onMenu`      | `() => void`           | —       |                                   |
| ...rest       | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
