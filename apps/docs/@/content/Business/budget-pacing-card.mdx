import { BudgetPacingCard } from "@chaos_team/chaos-ui/business"
import { CodeBlock } from "@/components/code-block"

# BudgetPacingCard 预算节奏卡片

> 业务组件 / Domain-specific business components for ERP scenarios.

预算节奏卡片 — 面向 ERP/企业场景的业务组件。
BudgetPacingCard — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

需要预算节奏卡片功能时使用本组件。

Use when you need a budget pacing card.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { BudgetPacingCard } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <BudgetPacingCard />
}`} lang="tsx" />


## API / Props

### BudgetPacingCard

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| `budget` | `number` | — |  |
| `spent` | `number` | — |  |
| `period` | `string` | `本月` |  |
| `label` | `string` | `预算消耗` | 标签文本 <br/> Label text |
| `currency` | `string` | `¥` |  |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-budgetpacingcard--docs)。
