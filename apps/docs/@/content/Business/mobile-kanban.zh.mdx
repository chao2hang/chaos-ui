import { MobileKanban } from "@/components/business/mobile-kanban";
import { CodeBlock } from "@/components/code-block";

# MobileKanban 移动端看板

> 业务组件

移动端看板 — 面向 ERP/企业场景的业务组件。

## 何时使用

需要移动端看板功能时使用本组件。

## 代码示例

### 基础用法

<CodeBlock code={`import { MobileKanban } from "@/components/business/mobile-kanban"

export function Basic() {
  return <MobileKanban />
}`} lang="tsx" />

## API / Props

### MobileKanban

| Prop              | Type                   | Default | 说明              |
| ----------------- | ---------------------- | ------- | ----------------- |
| `className`       | `string`               | —       | 自定义样式类名    |
| `columns`         | `KanbanColumnData[]`   | —       | 列配置            |
| `onColumnsChange` | `() => void`           | —       |                   |
| `renderCard`      | `(...args) => unknown` | —       |                   |
| ...rest           | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-mobile-kanban--docs)。
