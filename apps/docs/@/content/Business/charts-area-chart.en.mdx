import { AreaChart } from "@/components/business/charts/area-chart";
import { CodeBlock } from "@/components/code-block";

# AreaChart

> Domain-specific business components for ERP scenarios.

AreaChart — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need an area chart.

## Examples

### Basic Usage

<CodeBlock code={`import { AreaChart } from "@/components/business/charts/area-chart"

export function Basic() {
  return <AreaChart />
}`} lang="tsx" />

## API / Props

### AreaChart

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
