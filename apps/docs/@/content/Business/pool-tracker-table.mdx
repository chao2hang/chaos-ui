import { PoolTrackerTable } from "@chaos_team/chaos-ui/business"
import { CodeBlock } from "@/components/code-block"

# PoolTrackerTable Pool Tracker Table

> 业务组件 / Domain-specific business components for ERP scenarios.

Tracks each fund pool and renders a usage bar so finance teams can spot pools that are running low.
PoolTrackerTable — pool tracker table component.

## 何时使用 / When to Use

适用于Pool Tracker Table相关场景。

Use when you need a pool tracker table.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { PoolTrackerTable } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <PoolTrackerTable />
}`} lang="tsx" />


## API / Props

### PoolTrackerTable

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| `pools` | `Array<{` | — | Fund pools to track. |
| `id` | `string` | — | DOM 元素 id <br/> DOM element id |
| `name` | `string` | — | 表单字段名 <br/> Form field name |
| `total` | `number` | — | Total capacity of the pool. |
| `used` | `number` | — | Amount currently in use. |
| `available` | `number` | — | Remaining available balance. |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-pooltrackertable--docs)。
