import { TaxDetailTable } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# TaxDetailTable Tax Detail Table

> Domain-specific business components for ERP scenarios.

TaxDetailTable — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a tax detail table.

## Examples

### Basic Usage

<CodeBlock code={`import { TaxDetailTable } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <TaxDetailTable />
}`} lang="tsx" />

## API / Props

### TaxDetailTable

| Prop        | Type                   | Default | Description                                |
| ----------- | ---------------------- | ------- | ------------------------------------------ |
| `className` | `string`               | —       | Custom CSS class                           |
| `rows`      | `Array<{`              | —       | Taxable line items.                        |
| `id`        | `string`               | —       | DOM element id                             |
| `name`      | `string`               | —       | Form field name                            |
| `amount`    | `number`               | —       | Taxable amount (currency units).           |
| `taxRate`   | `number`               | —       | Tax rate as a fraction, e.g. 0.13 for 13%. |
| `taxAmount` | `number`               | —       | Computed tax payable for this line.        |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes          |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
