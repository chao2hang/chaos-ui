import { OeeDashboard } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# OeeDashboard Oee Dashboard

> Domain-specific business components for ERP scenarios.

OeeDashboard — a business component for ERP / enterprise scenarios.

## When to Use

Use OeeDashboard when you need: OeeDashboard: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { OeeDashboard } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <OeeDashboard />
}`} lang="tsx" />

## API / Props

### OeeDashboard

| Prop        | Type                   | Default         | Description                           |
| ----------- | ---------------------- | --------------- | ------------------------------------- |
| `className` | `string`               | —               | Custom CSS class                      |
| `equipment` | `OeeEquipment`         | —               | Equipment data.                       |
| `losses`    | `OeeLoss[]`            | —               | Loss breakdown items.                 |
| `title`     | `string`               | `OEE Dashboard` | Dashboard title.                      |
| `targetOee` | `number`               | `85`            | Target OEE percentage for comparison. |
| ...rest     | `React.HTMLAttributes` | —               | Inherits standard HTML attributes     |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
