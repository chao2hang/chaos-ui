import { AnnouncementCard } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# AnnouncementCard Announcement Card

> Domain-specific business components for ERP scenarios.

AnnouncementCard — a business component for ERP / enterprise scenarios.

## When to Use

Use AnnouncementCard when you need: AnnouncementCard: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { AnnouncementCard } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <AnnouncementCard />
}`} lang="tsx" />

## API / Props

### AnnouncementCard

| Prop        | Type                   | Default | Description                       |
| ----------- | ---------------------- | ------- | --------------------------------- |
| `className` | `string`               | —       | Custom CSS class                  |
| `title`     | `string`               | —       | Title                             |
| `content`   | `string`               | —       | Content                           |
| `date`      | `string`               | —       |                                   |
| `pinned`    | `boolean`              | —       |                                   |
| ...rest     | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
