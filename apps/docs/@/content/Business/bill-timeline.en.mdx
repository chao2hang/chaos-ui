import { BillTimeline } from "@/components/business/bill-timeline";
import { CodeBlock } from "@/components/code-block";

# BillTimeline Bill Timeline

> Domain-specific business components for ERP scenarios.

BillTimeline — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a bill timeline.

## Examples

### Basic Usage

<CodeBlock code={`import { BillTimeline } from "@/components/business/bill-timeline"

export function Basic() {
  return <BillTimeline />
}`} lang="tsx" />

## API / Props

### BillTimeline

| Prop          | Type                   | Default | Description                       |
| ------------- | ---------------------- | ------- | --------------------------------- |
| `className`   | `string`               | —       | Custom CSS class                  |
| `events`      | `Array<{`              | —       |                                   |
| `id`          | `string`               | —       | DOM element id                    |
| `title`       | `string`               | —       | Title                             |
| `description` | `string`               | —       | Description text                  |
| `timestamp`   | `string`               | —       |                                   |
| `status`      | `string`               | —       | Status                            |
| ...rest       | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
