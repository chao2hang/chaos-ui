import { BulkImportWizard } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# BulkImportWizard

> Domain-specific business components for ERP scenarios.

BulkImportWizard — a business component for ERP / enterprise scenarios.

## When to Use

Use BulkImportWizard when you need: BulkImportWizard: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { BulkImportWizard } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <BulkImportWizard />
}`} lang="tsx" />

## API / Props

### BulkImportWizard

| Prop        | Type                          | Default | Description                     |
| ----------- | ----------------------------- | ------- | ------------------------------- |
| `className` | `string`                      | —       | Custom CSS class                |
| `onImport`  | `() => void`                  | —       |                                 |
| `onCancel`  | `() => void`                  | —       |                                 |
| ...rest     | `React.ComponentProps<"div">` | —       | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
