import { Chip } from "@/components/business/chip"
import { CodeBlock } from "@/components/code-block"

# Chip 标签片

> 业务组件

标签片 — 面向 ERP/企业场景的业务组件。

## 何时使用

需要标签片功能时使用本组件。


## 代码示例

### 基础用法

<CodeBlock code={`import { Chip } from "@/components/business/chip"

export function Basic() {
  return <Chip />
}`} lang="tsx" />


## API / Props

### Chip

| Prop | Type | Default | 说明 |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 |
| `variant` | `ChipVariant` | `default` | 样式变体 |
| `size` | `ChipSize` | `default` | 组件尺寸 |
| `removable` | `boolean` | — |  |
| `onRemove` | `() => void` | — |  |
| `icon` | `React.ReactNode` | — | 图标元素 |
| ...rest | `React.ComponentProps<"span">` | — | 透传 `<span>` 原生属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **原生属性**: 继承所有 `<span>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-chip--docs)。

