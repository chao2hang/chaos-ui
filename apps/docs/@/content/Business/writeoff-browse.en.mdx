import { WriteoffBrowse } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# WriteoffBrowse Writeoff Browse

> Domain-specific business components for ERP scenarios.

WriteoffBrowse — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a writeoff browse.

## Examples

### Basic Usage

<CodeBlock code={`import { WriteoffBrowse } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <WriteoffBrowse />
}`} lang="tsx" />

## API / Props

### WriteoffBrowse

| Prop                | Type                   | Default | Description                       |
| ------------------- | ---------------------- | ------- | --------------------------------- |
| `className`         | `string`               | —       | Custom CSS class                  |
| `open`              | `boolean`              | —       | Open state (controlled)           |
| `onOpenChange`      | `() => void`           | —       | Callback when open state changes  |
| `onSelect`          | `() => void`           | —       | Selection handler                 |
| `items`             | `WriteoffBrowseItem[]` | —       | Items / data list                 |
| `title`             | `string`               | —       | Title                             |
| `searchPlaceholder` | `string`               | —       |                                   |
| `emptyText`         | `string`               | —       |                                   |
| ...rest             | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
