import {
  ErrorPage,
  NotFound,
  InternalError,
  Unauthorized,
} from "@/components/business/error-page";
import { CodeBlock } from "@/components/code-block";

# ErrorPage Business Error Page

> 业务组件 / Domain-specific business components for ERP scenarios.

Business Error Page — 面向 ERP/企业场景的业务组件。
ErrorPage — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Business Error Page相关场景。

Use when you need an error page.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { ErrorPage } from "@/components/business/error-page"

export function Basic() {
  return <ErrorPage>Default</ErrorPage>
}`} lang="tsx" />

## API / Props

### ErrorPage

| Prop           | Type                       | Default | 说明 / Description                                        |
| -------------- | -------------------------- | ------- | --------------------------------------------------------- |
| `className`    | `string`                   | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `status`       | `403 \| 404 \| 500 \| 503` | `404`   | 状态 <br/> Status                                         |
| `title`        | `string`                   | —       | 标题 <br/> Title                                          |
| `description`  | `string`                   | —       | 描述文本 <br/> Description text                           |
| `homeHref`     | `string`                   | `/`     |                                                           |
| `showHome`     | `boolean`                  | `true`  |                                                           |
| `showBack`     | `boolean`                  | `true`  |                                                           |
| `onBack`       | `() => void`               | —       |                                                           |
| `illustration` | `React.ReactNode`          | —       |                                                           |
| ...rest        | `React.HTMLAttributes`     | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **无障碍**: 组件遵循 WAI-ARIA 最佳实践。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-business-error-page--docs)。
