import { AudienceSegmentBuilder } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# AudienceSegmentBuilder

> Domain-specific business components for ERP scenarios.

AudienceSegmentBuilder — a business component for ERP / enterprise scenarios.

## When to Use

Use AudienceSegmentBuilder when you need: AudienceSegmentBuilder: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { AudienceSegmentBuilder } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <AudienceSegmentBuilder />
}`} lang="tsx" />

## API / Props

### AudienceSegmentBuilder

| Prop        | Type                          | Default | Description                     |
| ----------- | ----------------------------- | ------- | ------------------------------- |
| `className` | `string`                      | —       | Custom CSS class                |
| `rules`     | `SegmentRule[]`               | —       |                                 |
| `onChange`  | `() => void`                  | —       | Callback when value changes     |
| ...rest     | `React.ComponentProps<"div">` | —       | Inherits all `<div>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
