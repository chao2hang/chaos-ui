import { MarkdownEditor } from "@chaos_team/chaos-ui/business"
import { CodeBlock } from "@/components/code-block"

# MarkdownEditor Markdown Editor

> 业务组件

Markdown Editor — 面向 ERP/企业场景的业务组件。

## 何时使用

适用于Markdown Editor相关场景。


## 代码示例

### 基础用法

<CodeBlock code={`import { MarkdownEditor } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <MarkdownEditor />
}`} lang="tsx" />


## API / Props

### MarkdownEditor

| Prop | Type | Default | 说明 |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 |
| `value` | `string` | `` | 当前值(受控) |
| `onChange` | `() => void` | — | 值变化回调 |
| `placeholder` | `string` | `请输入 Markdown...` | 占位文本 |
| `mode` | `MarkdownMode` | — |  |
| `toolbar` | `boolean` | `true` |  |
| `height` | `number \| string` | `300` | 组件高度 |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-markdowneditor--docs)。

