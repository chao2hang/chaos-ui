import { ReconciliationLineEditor } from "@/components/business/reconciliation-line-editor";
import { CodeBlock } from "@/components/code-block";

# ReconciliationLineEditor Business Reconciliation Line Editor

> 业务组件 / Domain-specific business components for ERP scenarios.

Business Reconciliation Line Editor — 面向 ERP/企业场景的业务组件。
ReconciliationLineEditor — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Business Reconciliation Line Editor相关场景。

Use when you need a reconciliation line editor.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { ReconciliationLineEditor } from "@/components/business/reconciliation-line-editor"

export function Basic() {
  return <ReconciliationLineEditor />
}`} lang="tsx" />

## API / Props

### ReconciliationLineEditor

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `rows`      | `ReconciliationRow[]`  | —       | 行数据 <br/> Row data                                     |
| `onChange`  | `() => void`           | —       | 值变化回调 <br/> Callback when value changes              |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-business-reconciliation-line-editor--docs)。
