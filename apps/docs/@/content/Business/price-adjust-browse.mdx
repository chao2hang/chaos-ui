import { PriceAdjustBrowse } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# PriceAdjustBrowse Price Adjust Browse

> 业务组件 / Domain-specific business components for ERP scenarios.

Price Adjust Browse — 面向 ERP/企业场景的业务组件。
PriceAdjustBrowse — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

需要Price Adjust Browse时使用本组件。用于 ERP/业务域场景（单据、选择器、审批、对账等）；按业务语义接入而不是当纯 UI 积木。

Use PriceAdjustBrowse when you need: PriceAdjustBrowse: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { PriceAdjustBrowse } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <PriceAdjustBrowse />
}`} lang="tsx" />

## API / Props

### PriceAdjustBrowse

| Prop                | Type                      | Default | 说明 / Description                                        |
| ------------------- | ------------------------- | ------- | --------------------------------------------------------- |
| `className`         | `string`                  | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `open`              | `boolean`                 | —       | 是否打开(受控) <br/> Open state (controlled)              |
| `onOpenChange`      | `() => void`              | —       | 打开状态变化回调 <br/> Callback when open state changes   |
| `onSelect`          | `() => void`              | —       | 选中回调 <br/> Selection handler                          |
| `items`             | `PriceAdjustBrowseItem[]` | —       | 选项/数据项列表 <br/> Items / data list                   |
| `title`             | `string`                  | —       | 标题 <br/> Title                                          |
| `searchPlaceholder` | `string`                  | —       |                                                           |
| `emptyText`         | `string`                  | —       |                                                           |
| ...rest             | `React.HTMLAttributes`    | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](http://localhost:6006/?path=/docs/business-priceadjustbrowse--docs)。
