import { PermissionWrapper } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# PermissionWrapper 权限包装器

> 业务组件 / Domain-specific business components for ERP scenarios.

权限包装器 — 面向 ERP/企业场景的业务组件。
PermissionWrapper — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

需要权限包装器功能时使用本组件。

Use when you need a permission wrapper.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { PermissionWrapper } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <PermissionWrapper />
}`} lang="tsx" />

## API / Props

### PermissionWrapper

| Prop              | Type                   | Default | 说明 / Description                                        |
| ----------------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className`       | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `permission`      | `string \| string[]`   | —       |                                                           |
| `permissions`     | `string[]`             | —       |                                                           |
| `mode`            | `"all" \| "any"`       | `any`   |                                                           |
| `fallback`        | `React.ReactNode`      | —       |                                                           |
| `userPermissions` | `string[]`             | —       |                                                           |
| ...rest           | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-permissionwrapper--docs)。
