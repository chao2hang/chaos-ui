import { BatchPrintDialog } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# BatchPrintDialog Batch Print Dialog

> 业务组件

Batch Print Dialog — 面向 ERP/企业场景的业务组件。

## 何时使用

需要弹层完成确认、短表单或辅助操作且尽量保留当前页上下文时使用。（组件：Batch Print Dialog）

## 代码示例

### 基础用法

<CodeBlock code={`import { BatchPrintDialog } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <BatchPrintDialog />
}`} lang="tsx" />

## API / Props

### BatchPrintDialog

| Prop           | Type                   | Default | 说明              |
| -------------- | ---------------------- | ------- | ----------------- |
| `className`    | `string`               | —       | 自定义样式类名    |
| `open`         | `boolean`              | —       | 是否打开(受控)    |
| `onOpenChange` | `() => void`           | —       | 打开状态变化回调  |
| `items`        | `Array<T>`             | —       | 选项/数据项列表   |
| `onPrint`      | `() => void`           | —       |                   |
| ...rest        | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](http://localhost:6006/?path=/docs/business-batchprintdialog--docs)。
