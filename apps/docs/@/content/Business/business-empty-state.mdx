import { EmptyState, Config } from "@/components/business/empty-state";
import { CodeBlock } from "@/components/code-block";

# EmptyState Business Empty State

> 业务组件 / Domain-specific business components for ERP scenarios.

Business Empty State — 面向 ERP/企业场景的业务组件。
EmptyState — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Business Empty State相关场景。

Use when you need an empty state.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { EmptyState, Config } from "@/components/business/empty-state"

export function Basic() {
  return (
    <div className="space-y-4">
  <EmptyState />
  <Config />
    </div>
  )
}`} lang="tsx" />

## API / Props

### EmptyState

| Prop        | Type                   | Default | 说明 / Description                                        |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-business-empty-state--docs)。
