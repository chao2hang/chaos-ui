import { CalendarView } from "@/components/business/calendar-view";
import { CodeBlock } from "@/components/code-block";

# CalendarView Calendar View

> 业务组件 / Domain-specific business components for ERP scenarios.

<CalendarView events={[{ id: "1", date: "2026-06-15", title: "盘点" }]} />
CalendarView — calendar view component.

## 何时使用 / When to Use

适用于Calendar View相关场景。

Use when you need a calendar view.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { CalendarView } from "@/components/business/calendar-view"

export function Basic() {
  return <CalendarView />
}`} lang="tsx" />

### 状态 / States

<CodeBlock
  code={`<CalendarView>Normal</CalendarView>
<CalendarView disabled>Disabled</CalendarView>`}
  lang="tsx"
/>

## API / Props

### CalendarView

| Prop           | Type                   | Default | 说明 / Description                                        |
| -------------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className`    | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `events`       | `Array<T>`             | —       |                                                           |
| `onDateSelect` | `() => void`           | —       |                                                           |
| ...rest        | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-calendar-view--docs)。
