import { CalendarView } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# CalendarView Calendar View

> 业务组件 / Domain-specific business components for ERP scenarios.

<CalendarView events={[{ id: "1", date: "2026-06-15", title: "盘点" }]} />
CalendarView — calendar view component.

## 何时使用 / When to Use

需要Calendar View时使用本组件。用于 ERP/业务域场景（单据、选择器、审批、对账等）；按业务语义接入而不是当纯 UI 积木。

Use CalendarView when this capability is required. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { CalendarView } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <CalendarView />
}`} lang="tsx" />

### 状态 / States

<CodeBlock
  code={`<CalendarView>Normal</CalendarView>
<CalendarView disabled>Disabled</CalendarView>`}
  lang="tsx"
/>

## API / Props

### CalendarView

| Prop           | Type                   | Default | 说明 / Description                                        |
| -------------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className`    | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `events`       | `Array<T>`             | —       |                                                           |
| `onDateSelect` | `() => void`           | —       |                                                           |
| ...rest        | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](http://localhost:6006/?path=/docs/business-calendarview--docs)。
