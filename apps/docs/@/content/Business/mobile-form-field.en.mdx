import { MobileFormField } from "@/components/mobile/mobile-form-field";
import { CodeBlock } from "@/components/code-block";

# MobileFormField

> Domain-specific business components for ERP scenarios.

MobileFormField — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a mobile form field.

## Examples

### Basic Usage

<CodeBlock code={`import { MobileFormField } from "@/components/mobile/mobile-form-field"

export function Basic() {
  return <MobileFormField />
}`} lang="tsx" />

## API / Props

### MobileFormField

| Prop          | Type                   | Default | Description                       |
| ------------- | ---------------------- | ------- | --------------------------------- |
| `className`   | `string`               | —       | Custom CSS class                  |
| `label`       | `string`               | —       | Label text                        |
| `description` | `string`               | —       | Description text                  |
| `error`       | `string`               | —       |                                   |
| `required`    | `boolean`              | —       | Whether required                  |
| ...rest       | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
