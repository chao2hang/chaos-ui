import { TaskListTable } from "@/components/business/task-list-table";
import { CodeBlock } from "@/components/code-block";

# TaskListTable Task List Table

> 业务组件

<TaskListTable
  tasks={[{ id: "1", title: "对账", status: "进行中", priority: "高" }]}
/>

## 何时使用

适用于Task List Table相关场景。

## 代码示例

### 基础用法

<CodeBlock code={`import { TaskListTable } from "@/components/business/task-list-table"

export function Basic() {
  return <TaskListTable />
}`} lang="tsx" />

## API / Props

### TaskListTable

| Prop        | Type                   | Default | 说明              |
| ----------- | ---------------------- | ------- | ----------------- |
| `className` | `string`               | —       | 自定义样式类名    |
| `tasks`     | `Array<{`              | —       |                   |
| `id`        | `string`               | —       | DOM 元素 id       |
| `title`     | `string`               | —       | 标题              |
| `status`    | `string`               | —       | 状态              |
| `priority`  | `string`               | —       |                   |
| `assignee`  | `string`               | —       |                   |
| `deadline`  | `string`               | —       |                   |
| ...rest     | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-tasklisttable--docs)。
