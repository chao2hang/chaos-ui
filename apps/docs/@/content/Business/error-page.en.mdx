import { ErrorPage, NotFound, InternalError, Unauthorized } from "@/components/business/error-page"
import { CodeBlock } from "@/components/code-block"

# ErrorPage

> Domain-specific business components for ERP scenarios.

ErrorPage — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need an error page.


## Examples

### Basic Usage

<CodeBlock code={`import { ErrorPage } from "@/components/business/error-page"

export function Basic() {
  return <ErrorPage>Default</ErrorPage>
}`} lang="tsx" />


## API / Props

### ErrorPage

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `status` | `403 \| 404 \| 500 \| 503` | `404` | Status |
| `title` | `string` | — | Title |
| `description` | `string` | — | Description text |
| `homeHref` | `string` | `/` |  |
| `showHome` | `boolean` | `true` |  |
| `showBack` | `boolean` | `true` |  |
| `onBack` | `() => void` | — |  |
| `illustration` | `React.ReactNode` | — |  |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **无障碍**: 组件遵循 WAI-ARIA 最佳实践。


