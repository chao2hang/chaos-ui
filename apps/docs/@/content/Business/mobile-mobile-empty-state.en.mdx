import { MobileEmptyState } from "@/components/mobile/mobile-empty-state";
import { CodeBlock } from "@/components/code-block";

# MobileEmptyState Mobile Mobile Empty State

> Domain-specific business components for ERP scenarios.

MobileEmptyState — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a mobile empty state.

## Examples

### Basic Usage

<CodeBlock code={`import { MobileEmptyState } from "@/components/mobile/mobile-empty-state"

export function Basic() {
  return <MobileEmptyState />
}`} lang="tsx" />

## API / Props

### MobileEmptyState

| Prop          | Type                   | Default | Description                       |
| ------------- | ---------------------- | ------- | --------------------------------- |
| `className`   | `string`               | —       | Custom CSS class                  |
| `variant`     | `string`               | —       | Style variant                     |
| `icon`        | `React.ElementType`    | —       | Icon element                      |
| `title`       | `string`               | —       | Title                             |
| `description` | `string`               | —       | Description text                  |
| `action`      | `React.ReactNode`      | —       |                                   |
| ...rest       | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
