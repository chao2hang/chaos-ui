import { MobileBottomNav } from "@chaos_team/chaos-ui/mobile";
import { CodeBlock } from "@/components/code-block";

# MobileBottomNav Mobile Mobile Bottom Nav

> Domain-specific business components for ERP scenarios.

MobileBottomNav — a business component for ERP / enterprise scenarios.

## When to Use

Use MobileMobileBottomNav when this capability is required. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { MobileBottomNav } from "@chaos_team/chaos-ui/mobile"

export function Basic() {
  return <MobileBottomNav />
}`} lang="tsx" />

## API / Props

### MobileBottomNav

| Prop        | Type                      | Default   | Description                       |
| ----------- | ------------------------- | --------- | --------------------------------- |
| `className` | `string`                  | —         | Custom CSS class                  |
| `items`     | `MobileNavItem[]`         | —         | Items / data list                 |
| `onChange`  | `() => void`              | —         | Callback when value changes       |
| `variant`   | `"default" \| "floating"` | `default` | Style variant                     |
| ...rest     | `React.HTMLAttributes`    | —         | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
