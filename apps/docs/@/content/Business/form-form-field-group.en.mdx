import { CodeBlock } from "@/components/code-block";

# FormFieldGroup

> Domain-specific business components for ERP scenarios.

FormFieldGroup — a business component for ERP / enterprise scenarios.

## When to Use

Use FormFormFieldGroup when this capability is required. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock
  code={`export function Basic() {
  return <FormFieldGroup />
}`}
  lang="tsx"
/>

## API / Props

### FormFieldGroup

| Prop          | Type                               | Default | Description                          |
| ------------- | ---------------------------------- | ------- | ------------------------------------ |
| `className`   | `string`                           | —       | Custom CSS class                     |
| `legend`      | `string`                           | —       |                                      |
| `description` | `string`                           | —       | Description text                     |
| `required`    | `boolean`                          | —       | Whether required                     |
| `columns`     | `1 \| 2 \| 3`                      | `1`     | Column configuration                 |
| ...rest       | `React.ComponentProps<"fieldset">` | —       | Inherits all `<fieldset>` attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
