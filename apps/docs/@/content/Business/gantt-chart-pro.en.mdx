import { GanttChartPro } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# GanttChartPro Gantt Chart Pro

> Domain-specific business components for ERP scenarios.

GanttChartPro — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a gantt chart pro.

## Examples

### Basic Usage

<CodeBlock code={`import { GanttChartPro } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <GanttChartPro />
}`} lang="tsx" />

## API / Props

### GanttChartPro

| Prop                 | Type                   | Default | Description                                 |
| -------------------- | ---------------------- | ------- | ------------------------------------------- |
| `className`          | `string`               | —       | Custom CSS class                            |
| `tasks`              | `GanttTask[]`          | —       | Task list                                   |
| `dependencies`       | `GanttDependency[]`    | —       | Dependency links between tasks              |
| `zoom`               | `ZoomLevel`            | `day`   | Timeline zoom level (default: "day")        |
| `showToday`          | `boolean`              | `true`  | Show today marker (default: true)           |
| `showWeekSeparators` | `boolean`              | `true`  | Show week column separators (default: true) |
| `showTaskList`       | `boolean`              | `true`  | Show task list sidebar (default: true)      |
| `rowHeight`          | `number`               | `36`    | Row height in px (default: 36)              |
| `onTaskClick`        | `() => void`           | —       | Allow task bar click                        |
| ...rest              | `React.HTMLAttributes` | —       | Inherits standard HTML attributes           |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
