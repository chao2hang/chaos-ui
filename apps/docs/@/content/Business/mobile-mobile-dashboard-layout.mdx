import { MobileDashboardLayout } from "@/components/mobile/mobile-dashboard-layout";
import { CodeBlock } from "@/components/code-block";

# MobileDashboardLayout Mobile Mobile Dashboard Layout

> 业务组件 / Domain-specific business components for ERP scenarios.

Mobile Mobile Dashboard Layout — 面向 ERP/企业场景的业务组件。
MobileDashboardLayout — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Mobile Mobile Dashboard Layout相关场景。

Use when you need a mobile dashboard layout.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { MobileDashboardLayout } from "@/components/mobile/mobile-dashboard-layout"

export function Basic() {
  return <MobileDashboardLayout />
}`} lang="tsx" />

## API / Props

### MobileDashboardLayout

| Prop          | Type                   | Default | 说明 / Description                                        |
| ------------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className`   | `string`               | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `title`       | `string`               | —       | 标题 <br/> Title                                          |
| `description` | `string`               | —       | 描述文本 <br/> Description text                           |
| `actions`     | `React.ReactNode`      | —       |                                                           |
| `onBack`      | `() => void`           | —       |                                                           |
| `onMenu`      | `() => void`           | —       |                                                           |
| ...rest       | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-mobile-mobile-dashboard-layout--docs)。
