import { InboxList } from "@/components/business/inbox-list";
import { CodeBlock } from "@/components/code-block";

# InboxList

> Domain-specific business components for ERP scenarios.

InboxList — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need an inbox list.


## Examples

### Basic Usage

<CodeBlock code={`import { InboxList } from "@/components/business/inbox-list"

export function Basic() {
  return <InboxList />
}`} lang="tsx" />

## API / Props

### InboxList

| Prop | Type | Default | Description |
| ----------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string` | — | Custom CSS class |
| `items` | `InboxItem[]` | — | Items / data list |
| `selected`  | `string`               | —       |                                                           |
| `onSelect` | `() => void` | — | Selection handler |
| `onStar`    | `() => void`           | —       |                                                           |
| `onSearch`  | `() => void`           | —       |                                                           |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


