import { OperationLog } from "@/components/business/operation-log"
import { CodeBlock } from "@/components/code-block"

# OperationLog Operation Log

> 业务组件 / Domain-specific business components for ERP scenarios.

<OperationLog logs={[{ id:"1", action:"审批通过", operator:"李四", timestamp:"2026-01-01T09:00:00Z" }]} />
OperationLog — operation log component.

## 何时使用 / When to Use

适用于Operation Log相关场景。

Use when you need an operation log.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { OperationLog } from "@/components/business/operation-log"

export function Basic() {
  return <OperationLog />
}`} lang="tsx" />


## API / Props

### OperationLog

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| `logs` | `Array<{` | — |  |
| `id` | `string` | — | DOM 元素 id <br/> DOM element id |
| `action` | `string` | — |  |
| `operator` | `string` | — |  |
| `timestamp` | `string` | — |  |
| `detail` | `string` | — |  |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-operationlog--docs)。
