import { Tracking } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# Tracking

> 业务组件 / Domain-specific business components for ERP scenarios.

<Tracking target={100} actual={76} label="回款率" />
Tracking — tracking component.

## 何时使用 / When to Use

需要Tracking时使用本组件。用于 ERP/业务域场景（单据、选择器、审批、对账等）；按业务语义接入而不是当纯 UI 积木。

Use Tracking when this capability is required. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { Tracking } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <Tracking />
}`} lang="tsx" />

## API / Props

### Tracking

| Prop        | Type                   | Default  | 说明 / Description                                        |
| ----------- | ---------------------- | -------- | --------------------------------------------------------- |
| `className` | `string`               | —        | 自定义样式类名 <br/> Custom CSS class                     |
| `target`    | `number`               | —        | 链接打开目标 <br/> Link target                            |
| `actual`    | `number`               | —        |                                                           |
| `label`     | `string`               | `追踪项` | 标签文本 <br/> Label text                                 |
| ...rest     | `React.HTMLAttributes` | —        | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **无障碍**: 组件遵循 WAI-ARIA 最佳实践。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](http://localhost:6006/?path=/docs/business-tracking--docs)。
