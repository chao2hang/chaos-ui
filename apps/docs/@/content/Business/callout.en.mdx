import { Callout } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# Callout

> Domain-specific business components for ERP scenarios.

<Callout variant="warning" title="注意">
  请核对数据
</Callout>
Callout — callout component.

## When to Use

Use Callout when this capability is required. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { Callout } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <Callout />
}`} lang="tsx" />

## API / Props

### Callout

| Prop        | Type                                       | Default   | Description                       |
| ----------- | ------------------------------------------ | --------- | --------------------------------- |
| `className` | `string`                                   | —         | Custom CSS class                  |
| `title`     | `string`                                   | —         | Title                             |
| `variant`   | `"default" \| "info" \| "success" \| "...` | `default` | Style variant                     |
| ...rest     | `React.HTMLAttributes`                     | —         | Inherits standard HTML attributes |

## Notes

- **无障碍**: 组件遵循 WAI-ARIA 最佳实践。
