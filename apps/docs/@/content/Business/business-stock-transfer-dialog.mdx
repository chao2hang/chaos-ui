import { StockTransferDialog } from "@/components/business/stock-transfer-dialog";
import { CodeBlock } from "@/components/code-block";

# StockTransferDialog Business Stock Transfer Dialog

> 业务组件 / Domain-specific business components for ERP scenarios.

Business Stock Transfer Dialog — 面向 ERP/企业场景的业务组件。
StockTransferDialog — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Business Stock Transfer Dialog相关场景。

Use when you need a stock transfer dialog.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { StockTransferDialog } from "@/components/business/stock-transfer-dialog"

export function Basic() {
  return <StockTransferDialog />
}`} lang="tsx" />

## API / Props

### StockTransferDialog

| Prop                    | Type                    | Default   | 说明 / Description                                        |
| ----------------------- | ----------------------- | --------- | --------------------------------------------------------- |
| `className`             | `string`                | —         | 自定义样式类名 <br/> Custom CSS class                     |
| `transferNo`            | `string`                | —         |                                                           |
| `fromWarehouse`         | `string`                | —         |                                                           |
| `toWarehouse`           | `string`                | —         |                                                           |
| `onFromWarehouseChange` | `() => void`            | —         |                                                           |
| `onToWarehouseChange`   | `() => void`            | —         |                                                           |
| `warehouses`            | `string[]`              | —         |                                                           |
| `lines`                 | `TransferLine[]`        | —         |                                                           |
| `onLinesChange`         | `() => void`            | —         |                                                           |
| `transferType`          | `"regular" \| "urgent"` | `regular` |                                                           |
| `onTransferTypeChange`  | `() => void`            | —         |                                                           |
| `remark`                | `string`                | —         |                                                           |
| `onRemarkChange`        | `() => void`            | —         |                                                           |
| `onSubmit`              | `() => void`            | —         | 提交回调 <br/> Submit handler                             |
| `onCancel`              | `() => void`            | —         |                                                           |
| `readOnly`              | `boolean`               | `false`   | 是否只读 <br/> Whether read-only                          |
| ...rest                 | `React.HTMLAttributes`  | —         | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-business-stock-transfer-dialog--docs)。
