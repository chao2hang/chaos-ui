import { PDFViewer } from "@/components/business/pdf-viewer"
import { CodeBlock } from "@/components/code-block"

# PdfViewer PDF 查看器

> 业务组件 / Domain-specific business components for ERP scenarios.

PDF 查看器 — 面向 ERP/企业场景的业务组件。
PdfViewer — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于PDF 查看器相关场景。

Use when you need a pdf viewer.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { PDFViewer } from "@/components/business/pdf-viewer"

export function Basic() {
  return <PDFViewer />
}`} lang="tsx" />


## API / Props

### PDFViewer

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| `src` | `string` | — | 资源地址 <br/> Source URL |
| `title` | `string` | — | 标题 <br/> Title |
| ...rest | `React.ComponentProps<"div">` | — | 透传 `<div>` 原生属性 <br/> Inherits all `<div>` attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-pdf-viewer--docs)。
