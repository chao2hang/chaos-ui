import { DockPanel } from "@/components/business/dock-panel";
import { CodeBlock } from "@/components/code-block";

# DockPanel Business Dock Panel

> 业务组件 / Domain-specific business components for ERP scenarios.

<DockPanel side="left" collapsed={false} onToggle={() => {}}>
  {" "}
  <p>面板内容</p>{" "}
</DockPanel>
DockPanel — business dock panel component.

## 何时使用 / When to Use

适用于Business Dock Panel相关场景。

Use when you need a dock panel.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { DockPanel } from "@/components/business/dock-panel"

export function Basic() {
  return <DockPanel />
}`} lang="tsx" />

## API / Props

### DockPanel

| Prop        | Type                            | Default | 说明 / Description                                        |
| ----------- | ------------------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`                        | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `side`      | `"left" \| "right" \| "bottom"` | —       |                                                           |
| `collapsed` | `boolean`                       | `false` |                                                           |
| `onToggle`  | `() => void`                    | —       |                                                           |
| ...rest     | `React.HTMLAttributes`          | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-business-dock-panel--docs)。
