import { TimeSlotPicker } from "@/components/business/calendar/time-slot-picker";
import { CodeBlock } from "@/components/code-block";

# TimeSlotPicker

> Domain-specific business components for ERP scenarios.

TimeSlotPicker — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a time slot picker.


## Examples

### Basic Usage

<CodeBlock code={`import { TimeSlotPicker } from "@/components/business/calendar/time-slot-picker"

export function Basic() {
  return <TimeSlotPicker />
}`} lang="tsx" />

### States

<CodeBlock
  code={`<TimeSlotPicker>Normal</TimeSlotPicker>
<TimeSlotPicker disabled>Disabled</TimeSlotPicker>`}
  lang="tsx"
/>

## API / Props

### TimeSlotPicker

| Prop | Type | Default | Description |
| ------------- | ---------------------- | ------- | --------------------------------------------------------- |
| `className` | `string` | — | Custom CSS class |
| `date`        | `Date`                 | —       |                                                           |
| `slots`       | `TimeSlot[]`           | —       |                                                           |
| `selected`    | `Date`                 | —       |                                                           |
| `onSelect` | `() => void` | — | Selection handler |
| `startHour`   | `number`               | `9`     |                                                           |
| `endHour`     | `number`               | `18`    |                                                           |
| `stepMinutes` | `number`               | `30`    |                                                           |
| `locale`      | `string`               | `zh-CN` |                                                           |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


