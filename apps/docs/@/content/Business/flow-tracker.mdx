import { FlowTracker } from "@chaos_team/chaos-ui/business"
import { CodeBlock } from "@/components/code-block"

# FlowTracker Flow Tracker

> 业务组件 / Domain-specific business components for ERP scenarios.

<FlowTracker steps={[{ id:"1", name:"提交", status:"done", operator:"张三", time:"2026-01-01 09:00" }]} />
FlowTracker — flow tracker component.

## 何时使用 / When to Use

适用于Flow Tracker相关场景。

Use when you need a flow tracker.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { FlowTracker } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <FlowTracker />
}`} lang="tsx" />


## API / Props

### FlowTracker

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| `steps` | `Array<{` | — |  |
| `id` | `string` | — | DOM 元素 id <br/> DOM element id |
| `name` | `string` | — | 表单字段名 <br/> Form field name |
| `status` | `"pending" \| "active" \| "done" \| "r...` | — | 状态 <br/> Status |
| `operator` | `string` | — |  |
| `time` | `string` | — |  |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-flowtracker--docs)。
