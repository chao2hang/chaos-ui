import { ContractTemplate } from "@/components/business/contract-template";
import { CodeBlock } from "@/components/code-block";

# ContractTemplate Business Contract Template

> 业务组件 / Domain-specific business components for ERP scenarios.

Business Contract Template — 面向 ERP/企业场景的业务组件。
ContractTemplate — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Business Contract Template相关场景。

Use when you need a contract template.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { ContractTemplate } from "@/components/business/contract-template"

export function Basic() {
  return <ContractTemplate />
}`} lang="tsx" />

## API / Props

### ContractTemplate

| Prop         | Type                          | Default    | 说明 / Description                                          |
| ------------ | ----------------------------- | ---------- | ----------------------------------------------------------- |
| `className`  | `string`                      | —          | 自定义样式类名 <br/> Custom CSS class                       |
| `title`      | `string`                      | `合同模板` | 标题 <br/> Title                                            |
| `fields`     | `ContractField[]`             | —          |                                                             |
| `onChange`   | `() => void`                  | —          | 值变化回调 <br/> Callback when value changes                |
| `onPreview`  | `() => void`                  | —          |                                                             |
| `onDownload` | `() => void`                  | —          |                                                             |
| ...rest      | `React.ComponentProps<"div">` | —          | 透传 `<div>` 原生属性 <br/> Inherits all `<div>` attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-business-contract-template--docs)。
