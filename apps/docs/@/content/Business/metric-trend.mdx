import { Sparkline, MetricTrend } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# MetricTrend 指标趋势

> 业务组件 / Domain-specific business components for ERP scenarios.

指标趋势 — 面向 ERP/企业场景的业务组件。
MetricTrend — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

需要指标趋势功能时使用本组件。

Use when you need a metric trend.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { Sparkline, MetricTrend } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return (
    <div className="space-y-4">
  <Sparkline />
  <MetricTrend />
    </div>
  )
}`} lang="tsx" />

## API / Props

### MetricTrend

| Prop             | Type                                       | Default   | 说明 / Description                                        |
| ---------------- | ------------------------------------------ | --------- | --------------------------------------------------------- |
| `className`      | `string`                                   | —         | 自定义样式类名 <br/> Custom CSS class                     |
| `label`          | `string`                                   | —         | 标签文本 <br/> Label text                                 |
| `value`          | `number \| string`                         | —         | 当前值(受控) <br/> Current value (controlled)             |
| `unit`           | `string`                                   | —         |                                                           |
| `change`         | `number`                                   | —         |                                                           |
| `changeLabel`    | `string`                                   | `vs 上期` |                                                           |
| `format`         | `"number" \| "percent" \| "compact" \|...` | `number`  |                                                           |
| `sparklineData`  | `number[]`                                 | —         |                                                           |
| `trendDirection` | `"up-good" \| "down-good" \| "neutral"`    | `up-good` |                                                           |
| `icon`           | `React.ReactNode`                          | —         | 图标元素 <br/> Icon element                               |
| `loading`        | `boolean`                                  | —         | 是否加载中 <br/> Loading state                            |
| ...rest          | `React.HTMLAttributes`                     | —         | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-metrictrend--docs)。
