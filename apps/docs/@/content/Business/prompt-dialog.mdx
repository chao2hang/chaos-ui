import { PromptDialog } from "@/components/business/prompt-dialog"
import { CodeBlock } from "@/components/code-block"

# PromptDialog 提示对话框

> 业务组件 / Domain-specific business components for ERP scenarios.

提示对话框 — 面向 ERP/企业场景的业务组件。
PromptDialog — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

需要提示对话框功能时使用本组件。

Use when you need a prompt dialog.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { PromptDialog } from "@/components/business/prompt-dialog"

export function Basic() {
  return <PromptDialog />
}`} lang="tsx" />


## API / Props

### PromptDialog

| Prop | Type | Default | 说明 / Description |
|---|---|---|---|
| `className` | `string` | — | 自定义样式类名 <br/> Custom CSS class |
| `open` | `boolean` | — | 是否打开(受控) <br/> Open state (controlled) |
| `onOpenChange` | `() => void` | — | 打开状态变化回调 <br/> Callback when open state changes |
| `title` | `string` | `请输入` | 标题 <br/> Title |
| `description` | `React.ReactNode` | — | 描述文本 <br/> Description text |
| `label` | `string` | — | 标签文本 <br/> Label text |
| `placeholder` | `string` | — | 占位文本 <br/> Placeholder text |
| `defaultValue` | `string` | `` | 默认值(非受控) <br/> Default value (uncontrolled) |
| `confirmText` | `string` | `确认` |  |
| `cancelText` | `string` | `取消` |  |
| `onConfirm` | `() => void` | — |  |
| `required` | `boolean` | `true` | 是否必填 <br/> Whether required |
| `inputType` | `"text" \| "email" \| "password" \| "n...` | `text` |  |
| ...rest | `React.HTMLAttributes` | — | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-promptdialog--docs)。
