import { EquipmentCard } from "@/components/business/equipment-card";
import { CodeBlock } from "@/components/code-block";

# EquipmentCard 设备卡片

> 业务组件

设备卡片 — 面向 ERP/企业场景的业务组件。

## 何时使用

需要设备卡片功能时使用本组件。

## 代码示例

### 基础用法

<CodeBlock code={`import { EquipmentCard } from "@/components/business/equipment-card"

export function Basic() {
  return <EquipmentCard />
}`} lang="tsx" />

## API / Props

### EquipmentCard

| Prop              | Type                                       | Default  | 说明              |
| ----------------- | ------------------------------------------ | -------- | ----------------- |
| `className`       | `string`                                   | —        | 自定义样式类名    |
| `name`            | `string`                                   | —        | 表单字段名        |
| `model`           | `string`                                   | —        |                   |
| `serialNumber`    | `string`                                   | —        |                   |
| `status`          | `"normal" \| "maintenance" \| "fault" ...` | `normal` | 状态              |
| `location`        | `string`                                   | —        |                   |
| `lastMaintenance` | `string`                                   | —        |                   |
| ...rest           | `React.HTMLAttributes`                     | —        | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-equipmentcard--docs)。
