import { ChatShell } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# ChatShell Chat Shell

> Domain-specific business components for ERP scenarios.

ChatShell — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a chat shell.

## Examples

### Basic Usage

<CodeBlock code={`import { ChatShell } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return <ChatShell />
}`} lang="tsx" />

## API / Props

### ChatShell

| Prop          | Type                   | Default | Description                       |
| ------------- | ---------------------- | ------- | --------------------------------- |
| `className`   | `string`               | —       | Custom CSS class                  |
| `sidebar`     | `React.ReactNode`      | —       |                                   |
| `header`      | `React.ReactNode`      | —       |                                   |
| `detailPanel` | `React.ReactNode`      | —       |                                   |
| ...rest       | `React.HTMLAttributes` | —       | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
