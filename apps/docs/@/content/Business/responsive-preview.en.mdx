import { ResponsivePreview, Sizes } from "@chaos_team/chaos-ui/business";
import { CodeBlock } from "@/components/code-block";

# ResponsivePreview

> Domain-specific business components for ERP scenarios.

ResponsivePreview — a business component for ERP / enterprise scenarios.

## When to Use

Use ResponsivePreview when you need: ResponsivePreview: enterprise business component — see Storybook for interactive demos. Use for ERP/domain workflows (docs, pickers, approval, settlement) with business semantics.

## Examples

### Basic Usage

<CodeBlock code={`import { ResponsivePreview, Sizes } from "@chaos_team/chaos-ui/business"

export function Basic() {
  return (
    <div className="space-y-4">
  <ResponsivePreview />
  <Sizes />
    </div>
  )
}`} lang="tsx" />

## API / Props

### ResponsivePreview

| Prop        | Type                                | Default   | Description                       |
| ----------- | ----------------------------------- | --------- | --------------------------------- |
| `className` | `string`                            | —         | Custom CSS class                  |
| `device`    | `"mobile" \| "tablet" \| "desktop"` | `desktop` |                                   |
| `showFrame` | `boolean`                           | `true`    |                                   |
| `showLabel` | `boolean`                           | `true`    |                                   |
| ...rest     | `React.HTMLAttributes`              | —         | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
