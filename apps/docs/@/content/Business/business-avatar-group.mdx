import { AvatarGroup } from "@/components/business/avatar-group";
import { CodeBlock } from "@/components/code-block";

# AvatarGroup Business Avatar Group

> 业务组件 / Domain-specific business components for ERP scenarios.

Business Avatar Group — 面向 ERP/企业场景的业务组件。
AvatarGroup — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Business Avatar Group相关场景。

Use when you need an avatar group.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { AvatarGroup } from "@/components/business/avatar-group"

export function Basic() {
  return <AvatarGroup />
}`} lang="tsx" />

## API / Props

### AvatarGroup

| Prop              | Type                                | Default   | 说明 / Description                                          |
| ----------------- | ----------------------------------- | --------- | ----------------------------------------------------------- |
| `className`       | `string`                            | —         | 自定义样式类名 <br/> Custom CSS class                       |
| `users`           | `AvatarUser[]`                      | —         |                                                             |
| `max`             | `number`                            | `4`       | 最大值 <br/> Maximum value                                  |
| `size`            | `"sm" \| "default" \| "lg" \| "xl"` | `default` | 组件尺寸 <br/> Component size                               |
| `showOverflow`    | `boolean`                           | `true`    |                                                             |
| `onOverflowClick` | `() => void`                        | —         |                                                             |
| ...rest           | `React.ComponentProps<"div">`       | —         | 透传 `<div>` 原生属性 <br/> Inherits all `<div>` attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。
- **原生属性**: 继承所有 `<div>` 原生 HTML 属性。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-business-avatar-group--docs)。
