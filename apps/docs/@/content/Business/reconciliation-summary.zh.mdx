import { ReconciliationSummary } from "@/components/business/reconciliation-summary";
import { CodeBlock } from "@/components/code-block";

# ReconciliationSummary Reconciliation Summary

> 业务组件

Reconciliation Summary — 面向 ERP/企业场景的业务组件。

## 何时使用

适用于Reconciliation Summary相关场景。

## 代码示例

### 基础用法

<CodeBlock code={`import { ReconciliationSummary } from "@/components/business/reconciliation-summary"

export function Basic() {
  return <ReconciliationSummary />
}`} lang="tsx" />

## API / Props

### ReconciliationSummary

| Prop              | Type                   | Default | 说明              |
| ----------------- | ---------------------- | ------- | ----------------- |
| `className`       | `string`               | —       | 自定义样式类名    |
| `totalAmount`     | `number`               | —       |                   |
| `matchedAmount`   | `number`               | —       |                   |
| `unmatchedAmount` | `number`               | —       |                   |
| `matchedCount`    | `number`               | —       |                   |
| `unmatchedCount`  | `number`               | —       |                   |
| ...rest           | `React.HTMLAttributes` | —       | 透传原生 DOM 属性 |

## 注意事项

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/business-reconciliationsummary--docs)。
