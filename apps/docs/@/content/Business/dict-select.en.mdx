import { DictSelect } from "@/components/business/dict-select"
import { CodeBlock } from "@/components/code-block"

# DictSelect

> Domain-specific business components for ERP scenarios.

DictSelect — a business component for ERP / enterprise scenarios.

## When to Use

Use when you need a dict select.


## Examples

### Basic Usage

<CodeBlock code={`import { DictSelect } from "@/components/business/dict-select"

export function Basic() {
  return <DictSelect />
}`} lang="tsx" />


## API / Props

### DictSelect

| Prop | Type | Default | Description |
|---|---|---|---|
| `className` | `string` | — | Custom CSS class |
| `dictCode` | `string` | — |  |
| `options` | `DictOption[]` | — | Available options |
| `value` | `string` | — | Current value (controlled) |
| `onValueChange` | `() => void` | — |  |
| `placeholder` | `string` | `请选择` | Placeholder text |
| ...rest | `React.HTMLAttributes` | — | Inherits standard HTML attributes |

## Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。


