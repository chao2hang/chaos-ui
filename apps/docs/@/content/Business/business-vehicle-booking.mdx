import { VehicleBooking } from "@/components/business/vehicle-booking";
import { CodeBlock } from "@/components/code-block";

# VehicleBooking Business Vehicle Booking

> 业务组件 / Domain-specific business components for ERP scenarios.

Business Vehicle Booking — 面向 ERP/企业场景的业务组件。
VehicleBooking — a business component for ERP / enterprise scenarios.

## 何时使用 / When to Use

适用于Business Vehicle Booking相关场景。

Use when you need a vehicle booking.

## 代码示例 / Examples

### 基础用法 / Basic Usage

<CodeBlock code={`import { VehicleBooking } from "@/components/business/vehicle-booking"

export function Basic() {
  return <VehicleBooking />
}`} lang="tsx" />

## API / Props

### VehicleBooking

| Prop        | Type                    | Default | 说明 / Description                                        |
| ----------- | ----------------------- | ------- | --------------------------------------------------------- |
| `className` | `string`                | —       | 自定义样式类名 <br/> Custom CSS class                     |
| `vehicles`  | `Vehicle[]`             | —       | Available vehicles.                                       |
| `bookings`  | `VehicleBookingEntry[]` | —       | Existing bookings for conflict detection.                 |
| `onSubmit`  | `() => void`            | —       | Submit handler.                                           |
| `onCancel`  | `() => void`            | —       | Cancel handler.                                           |
| `readOnly`  | `boolean`               | `false` | Read-only mode.                                           |
| ...rest     | `React.HTMLAttributes`  | —       | 透传原生 DOM 属性 <br/> Inherits standard HTML attributes |

## 注意事项 / Notes

- **Client Component**: 使用 `"use client"`,依赖 React hooks/状态,需在客户端渲染。

> 📘 完整交互式示例与控件属性请查看 [Storybook 文档](?path=/docs/components-business-vehicle-booking--docs)。
