import type * as React from "react";
/** Typecheck stub — runtime module is @/components/mdx-loaders.ts */
export declare const mdxLoaders: Record<
  string,
  () => Promise<{ default: React.ComponentType<Record<string, unknown>> }>
>;
